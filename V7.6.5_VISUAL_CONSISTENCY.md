# v7.6.5 Visual Consistency Pass

## Fixed

1. Member secondary pills
   - `.trip-library-pill` now uses the same inline-flex centering model as the member/you pill.
   - Fixes 「最高權限」 / 「可使用支出」 text appearing too high.

2. Dark Mode status slabs
   - Profile `.trip-library-status` is now plain status/help text with no pale full-width backing rectangle.

3. Cross-page Liquid Glass
   - Itinerary remains the visual reference.
   - 收藏 removes the outer glass-on-glass wrapper; individual saved-place cards use itinerary glass in Light Mode.
   - 資料 removes the outer opaque Trip Library shell; individual team cards use itinerary glass in Light Mode.
   - 支出 major cards use the same light-mode glass opacity / border / shadow as itinerary cards.
   - 我的 intentionally keeps the denser Apple Settings glass.

No Firestore Rules or index changes.
The v7.6.4.1 membership rules remain bundled.
v7.3.13 navigation compositor is unchanged.
