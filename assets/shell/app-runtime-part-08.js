window.__travelRuntimeParts.push("r).catch(error=>console.warn(\"Restored Trip bootstrap\",error));\n      refreshAppEntryState();\n    }\n  }catch(error){\n    console.error(\"Personal Trip archive action\",error);\n    alert(tripImportPermissionMessage(error));\n    button.disabled=false;\n  }\n}\ndocument.getElementById(\"trip-library-list\")?.addEventListener(\"click\",event=>{\n  const archive=event.target.closest?.(\"[data-trip-archive-id]\"),restore=event.target.closest?.(\"[data-trip-restore-id]\");\n  if(archive)handleTripArchiveAction(archive,true);else if(restore)handleTripArchiveAction(restore,false);\n});\ndocument.getElementById(\"trip-archive-list\")?.addEventListener(\"click\",event=>{\n  const button=event.target.closest?.(\"[data-trip-restore-id]\");if(button)handleTripArchiveAction(button,false);\n});\ndocument.getElementById(\"entry-archive-list\")?.addEventListener(\"click\",event=>{\n  const button=event.target.closest?.(\"[data-trip-restore-id]\");if(button)handleTripArchiveAction(button,false);\n});\n\nlet tripBackupServicePromise=null;\nlet tripBackupPackageServicePromise=null;\nlet tripBackupSnapshots=[];\nlet tripBackupSelectedSnapshot=null;\nlet tripBackupBusy=false;\nlet dataOperationState={kind:\"\",busy:false,done:false,payload:null,scope:\"all\",handler:null,cancellable:false,abortController:null,runToken:0};\nlet dataOperationRunToken=0;\nlet tripDeleteBackupState={tripId:\"\",archiveGrade:false,preparedAt:0,filename:\"\"};\nlet tripDeleteServicePromise=null;\nlet tripDeleteResumeTimer=0;\nlet tripDeleteResumeInFlight=false;\nlet tripDeleteResumeAttemptKey=\"\";\n// v7.7.7.4 · Local First Export Engine\n// iOS file handoff can leave explicit Firestore reads unusable until the PWA is\n// restarted. Prepare every read-only export source before the first download,\n// then build JSON / XLSX files only from the already-synchronised local state.\n// Cloud writes (Import / Restore / Snapshot creation) remain Firebase-backed.\nlet dataManagementLocalPreparePromise=null;\nlet dataManagementLocalPreparedTripId=\"\";\nlet dataManagementSnapshotCacheTripId=\"\";\nlet dataManagementSnapshotCacheReady=false;\nfunction localExportClone(value){\n  try{return JSON.parse(JSON.stringify(value));}catch(error){return null;}\n}\nfunction localExportCurrentTrip(){\n  const tripId=currentBackupTripId();\n  const data=dataTripId(window.tripData)===tripId?window.tripData:null;\n  const days=Array.isArray(data?.days)?data.days:[];\n  if(!data||!tripId||!days.length||days.some(day=>!Array.isArray(day?.items))){\n    const error=new Error(\"目前行程本機資料尚未完整準備。\");\n    error.code=\"local-export-incomplete\";\n    throw error;\n  }\n  return localExportClone(data);\n}\nfunction localExportAccess(){\n  const access=currentTripMemberAccess?.()||window.__appTripAccess||{};\n  const tripId=currentBackupTripId();\n  if(String(access.tripId||\"\")!==tripId||access.ready!==true||!access.role){\n    const error=new Error(\"旅程權限仍在同步，暫未能建立本機匯出資料。\");\n    error.code=\"local-export-incomplete\";\n    throw error;\n  }\n  return {role:String(access.role||\"\"),tripId};\n}\nfunction captureCurrentExpenseExportSnapshot(){\n  const tripId=currentBackupTripId();\n  const enabled=window.tripData?.meta?.expenses?.enabled===true;\n  if(!enabled){\n    const tripFresh=currentTripFreshnessSnapshot();\n    return {\n      tripId,ready:true,settings:localExportClone(window.tripData?.meta?.expenses||{}),expenses:[],settlements:[],activityLogs:[],capturedAt:new Date().toISOString(),\n      freshness:{serverConfirmed:tripFresh.serverConfirmed===true&&!tripFresh.hasPendingWrites,hasPendingWrites:tripFresh.hasPendingWrites===true,sources:{disabled:{seen:true,fromCache:tripFresh.serverConfirmed!==true,serverConfirmed:tripFresh.serverConfirmed===true&&!tripFresh.hasPendingWrites,hasPendingWrites:tripFresh.hasPendingWrites===true}}}\n    };\n  }\n  const snap=typeof window.__getExpenseLocalExportSnapshot===\"function\"?window.__getExpenseLocalExportSnapshot():null;\n  if(snap&&String(snap.tripId||\"\")===tripId&&snap.ready===true)return snap;\n  const error=new Error(\"支出資料仍在用現有 Firebase listeners 同步，暫未能建立完整 Backup。\");\n  error.code=\"local-export-incomplete\";\n  throw error;\n}\nfunction waitForLocalExpenseExportSnapshot(timeoutMs=15000){\n  const tripId=currentBackupTripId();\n  const enabled=window.tripData?.meta?.expenses?.enabled===true;\n  if(!enabled){\n    const tripFresh=currentTripFreshnessSnapshot();\n    return Promise.resolve({\n      tripId,\n      ready:true,\n      settings:localExportClone(window.tripData?.meta?.expenses||{}),\n      expenses:[],\n      settlements:[],\n      activityLogs:[],\n      capturedAt:new Date().toISOString(),\n      freshness:{\n        serverConfirmed:tripFresh.serverConfirmed===true,\n        hasPendingWrites:tripFresh.hasPendingWrites===true,\n        sources:{disabled:{serverConfirmed:tripFresh.serverConfirmed===true,hasPendingWrites:tripFresh.hasPendingWrites===true}}\n      }\n    });\n  }\n  initExpensesViewSafe();\n  return new Promise((resolve,reject)=>{\n    const started=Date.now();\n    const poll=()=>{\n      try{\n        const snap=typeof window.__getExpenseLocalExportSnapshot===\"function\"?window.__getExpenseLocalExportSnapshot():null;\n        if(snap&&String(snap.tripId||\"\")===tripId&&snap.ready===true){resolve(snap);return;}\n      }catch(error){}\n      if(Date.now()-started>=timeoutMs){\n        const error=new Error(\"支出資料尚未完整同步，暫未能建立完整本機匯出資料。\");\n        error.code=\"local-export-incomplete\";reject(error);return;\n      }\n      setTimeout(poll,180);\n    };\n    poll();\n  });\n}\nasync function prepareDataManagementLocalExports(){\n  const tripId=currentBackupTripId();\n  if(!tripId){const error=new Error(\"暫時未有目前旅程。\");error.code=\"local-export-incomplete\";throw error;}\n  if(dataManagementLocalPreparedTripId!==tripId){\n    dataManagementLocalPreparePromise=null;\n    dataManagementLocalPreparedTripId=tripId;\n    dataManagementSnapshotCacheTripId=\"\";\n    dataManagementSnapshotCacheReady=false;\n  }\n  if(dataManagementLocalPreparePromise)return dataManagementLocalPreparePromise;\n  dataManagementLocalPreparePromise=(async()=>{\n    const service=await getTripBackupService();\n    const user=await getImportFirebaseUser();\n    if(!user?.uid){const error=new Error(\"請先登入 Google 帳戶。\");error.code=\"auth-required\";throw error;}\n    const access=localExportAccess();\n    const trip=localExportCurrentTrip();\n    const expenseSnapshot=await waitForLocalExpenseExportSnapshot(15000);\n    let snapshots=[];\n    if([\"owner\",\"admin\"].includes(access.role)){\n      const result=await service.listTripSnapshots(tripId,{user,maxItems:10,knownRole:access.role});\n      snapshots=result.snapshots||[];\n      tripBackupSnapshots=snapshots;\n      dataManagementSnapshotCacheTripId=tripId;\n      dataManagementSnapshotCacheReady=true;\n    }else{\n      dataManagementSnapshotCacheTripId=tripId;\n      dataManagementSnapshotCacheReady=true;\n    }\n    return {tripId,user,role:access.role,trip,expenseSnapshot,snapshots,preparedAt:Date.now()};\n  })().catch(error=>{\n    dataManagementLocalPreparePromise=null;\n    throw error;\n  });\n  return dataManagementLocalPreparePromise;\n}\nfunction refreshLocalExportCapture(prepared){\n  const trip=localExportCurrentTrip();\n  let expenseSnapshot=prepared?.expenseSnapshot||null;\n  if(typeof window.__getExpenseLocalExportSnapshot===\"function\"){\n    try{\n      const live=window.__getExpenseLocalExportSnapshot();\n      if(live&&String(live.tripId||\"\")===prepared.tripId&&live.ready===true)expenseSnapshot=live;\n    }catch(error){}\n  }\n  return {...prepared,trip,expenseSnapshot,capturedAt:Date.now()};\n}\nfunction currentTripFreshnessSnapshot(){\n  const tripId=currentBackupTripId();\n  return {\n    tripId,\n    source:String(tripLoaderState?.source||\"local\"),\n    serverConfirmed:tripLoaderState?.status===\"ready\"&&tripLoaderState?.tripId===tripId&&tripLoaderState?.serverConfirmed===true,\n    hasPendingWrites:tripLoaderState?.hasPendingWrites===true,\n    updatedAt:String(tripLoaderState?.updatedAt||\"\")\n  };\n}\nlet fullBackupSyncGateWatchTimer=0;\nlet fullBackupSyncGateWatchStartedAt=0;\nlet fullBackupSyncGatePendingSince=0;\nlet fullBackupSyncGatePendingTripId=\"\";\nconst FULL_BACKUP_SYNC_STATUS_TIMEOUT_MS=25000;\nconst FULL_BACKUP_SYNC_GATE_VISIBILITY_GRACE_MS=900;\nfunction currentFullBackupSyncGate(){\n  const tripId=currentBackupTripId();\n  const loader=tripLoaderState||{};\n  const sameTrip=!!tripId&&String(loader.tripId||\"\")===tripId;\n  const observedOnline=globalThis?.window?.__cloudSafetyState?.online;\n  const online=(typeof navigator===\"undefined\"||navigator.onLine!==false)&&observedOnline!==false;\n  const tripPending=sameTrip&&loader.hasPendingWrites===true;\n  const tripConfirmed=sameTrip&&loader.status===\"ready\"&&loader.serverConfirmed===true&&!tripPending;\n  const mediaMutationBusy=window.__tripMediaMutationBusy===true||Number(window.__tripMediaPendingByTrip?.[tripId]||0)>0;\n  const expensesEnabled=window.tripData?.meta?.expenses?.enabled===true;\n  let expenseReady=!expensesEnabled,expenseConfirmed=!expensesEnabled&&tripConfirmed,expensePending=false;\n  if(expensesEnabled&&typeof window.__getExpenseLocalExportSnapshot===\"function\"){\n    try{\n      const snap=window.__getExpenseLocalExportSnapshot();\n      if(snap&&String(snap.tripId||\"\")===tripId){\n        expenseReady=snap.ready===true;\n        expensePending=snap?.freshness?.hasPendingWrites===true;\n        expenseConfirmed=expenseReady&&snap?.freshness?.serverConfirmed===true&&!expensePending;\n      }\n    }catch(error){}\n  }\n  const canBackup=online&&tripConfirmed&&expenseConfirmed&&!mediaMutationBusy;\n  const transportUnavailable=!online||!tripId||[\"error\",\"recovery-needed\",\"permission-denied\",\"not-found\",\"local\"].includes(String(loader.status||\"\"))||Boolean(loader.error&&!loader.serverConfirmed);\n  if(fullBackupSyncGatePendingTripId!==tripId){fullBackupSyncGatePendingTripId=tripId;fullBackupSyncGatePendingSince=0;}\n  if(canBackup||transportUnavailable||mediaMutationBusy){fullBackupSyncGatePendingSince=0;}\n  else if(!fullBackupSyncGatePendingSince){fullBackupSyncGatePendingSince=Date.now();}\n  const timedOut=!canBackup&&!transportUnavailable&&fullBackupSyncGatePendingSince>0&&(Date.now()-fullBackupSyncGatePendingSince>=FULL_BACKUP_SYNC_STATUS_TIMEOUT_MS);\n  const status=canBackup?\"synced\":((transportUnavailable||timedOut)?\"unconfirmed\":\"syncing\");\n  return {status,canBackup,tripId,online,tripConfirmed,tripPending,expenseReady,expenseConfirmed,expensePending,mediaMutationBusy,timedOut};\n}\nfunction fullBackupSyncGateCopy(gate,{deleteFlow=false}={}){\n  if(gate.mediaMutationBusy)return \"Firebase：媒體更新中 · Backup 暫時未開放\";\n  if(gate.status===\"synced\")return deleteFlow?\"Firebase：已同步 · 可建立刪除前 Data + Media ZIP\":\"Firebase：已同步 · Data + Media ZIP 可建立\";\n  if(gate.status===\"unconfirmed\"){\n    if(!gate.online)return \"Firebase：離線 · Backup 暫時未開放\";\n    if(!gate.tripConfirmed)return \"Firebase：行程未能確認同步 · Backup 暫時未開放\";\n    return \"Firebase：支出未能確認同步 · Backup 暫時未開放\";\n  }\n  if(!gate.tripConfirmed)return \"Firebase：行程同步中 · Backup 暫時未開放\";\n  if(!gate.expenseConfirmed)return \"Firebase：支出同步中 · Backup 暫時未開放\";\n  return \"Firebase：同步中 · Backup 暫時未開放\";\n}\nfunction applyFullBackupGateToButton(button,subtitle,gate,{deleteFlow=false,ownerAllowed=true}={}){\n  if(!button)return;\n  const enabled=ownerAllowed&&gate.canBackup;\n  button.disabled=!enabled;\n  button.setAttribute(\"aria-disabled\",enabled?\"false\":\"true\");\n  button.classList.toggle(\"profile-row-disabled\",!enabled);\n  if(subtitle)subtitle.textContent=fullBackupSyncGateCopy(gate,{deleteFlow});\n}\nfunction refreshFullBackupSyncGate(){\n  const gate=currentFullBackupSyncGate();\n  applyFullBackupGateToButton(document.getElementById(\"trip-full-backup-export\"),document.getElementById(\"trip-full-backup-export-subtitle\"),gate);\n  const owner=currentTripOwnerRecord?.();\n  applyFullBackupGateToButton(document.getElementById(\"trip-delete-backup-btn\"),document.getElementById(\"trip-delete-backup-subtitle\"),gate,{deleteFlow:true,ownerAllowed:Boolean(owner?.isOwner&&gate.tripId)});\n  window.__fullBackupSyncGate={...gate,evaluatedAt:Date.now()};\n  return gate;\n}\nfunction stopFullBackupSyncGateWatch(){\n  if(fullBackupSyncGateWatchTimer){clearInterval(fullBackupSyncGateWatchTimer);fullBackupSyncGateWatchTimer=0;}\n}\nfunction fullBackupGatePageVisible(){\n  const dataPage=document.querySelector('[data-profile-page=\"data-management\"]');\n  const deletePage=document.querySelector('[data-profile-page=\"trip-delete\"]');\n  if((dataPage&&!dataPage.hidden)||(deletePage&&!deletePage.hidden))return true;\n  // Profile navigation uses a protected 430 ms snapshot compositor. The live\n  // destination page remains hidden during that handoff, so the Backup watcher\n  // must not mistake the transition frame for a real page exit.\n  if(profileNavigationTransitioning&&Date.now()-fullBackupSyncGateWatchStartedAt<FULL_BACKUP_SYNC_GATE_VISIBILITY_GRACE_MS)return true;\n  return false;\n}\nfunction startFullBackupSyncGateWatch(){\n  stopFullBackupSyncGateWatch();\n  fullBackupSyncGateWatchStartedAt=Date.now();\n  if(window.tripData?.meta?.expenses?.enabled===true)try{initExpensesViewSafe();}catch(error){}\n  refreshFullBackupSyncGate();\n  fullBackupSyncGateWatchTimer=setInterval(()=>{\n    if(!fullBackupGatePageVisible()){stopFullBackupSyncGateWatch();return;}\n    refreshFullBackupSyncGate();\n  },350);\n}\nasync function prepareFullBackupLocalExportPack(){\n  const gate=refreshFullBackupSyncGate();\n  if(!gate.canBackup){\n    const error=new Error(gate.online?\"Backup state is not yet server confirmed\":\"Device is offline\");\n    error.code=gate.online?\"backup-freshness-unconfirmed\":\"backup-freshness-offline\";\n    throw error;\n  }\n  const user=await getImportFirebaseUser();\n  if(!user?.uid){const error=new Error(\"請先登入 Google 帳戶。\");error.code=\"auth-required\";throw error;}\n  const access=localExportAccess();\n  const trip=localExportCurrentTrip();\n  const expenseSnapshot=captureCurrentExpenseExportSnapshot();\n  return {tripId:gate.tripId,user,role:access.role,trip,expenseSnapshot,tripFreshness:currentTripFreshnessSnapshot(),capturedAt:Date.now()};\n}\nwindow.addEventListener(\"online\",refreshFullBackupSyncGate);\nwindow.addEventListener(\"offline\",refreshFullBackupSyncGate);\nwindow.addEventListener(\"expense-backup-freshness-change\",refreshFullBackupSyncGate);\nfunction getTripBackupService(){\n  if(!tripBackupServicePromise){\n    tripBackupServicePromise=import(`./assets/js/trip-backup-service.js?build=${APP_VERSION}`)\n      .catch(error=>{tripBackupServicePromise=null;throw error;});\n  }\n  return tripBackupServicePromise;\n}\nfunction getTripBackupPackageService(){\n  if(!tripBackupPackageServicePromise){\n    tripBackupPackageServicePromise=import(`./assets/js/trip-backup-package-service.js?build=${APP_VERSION}`)\n      .catch(error=>{tripBackupPackageServicePromise=null;throw error;});\n  }\n  return tripBackupPackageServicePromise;\n}\nfunction currentBackupTripId(){return String(tripLibrarySummary?.tripId||window.__activeTripId||\"\").trim();}\nfunction permanentDeleteStageLabel(stageInput){\n  const stage=String(stageInput||\"\").trim();\n  return ({\n    \"trip-child-cleanup\":\"清除 Trip 子資料\",\n    \"storage-cleanup\":\"清除 Storage 媒體\",\n    \"cross-reference-cleanup\":\"清除跨 Trip references\",\n    \"member-cleanup\":\"清除成員資料\",\n    \"pre-root-verification\":\"刪除前 cleanup verification\",\n    \"root-delete\":\"最後刪除 Trip root\",\n    \"root-verification\":\"驗證 Trip root 已刪除\",\n    \"post-root-verification\":\"最終殘留資料驗證\"\n  })[stage]||\"Firebase cleanup\";\n}\nfunction backupFriendlyError(error){\n  if(error?.code===\"auth-required\")return \"請先登入 Google 帳戶。\";\n  if(error?.code===\"insufficient-role\")return \"只有 Owner 或 Admin 可以查看及還原 Snapshot。\";\n  if(error?.code===\"trip-not-found\")return \"Firebase 暫時未有呢個旅程。請先匯入 trip.json。\";\n  if(error?.code===\"snapshot-not-found\")return \"搵唔到呢個 Snapshot，可能已經被刪除。\";\n  if(error?.code===\"snapshot-invalid\")return \"呢個 Snapshot 格式不完整，未能匯出或還原。\";\n  if(error?.code===\"snapshot-too-large\")return \"目前旅程太大，未能建立單一 Snapshot。\";\n  if(error?.code===\"backup-invalid-json\")return \"呢個檔案唔係有效 JSON。\";\n  if(error?.code===\"backup-invalid\"||error?.code===\"backup-unsupported\")return \"呢個檔案唔係支援嘅 Full Backup JSON。\";\n  if(error?.code===\"backup-trip-mismatch\")return \"Backup Trip ID 同目前旅程唔一致。請切換到 Backup 所屬旅程；如果原 Trip 已刪除，可以由 Access Lobby 用 Full Backup 重建。\";\n  if(error?.code===\"trip-global-locked\")return \"旅程已全域鎖定。Backup 仍然可以匯出，但 Restore 要先到「我的 → 旅程設定 → 旅程鎖定」解除鎖定。\";\n  if(error?.code===\"permission-denied\"||error?.code===\"firestore/permission-denied\")return \"Firebase 權限不足，請確認旅程角色。\";\n  if(error?.code===\"local-export-incomplete\")return error?.message||\"本機資料尚未完整同步，暫未能匯出。\";\n  if(error?.code===\"restore-preflight-timeout\")return `${error?.stage||\"Restore 準備階段\"}逾時。還原尚未開始寫入旅程資料；請關閉後再試一次。如果只係某一個 Trip 出現，呢個階段名稱可以用嚟定位問題。`;\n  if(error?.code===\"restore-operation-timeout\")return \"取得旅程操作鎖逾時。旅程內容尚未開始還原；系統會自動清理任何遲到嘅操作鎖。請關閉後再試一次。\";\n  if(error?.code===\"data-operation-timeout\")return `${error?.stage||\"資料操作\"}逾時。今次操作已停止，唔需要 Quit App；可以直接再試。`;\n  if(error?.code===\"offline\"||error?.code===\"operation-busy\"||error?.code===\"trip-operation-busy\")return cloudSafetyFriendlyError(error);\n  if(error?.code===\"backup-package-invalid\"||error?.code===\"backup-package-invalid-json\"||error?.code===\"backup-package-unsupported\")return \"呢個 Full Backup ZIP 格式無效或唔受支援。\";\n  if(error?.code===\"backup-package-integrity-mismatch\")return \"Full Backup ZIP 完整性驗證失敗。資料或媒體檔案同建立時嘅 SHA-256 / CRC 不一致，已停止還原。\";\n  if(error?.code===\"backup-package-media-missing\")return \"Full Backup ZIP 缺少所需媒體檔案，已停止還原。\";\n  if(error?.code===\"backup-package-compression-unsupported\")return \"呢個 Backup ZIP 曾被重新壓縮，格式唔符合 App 建立嘅 Media Backup Package。請使用原始下載檔案。\";\n  if(error?.code===\"backup-package-required\")return \"呢份 Backup 包含媒體資料，請選擇原本下載嘅 Full Backup ZIP，而唔係單獨抽出 trip-data.json。\";\n  if(error?.code===\"backup-media-reference-invalid\"||error?.code===\"backup-media-reference-conflict\")return \"目前旅程有媒體 reference 資料不完整或互相衝突。為免建立缺圖 Backup，系統已停止匯出。\";\n  if(error?.code===\"backup-media-reference-missing\"||error?.code===\"backup-media-reference-mismatch\")return \"目前旅程引用嘅其中一張 Firebase 相片未能同 Media Registry 完整對上。為免建立表面成功但實際缺圖嘅 Backup，系統已停止匯出。\";\n  if(error?.code===\"backup-integrity-mismatch\")return \"Backup 完整性驗證失敗。檔案內容同建立時嘅 SHA-256 不一致，已停止還原。\";\n  if(error?.code===\"backup-integrity-unavailable\")return \"目前瀏覽器未能執行 Backup SHA-256 驗證。請更新或重新開啟 App 後再試。\";\n  if(error?.code===\"backup-freshness-offline\")return \"目前離線，只能建立 Last Synced Backup。永久刪除前請重新連線，等 Firebase Server confirmation 完成後再下載。\";\n  if(error?.code===\"backup-freshness-unconfirmed\")return \"Firebase 最新狀態尚未完整確認。請保持 App 開啟幾秒後再試；系統唔會用未確認資料當刪除前 Backup。\";\n  if(error?.code===\"creator-required\")return \"目前帳戶未獲授權建立新旅程，無法用 Backup 重建已刪除 Trip。\";\n  if(error?.code===\"backup-trip-id-used\"||error?.code===\"trip-id-unavailable\")return \"呢個 Trip ID 已被使用。請輸入另一個未使用嘅 canonical Trip ID 再重建。\";\n  if(error?.code===\"backup-trip-id-invalid\"||error?.code===\"invalid-trip-id\")return \"請輸入有效嘅新 Trip ID；唔可以留空、包含 /，長度亦唔可以超過 160 個字元。\";\n  if(error?.code===\"backup-accessible-other-trip\")return \"呢份 Backup 屬於你另一個現有旅程。請先切換到該 Trip，再使用『還原完整 Backup』。\";\n  if(error?.code===\"delete-already-running\")return \"Firebase Server 仍然執行緊上一個永久刪除流程。App 會保留重試標記並稍後自動再確認，唔需要重新按刪除。\";\n  if(error?.code===\"delete-verification-failed\")return `永久刪除喺「${permanentDeleteStageLabel(error?.details?.stage||error?.customData?.details?.stage)}」未通過 cleanup verification。操作可以重試，系統唔會假裝已刪除完成。`;\n  if(error?.code===\"delete-interrupted\"||error?.code===\"functions/internal\")return `永久刪除喺「${permanentDeleteStageLabel(error?.details?.stage||error?.customData?.details?.stage)}」受阻。清理流程可以重試；請保持連線後再次執行永久刪除。`;\n  if(error?.code===\"functions/permission-denied\")return \"只有 Trip Owner 可以永久刪除旅程。\";\n  if(error?.code===\"functions/unauthenticated\")return \"請先登入 Google 帳戶。\";\n  if(error?.code===\"operation-aborted\")return \"操作已取消。\";\n  if(error?.code===\"media-operation-timeout\")return \"Firebase 媒體讀取逾時，請保持網絡連線後再試。\";\n  const rawMessage=String(error?.message||\"\");\n  if(tripCloudOffline())return \"目前離線，無法完成呢個資料操作。請重新連線後再試。\";\n  if(/module script failed|failed to fetch dynamically imported module/i.test(rawMessage))return \"資料管理模組未能載入。請重新整理 App 後再試。\";\n  return rawMessage||\"操作未完成。\";\n}\nfunction backupFormatDate(value){\n  if(!value)return \"—\";\n  const d=new Date(value);\n  if(!Number.isFinite(d.getTime()))return \"—\";\n  try{return new Intl.DateTimeFormat(\"zh-HK\",{month:\"short\",day:\"numeric\",hour:\"2-digit\",minute:\"2-digit\",hour12:false}).format(d);}catch(error){return d.toLocaleString();}\n}\nfunction backupPrepareJsonDownload(json,filename){\n  return {\n    blob:new Blob([JSON.stringify(json,null,2)],{type:\"application/json;charset=utf-8\"}),\n    filename:filename||\"trip.json\"\n  };\n}\nasync function prepareMediaAwareFullBackupPackage(result,{stagePrefix=\"Backup\",onProgress=null,signal=null}={}){\n  const backupService=await getTripBackupService();\n  const packageService=await getTripBackupPackageService();\n  const tripId=String(result?.tripId||result?.json?.tripId||\"\").trim();\n  const collected=await packageService.collectTripMediaForBackup(tripId,{backupJson:result.json,signal,onProgress:progress=>{\n    if(typeof onProgress===\"function\")onProgress(progress);\n  }});\n  const manifest=packageService.mediaManifestFromCollected(collected);\n  const finalJson=await backupService.withFullBackupMediaManifest(result.json,manifest);\n  const revision=Math.max(1,Number(result?.revision||finalJson?.sourceRevision)||1);\n  const filename=`${tripId}-full-backup-v1-r${revision}.zip`;\n  const packaged=await packageService.buildFullBackupPackage(finalJson,collected,{filename,signal,onProgress:progress=>{if(typeof onProgress===\"function\")onProgress(progress);}});\n  return {...packaged,tripId,revision,mediaCount:collected.mediaCount||0,mediaBytes:collected.totalBytes||0,registrySource:collected.registrySource||\"\",registryStale:collected.registryStale===true,json:finalJson};\n}\nlet activeBackupDownloadObjectUrl=\"\";\nlet activeBackupDownloadReleaseTimer=null;\nfunction releaseBackupDownloadObjectUrl(){\n  if(activeBackupDownloadReleaseTimer){clearTimeout(activeBackupDownloadReleaseTimer);activeBackupDownloadReleaseTimer=null;}\n  const url=activeBackupDownloadObjectUrl;activeBackupDownloadObjectUrl=\"\";\n  if(url){try{URL.revokeObjectURL(url);}catch(error){}}\n}\nasync function prepareForNextBackupExport(signal=null){\n  // Never wait on requestAnimationFrame here. iOS can suspend rAF while the\n  // previous file/share handoff is covering the PWA, which turns a harmless\n  // cleanup pause into an unbounded wait on the next export.\n  releaseBackupDownloadObjectUrl();\n  const last=Number(window.__lastDataOperationDownloadHandoffAt)||0;\n  const wait=Math.max(0,Math.min(500,650-(Date.now()-last)));\n  if(signal?.aborted){const error=new Error(\"Backup export cancelled\");error.code=\"operation-aborted\";throw error;}\n  if(wait>0)await new Promise((resolve,reject)=>{\n    const timer=setTimeout(resolve,wait);\n    if(signal)signal.addEventListener(\"abort\",()=>{clearTimeout(timer);const error=new Error(\"Backup export cancelled\");error.code=\"operation-aborted\";reject(error);},{once:true});\n  });\n  await Promise.resolve();\n}\nfunction backupDownloadBlob(blob,filename){\n  if(!(blob instanceof Blob))throw new Error(\"Download payload is unavailable.\");\n  releaseBackupDownloadObjectUrl();\n  const url=URL.createObjectURL(blob);\n  activeBackupDownloadObjectUrl=url;\n  const a=document.createElement(\"a\");\n  a.href=url;a.download=filename||\"download\";a.style.display=\"none\";\n  document.body.appendChild(a);\n  try{a.click();}finally{\n    a.remove();\n    // Keep the current URL alive long enough for the iOS handoff, but never\n    // allow an old export URL to overlap indefinitely with the next export.\n    activeBackupDownloadReleaseTimer=setTimeout(()=>{\n      if(activeBackupDownloadObjectUrl===url)releaseBackupDownloadObjectUrl();\n    },4000);\n  }\n}\nwindow.addEventListener(\"pagehide\",releaseBackupDownloadObjectUrl);\nfunction dataOperationHandoffDownload(download){\n  if(typeof download!==\"function\")return;\n  // v7.7.7.4: read-only export data has already been frozen locally before\n  // handing the file to iOS, so no Firestore recovery is required afterwards.\n  window.__lastDataOperationDownloadHandoffAt=Date.now();\n  requestAnimationFrame(()=>setTimeout(()=>{\n    try{download();}catch(error){console.error(\"Download handoff\",error);}\n  },0));\n}\nfunction backupSetStatus(message,{error=false,hidden=false}={}){\n  const el=document.getElementById(\"trip-backup-status\");if(!el)return;\n  el.hidden=hidden;el.textContent=message||\"\";el.style.color=error?\"#c8352d\":\"\";\n}\nfunction renderTripBackupList(){\n  const list=document.getElementById(\"trip-backup-list\");\n  const count=document.getElementById(\"trip-backup-count\");\n  if(count)count.textContent=String(tripBackupSnapshots.length);\n  if(!list)return;\n  list.innerHTML=tripBackupSnapshots.map(snapshot=>{\n    const meta=[`Revision ${snapshot.sourceRevision||0}`,`${snapshot.dayCount||0} Days`,`${snapshot.itemCount||0} Items`];\n    return `<button class=\"profile-row\" type=\"button\" data-snapshot-open=\"${escapeHtmlText(snapshot.snapshotId)}\"><span class=\"profile-row-icon profile-icon-grey\">↶</span><span class=\"profile-row-copy\"><strong class=\"trip-backup-row-time\">${escapeHtmlText(backupFormatDate(snapshot.createdAtIso||snapshot.createdAtMillis))}</strong><small>${escapeHtmlText(snapshot.typeLabel||\"旅程備份\")}</small><span class=\"trip-backup-row-meta\">${meta.map(v=>`<span>${escapeHtmlText(v)}</span>`).join(\"\")}</span></span><span class=\"profile-chevron\">›</span></button>`;\n  }).join(\"\");\n}\nasync function loadTripBackupPage(){\n  if(tripBackupBusy)return;\n  const tripId=currentBackupTripId();\n  if(!tripId){backupSetStatus(\"暫時未有目前旅程。\",{error:true});return;}\n  if(dataManagementLocalPreparePromise&&dataManagementLocalPreparedTripId===tripId){\n    try{await dataManagementLocalPreparePromise;}catch(error){}\n  }\n  if(dataManagementSnapshotCacheReady&&dataManagementSnapshotCacheTripId===tripId){\n    renderTripBackupList();\n    backupSetStatus(tripBackupSnapshots.length?`顯示最近 ${tripBackupSnapshots.length} 個 Snapshot（已預載）。`:`目前未有 Snapshot。`);\n    return;\n  }\n  tripBackupBusy=true;backupSetStatus(\"正在讀取 Firebase Snapshot…\");\n  try{\n    const service=await getTripBackupService();\n    const user=await getImportFirebaseUser();\n    const roleInfo=await service.getTripBackupRole(tripId,{user});\n    if(!roleInfo.canManage){\n      tripBackupSnapshots=[];renderTripBackupList();\n      backupSetStatus(`目前身份：${roleInfo.roleLabel||\"未有角色\"}。只有 Owner / Admin 可以查看 Snapshot。`);\n      return;\n    }\n    const result=await service.listTripSnapshots(tripId,{user,maxItems:10});\n    tripBackupSnapshots=result.snapshots||[];dataManagementSnapshotCacheTripId=tripId;dataManagementSnapshotCacheReady=true;renderTripBackupList();\n    backupSetStatus(tripBackupSnapshots.length?`顯示最近 ${tripBackupSnapshots.length} 個 Snapshot。`:`目前未有 Snapshot。`);\n  }catch(error){\n    console.error(\"Load trip backups\",error);tripBackupSnapshots=[];renderTripBackupList();backupSetStatus(backupFriendlyError(error),{error:true});\n  }finally{tripBackupBusy=false;}\n}\nasync function openSnapshotDetail(snapshotId){\n  const summary=tripBackupSnapshots.find(item=>item.snapshotId===snapshotId)||null;\n  tripBackupSelectedSnapshot=summary;\n  document.getElementById(\"snapshot-detail-time\").textContent=summary?backupFormatDate(summary.createdAtIso||summary.createdAtMillis):\"正在讀取…\";\n  document.getElementById(\"snapshot-detail-type\").textContent=summary?.typeLabel||\"正在讀取…\";\n  document.getElementById(\"snapshot-detail-revision\").textContent=summary?String(summary.sourceRevision||0):\"—\";\n  document.getElementById(\"snapshot-detail-counts\").textContent=summary?`${summary.dayCount||0} Days · ${summary.itemCount||0} Items · ${summary.savedPlaceCount||0} Saved`:\"—\";\n  document.getElementById(\"snapshot-detail-id\").textContent=snapshotId;\n  document.getElementById(\"snapshot-detail-status\").textContent=currentTripGlobalLocked()?\"旅程目前已鎖定；可以查看或匯出 Snapshot，但要先解除鎖定先可以還原。\":\"Snapshot 只影響 Firebase 行程內容及設定，不會刪除成員或實際支出交易。\";\n  renderDataManagementLockState();\n  openProfilePage(\"snapshot-detail\");\n}\ndocument.getElementById(\"trip-backup-open-btn\")?.addEventListener(\"click\",()=>setTimeout(loadTripBackupPage,40));\ndocument.getElementById(\"trip-backup-list\")?.addEventListener(\"click\",event=>{\n  const button=event.target.closest?.(\"[data-snapshot-open]\");if(button)openSnapshotDetail(button.dataset.snapshotOpen);\n});\n\nfunction dataOperationElements(){\n  return {\n    backdrop:document.getElementById(\"data-operation-backdrop\"),title:document.getElementById(\"data-operation-title\"),subtitle:document.getElementById(\"data-operation-subtitle\"),\n    name:document.getElementById(\"data-operation-name\"),id:document.getElementById(\"data-operation-id\"),mode:document.getElementById(\"data-operation-mode\"),\n    warnings:document.getElementById(\"data-operation-warnings\"),error:document.getElementById(\"data-operation-error\"),options:document.getElementById(\"data-operation-options\"),secondary:document.getElementById(\"data-operation-secondary\"),\n    progress:document.getElementById(\"data-operation-progress\"),confirm:document.getElementById(\"data-operation-confirm\"),cancel:document.getElementById(\"data-operation-cancel\")\n  };\n}\nfunction dataOperationCurrentSummary(){\n  const data=(typeof tripData!==\"undefined\"&&tripData)?tripData:{};\n  const days=Array.isArray(data.days)?data.days:[];\n  const items=days.reduce((sum,day)=>sum+(Array.isArray(day?.items)?day.items.length:0),0);\n  const saved=Array.isArray(data.savedPlaces)?data.savedPlaces.length:(Array.isArray(data?.snacks?.items)?data.snacks.items.length:0);\n  const title=String(data?.meta?.title||data?.title||tripLibrarySummary?.title||currentBackupTripId()||\"目前旅程\");\n  return {title,tripId:currentBackupTripId(),dayCount:days.length,itemCount:items,savedPlaceCount:saved};\n}\nfunction dataOperationSetStats(stats=[]){\n  for(let i=0;i<4;i++){\n    const item=stats[i]||{value:\"—\",label:\"—\"};\n    const value=document.getElementById(`data-operation-stat-${i+1}`),label=document.getElementById(`data-operation-label-${i+1}`);\n    if(value)value.textContent=String(item.value??\"—\");if(label)label.textContent=String(item.label||\"—\");\n  }\n}\nfunction dataOperationSetError(message=\"\"){\n  const el=dataOperationElements().error;if(!el)return;el.textContent=message;el.hidden=!message;\n}\nfunction dataOperationOpen(config={}){\n  const el=dataOperationElements();\n  dataOperationState={kind:config.kind||\"\",busy:false,done:false,payload:config.payload||null,scope:config.scope||\"all\",handler:config.handler||null,cancellable:config.cancellable===true,abortController:null,runToken:0};\n  el.title.textContent=config.title||\"資料管理\";el.subtitle.textContent=config.subtitle||\"確認內容後再繼續。\";\n  el.name.textContent=config.name||\"目前旅程\";el.id.textContent=config.id||`tripId · ${currentBackupTripId()||\"—\"}`;\n  dataOperationSetStats(config.stats||[]);\n  el.mode.innerHTML=config.modeHtml||escapeHtmlText(config.mode||\"\");\n  el.warnings.innerHTML=(config.warnings||[]).map(text=>`<div class=\"trip-import-warning\">${escapeHtmlText(text)}</div>`).join(\"\");\n  dataOperationSetError(\"\");\n  if(el.progress){el.progress.hidden=true;const bar=el.progress.querySelector(\"span\");if(bar)bar.style.width=\"0%\";}\n  if(el.secondary){el.secondary.hidden=!config.secondaryHtml;el.secondary.innerHTML=config.secondaryHtml||\"\";}\n  if(el.options){\n    el.options.hidden=!Array.isArray(config.options)||!config.options.length;\n    el.options.innerHTML=(config.options||[]).map(opt=>`<button class=\"data-operation-option${opt.value===dataOperationState.scope?\" is-selected\":\"\"}\" type=\"button\" data-data-operation-scope=\"${escapeHtmlText(opt.value)}\"><span><strong>${escapeHtmlText(opt.label)}</strong><small>${escapeHtmlText(opt.detail||\"\")}</small></span></button>`).join(\"\");\n  }\n  el.cancel.textContent=\"取消\";el.cancel.disabled=false;el.confirm.textContent=config.confirmLabel||\"繼續\";el.confirm.disabled=Boolean(config.confirmDisabled);\n  el.backdrop.classList.add(\"active\");el.backdrop.setAttribute(\"aria-hidden\",\"false\");document.body.classList.add(\"modal-open\");\n}\nfunction dataOperationClose(){\n  if(dataOperationState.busy){\n    if(!dataOperationState.cancellable)return;\n    try{dataOperationState.abortController?.abort();}catch(error){}\n    dataOperationRunToken+=1;\n  }\n  const el=dataOperationElements();el.backdrop?.classList.remove(\"active\");el.backdrop?.setAttribute(\"aria-hidden\",\"true\");document.body.classList.remove(\"modal-open\");\n  dataOperationState={kind:\"\",busy:false,done:false,payload:null,scope:\"all\",handler:null,cancellable:false,abortController:null,runToken:0};\n}\nfunction dataOperationMarkDone({title=\"完成\",detail=\"操作已完成。\"}={}){\n  const el=dataOperationElements();dataOperationState.done=true;dataOperationState.busy=false;\n  el.mode.innerHTML=`<strong>${escapeHtmlText(title)}</strong><br>${escapeHtmlText(detail)}`;\n  el.confirm.textContent=\"完成\";el.confirm.disabled=false;el.cancel.disabled=true;\n  if(el.progress){el.progress.hidden=true;}\n}\nfunction dataOperationSetBusy(busy,detail=\"\"){\n  const el=dataOperationElements();dataOperationState.busy=busy;el.cancel.disabled=busy&&!dataOperationState.cancellable;el.confirm.disabled=busy;\n  if(busy&&detail)el.mode.innerHTML=`<strong>處理中</strong><br>${escapeHtmlText(detail)}`;\n  if(el.progress){el.progress.hidden=!busy;const bar=el.progress.querySelector(\"span\");if(bar)bar.style.width=busy?\"8%\":\"0%\";}\n}\nfunction dataOperationSetStage(title,detail,percent){\n  const el=dataOperationElements();\n  const pct=Math.max(0,Math.min(100,Number(percent)||0));\n  dataOperationState.stage=String(title||\"\");\n  window.__lastDataOperationDiagnostic={kind:dataOperationState.kind||\"\",stage:dataOperationState.stage,detail:String(detail||\"\"),percent:pct,at:Date.now()};\n  if(title||detail)el.mode.innerHTML=`<strong>${escapeHtmlText(title||\"處理中\")}</strong><br>${escapeHtmlText(detail||\"\")}`;\n  if(el.progress){el.progress.hidden=false;const bar=el.progress.querySelector(\"span\");if(bar)bar.style.width=`${pct}%`;}\n}\nfunction dataOperationStageTimeout(stage,timeoutMs){\n  const error=new Error(`${stage}逾時`);\n  error.code=\"data-operation-timeout\";\n  error.stage=stage;\n  error.timeoutMs=timeoutMs;\n  return error;\n}\nasync function dataOperationAwait(promise,{stage=\"處理中\",detail=\"\",percent=18,timeoutMs=12000,abortController=null}={}){\n  dataOperationSetStage(stage,detail,percent);\n  let timer=null;\n  try{\n    return await Promise.race([\n      Promise.resolve(promise),\n      new Promise((_,reject)=>{timer=setTimeout(()=>{try{abortController?.abort();}catch(error){}reject(dataOperationStageTimeout(stage,timeoutMs));},Math.max(1000,timeoutMs));})\n    ]);\n  }finally{if(timer)clearTimeout(timer);}\n}\nfunction dataOperationBaseConfig({kind,title,subtitle,mode,confirmLabel,typeLabel=\"Data\",warnings=[],cancellable=false,handler}={}){\n  const summary=dataOperationCurrentSummary();\n  return {kind,title,subtitle,name:summary.title,id:`tripId · ${summary.tripId||\"—\"}`,stats:[{value:summary.dayCount,label:\"Days\"},{value:summary.itemCount,label:\"Items\"},{value:summary.savedPlaceCount,label:\"Saved\"},{value:typeLabel,label:\"Type\"}],mode,warnings,confirmLabel,cancellable,handler};\n}\nfunction openFullBackupExportOperation(){\n  const gate=refreshFullBackupSyncGate();if(!gate.canBackup)return;\n  dataOperationOpen(dataOperationBaseConfig({kind:\"export-full\",title:\"匯出完整 Backup\",subtitle:\"使用目前已同步嘅 Data + Media 建立完整 Backup Package。\",typeLabel:\"Full\",mode:\"只封裝現有 autosync 已經 Server Confirmed 嘅 canonical data；按 Backup 唔會另外觸發 Firebase 同步。\",warnings:[\"同步中、離線或未能取得 Server confirmation 時，Backup 會保持停用。\"],confirmLabel:\"匯出 Backup\",cancellable:true,handler:async()=>{\n    const liveGate=refreshFullBackupSyncGate();\n    if(!liveGate.canBackup){const error=new Error(liveGate.online?\"Backup state is not yet server confirmed\":\"Device is offline\");error.code=liveGate.online?\"backup-freshness-unconfirmed\":\"backup-freshness-offline\";throw error;}\n    dataOperationSetStage(\"準備匯出\",\"正在釋放上一份 Backup 嘅下載資源。\",8);\n    const operationSignal=dataOperationState.abortController?.signal||null;\n    await prepareForNextBackupExport(operationSignal);\n    const service=await dataOperationAwait(getTripBackupService(),{stage:\"載入備份引擎\",detail:\"正在準備資料管理模組。\",percent:12,timeoutMs:8000});\n    const localPack=await dataOperationAwait(prepareFullBackupLocalExportPack(),{stage:\"準備已同步資料\",detail:\"使用現有 Firebase listeners 已確認嘅 canonical data。\",percent:30,timeoutMs:8000});\n    const frozen=refreshLocalExportCapture(localPack);\n    const result=await service.exportLocalFullBackup(frozen.trip,frozen.expenseSnapshot,{user:frozen.user,role:frozen.role,tripFreshness:currentTripFreshnessSnapshot(),requireArchiveGrade:true});\n    const packaged=await dataOperationAwait(prepareMediaAwareFullBackupPackage(result,{signal:operationSignal,onProgress:progress=>{\n      const stage=String(progress?.stage||\"\");\n      const total=Math.max(0,Number(progress?.total)||0),completed=Math.max(0,Number(progress?.completed)||0);\n      if(stage===\"registry-read\"){dataOperationSetStage(\"確認媒體清單\",\"正在讀取 Backup 所需 media registry。\",48);return;}\n      if(stage===\"registry-ready\"){dataOperationSetStage(\"媒體清單已確認\",total?`準備封裝 ${total} 個媒體項目。`:`目前旅程未有 Firebase Storage media。`,52);return;}\n      if(stage===\"media-download\"){\n        const ratio=total?Math.min(1,completed/total):1;\n        dataOperationSetStage(\"收集媒體\",total?`正在封裝媒體 ${Math.min(total,completed+1)} / ${total}。`:`媒體收集完成。`,54+ratio*20);return;\n      }\n      if(stage===\"zip-start\"){dataOperationSetStage(\"建立 Backup Package\",\"正在建立 ZIP 同完整性 manifest。\",78);return;}\n      if(stage===\"zip-entry\"||stage===\"zip-build\"){\n        const ratio=total?Math.min(1,completed/total):0;\n        dataOperationSetStage(\"建立 Backup Package\",`正在封裝 ZIP ${Math.round(ratio*100)}%。`,80+ratio*12);\n      }\n    }}),{stage:\"建立完整 Backup\",detail:\"正在收集媒體並建立 ZIP。\",percent:48,timeoutMs:90000,abortController:dataOperationState.abortController});\n    dataOperationSetStage(\"準備下載\",\"Server Confirmed Media Backup Package 已建立，準備交畀 iOS。\",94);\n    return {title:\"Backup 已匯出\",detail:`Server Confirmed${packaged.registryStale?\" · Media Last Confirmed\":\"\"} · ${packaged.mediaCount||0} Media · ${result.counts.expenseCount||0} Expenses · ${result.counts.settlementCount||0} Settlements`,download:()=>backupDownloadBlob(packaged.blob,packaged.filename)};\n  }}));\n}\nfunction openTripJsonExportOperation(){\n  dataOperationOpen(dataOperationBaseConfig({kind:\"export-trip\",title:\"匯出 trip.json\",subtitle:\"建立輕量 Portable Trip Data。\",typeLabel:\"JSON\",mode:\"適合分享、AI／人手修改及再次匯入；不包含實際支出交易。\",confirmLabel:\"匯出 trip.json\",handler:async()=>{\n    const service=await dataOperationAwait(getTripBackupService(),{stage:\"載入備份引擎\",detail:\"正在準備 Portable Trip exporter。\",percent:12,timeoutMs:8000});\n    const localPack=await dataOperationAwait(prepareDataManagementLocalExports(),{stage:\"準備本機資料\",detail:\"正在凍結目前已同步嘅 Trip Data。\",percent:32,timeoutMs:22000});\n    const frozen=refreshLocalExportCapture(localPack);\n    const result=service.exportLocalTrip(frozen.trip,{role:frozen.role});\n    dataOperationSetStage(\"建立 trip.json\",\"正在整理 Portable Trip Data。\",78);\n    const prepared=backupPrepareJsonDownload(result.json,result.filename);\n    dataOperationSetStage(\"準備下載\",\"檔案已建立，準備交畀 iOS。\",94);\n    return {title:\"trip.json 已匯出\",detail:`Revision ${result.revision} · ${result.counts.dayCount} Days · ${result.counts.itemCount} Items`,download:()=>backupDownloadBlob(prepared.blob,prepared.filename)};\n  }}));\n}\nfunction openManualSnapshotOperation(){\n  dataOperationOpen(dataOperationBaseConfig({kind:\"snapshot-create\",title:\"建立手動 Snapshot\",subtitle:\"保存目前 Firebase 行程版本，方便日後回復。\",typeLabel:\"Snap\",mode:\"Snapshot 只保存行程版本及設定，不包含實際支出交易。\",confirmLabel:\"建立 Snapshot\",handler:async()=>{\n    const service=await getTripBackupService(),user=await getImportFirebaseUser();\n    const result=await service.createManualSnapshot(currentBackupTripId(),{user});tripBackupBusy=false;\n    return {title:\"Snapshot 已建立\",detail:`Revision ${result.sourceRevision}`};\n  }}));\n}\nfunction openExpenseExcelOperation(){\n  dataOperationOpen(dataOperationBaseConfig({kind:\"expenses-excel\",title:\"匯出支出 Excel\",subtitle:\"建立方便對數、報銷及保存嘅 Excel 報表。\",typeLabel:\"Excel\",mode:\"Excel 係人用報表，不參與 Backup／Restore。\",confirmLabel:\"匯出 Excel\",handler:async()=>{\n    await dataOperationAwait(prepareDataManagementLocalExports(),{stage:\"準備本機資料\",detail:\"正在確認支出、找數及操作記錄已完整同步。\",percent:28,timeoutMs:22000});\n    dataOperationSetStage(\"建立 Excel\",\"使用已同步本機支出資料建立報表。\",56);\n    return await new Promise((resolve,reject)=>{\n      let settled=false;\n      const timer=setTimeout(()=>{if(settled)return;settled=true;window.removeEventListener(\"expense-excel-export-result\",onResult);reject(new Error(\"支出 Excel 匯出逾時，請稍後再試。\"));},12000);\n      const onResult=event=>{\n        if(settled)return;settled=true;clearTimeout(timer);window.removeEventListener(\"expense-excel-export-result\",onResult);\n        if(event?.detail?.ok){\n          const blob=event.detail.blob,filename=event.detail.filename||\"travel-expenses.xlsx\";\n          dataOperationSetStage(\"準備下載\",\"Excel 已建立，準備交畀 iOS。\",94);\n          resolve({title:\"Excel 已匯出\",detail:\"報表已交畀瀏覽器下載。\",download:()=>backupDownloadBlob(blob,filename)});\n        }else reject(new Error(event?.detail?.message||\"匯出 Excel 失敗。\"));\n      };\n      window.addEventListener(\"expense-excel-export-result\",onResult);\n      window.__pendingExpenseUiAction=\"export:excel\";initExpensesViewSafe();window.dispatchEvent(new CustomEvent(\"expense-ui-action\",{detail:{action:\"export:excel\"}}));\n    });\n  }}));\n}\nfunction openSnapshotExportOperation(snapshot){\n  dataOperationOpen({kind:\"snapshot-export\",title:\"匯出此版本\",subtitle:\"將所選 Snapshot 轉成 Portable trip.json。\",name:`${backupFormatDate(snapshot.createdAtIso||snapshot.createdAtMillis)} · ${snapshot.typeLabel||\"Snapshot\"}`,id:`Snapshot · ${snapshot.snapshotId||\"—\"}`,stats:[{value:snapshot.sourceRevision||0,label:\"Revision\"},{value:snapshot.dayCount||0,label:\"Days\"},{value:snapshot.itemCount||0,label:\"Items\"},{value:snapshot.savedPlaceCount||0,label:\"Saved\"}],mode:\"匯出只建立檔案，不會修改目前 Firebase 旅程。\",confirmLabel:\"匯出此版本\",payload:snapshot,handler:async()=>{\n    const service=await dataOperationAwait(getTripBackupService(),{stage:\"載入備份引擎\",detail:\"正在準備 Snapshot exporter。\",percent:12,timeoutMs:8000});\n    await dataOperationAwait(prepareDataManagementLocalExports(),{stage:\"準備本機資料\",detail:\"正在確認版本 payload 已預載。\",percent:32,timeoutMs:22000});\n    const cached=tripBackupSnapshots.find(item=>item.snapshotId===snapshot.snapshotId)||snapshot;\n    const result=service.exportLocalSnapshotTrip(cached);\n    dataOperationSetStage(\"建立版本檔案\",\"正在轉換成 Portable trip.json。\",78);\n    const prepared=backupPrepareJsonDownload(result.json,result.filename);\n    dataOperationSetStage(\"準備下載\",\"檔案已建立，準備交畀 iOS。\",94);\n    return {title:\"版本已匯出\",detail:`Snapshot Revision ${result.sourceRevision}`,download:()=>backupDownloadBlob(prepared.blob,prepared.filename)};\n  }});\n}\nfunction openSnapshotRestoreOperation(snapshot){\n  dataOperationOpen({kind:\"snapshot-restore\",title:`還原 Revision ${snapshot.sourceRevision||0}`,subtitle:\"系統會先保存目前版本，再還原所選 Snapshot。\",name:`${backupFormatDate(snapshot.createdAtIso||snapshot.createdAtMillis)} · ${snapshot.typeLabel||\"Snapshot\"}`,id:`Snapshot · ${snapshot.snapshotId||\"—\"}`,stats:[{value:snapshot.sourceRevision||0,label:\"Revision\"},{value:snapshot.dayCount||0,label:\"Days\"},{value:snapshot.itemCount||0,label:\"Items\"},{value:snapshot.savedPlaceCount||0,label:\"Saved\"}],mode:\"還原只影響行程內容及設定；成員權限及實際支出交易不會被刪除。\",warnings:[\"開始前會自動建立還原前安全 Snapshot。\"],confirmLabel:\"開始還原\",payload:snapshot,handler:async()=>{\n    const service=await getTripBackupService(),user=await getImportFirebaseUser();\n    appProgressOpen({kicker:\"SNAPSHOT RESTORE\",title:`還原 Revision ${snapshot.sourceRevision||0}`,subtitle:\"系統會先保存目前版本，再還原所選 Snapshot。完成前請保持 App 開啟。\",percent:4,stageTitle:\"準備還原\",stageDetail:\"正在讀取目前 Firebase 旅程及 Snapshot。\",icon:\"↶\"});\n    try{\n      const result=await service.restoreTripSnapshot(currentBackupTripId(),snapshot.snapshotId,{user,onProgress:progress=>appProgressUpdate(restoreProgressPresentation(progress))});\n      const liveSynced=result.tripId===tripLoaderActiveId?await waitForTripLoaderRevision(result.tripId,result.revision,7000):true;\n      appProgressFinish({title:\"還原完成\",detail:liveSynced?`Revision ${result.revision} 已建立，行程畫面已同步。`:`Revision ${result.revision} 已建立；畫面仍在背景同步。`});\n      tripBackupBusy=false;setTimeout(loadTripBackupPage,120);\n      return {title:\"Snapshot 還原完成\",detail:`新 Revision ${result.revision}`};\n    }catch(error){appProgressFail(backupFriendlyError(error));throw error;}\n  }});\n}\nfunction openFullBackupRestoreOperation(selected){\n  const inspection=selected.inspection||{},c=inspection.counts||{};\n  dataOperationOpen({kind:\"restore-full\",title:\"還原完整 Backup\",subtitle:\"Backup 已驗證。選擇還原範圍，再寫入 Firebase。\",name:selected.raw?.data?.portableTrip?.meta?.title||inspection.tripId||\"Full Backup\",id:`tripId · ${inspection.tripId||\"—\"}`,stats:[{value:c.dayCount||0,label:\"Days\"},{value:c.itemCount||0,label:\"Items\"},{value:c.savedPlaceCount||0,label:\"Saved\"},{value:c.expenseCount||0,label:\"Expenses\"}],mode:`Backup v${inspection.backupVersion||1} · ${inspection.mediaIncluded?`${c.mediaCount||0} Media · ZIP Package`:\"Data Only · 不含媒體\"}`,secondaryHtml:`<span>找數</span><strong>${escapeHtmlText(String(c.settlementCount||0))} Settlements</strong>`,warnings:[\"原 Trip 內 Audit Log 採 append-only；還原不會刪除或倒退現有操作記錄。\",...(inspection.mediaIncluded?[\"完整／行程還原會同時還原 Storage media，並移除 Backup 以外嘅舊 media；只還原支出則完全唔郁媒體。\"]:[])],options:[{value:\"all\",label:\"完整還原\",detail:\"還原行程、媒體及支出資料；成員權限不變。\"},{value:\"trip\",label:\"只還原行程\",detail:\"還原行程同媒體；保留目前所有支出及找數紀錄。\"},{value:\"expenses\",label:\"只還原支出\",detail:\"保留目前行程、媒體、收藏及一般顯示設定。\"}],scope:\"all\",confirmLabel:\"開始完整還原\",payload:selected,handler:async()=>{\n    const scope=dataOperationState.scope,label=scope===\"all\"?\"完整還原\":scope===\"trip\"?\"只還原行程\":\"只還原支出\";\n    const service=await getTripBackupService(),user=await getImportFirebaseUser();\n    if((scope===\"all\"||scope===\"trip\")&&inspection.mediaIncluded&&!selected.package){const error=new Error(\"Media Backup package is required\");error.code=\"backup-package-required\";throw error;}\n    appProgressOpen({kicker:\"FULL BACKUP\",title:label,subtitle:inspection.mediaIncluded&&scope!==\"expenses\"?\"正在安全還原 Firebase Data + Storage Media。完成前請保持 App 開啟。\":\"正在安全還原 Firebase Data。完成前請保持 App 開啟。\",percent:5,stageTitle:\"準備還原\",stageDetail:\"正在驗證 Backup 同目前 Trip。\",icon:\"↶\"});\n    try{\n      const hasMedia=inspection.mediaIncluded&&(scope===\"all\"||scope===\"trip\");\n      const result=await service.restoreFullBackup(selected.raw,{scope,user,onProgress:progress=>{const view=restoreProgressPresentation(progress);appProgressUpdate({...view,percent:hasMedia?Math.max(5,Math.min(74,5+(Number(view.percent)||0)*.69)):view.percent});}});\n      let mediaResult=null;\n      if(hasMedia){\n        const packageService=await getTripBackupPackageService();\n        appProgressUpdate({percent:76,stageTitle:\"還原 Storage Media\",stageDetail:\"正在驗證並上載 Backup 媒體。\",icon:\"↶\"});\n        mediaResult=await packageService.restoreFullBackupPackageMedia(selected.raw,selected.package,{user,reconcile:true,onProgress:progress=>{const total=Math.max(1,Number(progress?.total)||1),completed=Math.max(0,Number(progress?.completed)||0);appProgressUpdate({percent:76+Math.min(21,(completed/total)*21),stageTitle:\"還原 Storage Media\",stageDetail:`已處理 ${Math.min(total,completed+1)} / ${total} 個媒體項目。`,icon:\"↶\"});}});\n      }\n      if(result.scope!==\"expenses\"&&result.tripId===tripLoaderActiveId)await waitForTripLoaderRevision(result.tripId,result.revision,7000);\n      appProgressFinish({title:`${label}完成`,detail:mediaResult?`Firebase Data 同 ${mediaResult.restored||0} 個 Media 已完成還原。成員權限及目前 Audit Log 不受回滾。`:\"Firebase Data 已完成還原。成員權限及目前 Audit Log 不受回滾。\"});\n      tripBackupBusy=false;\n      return {title:`${label}完成`,detail:mediaResult?`Data + ${mediaResult.restored||0} Media 已完成還原；Audit Log 保持 append-only。`:\"Firebase Data 已完成還原；Audit Log 保持 append-only。\"};\n    }catch(error){appProgressFail(backupFriendlyError(error));throw error;}\n  }});\n}\n\nasync function openFullBackupRestoreAsNewOperation(selected,{alternateIdRequired=false}={}){\n  const inspection=selected.inspection||{},c=inspection.counts||{},originalTripId=String(inspection.tripId||\"\").trim();\n  co");
