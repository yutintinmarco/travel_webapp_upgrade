window.__travelRuntimeParts.push("{(-5*eased).toFixed(2)}px,0)`;\n    if(largeHeaderSmallEl)largeHeaderSmallEl.style.opacity=String(1-progress*0.70);\n    if(largeHeaderSubRowEl)largeHeaderSubRowEl.style.opacity=String(1-progress*0.88);\n  }\n\n  if(dayTabsChromeEl)dayTabsChromeEl.classList.toggle(\"is-stuck\",isStuck);\n}\nfunction scheduleCollapseUpdate(){\n  if(collapseRaf) return;\n  collapseRaf=requestAnimationFrame(updateCollapsingChrome);\n}\n\nfunction updateBottomNavIndicator(){\n  const indicator=document.getElementById(\"bottom-nav-indicator\");\n  const activeButton=bottomNav?.querySelector(\".bottom-nav-item.active:not([style*='display:none'])\");\n  if(!bottomNav||!indicator||!activeButton){if(indicator)indicator.style.opacity=\"0\";return;}\n  const navRect=bottomNav.getBoundingClientRect();\n  const buttonRect=activeButton.getBoundingClientRect();\n  indicator.style.opacity=\"1\";\n  indicator.style.width=`${buttonRect.width}px`;\n  indicator.style.transform=`translate3d(${buttonRect.left-navRect.left-6}px,0,0)`;\n}\n\nfunction animateView(viewEl){\n  // Intentionally no parent animation: iOS Safari briefly drops backdrop-filter\n  // layers when a newly displayed parent is transformed or faded.\n  if(!viewEl) return;\n  viewEl.classList.remove(\"view-entering\");\n}\n\nfunction setView(v,{restoreScroll=true}={}){\n  if(!viewElements[v]) v=\"itinerary\";\n  if(tripEditSession&&v!==\"itinerary\"){window.alert(\"請先儲存或取消今次行程編輯。\");return;}\n  if(scrollShell) viewScrollPositions[currentAppView]=scrollShell.scrollTop;\n  currentAppView=v;\n  try{sessionStorage.setItem(\"trip_current_view\",v);}catch(e){}\n\n  Object.entries(viewButtons).forEach(([key,button])=>button?.classList.toggle(\"active\",key===v));\n  Object.entries(viewElements).forEach(([key,view])=>{\n    if(!view)return;\n    const active=key===v;\n    view.style.display=active?\"block\":\"none\";\n    view.classList.toggle(\"active-view\",active);\n  });\n\n  placeAppVersionNote(v);\n\n  document.body.classList.remove(\"view-itinerary\",\"view-snacks\",\"view-info\",\"view-expenses\",\"view-profile\");\n  document.body.classList.add(`view-${v}`);\n  document.body.classList.toggle(\"expenses-view-active\",v===\"expenses\");\n  if(v===\"expenses\"){\n    const visibleTripId=dataTripId(window.tripData);\n    const mountedExpenseTrip=String(window.__expensesModuleTripId||\"\");\n    if(mountedExpenseTrip&&visibleTripId&&mountedExpenseTrip!==visibleTripId){\n      // The old Expenses module intentionally stays suspended after a Trip switch.\n      // Reload only when the user next opens 支出, by which time the replacement\n      // Trip is already visible and cached. This avoids using reload as a routing\n      // mechanism during revoke while preserving absolute write isolation.\n      try{localStorage.setItem(\"travel_active_trip_id\",visibleTripId);}catch(error){}\n      Promise.race([Promise.resolve(persistInstantTripCache(window.tripData)),new Promise(resolve=>setTimeout(resolve,420))])\n        .finally(()=>location.reload());\n      return;\n    }\n    initExpensesViewSafe();\n  }\n\n  updateLargeHeaderForView();\n  updateCompactHeader();\n  tripEditSyncHeader();\n  animateView(viewElements[v]);\n  requestAnimationFrame(()=>{\n    if(scrollShell){\n      const nextTop=restoreScroll?(viewScrollPositions[v]||0):0;\n      scrollShell.scrollTo({top:nextTop,behavior:\"auto\"});\n    }\n    updateBottomNavIndicator();\n    updateCollapsingChrome();\n    updateNowIndicator();\n  });\n}\n\nbti.addEventListener(\"click\",()=>setView(\"itinerary\"));\nbts.addEventListener(\"click\",()=>setView(\"snacks\"));\nbtinfo.addEventListener(\"click\",()=>setView(\"info\"));\nbtexp.addEventListener(\"click\",()=>setView(\"expenses\"));\nbtprofile.addEventListener(\"click\",()=>setView(\"profile\"));\n\nconst signedOutProfileIcon = `<svg viewBox=\"0 0 24 24\" fill=\"none\"><path d=\"M12 12.2a4.1 4.1 0 1 0 0-8.2 4.1 4.1 0 0 0 0 8.2Z\" stroke=\"currentColor\" stroke-width=\"1.8\"/><path d=\"M4.8 20c.65-3.55 3.28-5.55 7.2-5.55s6.55 2 7.2 5.55\" stroke=\"currentColor\" stroke-width=\"1.8\" stroke-linecap=\"round\"/></svg>`;\nfunction profileInitials(user){\n  const source=String(user?.displayName||user?.email||\"U\").trim();\n  return (source.slice(0,1)||\"U\").toUpperCase();\n}\nfunction renderProfileAuthState(user){\n  const avatar=document.getElementById(\"profile-avatar\");\n  const name=document.getElementById(\"profile-user-name\");\n  const email=document.getElementById(\"profile-user-email\");\n  const status=document.getElementById(\"profile-auth-status\");\n  const accountButton=document.getElementById(\"my-account-btn\");\n  const signoutCard=document.getElementById(\"profile-signout-card\");\n  const actionTitle=document.getElementById(\"profile-account-action-title\");\n  const actionSubtitle=document.getElementById(\"profile-account-action-subtitle\");\n  if(!avatar||!name||!email||!status) return;\n\n  avatar.replaceChildren();\n  avatar.classList.toggle(\"is-signed-in\",!!user);\n  if(accountButton) accountButton.hidden=!!user;\n  if(signoutCard) signoutCard.hidden=!user;\n  if(!user){\n    avatar.innerHTML=signedOutProfileIcon;\n    name.textContent=\"尚未登入\";\n    email.textContent=\"使用 Google 帳戶登入後顯示資料\";\n    status.textContent=\"未登入\";\n    if(actionTitle) actionTitle.textContent=\"登入 Google 帳戶\";\n    if(actionSubtitle) actionSubtitle.textContent=\"登入後顯示你參與嘅旅程及目前身份\";\n    return;\n  }\n\n  const fallback=()=>{\n    avatar.replaceChildren();\n    avatar.textContent=profileInitials(user);\n  };\n  if(user.photoURL){\n    const img=document.createElement(\"img\");\n    img.alt=\"\";\n    img.src=user.photoURL;\n    img.referrerPolicy=\"no-referrer\";\n    img.addEventListener(\"error\",fallback,{once:true});\n    avatar.appendChild(img);\n  }else{\n    fallback();\n  }\n  name.textContent=user.displayName||\"Google 使用者\";\n  email.textContent=user.email||\"已連接 Google 帳戶\";\n  status.textContent=\"已登入\";\n}\n\nlet appAdminServicePromise=null;\nlet appAdminState={status:\"idle\",enabled:false,error:null};\nfunction getAppAdminService(){\n  if(!appAdminServicePromise)appAdminServicePromise=import(`./assets/js/app-admin-service.js?build=${APP_VERSION}`).catch(error=>{appAdminServicePromise=null;throw error;});\n  return appAdminServicePromise;\n}\nfunction renderAppAdminControls(){\n  const profileButton=document.getElementById(\"system-admin-open-btn\");\n  const entryCard=document.getElementById(\"entry-system-admin-card\");\n  const gateway=document.getElementById(\"app-entry-gateway\");\n  const enabled=appAdminState.status===\"ready\"&&appAdminState.enabled===true&&!!window.__appAuthUser?.uid;\n  if(profileButton)profileButton.hidden=!enabled;\n  if(entryCard)entryCard.hidden=!(enabled&&gateway?.dataset?.entryState===\"lobby\");\n}\nasync function refreshAppAdminEntitlement(user,{force=false}={}){\n  if(!user?.uid){appAdminState={status:\"idle\",enabled:false,error:null};renderAppAdminControls();return appAdminState;}\n  appAdminState={status:\"loading\",enabled:false,error:null};renderAppAdminControls();\n  try{\n    const service=await getAppAdminService();\n    const result=await service.getAppAdminEntitlement(user,{force});\n    appAdminState={status:\"ready\",enabled:result?.enabled===true,error:null,source:result?.source||\"\"};\n  }catch(error){\n    if(error?.code!==\"permission-denied\")console.warn(\"App Admin entitlement\",error);\n    appAdminState={status:\"ready\",enabled:false,error};\n  }\n  renderAppAdminControls();\n  return appAdminState;\n}\n\nlet currentProfileAccess=null;\nfunction renderProfileAccessState(access){\n  currentProfileAccess=access||null;\n  const status=document.getElementById(\"profile-auth-status\");\n  if(!status) return;\n  if(!window.__appAuthUser){ status.textContent=\"未登入\"; return; }\n  if(access?.roleLabel){ status.textContent=access.roleLabel; return; }\n  status.textContent=access?.source===\"no-membership\"?\"未有角色\":\"已登入\";\n}\n\nlet globalAuthServicePromise=null;\nlet globalAuthInitStarted=false;\nfunction getGlobalAuthService(){\n  if(!globalAuthServicePromise) globalAuthServicePromise=import(\"./assets/js/auth-service.js\");\n  return globalAuthServicePromise;\n}\nfunction initGlobalAuthSafe(){\n  if(globalAuthInitStarted)return;globalAuthInitStarted=true;\n  getGlobalAuthService()\n    .then(({subscribeAuthState})=>{\n      subscribeAuthState((user,safeUser)=>{\n        perfLiteMark(\"authReady\");\n        const displayUser=safeUser||user||null;\n        appEntryAuthResolved=true;\n        window.__appAuthUser=displayUser;\n        reconcileWarmRefreshIdentity(displayUser);\n        renderProfileAuthState(displayUser);\n        renderEntryAuthState(displayUser);\n        renderProfileAccessState(window.__appTripAccess||currentProfileAccess);\n        tripEditSyncHeader();\n        refreshAppEntryState();\n        schedulePendingPermanentDeleteResume(displayUser);\n      });\n    })\n    .catch(error=>console.error(\"Global auth service load error\",error));\n}\nwindow.addEventListener(\"app-auth-state\",event=>{\n  appEntryAuthResolved=true;\n  window.__appAuthUser=event.detail?.user||null;\n  reconcileWarmRefreshIdentity(window.__appAuthUser);\n  renderProfileAuthState(window.__appAuthUser);\n  renderEntryAuthState(window.__appAuthUser);\n  renderProfileAccessState(window.__appTripAccess||currentProfileAccess);\n  tripEditSyncHeader();\n  refreshAppAdminEntitlement(window.__appAuthUser).catch(()=>{});\n  refreshAppEntryState();\n  schedulePendingPermanentDeleteResume(window.__appAuthUser);\n  refreshAppDataDiagnostics();\n});\nwindow.addEventListener(\"app-trip-access\",event=>{\n  const access=event.detail||null;\n  if(access?.serverConfirmed===true&&String(access?.tripId||\"\")===String(appEntryResumeLatch.tripId||\"\")){\n    if(access?.role)maybeReleaseAppEntryResumeLatch();\n    else{appEntryOptimisticTripId=\"\";appEntryOptimisticUid=\"\";clearAppEntryResumeLatch();endWarmRefreshGate();}\n  }\n  renderProfileAccessState(access);tripEditSyncHeader();refreshAppEntryState();refreshAppDataDiagnostics();\n});\nrenderProfileAuthState(null);\nrenderEntryAuthState(null);\n\n// =========================================================================\n// Phase 2G.1 · Root Entry / Access Gate\n// =========================================================================\nlet appEntryServicePromise=null;\nlet appEntryService=null;\nlet appEntryAuthResolved=false;\nlet appEntryState=\"pending\";\nlet appEntryCreatorState={status:\"idle\",enabled:false,error:null};\nlet appEntryTargetInviteId=(()=>{try{const direct=String(new URL(location.href).searchParams.get(\"invite\")||\"\").trim();if(direct)return direct;return String(sessionStorage.getItem(\"travel_entry_invite_return_v1\")||\"\").trim();}catch(error){return\"\";}})();\nlet appEntryRegistryBackfillBusy=false;\nlet appEntryForcedTripId=\"\";\n// v7.8.0.5 · Speed-first travel boot. After Firebase Auth confirms the same\n// Google UID, a trusted user-bound IndexedDB Trip cache may render immediately\n// while membership is revalidated in the background. A server-confirmed revoke\n// clears this optimistic window and ejects the Trip immediately.\nlet appEntryOptimisticTripId=\"\";\nlet appEntryOptimisticUid=\"\";\nconst APP_ENTRY_LOGIN_HANDOFF_KEY=\"travel_entry_login_handoff_v1\";\nlet appEntryLoginHandoff=(()=>{try{return sessionStorage.getItem(APP_ENTRY_LOGIN_HANDOFF_KEY)===\"1\";}catch(error){return false;}})();\nlet appEntryWarmRefresh=window.__entryWarmRefreshCandidate===true;\nconst appEntryWarmRefreshUid=(()=>{try{return String(localStorage.getItem(\"travel_last_auth_uid\")||\"\").trim();}catch(error){return\"\";}})();\n// v7.8.0.5 · Once a valid user-bound local Trip has been selected for Fast\n// Resume, keep that workspace latched on screen while Firebase Auth, catalog and\n// realtime listeners reconnect. This prevents the Entry Gateway from briefly\n// replacing an already-visible Trip during ordinary launch / refresh.\nlet appEntryResumeLatch={tripId:\"\",uid:\"\"};\nfunction setAppEntryResumeLatch(tripIdInput,uidInput){\n  const tripId=String(tripIdInput||\"\").trim(),uid=String(uidInput||\"\").trim();\n  appEntryResumeLatch=tripId&&uid?{tripId,uid}:{tripId:\"\",uid:\"\"};\n}\nfunction clearAppEntryResumeLatch(){appEntryResumeLatch={tripId:\"\",uid:\"\"};}\n// v7.8.1.3 · Revoke / Trip-switch handoff. Keep the already-rendered Workspace\n// visible while a replacement Trip is selected and hydrated. This intentionally\n// favours travel UX continuity over hiding a just-revoked cached itinerary for\n// the few seconds required to hand off to another Trip. Firebase writes remain\n// protected by Rules and the revoked listener is stopped immediately.\nlet appEntryTripSwitchHandoff={fromTripId:\"\",toTripId:\"\",uid:\"\"};\nfunction setAppEntryTripSwitchHandoff(fromTripIdInput,toTripIdInput,uidInput){\n  const fromTripId=String(fromTripIdInput||\"\").trim(),toTripId=String(toTripIdInput||\"\").trim(),uid=String(uidInput||\"\").trim();\n  appEntryTripSwitchHandoff=fromTripId&&toTripId&&uid?{fromTripId,toTripId,uid}:{fromTripId:\"\",toTripId:\"\",uid:\"\"};\n}\nfunction clearAppEntryTripSwitchHandoff(){appEntryTripSwitchHandoff={fromTripId:\"\",toTripId:\"\",uid:\"\"};}\nfunction appEntryTripSwitchHandoffKeepsWorkspace(){\n  const handoff=appEntryTripSwitchHandoff||{};\n  if(!handoff.fromTripId||!handoff.toTripId||!handoff.uid)return false;\n  const liveUid=String(window.__appAuthUser?.uid||tripLibraryCurrentUser?.uid||\"\").trim();\n  if(liveUid&&liveUid!==handoff.uid)return false;\n  const visibleId=dataTripId(window.tripData);\n  return visibleId===handoff.fromTripId||visibleId===handoff.toTripId;\n}\nfunction appEntryResumeLatchMatchesVisible(){\n  const tripId=String(appEntryResumeLatch.tripId||\"\").trim(),uid=String(appEntryResumeLatch.uid||\"\").trim();\n  if(!tripId||!uid)return false;\n  const visibleId=dataTripId(window.tripData);\n  if(visibleId&&visibleId!==tripId)return false;\n  const liveUid=String(window.__appAuthUser?.uid||\"\").trim();\n  return !liveUid||liveUid===uid;\n}\nfunction maybeReleaseAppEntryResumeLatch(){\n  const tripId=String(appEntryResumeLatch.tripId||\"\").trim();\n  if(!tripId)return false;\n  const access=window.__appTripAccess||currentProfileAccess||null;\n  const visibleId=dataTripId(window.tripData);\n  const loaderReady=tripLoaderState?.status===\"ready\"&&tripLoaderState?.source!==\"local\"&&!!visibleId&&visibleId===tripId;\n  const accessReady=access?.serverConfirmed===true&&!!access?.role&&String(access?.tripId||\"\")===tripId;\n  if(!loaderReady||!accessReady)return false;\n  clearAppEntryResumeLatch();endWarmRefreshGate();\n  return true;\n}\nfunction setAppEntryLoginHandoff(active){\n  appEntryLoginHandoff=!!active;\n  try{if(appEntryLoginHandoff)sessionStorage.setItem(APP_ENTRY_LOGIN_HANDOFF_KEY,\"1\");else sessionStorage.removeItem(APP_ENTRY_LOGIN_HANDOFF_KEY);}catch(error){}\n  const gateway=document.getElementById(\"app-entry-gateway\");if(gateway)gateway.dataset.loginHandoff=appEntryLoginHandoff?\"true\":\"false\";\n}\nfunction endWarmRefreshGate(){\n  if(!appEntryWarmRefresh)return;\n  appEntryWarmRefresh=false;\n  document.documentElement.classList.remove(\"app-entry-refresh-warm\");\n}\nfunction reconcileWarmRefreshIdentity(user){\n  if(!appEntryWarmRefresh&&!appEntryResumeLatch.uid)return;\n  const uid=String(user?.uid||\"\").trim();\n  const expectedUid=String(appEntryResumeLatch.uid||appEntryWarmRefreshUid||\"\").trim();\n  if(!uid||!expectedUid||uid!==expectedUid){\n    appEntryOptimisticTripId=\"\";appEntryOptimisticUid=\"\";clearAppEntryResumeLatch();clearAppEntryTripSwitchHandoff();endWarmRefreshGate();\n    return;\n  }\n  // Identity matched the remembered cache owner. The normal body state now owns\n  // visibility, so the early HTML warm class can retire without showing Gateway.\n  endWarmRefreshGate();\n}\n\nfunction getAppEntryService(){\n  if(!appEntryServicePromise)appEntryServicePromise=import(`./assets/js/app-entry-service.js?build=${APP_VERSION}`).then(service=>{appEntryService=service;appEntryTargetInviteId=appEntryTargetInviteId||service.inviteIdFromLocation?.()||\"\";return service;});\n  return appEntryServicePromise;\n}\nfunction entryUserInitials(user){return profileInitials(user);}\nfunction renderEntryAuthState(user){\n  const avatar=document.getElementById(\"entry-avatar\"),name=document.getElementById(\"entry-user-name\"),email=document.getElementById(\"entry-user-email\"),status=document.getElementById(\"entry-auth-status\"),account=document.getElementById(\"entry-account-btn\"),signout=document.getElementById(\"entry-signout-card\");\n  if(!avatar||!name||!email||!status)return;\n  avatar.replaceChildren();avatar.classList.toggle(\"is-signed-in\",!!user);\n  if(account)account.hidden=!!user;if(signout)signout.hidden=!user;\n  if(!user){avatar.innerHTML=signedOutProfileIcon;name.textContent=\"尚未登入\";email.textContent=\"使用 Google 帳戶登入後顯示你有權使用嘅旅程\";status.textContent=\"未登入\";return;}\n  const fallback=()=>{avatar.replaceChildren();avatar.textContent=entryUserInitials(user);};\n  if(user.photoURL){const img=document.createElement(\"img\");img.alt=\"\";img.src=user.photoURL;img.referrerPolicy=\"no-referrer\";img.addEventListener(\"error\",fallback,{once:true});avatar.appendChild(img);}else fallback();\n  name.textContent=user.displayName||\"Google 使用者\";email.textContent=user.email||\"已連接 Google 帳戶\";status.textContent=\"已登入\";\n}\nfunction entryInviteRowsMarkup(rows,{targetId=\"\"}={}){\n  const target=String(targetId||\"\").trim();\n  return rows.map(invite=>`<div class=\"trip-invite-row${invite.inviteId===target?\" is-target-invite\":\"\"}\">\n    <span class=\"trip-member-avatar\"><span>✉️</span></span>\n    <span class=\"trip-member-copy\">\n      <strong>${tripMemberEsc(invite.tripTitle||invite.tripId)}</strong>\n      <small>${tripMemberEsc(invite.tripDateRange||invite.tripId)}</small>\n      <span class=\"trip-member-meta\"><span class=\"trip-member-role-pill\">${tripMemberEsc(tripLibraryRoleLabel(invite.role)||invite.role)}</span>${invite.invitedByName?`<span class=\"trip-library-pill\">由 ${tripMemberEsc(invite.invitedByName)} 邀請</span>`:\"\"}</span>\n    </span>\n    <span class=\"trip-invite-actions\">\n      <button class=\"trip-invite-action is-decline\" type=\"button\" data-trip-invite-decline=\"${tripMemberEsc(invite.inviteId)}\">拒絕</button>\n      <button class=\"trip-invite-action is-accept\" type=\"button\" data-trip-invite-accept=\"${tripMemberEsc(invite.inviteId)}\">加入</button>\n    </span>\n  </div>`).join(\"\");\n}\nfunction renderEntryInvites(){\n  const card=document.getElementById(\"entry-invite-card\"),list=document.getElementById(\"entry-invite-list\"),count=document.getElementById(\"entry-invite-count\"),status=document.getElementById(\"entry-invite-status\"),targetNote=document.getElementById(\"entry-invite-target-note\");\n  if(!card||!list||!count||!status)return;\n  const rows=Array.isArray(tripMyPendingInvites)?[...tripMyPendingInvites]:[];\n  const target=String(appEntryTargetInviteId||\"\").trim();\n  rows.sort((a,b)=>(a.inviteId===target?-1:0)-(b.inviteId===target?-1:0));\n  const show=!!tripLibraryCurrentUser&&(rows.length>0||!!target);\n  card.hidden=!show;count.textContent=String(rows.length);list.innerHTML=entryInviteRowsMarkup(rows,{targetId:target});\n  if(rows.length)status.textContent=\"邀請綁定 Google email；加入後會直接進入你獲授權嘅 Firebase Trip。\";\n  else status.textContent=target?\"正在確認指定旅程邀請…\":\"暫時未有待接受邀請。\";\n  if(targetNote){\n    const found=rows.some(invite=>invite.inviteId===target);\n    targetNote.hidden=!target||found||tripMyInviteState.status===\"loading\";\n    targetNote.textContent=target&&!found&&tripMyInviteState.status===\"ready\"?\"指定邀請可能已接受、已取消，或者唔屬於目前登入嘅 Google 帳戶。\":\"\";\n  }\n}\nfunction entryWorkspaceVisualReady(service){\n  if(!service||appEntryState===\"login\")return false;\n  // Returning users may keep the local workspace visible while Auth / Firestore\n  // reconnect. The latch is user-bound and is cleared immediately if Auth later\n  // resolves to a different / signed-out account or Firebase explicitly revokes it.\n  if(!appEntryTargetInviteId&&appEntryResumeLatchMatchesVisible())return true;\n  if(!window.__appAuthUser?.uid)return false;\n  const access=window.__appTripAccess||currentProfileAccess||null;\n  const online=navigator.onLine!==false;\n  const catalog=tripLibraryCloudState||{};\n  const allowed=new Set((catalog.trips||[]).filter(trip=>trip?.archived!==true).map(trip=>String(trip.tripId||\"\").trim()).filter(Boolean));\n  const catalogReady=service.catalogIsAuthoritative?.(catalog,{online})===true;\n  const accessTripId=String(access?.tripId||\"\").trim();\n  const accessTripActive=!catalogReady||(catalog.trips||[]).some(trip=>String(trip?.tripId||\"\")===accessTripId&&trip?.archived!==true);\n  if(service.accessIsVerified?.(access,{online})&&accessTripId&&accessTripActive)allowed.add(accessTripId);\n  const visibleId=dataTripId(window.tripData),forced=String(appEntryForcedTripId||\"\").trim();\n  if(forced&&visibleId!==forced)return false;\n  const optimistic=!!visibleId\n    && visibleId===String(appEntryOptimisticTripId||\"\")\n    && String(window.__appAuthUser?.uid||\"\")===String(appEntryOptimisticUid||\"\")\n    && tripLoaderState?.status===\"ready\"\n    && [\"instant-cache\",\"cache\",\"server\"].includes(String(tripLoaderState?.source||\"\"));\n  if(optimistic)return true;\n  const ready=tripLoaderState?.status===\"ready\"&&tripLoaderState?.source!==\"local\"&&!!visibleId&&allowed.has(visibleId);\n  if(ready&&forced===visibleId)appEntryForcedTripId=\"\";\n  return ready;\n}\nfunction applyAppEntryState(nextState){\n  const gateway=document.getElementById(\"app-entry-gateway\"),title=document.getElementById(\"entry-state-title\"),subtitle=document.getElementById(\"entry-state-subtitle\"),pill=document.getElementById(\"entry-state-pill\"),icon=document.getElementById(\"entry-state-icon\"),creator=document.getElementById(\"entry-creator-card\"),openingText=document.getElementById(\"entry-login-opening-text\");\n  appEntryState=String(nextState||\"pending\");\n  if([\"workspace\",\"lobby\",\"error\"].includes(appEntryState)&&appEntryLoginHandoff)setAppEntryLoginHandoff(false);\n  if(gateway){gateway.dataset.entryState=appEntryState;gateway.dataset.loginHandoff=appEntryLoginHandoff?\"true\":\"false\";gateway.setAttribute(\"aria-busy\",[\"pending\",\"resolving\"].includes(appEntryState)?\"true\":\"false\");}\n  if(openingText)openingText.textContent=appEntryLoginHandoff&&appEntryState===\"resolving\"?\"正在開啟旅程…\":\"正在開啟 Travel App…\";\n  const gated=appEntryState!==\"workspace\";\n  document.body.classList.toggle(\"app-entry-gated\",gated);document.body.classList.toggle(\"app-entry-workspace\",!gated);document.body.classList.toggle(\"app-entry-pending\",[\"pending\",\"resolving\"].includes(appEntryState));\n  if(appEntryState===\"pending\"){if(title)title.textContent=\"正在確認帳戶\";if(subtitle)subtitle.textContent=\"正在讀取 Firebase Authentication 狀態。\";if(pill)pill.textContent=\"確認中\";}\n  else if(appEntryState===\"resolving\"){if(title)title.textContent=\"正在確認旅程存取權\";if(subtitle)subtitle.textContent=\"安全確認完成後會直接進入你上次使用嘅旅程。\";if(pill)pill.textContent=navigator.onLine===false?\"離線確認\":\"確認中\";}\n  else if(appEntryState===\"lobby\"){\n    const targeted=!!appEntryTargetInviteId&&tripMyPendingInvites.some(invite=>invite.inviteId===appEntryTargetInviteId);\n    const archivedCount=(tripLibraryCloudState.trips||[]).filter(trip=>trip?.archived===true).length;\n    if(title)title.textContent=targeted?\"收到旅程邀請\":(archivedCount?\"旅程已封存\":\"暫時未有旅程\");\n    if(subtitle)subtitle.textContent=targeted?\"確認邀請後會直接進入指定旅程。\":(archivedCount?\"你仍然保留旅程存取權；由下方還原後可以重新進入 Workspace。\":\"收到旅程邀請後可以喺呢度加入；未獲授權前唔會顯示 Trip workspace。\");\n    if(pill)pill.textContent=targeted?\"待確認\":(archivedCount?\"個人封存\":\"等待邀請\");\n  }\n  else if(appEntryState===\"error\"){if(title)title.textContent=\"暫時未能確認旅程\";if(subtitle)subtitle.textContent=tripLibraryCloudState.status===\"index-required\"?\"Firestore index 尚未完成，請先部署今版所需設定。\":\"Firebase 存取檢查未完成；現有資料冇被修改。\";if(pill)pill.textContent=\"需處理\";}\n  if(icon){icon.innerHTML=[\"pending\",\"resolving\"].includes(appEntryState)?'<span class=\"trip-boot-spinner\" aria-hidden=\"true\"></span>':(appEntryState===\"lobby\"?\"🧳\":appEntryState===\"error\"?\"!\":\"✓\");}\n  if(creator)creator.hidden=!(appEntryState===\"lobby\"&&!appEntryTargetInviteId&&!!window.__appAuthUser?.uid&&appEntryCreatorState.status===\"ready\"&&appEntryCreatorState.enabled===true);\n  renderAppAdminControls();\n  renderEntryInvites();\n  renderEntryArchivedTrips();\n}\nasync function refreshAppEntryState(){\n  let service=appEntryService;\n  if(!service){try{service=await getAppEntryService();}catch(error){console.error(\"Entry service\",error);applyAppEntryState(\"error\");return;}}\n  const online=navigator.onLine!==false,user=window.__appAuthUser||null;\n  let next=service.deriveAppEntryState?.({authResolved:appEntryAuthResolved,user,access:window.__appTripAccess||currentProfileAccess||null,catalog:tripLibraryCloudState||null,online})||\"pending\";\n  if(user?.uid&&appEntryTargetInviteId){\n    const targetPending=tripMyPendingInvites.some(invite=>invite.inviteId===appEntryTargetInviteId);\n    if(targetPending)next=\"lobby\";\n    else if([\"idle\",\"loading\"].includes(String(tripMyInviteState.status||\"idle\")))next=\"resolving\";\n    else if(tripMyInviteState.status===\"ready\"){\n      // The deep link no longer points to a pending invite for this account.\n      // Clear only the routing hint; normal membership/catalog rules decide the\n      // workspace from here without leaking any private Trip information.\n      service.clearInviteFromLocation?.({keepTrip:true});appEntryTargetInviteId=\"\";\n    }\n  }\n  const resumeLatched=!appEntryTargetInviteId&&appEntryResumeLatchMatchesVisible();\n  const switchHandoff=!appEntryTargetInviteId&&appEntryTripSwitchHandoffKeepsWorkspace();\n  const optimisticReady=!appEntryTargetInviteId&&entryWorkspaceVisualReady(service)\n    && String(appEntryOptimisticTripId||\"\")===dataTripId(window.tripData);\n  if(resumeLatched||optimisticReady||switchHandoff)next=\"workspace\";\n  if(next===\"workspace\"&&!entryWorkspaceVisualReady(service)&&!switchHandoff)next=\"resolving\";\n  applyAppEntryState(next);\n}\nasync function refreshTripCreatorEntitlement(user,{force=false}={}){\n  if(!user?.uid){appEntryCreatorState={status:\"idle\",enabled:false,error:null};await refreshAppEntryState();return;}\n  appEntryCreatorState={status:\"loading\",enabled:false,error:null};\n  try{\n    const service=await import(`./assets/js/trip-creator-service.js?build=${APP_VERSION}`);\n    const result=await service.getTripCreatorEntitlement(user,{force});\n    appEntryCreatorState={status:\"ready\",enabled:result?.enabled===true,error:null};\n  }catch(error){\n    if(error?.code!==\"permission-denied\")console.warn(\"Trip creator entitlement\",error);\n    appEntryCreatorState={status:\"ready\",enabled:false,error};\n  }\n  await refreshAppEntryState();\n}\nwindow.addEventListener(\"app-trip-creator-changed\",event=>{\n  const changedUid=String(event.detail?.uid||\"\").trim();\n  const currentUid=String(window.__appAuthUser?.uid||\"\").trim();\n  if(changedUid&&changedUid===currentUid){\n    import(`./assets/js/trip-creator-service.js?build=${APP_VERSION}`).then(service=>service.clearTripCreatorEntitlementCache?.()).catch(()=>{});\n    refreshTripCreatorEntitlement(window.__appAuthUser,{force:true}).catch(()=>{});\n  }\n});\n\nasync function backfillOwnerTripRegistries(state=tripLibraryCloudState){\n  if(appEntryRegistryBackfillBusy||!tripLibraryCurrentUser?.uid||state?.status!==\"ready\"||state?.serverConfirmed!==true)return;\n  const ownerTrips=(state.trips||[]).filter(trip=>trip.role===\"owner\");if(!ownerTrips.length)return;\n  appEntryRegistryBackfillBusy=true;\n  try{\n    const service=await import(`./assets/js/trip-creator-service.js?build=${APP_VERSION}`);\n    for(const trip of ownerTrips){\n      const key=`travel_trip_registry_v1:${tripLibraryCurrentUser.uid}:${trip.tripId}`;\n      try{if(localStorage.getItem(key)===\"1\")continue;}catch(error){}\n      try{await service.ensureTripIdRegistry(trip.tripId,{user:tripLibraryCurrentUser});try{localStorage.setItem(key,\"1\");}catch(error){}}\n      catch(error){if(error?.code!==\"permission-denied\")console.warn(\"Trip ID registry backfill\",trip.tripId,error);}\n    }\n  }finally{appEntryRegistryBackfillBusy=false;}\n}\n\n\nlet tripLibraryStopCatalog=null;\nlet tripLibraryAuthUnsubscribe=null;\nlet tripLibrarySummary=null;\nlet tripLibraryCatalogService=null;\nlet tripLibrarySchemaService=null;\nlet tripLibraryCurrentUser=null;\nlet tripLibraryCloudState={status:\"signed-out\",trips:[],error:null,fromCache:false,serverConfirmed:false};\nlet tripLibraryLastReadyCatalog={uid:\"\",trips:[]};\n\n// Phase 2E · Firebase is the live trip source once an authenticated member trip\n// is available. Bundled trip.json remains the safe bootstrap / signed-out fallback.\nlet bundledTripData=null;\nlet tripLoaderService=null;\nlet tripSessionService=null;\nlet tripAccessService=null;\nlet tripLoaderStop=null;\nlet tripLoaderActiveId=\"\";\nlet tripLoaderState={status:\"local\",source:\"local\",tripId:\"\",updatedAt:\"\",serverConfirmed:false,hasPendingWrites:false,error:null};\nfunction setTripRealtimeDay(dayIdInput){\n  const dayId=String(dayIdInput||\"\").trim();\n  if(!dayId)return;\n  try{tripLoaderStop?.setActiveDayId?.(dayId);}catch(error){}\n}\nfunction preferredRealtimeDayId(data){\n  const days=Array.isArray(data?.days)?data.days:[];\n  if(!days.length)return\"\";\n  const activeDomId=String(document.querySelector(\".day-tab.active\")?.dataset?.dayId||\"\").trim();\n  if(activeDomId&&days.some(day=>String(day?.dayId||\"\").trim()===activeDomId))return activeDomId;\n  try{\n    const cities=data?.meta?.cities||{};\n    const tz=Object.values(cities)[0]?.timezone||data?.meta?.weather?.timezone||\"UTC\";\n    const today=dateInTZ(tz);\n    const todayDay=days.find(day=>day?.isoDate===today);\n    if(todayDay?.dayId)return String(todayDay.dayId).trim();\n  }catch(error){}\n  return String(days[0]?.dayId||\"\").trim();\n}\nlet tripRenderCacheServicePromise=null;\nlet tripRenderCacheService=null;\n// v7.9.2.9 · Keep the last archive-grade confirmation monotonic for the\n// currently cached Trip. Ordinary cache-sourced loader emissions must never\n// erase a previously trusted same-revision seed. Runtime gate confirmation is\n// still reset on every Instant Cache paint until this session contacts server.\nlet tripRenderCacheLastConfirmedTripId=\"\";\nlet tripRenderCacheLastConfirmedRevision=0;\nlet tripInstantBootToken=0;\nlet tripRenderSignature=\"\";\nlet tripSnacksSignature=\"\";\nlet tripInfoSignature=\"\";\nlet tripWeatherSignature=\"\";\nlet tripVisualSignature=\"\";\nlet tripExpensesSignature=\"\";\nlet tripDestinationThemeSignature=\"\";\nlet tripTeamThemeSignature=\"\";\nlet tripLibrarySummaryDataSignature=\"\";\nlet nowIndicatorTimer=0;\n\nfunction tripLibraryEsc(value){return escapeHtmlText(value);}\nfunction tripLibraryRoleLabel(role){return({owner:\"Owner\",admin:\"Admin\",member:\"Member\",viewer:\"Viewer\"})[role]||\"\";}\nfunction tripLibraryDateText(trip){return trip?.dateRange||[trip?.startDate,trip?.endDate].filter(Boolean).join(\" → \")||trip?.tripId||\"\";}\nfunction tripLibraryStatusLabel(status){return({draft:\"草稿\",upcoming:\"即將出發\",active:\"旅程中\",completed:\"已完成\"})[status]||\"\";}\n\nfunction currentTripLibraryRow(){\n  if(!tripLibrarySummary)return\"\";\n  const currentId=tripLibrarySummary.tripId;\n  const cloudTrips=Array.isArray(tripLibraryCloudState.trips)?tripLibraryCloudState.trips:[];\n  const currentCloud=cloudTrips.find(trip=>trip.tripId===currentId)||null;\n  const currentRole=tripLibraryRoleLabel(currentCloud?.role);\n  const statusLabel=tripLibraryStatusLabel(currentCloud?.status);\n  const importPill=currentCloud?.importState===\"importing\"?'<span class=\"trip-library-pill\">匯入中</span>':\"\";\n  const lockPill=currentCloud?.globalLocked===true?'<span class=\"trip-library-pill is-archived\">已鎖定</span>':\"\";\n  const archiveAction=currentCloud?`<button class=\"trip-library-mini-action${currentCloud.archived===true?\" is-restore\":\"\"}\" type=\"button\" ${currentCloud.archived===true?`data-trip-restore-id=\"${tripLibraryEsc(currentId)}\"`:`data-trip-archive-id=\"${tripLibraryEsc(currentId)}\"`}>${currentCloud.archived===true?\"還原\":\"封存\"}</button>`:\"\";\n  return `<div class=\"trip-library-item is-current\">\n    <span class=\"trip-library-icon\">${tripIconMarkup({...tripLibrarySummary,...(currentCloud||{})})}</span>\n    <span class=\"trip-library-copy\">\n      <strong>${tripLibraryEsc(tripLibrarySummary.title||currentId)}</strong>\n      <small>${tripLibraryEsc(tripLibrarySummary.dateRange||[tripLibrarySummary.startDate,tripLibrarySummary.endDate].filter(Boolean).join(\" → \")||currentId)}</small>\n      <span class=\"trip-library-meta\"><span class=\"trip-library-pill is-current\">目前</span><span class=\"trip-library-pill\">${tripLoaderState.status===\"ready\"&&tripLoaderState.tripId===currentId?(tripLoaderState.source===\"instant-cache\"?\"即時快取\":(tripLoaderState.source===\"cache\"?\"Firebase 快取\":\"Firebase\")):\"本機 JSON\"}</span>${statusLabel?`<span class=\"trip-library-pill\">${statusLabel}</span>`:\"\"}${lockPill}${importPill}${currentRole?`<span class=\"trip-library-pill is-role\">${currentRole}</span>`:\"\"}</span>\n    </span>\n    <span class=\"trip-library-side\"><strong>${tripLibrarySummary.dayCount} 日</strong><small>${tripLibrarySummary.itemCount} 項</small>${archiveAction}</span>\n  </div>`;\n}\n\nfunction cloudTripLibraryRow(trip,{archived=false}={}){\n  const role=tripLibraryRoleLabel(trip.role);\n  const statusLabel=tripLibraryStatusLabel(trip.status);\n  const importPill=trip.importState===\"importing\"?'<span class=\"trip-library-pill\">匯入中</span>':\"\";\n  const lockPill=trip.globalLocked===true?'<span class=\"trip-library-pill is-archived\">已鎖定</span>':\"\";\n  const action=archived\n    ? `<button class=\"trip-library-mini-action is-restore\" type=\"button\" data-trip-restore-id=\"${tripLibraryEsc(trip.tripId)}\">還原</button>`\n    : `<button class=\"trip-library-mini-action\" type=\"button\" data-trip-archive-id=\"${tripLibraryEsc(trip.tripId)}\">封存</button>`;\n  return `<div class=\"trip-library-item${archived?\" is-archived\":\"\"}\" aria-disabled=\"true\">\n    <span class=\"trip-library-icon\">${archived?\"🗄️\":tripIconMarkup(trip)}</span>\n    <span class=\"trip-library-copy\">\n      <strong>${tripLibraryEsc(trip.title||trip.tripId)}</strong>\n      <small>${tripLibraryEsc(tripLibraryDateText(trip))}</small>\n      <span class=\"trip-library-meta\">${archived?'<span class=\"trip-library-pill is-archived\">已封存</span>':'<span class=\"trip-library-pill\">Firebase</span>'}${statusLabel?`<span class=\"trip-library-pill\">${statusLabel}</span>`:\"\"}${lockPill}${importPill}${role?`<span class=\"trip-library-pill is-role\">${role}</span>`:\"\"}</span>\n    </span>\n    <span class=\"trip-library-side\"><strong>${archived?\"個人封存\":\"已加入\"}</strong><small>${archived?\"只影響你\":\"2G 切換\"}</small>${action}</span>\n  </div>`;\n}\n\nfunction renderTripSwitcher(){\n  const list=document.getElementById(\"trip-switch-list\");\n  if(!list||!tripLibrarySummary)return;\n  const currentId=tripLibrarySummary.tripId;\n  const cloudTrips=Array.isArray(tripLibraryCloudState.trips)?tripLibraryCloudState.trips:[];\n  const others=cloudTrips.filter(trip=>trip.tripId!==currentId && trip.archived!==true);\n  const rows=[`<button class=\"trip-switch-row is-current\" type=\"button\" data-trip-switch-current=\"true\" role=\"menuitem\">\n    <span class=\"trip-switch-row-icon\">${tripIconMarkup({...tripLibrarySummary,...(cloudTrips.find(t=>t.tripId===currentId)||{})})}</span>\n    <span class=\"trip-switch-row-copy\"><strong>${tripLibraryEsc(tripLibrarySummary.title||currentId)}</strong></span>\n    <span class=\"trip-switch-check\">✓</span>\n  </button>`];\n  others.forEach(trip=>rows.push(`<button class=\"trip-switch-row\" type=\"button\" data-trip-switch-id=\"${tripLibraryEsc(trip.tripId)}\" role=\"menuitem\">\n    <span class=\"trip-switch-row-icon\">${tripIconMarkup(trip)}</span>\n    <span class=\"trip-switch-row-copy\"><strong>${tripLibraryEsc(trip.title||trip.tripId)}</strong></span>\n  </button>`));\n  list.innerHTML=rows.join(\"\");\n  // Storage-backed Trip icons are descriptor-only markup until hydrated.\n  // The quick switcher re-renders its innerHTML every time it opens, so the\n  // newly created <img> nodes must be rebound immediately instead of relying\n  // on the separate My Trips page render path.\n  hydrateTripLibraryMediaImages(list);\n}\n\nfunction renderEntryArchivedTrips(){\n  const card=document.getElementById(\"entry-archive-card\"),list=document.getElementById(\"entry-archive-list\"),count=document.getElementById(\"entry-archive-count\"),status=document.getElementById(\"entry-archive-status\");\n  if(!card||!list||!count||!status)return;\n  const trips=(Array.isArray(tripLibraryCloudState.trips)?tripLibraryCloudState.trips:[]).filter(trip=>trip?.archived===true);\n  const show=appEntryState===\"lobby\"&&!!tripLibraryCurrentUser&&trips.length>0;\n  card.hidden=!show;\n  if(!show){list.innerHTML=\"\";count.textContent=String(trips.length||0);return;}\n  count.textContent=String(trips.length);\n  list.innerHTML=trips.map(trip=>cloudTripLibraryRow(trip,{archived:true})).join(\"\");\n  status.textContent=\"個人封存只會整理你自己嘅旅程清單；其他成員完全不受影響。\";\n}\n\nfunction renderArchivedTrips(){\n  const panel=document.getElementById(\"trip-archive-panel\");\n  const list=document.getElementById(\"trip-archive-list\");\n  const status=document.getElementById(\"trip-archive-status\");\n  const count=document.getElementById(\"trip-archive-count\");\n  if(!panel||!list||!status||!count)return;\n  const state=tripLibraryCloudState.status;\n  const trips=(Array.isArray(tripLibraryCloudState.trips)?tripLibraryCloudState.trips:[]).filter(trip=>trip.archived===true);\n  if(state===\"signed-out\"){count.textContent=\"—\";list.innerHTML=\"\";status.textContent=\"登入 Google 後先可以查看已封存旅程。\";}\n  else if(state===\"loading\"){count.textContent=\"…\";list.innerHTML=\"\";status.textContent=\"正在同步個人旅程偏好…\";}\n  else if(state===\"rules-pending\"){count.textContent=\"—\";list.innerHTML=\"\";status.textContent=\"請先部署今版 firestore.rules，個人封存先可以同步。\";}\n  else if(state===\"ready\"){count.textContent=String(trips.length);list.innerHTML=trips.length?trips.map(trip=>cloudTripLibraryRow(trip,{archived:true})).join(\"\"):\"\";status.textContent=trips.length?\"封存只影響你自己；其他成員嘅旅程清單唔會改變。\":\"暫時未有已封存旅程。\";}\n  else{count.textContent=\"—\";list.innerHTML=\"\";status.textContent=\"暫時未能讀取已封存旅程。\";}\n  renderEntryArchivedTrips();\n}\n\nfunction renderTripLibrary(){\n  const list=document.getElementById(\"trip-library-list\");\n  const status=document.getElementById(\"trip-library-status\");\n  const count=document.getElementById(\"trip-library-count\");\n  if(!list||!tripLibrarySummary)return;\n\n  const currentId=tripLibrarySummary.tripId;\n  const allCloudTrips=Array.isArray(tripLibraryCloudState.trips)?tripLibraryCloudState.trips:[];\n  const cloudTrips=allCloudTrips.filter(trip=>trip.archived!==true);\n  const currentAny=allCloudTrips.find(trip=>trip.tripId===currentId)||null;\n  const currentCloud=cloudTrips.find(trip=>trip.tripId===currentId)||null;\n  const others=cloudTrips.filter(trip=>trip.tripId!==currentId);\n  const showLocalCurrent=tripLibraryCloudState.status!==\"ready\"||!currentAny||currentAny.archived!==true;\n  list.innerHTML=(showLocalCurrent?currentTripLibraryRow():\"\")+others.map(trip=>cloudTripLibraryRow(trip)).join(\"\");\n  if(count) count.textContent=String(tripLibraryCloudState.status===\"ready\"?cloudTrips.length:Math.max(1,cloudTrips.length+(currentCloud?0:1)));\n\n  if(status){\n    const state=tripLibraryCloudState.status;\n    if(state===\"signed-out\") status.textContent=\"目前使用本機 trip.json。登入 Google 後會顯示你有權使用嘅 Active Trips。\";\n    else if(state===\"loading\") status.textContent=\"已登入 Google，正在讀取未封存旅程…\";\n    else if(state===\"rules-pending\") status.textContent=\"Google 登入正常。請先部署 ZIP 內 firestore.rules，Phase 2G.1 Entry & Access 權限先可以安全寫入 Firebase。\";\n    else if(state===\"index-required\") status.textContent=\"Firestore 欠 Trip catalog composite index；按 Console error 提供嘅連結建立一次就得。\";\n    else if(state===\"ready\"&&cloudTrips.length===0) status.textContent=appEntryCreatorState.enabled?\"暫時未有雲端旅程。你已獲授權，可用「匯入 trip.json」建立新 Trip；亦可以等待旅程邀請。\":\"暫時未有雲端旅程。收到旅程邀請後會自動顯示喺呢度。\";\n    else if(state===\"ready\") status.textContent=`已讀取 ${cloudTrips.length} 個未封存雲端旅程。Firebase Loader 已連接，可直接切換。`;\n    else status.textContent=\"旅程清單暫時未能同步，但目前本機行程不受影響。\";\n  }\n  renderTripSwitcher();\n  renderArchivedTrips();\n  renderTripGlobalLockState();\n  tripEditSyncProfileEntry();\n  hydrateTripLibraryMediaImages();\n}\n\nfunction stopArchivedTripSubscription(){}\nfunction restartActiveTripCatalog(){\n  if(tripLibraryStopCatalog){tripLibraryStopCatalog();tripLibraryStopCatalog=null;}\n  if(!tripLibraryCurrentUser||!tripLibraryCatalogService)return;\n  tripLibraryCloudState={status:\"loading\",trips:[],error:null,fromCache:false,serverConfirmed:false};renderTripLibrary();refreshAppEntryState();\n  tripLibraryStopCatalog=tripLibraryCatalogService.subscribeUserTrips(tripLibraryCurrentUser,state=>{\n    tripLibraryCloudState=state;\n    if(state?.status===\"ready\")tripLibraryLastReadyCatalog={uid:String(tripLibraryCurrentUser?.uid||\"\"),trips:(state.trips||[]).map(trip=>({...trip}))};\n    if(state?.status===\"ready\"&&state?.serverConfirmed===true&&appEntryResumeLatch.tripId){\n      const latchedId=String(appEntryResumeLatch.tripId||\"\");\n      const stillActive=(state.trips||[]).some(trip=>String(trip?.tripId||\"\")===latchedId&&trip?.archived!==true);\n      if(!stillActive&&!appEntryTripSwitchHandoffKeepsWorkspace()){appEntryOptimisticTripId=\"\";appEntryOptimisticUid=\"\";clearAppEntryResumeLatch();endWarmRefreshGate();}\n    }\n    renderTripLibrary();refreshAppEntryState();\n    if(state?.status===\"ready\"&&state?.serverConfirmed===true)backfillOwnerTripRegistries(state).catch(()=>{});\n    reconcileFirebaseTripLoader().catch(error=>console.error(\"Firebase loader reconcile\",error));\n  });\n}\nfunction loadArchivedTrips(){ renderArchivedTrips(); }\n\nfunction initTripLibrarySafe(data){\n  bundledTripData=data;\n  if(!tripBootDeferred&&!tripRenderSignature)tripRenderSignature=tripContentRenderSignature(data);\n  if(!tripBootDeferred&&!tripSnacksSignature)tripSnacksSignature=snacksRenderSignature(data);\n  if(!tripBootDeferred&&!tripInfoSignature)tripInfoSignature=infoRenderSignature(data);\n  if(!tripBootDeferred&&!tripWeatherSignature)tripWeatherSignature=weatherRenderSignature(data);\n  if(!tripBootDeferred&&!tripVisualSignature)tripVisualSignature=tripVisualRenderSignature(data);\n  if(!tripBootDeferred&&!tripExpensesSignature)tripExpensesSignature=expensesRenderSignature(data);\n  if(!tripBootDeferred&&!tripDestinationThemeSignature)tripDestinationThemeSignature=destinationThemeRenderSignature(data);\n  if(!tripBootDeferred&&!tripTeamThemeSignature)tripTeamThemeSignature=teamThemeRenderSignature(data);\n  import(`./assets/js/trip-schema-service.js?build=${APP_VERSION}`)\n    .then(schemaService=>{\n      tripLibrarySchemaService=schemaService;\n      const summarySource=window.tripData&&dataTripId(window.tripData)?window.tripData:data;\n      const result=schemaService.validatePortableTrip(summarySource);\n      tripLibrarySummary=schemaService.getTripSummary(result.trip);\n      tripLibrarySummaryDataSignature=tripLibraryDataSignature(summarySource);\n      window.__portableTripSchema=result;\n      tripLibraryCloudState={status:window.__appAuthUser?\"loading\":\"signed-out\",trips:[],error:null,fromCache:false,serverConfirmed:false};\n      renderTripLibrary();\n\n      Promise.all([\n        import(`./assets/js/trip-catalog-service.js?build=${APP_VERSION}`),\n        getGlobalAuthService()\n      ]).then(([catalogService,authService])=>{\n        tripLibraryCatalogService=catalogService;\n        if(tripLibraryAuthUnsubscribe) tripLibraryAuthUnsubscribe();\n        tripLibraryAuthUnsubscribe=authService.subscribeAuthState(user=>{\n          tripLibraryCurrentUser=user||null;\n          const catalogUid=String(user?.uid||\"\");\n          if(String(tripLibraryLastReadyCatalog.uid||\"\")!==catalogUid)tripLibraryLastReadyCatalog={uid:catalogUid,trips:[]};\n          rememberCacheUid(user?.uid||\"\");\n          if(tripLibraryStopCatalog){tripLibraryStopCatalog();tripLibraryStopCatalog=null;}\n          stopArchivedTripSubscription();\n          const archivePanel=document.getElementById(\"trip-archive-panel\");\n          const archiveToggle=document.getElementById(\"trip-archive-toggle\");\n          if(archivePanel) archivePanel.hidden=true;\n          if(archiveToggle) archiveToggle.setAttribute(\"aria-expanded\",\"false\");\n          restartMyTripInviteSubscription(user||null);\n          refreshTripCreatorEntitlement(user||null).catch(()=>{});\n          if(!user){tripLibraryCloudState={status:\"signed-out\",trips:[],error:null,fromCache:false,serverConfirmed:false};renderTripLibrary();stopFirebaseTripLoader({fallback:false});refreshAppEntryState();return;}\n          // v7.8.0.5: Auth identity is enough to restore the same user's local\n          // Trip cache immediately. Membership is rechecked in the background; a\n          // server-confirmed revoke ejects the Trip and clears that cache.\n          bootstrapRememberedFirebaseTrip(user).catch(error=>console.warn(\"Optimistic Trip bootstrap\",error));\n          restartActiveTripCatalog();\n          refreshAppEntryState();\n        });\n      }).catch(error=>{\n        console.warn(\"Phase 2B cloud trip catalog unavailable\",error);\n        tripLibraryCloudState={status:\"error\",trips:[],error,fromCache:false,serverConfirmed:false};\n        renderTripLibrary();refreshAppEntryState();\n        if(tripBootDeferred)stopFirebaseTripLoader({fallback:true});\n      });\n    })\n    .catch(error=>{\n      console.error(\"Phase 2B portable trip schema init error\",error);\n      const status=document.getElementById(\"trip-library-status\");\n      if(status) status.textContent=\"旅程 Schema service 載入失敗；目前行程仍可正常使用。\";\n      if(tripBootDeferred){\n        const preferred=tripBootPreferredId||resolveBootPreferredTripId();\n        const localId=dataTripId(bundledTripData);\n        if(preferred&&localId===preferred)applyBundledTripFallback();\n        else showDeferredTripBootPlaceholder(\"旅程同步暫時未完成，正在重試…\");\n      }\n    });\n}\n\n\n// Phase 2E · v7.6.3 · Firebase live-loader wiring + legacy trip reset.\n// v7.6.0/v7.6.1 shipped the loader service but never subscribed the app renderer\n// to it. Firestore therefore changed while the visible itinerary kept rendering\n// bundled trip.json. This block makes Firebase the authenticated source of truth.\nlet tripLoaderInitPromise=null;\nlet tripLoaderReconcileToken=0;\nconst tripLoaderRevisionWaiters=new Set();\n\nfunction dataTripId(data){return String(data?.tripId||data?.meta?.tripId||data?.meta?.expenses?.tripId||\"\").trim();}\n\nconst TRIP_SESSION_STORAGE_KEY=\"travel_active_trip_id\";\nconst TRIP_LAST_AUTH_UID_KEY=\"travel_last_auth_uid\";\nfunction rememberedCacheUid(){try{return String(localStorage.getItem(TRIP_LAST_AUTH_UID_KEY)||\"\").trim();}catch(error){return\"\";}}\nfunction rememberCacheUid(uidInput){\n  const uid=String(uidInput||\"\").trim();\n  try{if(uid)localStorage.setItem(TRIP_LAST_AUTH_UID_KEY,uid);else localStorage.removeItem(TRIP_LAST_AUTH_UID_KEY);}catch(error){}\n}\nlet tripBootDeferred=false;\nlet tripBootPreferredId=\"\";\nlet tripBootFailSafeTimer=0;\nfunction clearDeferredTripBootFailSafe(){\n  if(tripBootFailSafeTimer){clearTimeout(tripBootFailSafeTimer);tripBootFailSafeTimer=0;}\n}\nfunction showDeferredTripBootPlaceholder(message=\"正在載入上次旅程…\"){\n  const placeholder=document.getElementById(\"trip-boot-placeholder\");\n  if(!placeholder)return;\n  const label=placeholder.querySelector(\"span:last-child\");\n  if(label)label.textContent=message;\n  placeholder.hidden=false;\n}\nfunction armDeferredTripBootFailSafe(){\n  clearDeferredTripBootFailSafe();\n  if(!tripBootDeferred)return;\n  tripBootFailSafeTimer=setTimeout(()=>{\n    tripBootFailSafeTimer=0;\n    if(!tripBootDeferred)return;\n    const preferred=tripBootPreferredId||resolveBootPreferredTripId();\n    const activeId=dataTripId(window.tripData);\n    if(activeId&&(!preferred||activeId===preferred)){\n      console.warn(\"Deferred Trip boot watchdog released an already-rendered Trip.\");\n      releaseDeferredTripBoot();\n      return;\n    }\n    const localId=dataTripId(bundledTripData);\n    if(preferred&&localId&&preferred===localId){\n      console.warn(\"Deferred Trip boot watchdog using same-Trip bundled fallback while Firebase continues in background.\");\n      applyBundledTripFallback();\n      return;\n    }\n    showDeferredTripBootPlaceholder(\"正在重新連接上次旅程…\");\n  },5000);\n}\nfunction resolveBootPreferredTripId(){\n  try{\n    const url=new URL(location.href);\n    const deep=String(url.searchParams.get(\"trip\")||url.searchParams.get(\"expensesTrip\")||\"\").trim();\n    if(deep)return deep;\n  }catch(error){}\n  try{return String(localStorage.getItem(TRIP_SESSION_STORAGE_KEY)||\"\").trim();}catch(error){return\"\";}\n}\nfunction beginDeferredTripBoot(bundledData){\n  tripBootPreferredId=resolveBootPreferredTripId();\n  ");
