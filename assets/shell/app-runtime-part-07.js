window.__travelRuntimeParts.push("ate.trips||[]).some(t=>t.tripId===id&&t.archived!==true);if(!allowed)return;\n  const user=window.__appAuthUser||tripLibraryCurrentUser||null;\n  const uid=String(user?.uid||\"\").trim();\n  const fromId=String(tripLoaderActiveId||dataTripId(window.tripData)||\"\").trim();\n  const {session,access}=await getTripLoaderServices();\n\n  // Manual switching now uses the same proven Workspace handoff as revoke\n  // routing. The old Trip stays visually latched only until the target cache or\n  // target realtime data is ready, so a transient access/loading frame can no\n  // longer hand control to the root Entry Gateway.\n  if(fromId&&uid)setAppEntryTripSwitchHandoff(fromId,id,uid);\n  appEntryOptimisticTripId=id;appEntryOptimisticUid=uid;\n  if(uid)setAppEntryResumeLatch(id,uid);\n  try{localStorage.setItem(\"travel_active_trip_id\",id);}catch(error){}\n  session.setActiveTripId(id);updateTripUrl(id);tripBootPreferredId=id;closeTripSwitcher();\n\n  const mountedExpenseTrip=String(window.__expensesModuleTripId||\"\");\n  if(mountedExpenseTrip&&mountedExpenseTrip!==id){\n    if(currentAppView===\"expenses\")setView(\"itinerary\",{restoreScroll:false});\n    try{window.__suspendExpensesForTripSwitch?.();}catch(error){console.warn(\"Suspend Expenses for Trip switch\",error);}\n  }\n  refreshAppEntryState();\n\n  const cacheTask=applyInstantTripCache(id,user);\n  const loaderTask=startFirebaseTripLoader(id);\n  try{access.initTripAccess?.(id);access.refreshTripAccess?.();}catch(error){console.warn(\"Manual Trip access refresh\",error);}\n  await Promise.race([Promise.resolve(cacheTask),new Promise(resolve=>setTimeout(resolve,160))]);\n  refreshAppEntryState();\n  Promise.allSettled([cacheTask,loaderTask]).then(()=>refreshAppEntryState()).catch(()=>{});\n  scheduleManualTripSwitchRecovery(id,user,fromId);\n}\n\nlet tripGlobalLockBusy=false;\nfunction currentTripCloudRecord(){\n  const tripId=String(tripLoaderActiveId||tripLibrarySummary?.tripId||dataTripId(window.tripData)||\"\").trim();\n  return (tripLibraryCloudState.trips||[]).find(trip=>String(trip?.tripId||\"\")===tripId)||null;\n}\nfunction currentTripGlobalLocked(){return currentTripCloudRecord()?.globalLocked===true||window.tripData?.meta?.globalLocked===true;}\nfunction renderDataManagementLockState(){\n  const locked=currentTripGlobalLocked();\n  const importBtn=document.getElementById(\"trip-import-btn\"),restoreBtn=document.getElementById(\"trip-full-backup-restore-open\"),snapshotRestore=document.getElementById(\"snapshot-detail-restore\");\n  const importSubtitle=document.getElementById(\"trip-import-menu-subtitle\"),restoreSubtitle=document.getElementById(\"trip-restore-menu-subtitle\");\n  [importBtn,restoreBtn,snapshotRestore].forEach(button=>{\n    if(!button)return;\n    button.disabled=locked;button.setAttribute(\"aria-disabled\",locked?\"true\":\"false\");\n    button.classList.toggle(\"profile-row-disabled\",locked);\n  });\n  if(importSubtitle)importSubtitle.textContent=locked?\"旅程已鎖定，請先解除鎖定後再匯入\":\"只接受 Portable Trip JSON；完整 Backup 會自動攔截\";\n  if(restoreSubtitle)restoreSubtitle.textContent=locked?\"旅程已鎖定，請先解除鎖定後再還原\":\"接受 Full Backup ZIP／舊版 JSON；可選完整、行程或支出還原\";\n}\nfunction renderTripGlobalLockState(){\n  const record=currentTripCloudRecord(),tripId=String(record?.tripId||tripLoaderActiveId||tripLibrarySummary?.tripId||\"\").trim();\n  const locked=currentTripGlobalLocked();\n  const role=String(record?.role||window.__appTripAccess?.role||\"\").trim();\n  const manager=role===\"owner\"||role===\"admin\";\n  const tripName=record?.title||tripLibrarySummary?.title||tripId||\"—\";\n  const menuStatus=document.getElementById(\"trip-global-lock-menu-status\"),menuSubtitle=document.getElementById(\"trip-global-lock-menu-subtitle\");\n  const tripEl=document.getElementById(\"trip-global-lock-trip\"),stateEl=document.getElementById(\"trip-global-lock-state\"),roleEl=document.getElementById(\"trip-global-lock-role\"),help=document.getElementById(\"trip-global-lock-help\"),title=document.getElementById(\"trip-global-lock-action-title\"),copy=document.getElementById(\"trip-global-lock-action-copy\"),button=document.getElementById(\"trip-global-lock-action\"),status=document.getElementById(\"trip-global-lock-status\");\n  if(menuStatus)menuStatus.textContent=locked?\"已鎖定\":\"可修改\";\n  if(menuSubtitle)menuSubtitle.textContent=locked?\"全域唯讀，所有成員仍可查看\":\"全域唯讀，獨立於支出鎖定\";\n  if(tripEl)tripEl.textContent=tripName;\n  if(stateEl)stateEl.textContent=locked?\"已鎖定 · 全域唯讀\":\"可正常修改\";\n  if(roleEl)roleEl.textContent=tripLibraryRoleLabel(role)||\"—\";\n  if(help)help.textContent=locked?\"目前旅程已全域鎖定。一般內容、匯入、還原及支出寫入會停止，但查看、備份同旅程權限管理仍然保留。\":\"鎖定後旅程內容會保持可查看，但一般修改、匯入、還原及支出寫入會停止；旅程權限管理仍然保留。\";\n  if(title)title.textContent=locked?\"解除旅程鎖定\":\"鎖定旅程\";\n  if(copy)copy.textContent=locked?\"解除後所有成員會恢復按角色進行一般修改。\":\"適合旅程完成後凍結內容，避免之後誤改。\";\n  if(button){button.textContent=locked?\"解除旅程鎖定\":\"鎖定旅程\";button.disabled=tripGlobalLockBusy||!tripId||!manager;button.dataset.locked=locked?\"true\":\"false\";}\n  if(status){\n    status.classList.remove(\"is-error\");\n    status.textContent=!tripId?\"暫時未有可管理旅程。\":(!manager?\"只有 Owner / Admin 可以鎖定或解鎖。\":(locked?\"全域鎖定已生效；安全權限管理仍然可以使用。\":\"鎖定係全 Trip 狀態，會同時影響所有成員。\"));\n  }\n  renderDataManagementLockState();\n  renderTripPermanentDeleteState();\n}\nasync function handleTripGlobalLockAction(){\n  if(tripGlobalLockBusy||tripCloudOffline()){if(tripCloudOffline())alert(\"目前離線，旅程鎖定狀態需要重新連線後先可以修改。\");return;}\n  const record=currentTripCloudRecord(),tripId=String(record?.tripId||tripLoaderActiveId||tripLibrarySummary?.tripId||\"\").trim();\n  if(!tripId)return;\n  const locked=record?.globalLocked===true;\n  const message=locked\n    ? `解除「${record?.title||tripId}」嘅旅程鎖定？\\n\\n解除後所有成員會重新按原有角色獲得一般修改權限。`\n    : `鎖定「${record?.title||tripId}」？\\n\\n所有成員仍然可以查看旅程，但一般內容修改、匯入、還原同支出寫入會停止。旅程權限管理同備份仍然保留。`;\n  if(!window.confirm(message))return;\n  tripGlobalLockBusy=true;renderTripGlobalLockState();\n  const status=document.getElementById(\"trip-global-lock-status\");if(status)status.textContent=locked?\"正在解除旅程鎖定…\":\"正在鎖定旅程…\";\n  try{\n    const service=await import(`./assets/js/trip-lifecycle-service.js?build=${APP_VERSION}`);\n    const result=await service.setGlobalTripLocked(tripId,!locked,{user:window.__appAuthUser});\n    tripLibraryCloudState={...tripLibraryCloudState,trips:(tripLibraryCloudState.trips||[]).map(item=>String(item?.tripId||\"\")===tripId?{...item,globalLocked:result.globalLocked===true}:item)};\n    if(window.tripData&&dataTripId(window.tripData)===tripId){window.tripData.meta={...(window.tripData.meta||{}),globalLocked:result.globalLocked===true};}\n    renderTripLibrary();\n    if(status)status.textContent=result.globalLocked?\"旅程已設為全域唯讀。\":\"旅程已解除全域唯讀。\";\n  }catch(error){\n    console.error(\"Global Trip Lock\",error);\n    if(status){status.classList.add(\"is-error\");status.textContent=tripImportPermissionMessage(error);}\n  }finally{tripGlobalLockBusy=false;renderTripGlobalLockState();}\n}\ndocument.getElementById(\"trip-permanent-delete-open-btn\")?.addEventListener(\"click\",()=>setTimeout(startFullBackupSyncGateWatch,0));\ndocument.getElementById(\"trip-global-lock-open-btn\")?.addEventListener(\"click\",()=>renderTripGlobalLockState());\ndocument.getElementById(\"trip-global-lock-action\")?.addEventListener(\"click\",handleTripGlobalLockAction);\n\nfunction currentTripOwnerRecord(){\n  const record=currentTripCloudRecord();\n  const role=String(record?.role||window.__appTripAccess?.role||\"\").trim();\n  return {record,role,isOwner:role===\"owner\"};\n}\nfunction renderTripPermanentDeleteState(){\n  const {record,role,isOwner}=currentTripOwnerRecord();\n  const tripId=String(record?.tripId||tripLoaderActiveId||tripLibrarySummary?.tripId||\"\").trim();\n  const section=document.getElementById(\"trip-permanent-delete-section\");\n  if(section)section.hidden=!isOwner||!tripId;\n  const name=record?.title||tripLibrarySummary?.title||tripId||\"—\";\n  const nameEl=document.getElementById(\"trip-delete-trip-name\"),idEl=document.getElementById(\"trip-delete-trip-id\"),roleEl=document.getElementById(\"trip-delete-role\");\n  if(nameEl)nameEl.textContent=name;if(idEl)idEl.textContent=tripId||\"—\";if(roleEl)roleEl.textContent=tripLibraryRoleLabel(role)||\"—\";\n  if(tripDeleteBackupState.tripId&&tripDeleteBackupState.tripId!==tripId)tripDeleteBackupState={tripId:\"\",archiveGrade:false,preparedAt:0,filename:\"\"};\n  const status=document.getElementById(\"trip-delete-backup-status\");\n  if(status){\n    if(tripDeleteBackupState.tripId===tripId&&tripDeleteBackupState.archiveGrade){\n      status.textContent=`已建立 Server Confirmed Backup · ${backupFormatDate(tripDeleteBackupState.preparedAt)} · ${tripDeleteBackupState.filename||\"Full Backup\"}`;\n    }else status.textContent=\"建議先保存 Full Backup；測試或即棄 Trip 亦可以選擇不備份繼續。\";\n  }\n  const input=document.getElementById(\"trip-delete-confirm-input\"),button=document.getElementById(\"trip-delete-confirm-btn\"),backupBtn=document.getElementById(\"trip-delete-backup-btn\");\n  if(button)button.disabled=!isOwner||!tripId||String(input?.value||\"\").trim()!==\"DELETE\";\n  if(backupBtn){\n    const gate=currentFullBackupSyncGate();\n    applyFullBackupGateToButton(backupBtn,document.getElementById(\"trip-delete-backup-subtitle\"),gate,{deleteFlow:true,ownerAllowed:Boolean(isOwner&&tripId)});\n  }\n}\nfunction getTripDeleteService(){\n  if(!tripDeleteServicePromise)tripDeleteServicePromise=import(`./assets/js/trip-delete-service.js?build=${APP_VERSION}`).catch(error=>{tripDeleteServicePromise=null;throw error;});\n  return tripDeleteServicePromise;\n}\nfunction resetLocalExportPreparationForTrip(tripId){\n  if(!tripId||dataManagementLocalPreparedTripId===tripId){dataManagementLocalPreparePromise=null;dataManagementLocalPreparedTripId=\"\";}\n  if(dataManagementSnapshotCacheTripId===tripId){dataManagementSnapshotCacheTripId=\"\";dataManagementSnapshotCacheReady=false;tripBackupSnapshots=[];tripBackupSelectedSnapshot=null;}\n}\nfunction openDeleteBackupExportOperation(){\n  const {record,isOwner}=currentTripOwnerRecord(),tripId=String(record?.tripId||tripLoaderActiveId||\"\").trim();\n  if(!isOwner||!tripId)return;\n  const summary=dataOperationCurrentSummary();\n  const gate=refreshFullBackupSyncGate();if(!gate.canBackup)return;\n  dataOperationOpen({kind:\"delete-backup\",title:\"刪除前完整 Backup\",subtitle:\"使用目前已同步並取得 Server confirmation 嘅資料，建立可供日後完整重建嘅 Backup Package。\",name:record?.title||summary.title,id:`tripId · ${tripId}`,stats:[{value:summary.dayCount,label:\"Days\"},{value:summary.itemCount,label:\"Items\"},{value:summary.savedPlaceCount,label:\"Saved\"},{value:\"Full\",label:\"Type\"}],mode:\"Backup 唔會另外觸發 Firebase 同步；只有現有 autosync 已確認最新狀態，而且冇 pending writes 時先可以開始。\",warnings:[\"Full Backup ZIP 包含 Data + Storage media，建立期間請保持連線同 App 開啟。\",\"成員權限唔屬於 Backup；日後重建時目前還原帳戶會成為新 Owner。\"],confirmLabel:\"下載完整 Backup\",handler:async()=>{\n    const service=await dataOperationAwait(getTripBackupService(),{stage:\"載入備份引擎\",detail:\"正在準備 Full Backup。\",percent:10,timeoutMs:8000});\n    const frozen=await dataOperationAwait(prepareFullBackupLocalExportPack(),{stage:\"準備已同步資料\",detail:\"使用現有 Firebase listeners 已確認嘅 canonical data。\",percent:28,timeoutMs:8000});\n    const result=await service.exportLocalFullBackup(frozen.trip,frozen.expenseSnapshot,{user:frozen.user,role:frozen.role,tripFreshness:frozen.tripFreshness,requireArchiveGrade:true});\n    dataOperationSetStage(\"收集媒體\",\"正在讀取 Storage media 並建立逐檔 SHA-256。\",52);\n    const packaged=await prepareMediaAwareFullBackupPackage(result,{onProgress:progress=>{\n      const total=Math.max(1,Number(progress?.total)||1),completed=Math.max(0,Number(progress?.completed)||0);\n      dataOperationSetStage(\"收集媒體\",`正在封裝媒體 ${Math.min(total,completed+1)} / ${total}。`,52+Math.min(22,(completed/total)*22));\n    }});\n    dataOperationSetStage(\"建立完整 Backup\",\"正在封裝 trip-data.json、manifest.json 同媒體檔案。\",80);\n    tripDeleteBackupState={tripId,archiveGrade:true,preparedAt:Date.now(),filename:packaged.filename};\n    renderTripPermanentDeleteState();\n    dataOperationSetStage(\"準備下載\",\"Server Confirmed Media Backup 已建立，準備交畀 iOS。\",94);\n    return {title:\"刪除前 Backup 已建立\",detail:`Server Confirmed · SHA-256 · ${result.counts.dayCount||0} Days · ${packaged.mediaCount||0} Media`,download:()=>backupDownloadBlob(packaged.blob,packaged.filename)};\n  }});\n}\nfunction resetPermanentDeleteProfileContext(){\n  const input=document.getElementById(\"trip-delete-confirm-input\");\n  if(input)input.value=\"\";\n  tripDeleteBackupState={tripId:\"\",archiveGrade:false,preparedAt:0,filename:\"\"};\n  // A deleted Trip must never hand the destructive page to the fallback Trip.\n  // The progress overlay is still covering the handoff, so reset the existing\n  // protected Profile compositor directly to its established root surface.\n  if(typeof profileSetLivePage===\"function\"&&currentAppView===\"profile\"){\n    profileNavStack=[{key:\"root\",scrollTop:0,snapshot:null}];\n    profileSetLivePage(\"root\",0);\n  }\n}\nasync function routeAfterPermanentDelete(tripId,user){\n  const activeBefore=String(tripLoaderActiveId||tripLibrarySummary?.tripId||dataTripId(window.tripData)||appEntryOptimisticTripId||\"\").trim();\n  const wasActive=activeBefore===String(tripId||\"\").trim();\n  try{await invalidateInstantTripCache(tripId,user);}catch(error){}\n  try{const {access}=await getTripLoaderServices();access.clearLastKnownTripAccess?.(user?.uid,tripId);}catch(error){}\n  resetLocalExportPreparationForTrip(tripId);\n  tripLibraryCloudState={...tripLibraryCloudState,trips:(tripLibraryCloudState.trips||[]).filter(item=>String(item?.tripId||\"\")!==tripId)};\n  if(String(tripLibraryLastReadyCatalog.uid||\"\")===String(user?.uid||\"\"))tripLibraryLastReadyCatalog={...tripLibraryLastReadyCatalog,trips:(tripLibraryLastReadyCatalog.trips||[]).filter(item=>String(item?.tripId||\"\")!==tripId)};\n  renderTripLibrary();\n  if(wasActive){\n    const routed=await routeAwayFromRevokedTrip(tripId);\n    resetPermanentDeleteProfileContext();\n    return routed;\n  }\n  refreshAppEntryState();\n  resetPermanentDeleteProfileContext();\n  return true;\n}\n\nfunction schedulePendingPermanentDeleteResume(userInput){\n  const user=userInput||window.__appAuthUser||null,uid=String(user?.uid||\"\").trim();\n  if(!uid)return;\n  clearTimeout(tripDeleteResumeTimer);\n  tripDeleteResumeTimer=setTimeout(()=>resumePendingPermanentDeleteIfNeeded(user).catch(error=>console.warn(\"Pending permanent delete resume\",error)),420);\n}\nasync function resumePendingPermanentDeleteIfNeeded(userInput){\n  const user=userInput||window.__appAuthUser||null,uid=String(user?.uid||\"\").trim();\n  if(!uid||tripDeleteResumeInFlight)return false;\n  const service=await getTripDeleteService();\n  const pending=service.listPendingTripDeletions?.(uid)||[];\n  const marker=pending[0];\n  if(!marker?.tripId)return false;\n  const key=`${uid}:${marker.tripId}:${Number(marker.startedAt)||0}`;\n  if(tripDeleteResumeAttemptKey===key)return false;\n  tripDeleteResumeAttemptKey=key;tripDeleteResumeInFlight=true;\n  appProgressOpen({kicker:\"PERMANENT DELETE\",title:\"繼續永久刪除\",subtitle:\"上次清理未完成，Firebase 會由目前 deleting 狀態繼續，確認全部清空後先完成。\",percent:18,stageTitle:\"重新連接刪除流程\",stageDetail:`正在繼續清理 ${marker.title||marker.tripId}。`,icon:\"🗑️\"});\n  try{\n    appProgressUpdate({percent:38,stageTitle:\"繼續 Firebase Cleanup\",stageDetail:\"正在清除剩餘 Trip 子資料、references 同 Storage prefix。\",icon:\"🗑️\"});\n    const result=await service.permanentDeleteTrip(marker.tripId,{user,title:marker.title||\"\"});\n    appProgressUpdate({percent:94,stageTitle:\"Cleanup verification\",stageDetail:\"Server 已再次驗證所有 Trip 專屬資料為零。\",icon:\"✓\"});\n    await routeAfterPermanentDelete(marker.tripId,user);\n    appProgressFinish({title:\"旅程已永久刪除\",detail:result?.status===\"already-deleted\"?\"Server 已確認上次其實已完成刪除，亦無殘留資料。\":\"中斷嘅刪除流程已完成，cleanup verification 已通過。\"});\n    tripDeleteResumeAttemptKey=\"\";\n    setTimeout(()=>schedulePendingPermanentDeleteResume(user),220);\n    return true;\n  }catch(error){\n    tripDeleteResumeAttemptKey=\"\";\n    appProgressFail(backupFriendlyError(error));\n    if(error?.code===\"delete-already-running\"){\n      const retryAfter=Math.max(2500,Math.min(11*60*1000,Number(error?.details?.retryAfterMs||error?.customData?.details?.retryAfterMs)||15000));\n      clearTimeout(tripDeleteResumeTimer);\n      tripDeleteResumeTimer=setTimeout(()=>schedulePendingPermanentDeleteResume(user),retryAfter+500);\n    }\n    return false;\n  }finally{tripDeleteResumeInFlight=false;}\n}\nfunction openPermanentDeleteOperation(){\n  const {record,isOwner}=currentTripOwnerRecord(),tripId=String(record?.tripId||tripLoaderActiveId||\"\").trim();\n  if(!isOwner||!tripId)return;\n  if(String(document.getElementById(\"trip-delete-confirm-input\")?.value||\"\").trim()!==\"DELETE\")return;\n  const backedUp=tripDeleteBackupState.tripId===tripId&&tripDeleteBackupState.archiveGrade===true;\n  const summary=dataOperationCurrentSummary();\n  dataOperationOpen({kind:\"permanent-delete\",title:\"永久刪除旅程\",subtitle:\"呢個操作會由 Firebase Server 執行可重試 cleanup，確認全部清空後先最後刪 Trip root。\",name:record?.title||summary.title,id:`tripId · ${tripId}`,stats:[{value:summary.dayCount,label:\"Days\"},{value:summary.itemCount,label:\"Items\"},{value:(record?.memberCount??\"—\"),label:\"Members\"},{value:\"永久\",label:\"Delete\"}],mode:backedUp?\"已建立 Server Confirmed 刪除前 Backup。\":\"你選擇繼續永久刪除，但今次尚未建立刪除前 Backup。\",warnings:[...(backedUp?[]:[\"尚未下載刪除前 Full Backup。刪除完成後 Firebase 端無法復原呢個 Trip。\"]),\"會清除 Trip 所有子資料、members、settings、snapshots、logs、operation locks、invites、Trip ID Registry、所有 user Trip preferences，以及 Phase 3A 後 Storage media。\",\"清理中途失敗時 Trip root 會保留 deleting 狀態，可以再次執行繼續清理。\"],confirmLabel:\"永久刪除\",handler:async()=>{\n    const user=await getImportFirebaseUser();if(!user?.uid){const error=new Error(\"Google sign-in required\");error.code=\"auth-required\";throw error;}\n    const service=await getTripDeleteService();\n    appProgressOpen({kicker:\"PERMANENT DELETE\",title:\"永久刪除旅程\",subtitle:\"Firebase 正在清理所有 Trip 專屬資料。完成前請保持 App 開啟。\",percent:8,stageTitle:\"鎖定刪除狀態\",stageDetail:\"正在驗證 Owner 同目前操作狀態。\",icon:\"🗑️\"});\n    try{\n      appProgressUpdate({percent:24,stageTitle:\"清理 Firebase 資料\",stageDetail:\"正在遞迴刪除 Trip 子資料、跨 Trip references 同 Storage prefix。\",icon:\"🗑️\"});\n      const result=await service.permanentDeleteTrip(tripId,{user,title:record?.title||summary.title||tripId});\n      appProgressUpdate({percent:94,stageTitle:\"Cleanup verification\",stageDetail:\"Server 已確認所有 Trip 專屬資料為零，Trip root 已最後刪除。\",icon:\"✓\"});\n      await routeAfterPermanentDelete(tripId,user);\n      tripDeleteBackupState={tripId:\"\",archiveGrade:false,preparedAt:0,filename:\"\"};\n      appProgressFinish({title:\"旅程已永久刪除\",detail:\"Firebase cleanup verification 已完成；如果之前保存咗 Full Backup，日後可用 Backup 重建旅程。\"});\n      return {title:\"旅程已永久刪除\",detail:result?.status===\"already-deleted\"?\"Server 已確認旅程早前已完成刪除，亦無殘留資料。\":\"Trip root 已最後刪除，cleanup verification 已通過。\"};\n    }catch(error){appProgressFail(backupFriendlyError(error));throw error;}\n  }});\n}\ndocument.getElementById(\"trip-permanent-delete-open-btn\")?.addEventListener(\"click\",()=>{const input=document.getElementById(\"trip-delete-confirm-input\");if(input)input.value=\"\";renderTripPermanentDeleteState();});\ndocument.getElementById(\"trip-delete-backup-btn\")?.addEventListener(\"click\",openDeleteBackupExportOperation);\ndocument.getElementById(\"trip-delete-confirm-input\")?.addEventListener(\"input\",renderTripPermanentDeleteState);\ndocument.getElementById(\"trip-delete-confirm-btn\")?.addEventListener(\"click\",openPermanentDeleteOperation);\n\nconst tripArchiveToggle=document.getElementById(\"trip-archive-toggle\");\ntripArchiveToggle?.addEventListener(\"click\",()=>{\n  const panel=document.getElementById(\"trip-archive-panel\");\n  if(!panel)return;\n  const willOpen=panel.hidden;\n  panel.hidden=!willOpen;\n  tripArchiveToggle.setAttribute(\"aria-expanded\",willOpen?\"true\":\"false\");\n  if(willOpen) loadArchivedTrips();\n});\n\n\n// Phase 2E · v7.6.1 · one blocking progress surface for multi-write operations.\nconst appProgressUi={\n  backdrop:document.getElementById(\"app-progress-backdrop\"),\n  kicker:document.getElementById(\"app-progress-kicker\"),\n  title:document.getElementById(\"app-progress-title\"),\n  subtitle:document.getElementById(\"app-progress-subtitle\"),\n  bar:document.getElementById(\"app-progress-bar\"),\n  percent:document.getElementById(\"app-progress-percent\"),\n  stageIcon:document.getElementById(\"app-progress-stage-icon\"),\n  stageTitle:document.getElementById(\"app-progress-stage-title\"),\n  stageDetail:document.getElementById(\"app-progress-stage-detail\"),\n  doneButton:document.getElementById(\"app-progress-done\")\n};\nlet appProgressHideTimer=0;\nlet appProgressDoneCallback=null;\nfunction appProgressSet({kicker,title,subtitle,percent,stageTitle,stageDetail,icon,state}={}){\n  const ui=appProgressUi;if(!ui.backdrop)return;\n  if(kicker!=null)ui.kicker.textContent=String(kicker);\n  if(title!=null)ui.title.textContent=String(title);\n  if(subtitle!=null)ui.subtitle.textContent=String(subtitle);\n  if(percent!=null){\n    const pct=Math.max(0,Math.min(100,Number(percent)||0));\n    if(ui.bar)ui.bar.style.width=`${pct}%`;\n    if(ui.percent)ui.percent.textContent=`${Math.round(pct)}%`;\n  }\n  if(stageTitle!=null)ui.stageTitle.textContent=String(stageTitle);\n  if(stageDetail!=null)ui.stageDetail.textContent=String(stageDetail);\n  if(icon!=null)ui.stageIcon.textContent=String(icon);\n  ui.backdrop.classList.toggle(\"is-success\",state===\"success\");\n  ui.backdrop.classList.toggle(\"is-error\",state===\"error\");\n}\nfunction appProgressOpen(options={}){\n  clearTimeout(appProgressHideTimer);\n  appProgressDoneCallback=null;\n  if(appProgressUi.doneButton){appProgressUi.doneButton.hidden=true;appProgressUi.doneButton.textContent=\"完成\";}\n  appProgressSet({...options,state:\"busy\"});\n  appProgressUi.backdrop?.setAttribute(\"aria-hidden\",\"false\");\n  document.body.classList.add(\"app-progress-active\");\n  requestAnimationFrame(()=>appProgressUi.backdrop?.classList.add(\"active\"));\n}\nfunction appProgressUpdate(options={}){appProgressSet(options);}\nfunction appProgressFinish({title=\"完成\",detail=\"Firebase 已同步最新版本。\",buttonText=\"完成\",onAcknowledge=null}={}){\n  appProgressSet({percent:100,stageTitle:title,stageDetail:detail,icon:\"✓\",state:\"success\"});\n  clearTimeout(appProgressHideTimer);\n  appProgressDoneCallback=typeof onAcknowledge===\"function\"?onAcknowledge:null;\n  if(appProgressUi.doneButton){appProgressUi.doneButton.hidden=false;appProgressUi.doneButton.textContent=buttonText||\"完成\";}\n}\nfunction appProgressFail(message=\"操作未完成\",{onAcknowledge=null}={}){\n  appProgressSet({stageTitle:\"未能完成\",stageDetail:message,icon:\"!\",state:\"error\"});\n  clearTimeout(appProgressHideTimer);\n  appProgressDoneCallback=typeof onAcknowledge===\"function\"?onAcknowledge:null;\n  if(appProgressUi.doneButton){appProgressUi.doneButton.hidden=false;appProgressUi.doneButton.textContent=\"關閉\";}\n}\nfunction appProgressClose(){\n  clearTimeout(appProgressHideTimer);\n  if(appProgressUi.doneButton)appProgressUi.doneButton.hidden=true;\n  appProgressUi.backdrop?.classList.remove(\"active\",\"is-success\",\"is-error\");\n  appProgressUi.backdrop?.setAttribute(\"aria-hidden\",\"true\");\n  document.body.classList.remove(\"app-progress-active\");\n}\nappProgressUi.doneButton?.addEventListener(\"click\",()=>{\n  const callback=appProgressDoneCallback;appProgressDoneCallback=null;appProgressClose();\n  try{callback?.();}catch(error){console.error(\"Progress acknowledge callback\",error);}\n});\nfunction importProgressPresentation(progress,mode){\n  const total=Math.max(1,Number(progress?.total)||1),completed=Math.max(0,Number(progress?.completed)||0);\n  const pct=Math.max(4,Math.min(99,(completed/total)*100));\n  const stage=String(progress?.stage||\"content\");\n  if(stage===\"snapshot\")return {percent:Math.max(8,pct),stageTitle:\"建立更新前備份\",stageDetail:\"先保存目前 Firebase 版本，避免更新後無法返回。\",icon:\"▣\"};\n  if(stage===\"prepare\")return {percent:Math.max(16,pct),stageTitle:mode===\"replace\"?\"準備更新旅程\":\"建立 Firebase 旅程\",stageDetail:mode===\"replace\"?\"已確認權限，準備寫入新版本。\":\"正在建立 Trip 同 Owner 權限。\",icon:\"↥\"};\n  if(stage===\"finalize\")return {percent:Math.max(92,pct),stageTitle:\"完成同步\",stageDetail:\"正在寫入 Revision 同操作記錄。\",icon:\"↥\"};\n  return {percent:Math.max(20,pct),stageTitle:\"寫入旅程內容\",stageDetail:progress?.detail?`${progress.detail} 項操作已處理`:`正在同步 Days、行程項目、收藏及設定。`,icon:\"↥\"};\n}\nfunction restoreProgressPresentation(progress){\n  const total=Math.max(1,Number(progress?.total)||1),completed=Math.max(0,Number(progress?.completed)||0);\n  const pct=Math.max(4,Math.min(99,(completed/total)*100));\n  const stage=String(progress?.stage||\"\");\n  const detail=String(progress?.detail||\"\");\n  if(stage===\"preflight-start\")return {percent:pct,stageTitle:\"驗證目前旅程\",stageDetail:detail||\"正在開始 Restore preflight。\",icon:\"↶\"};\n  if(stage===\"preflight-trip-root\")return {percent:pct,stageTitle:\"讀取目前旅程\",stageDetail:detail||\"正在讀取 Trip 主資料。\",icon:\"↶\"};\n  if(stage===\"preflight-trip-collections\")return {percent:pct,stageTitle:\"讀取旅程資料\",stageDetail:detail||\"正在讀取 Days、收藏及設定。\",icon:\"↶\"};\n  if(stage===\"preflight-day-items\"||stage===\"preflight-trip-ready\")return {percent:pct,stageTitle:\"讀取行程項目\",stageDetail:detail||\"正在確認目前行程內容。\",icon:\"↶\"};\n  if(stage===\"preflight-role\")return {percent:pct,stageTitle:\"驗證還原權限\",stageDetail:detail||\"正在確認 Owner / Admin 角色。\",icon:\"↶\"};\n  if(stage===\"preflight-lock\")return {percent:pct,stageTitle:\"檢查旅程狀態\",stageDetail:detail||\"正在確認旅程可以還原。\",icon:\"↶\"};\n  if(stage===\"preflight-operation\"||stage===\"preflight-operation-ready\")return {percent:pct,stageTitle:\"檢查操作狀態\",stageDetail:detail||\"正在避免多部裝置同時修改旅程。\",icon:\"↶\"};\n  if(stage===\"preflight-snapshot\")return {percent:pct,stageTitle:\"讀取 Snapshot\",stageDetail:detail||\"正在讀取所選版本。\",icon:\"↶\"};\n  if(stage===\"preflight-safety-snapshot\")return {percent:pct,stageTitle:\"建立安全 Snapshot\",stageDetail:detail||\"先保存目前版本，再開始還原。\",icon:\"▣\"};\n  if(stage===\"safety-snapshot\")return {percent:Math.max(32,pct),stageTitle:\"已保存目前版本\",stageDetail:detail||\"還原前安全 Snapshot 已建立。\",icon:\"▣\"};\n  if(stage===\"trip\")return {percent:Math.max(48,pct),stageTitle:\"切換旅程版本\",stageDetail:\"正在更新 Trip metadata 同 Revision。\",icon:\"↶\"};\n  if(stage===\"content\"||stage===\"trip-content\")return {percent:pct,stageTitle:\"還原行程資料\",stageDetail:detail?`${detail} 項操作已處理`:`正在還原 Days、Items、收藏及一般設定。`,icon:\"↶\"};\n  if(stage===\"expense-content\")return {percent:pct,stageTitle:\"還原支出資料\",stageDetail:\"正在同步支出、找數及支出設定。\",icon:\"↶\"};\n  if(stage===\"done\")return {percent:99,stageTitle:\"同步最新版本\",stageDetail:\"Firebase Loader 正在接收已還原內容。\",icon:\"↶\"};\n  return {percent:pct,stageTitle:\"準備還原\",stageDetail:detail||\"正在驗證 Backup 同目前 Trip。\",icon:\"↶\"};\n}\n\n// Phase 2C · Portable JSON -> Firestore import. This is deliberately isolated\n// from the critical trip.json renderer, so an import or rules failure can never\n// blank the itinerary.\nconst tripImportButton=document.getElementById(\"trip-import-btn\");\nconst tripImportFile=document.getElementById(\"trip-import-file\");\nconst tripImportBackdrop=document.getElementById(\"trip-import-backdrop\");\nconst tripImportConfirm=document.getElementById(\"trip-import-confirm\");\nconst tripImportCancel=document.getElementById(\"trip-import-cancel\");\nlet tripImportState={raw:null,inspection:null,done:false,busy:false};\nlet tripImportServicePromise=null;\nlet tripImportInspectionRun=0;\nconst TRIP_IMPORT_SLOW_INSPECTION_MS=8000;\nfunction tripCloudOffline(){\n  return navigator.onLine===false || cloudSafetyState?.online===false;\n}\nfunction getTripImportService(){\n  if(!tripImportServicePromise){\n    tripImportServicePromise=import(`./assets/js/trip-import-service.js?build=${APP_VERSION}`)\n      .catch(error=>{tripImportServicePromise=null;throw error;});\n  }\n  return tripImportServicePromise;\n}\nfunction warmTripImportDependencies(){\n  return Promise.allSettled([getTripImportService(),getGlobalAuthService()]);\n}\nfunction scheduleTripImportPrewarm(){\n  const warm=()=>{void warmTripImportDependencies();};\n  if(\"requestIdleCallback\" in window)window.requestIdleCallback(warm,{timeout:2500});\n  else setTimeout(warm,900);\n}\nscheduleTripImportPrewarm();\nfunction openTripImportModal(){\n  if(!tripImportBackdrop)return;\n  tripImportBackdrop.setAttribute(\"aria-hidden\",\"false\");\n  lockScroll();\n  requestAnimationFrame(()=>tripImportBackdrop.classList.add(\"active\"));\n}\nfunction closeTripImportModal(){\n  if(!tripImportBackdrop||tripImportState.busy)return;\n  tripImportInspectionRun+=1;\n  tripImportBackdrop.classList.remove(\"active\");\n  tripImportBackdrop.setAttribute(\"aria-hidden\",\"true\");\n  unlockScroll();\n  tripImportState={raw:null,inspection:null,done:false,busy:false};\n  if(tripImportFile)tripImportFile.value=\"\";\n}\nfunction tripImportSetError(message=\"\"){\n  const box=document.getElementById(\"trip-import-error\");\n  if(!box)return;\n  box.hidden=!message;\n  box.textContent=message;\n}\nfunction tripImportPermissionMessage(error){\n  if(error?.code===\"auth-required\") return \"請先登入 Google 帳戶，再繼續匯入。\";\n  if(error?.code===\"permission-denied\") return \"目前帳戶冇權限進行呢個 Firebase 操作。\";\n  if(error?.code===\"creator-required\") return \"目前 Google 帳戶未獲授權建立新 Trip。你仍然可以接受旅程邀請，或者使用已有權限嘅 Trip。\";\n  if(error?.code===\"trip-id-unavailable\"||error?.code===\"trip-exists\") return \"呢個 Trip ID 已被使用。為保護其他旅程私隱，系統唔會顯示該 Trip 嘅名稱、Owner 或其他資料。\";\n  if(error?.code===\"trip-global-locked\") return \"旅程已全域鎖定，目前只可查看或匯出；請先到「我的 → 旅程設定 → 旅程鎖定」解除鎖定。\";\n  if(error?.code===\"offline\") return \"目前離線，無法連接 Firebase。請重新連線後再試。\";\n  if(error?.code===\"operation-busy\"||error?.code===\"trip-operation-busy\") return cloudSafetyFriendlyError(error);\n  if(error?.code===\"snapshot-too-large\") return \"現有 Trip 太大，未能安全建立單一 pre-import snapshot。今次唔會覆蓋任何資料。\";\n  if(error?.code===\"insufficient-role\") return \"呢個 tripId 已存在，但目前帳戶唔係 Owner / Admin，所以唔可以覆蓋。\";\n  const rawMessage=String(error?.message||\"\");\n  if(tripCloudOffline()) return \"目前離線，無法連接 Firebase。你仍然可以查看已快取旅程；重新連線後先再匯入、封存或還原。\";\n  if(/module script failed|failed to fetch dynamically imported module|abbreviated destructuring/i.test(rawMessage)) return \"功能模組未能載入。請重新整理 App 後再試；現有 Firebase 旅程資料不受影響。\";\n  return rawMessage||\"暫時未能檢查或匯入呢份 trip.json。\";\n}\nfunction renderTripImportChanges(changeSummary){\n  const box=document.getElementById(\"trip-import-changes\");\n  if(!box)return;\n  if(!changeSummary||changeSummary.unchanged){box.hidden=true;box.innerHTML=\"\";return;}\n  const fmt=part=>`+${part?.added||0} · ~${part?.modified||0} · −${part?.deleted||0}`;\n  const extras=[];\n  if(changeSummary.tripChanged)extras.push(\"旅程資料 1\");\n  if(changeSummary.settingsChanged)extras.push(`設定 ${changeSummary.settingsChanged}`);\n  box.innerHTML=`<div class=\"trip-import-change-title\">偵測到變更</div><div class=\"trip-import-change-grid\"><div class=\"trip-import-change-row\"><span>行程項目</span><strong>${fmt(changeSummary.items)}</strong></div><div class=\"trip-import-change-row\"><span>日子</span><strong>${fmt(changeSummary.days)}</strong></div><div class=\"trip-import-change-row\"><span>收藏</span><strong>${fmt(changeSummary.savedPlaces)}</strong></div><div class=\"trip-import-change-row\"><span>其他</span><strong>${escapeHtmlText(extras.join(\" · \")||\"0\")}</strong></div></div>`;\n  box.hidden=false;\n}\nfunction renderTripImportPreview(inspection,{checking=false}={}){\n  const summary=inspection?.summary||{};\n  document.getElementById(\"trip-import-name\").textContent=summary.title||\"未命名旅程\";\n  document.getElementById(\"trip-import-id\").textContent=`tripId · ${summary.tripId||\"—\"}`;\n  document.getElementById(\"trip-import-days\").textContent=String(summary.dayCount??\"—\");\n  document.getElementById(\"trip-import-items\").textContent=String(summary.itemCount??\"—\");\n  document.getElementById(\"trip-import-saved\").textContent=String(summary.savedPlaceCount??\"—\");\n  document.getElementById(\"trip-import-maps\").textContent=summary.itemCount!=null?`${summary.mapReferenceCount||0}/${summary.itemCount}`:\"—\";\n  const mode=document.getElementById(\"trip-import-mode\");\n  const confirm=document.getElementById(\"trip-import-confirm\");\n  const warnings=document.getElementById(\"trip-import-warnings\");\n  warnings.innerHTML=(inspection?.warnings||[]).map(w=>`<div class=\"trip-import-warning\">${escapeHtmlText(w)}</div>`).join(\"\");\n  renderTripImportChanges(inspection?.changeSummary||null);\n  if(checking){mode.innerHTML=\"正在比較 Firebase 現有版本同呢份 JSON…\";confirm.textContent=\"正在檢查…\";confirm.disabled=true;return;}\n  if(inspection?.mode===\"wrong-file-full-backup\"){\n    mode.innerHTML=\"<strong>呢份係完整 Backup JSON</strong><br>唔可以經『匯入 trip.json』寫入 Firebase。請使用『還原完整 Backup』，系統已經阻止今次錯誤匯入。\";\n    confirm.textContent=\"改用還原完整 Backup\";confirm.disabled=false;return;\n  }\n  if(!inspection?.valid){mode.innerHTML=\"JSON 驗證未通過。\";confirm.textContent=\"匯入 Firebase\";confirm.disabled=true;return;}\n  if(inspection.mode===\"inspection-slow\"){\n    mode.innerHTML=\"<strong>Firebase 回應較慢</strong><br>JSON 已經驗證完成。你可以繼續等候，檢查完成後會自動更新；或者直接重新檢查，唔使再揀檔案。\";\n    confirm.textContent=\"重新檢查\";confirm.disabled=false;return;\n  }\n  if(inspection.mode===\"inspection-error\"){\n    mode.innerHTML=\"<strong>未能完成 Firebase 檢查</strong><br>JSON 已保留，可以直接重新檢查，唔使重新選擇檔案。\";\n    confirm.textContent=\"重新檢查\";confirm.disabled=false;return;\n  }\n  if(inspection.mode===\"auth-required\"){\n    mode.innerHTML=\"<strong>JSON 驗證完成</strong><br>登入 Google 後先檢查 Firebase 有冇相同 tripId，再決定建立或取代。\";\n    confirm.textContent=\"登入 Google 繼續\";confirm.disabled=false;return;\n  }\n  if(inspection.mode===\"creator-required\"){\n    mode.innerHTML=\"<strong>未獲授權建立新 Trip</strong><br>一般旅程成員可以接受邀請同使用已有 Trip，但建立全新 Firebase Trip 需要獨立 Creator 權限。\";\n    confirm.textContent=\"未有建立權限\";confirm.disabled=true;\n  }else if(inspection.mode===\"collision\"){\n    mode.innerHTML=\"<strong>Trip ID 已被使用</strong><br>呢個 canonical Trip ID 已經存在。系統唔會透露其他私人旅程嘅名稱、Owner 或成員資料。\";\n    confirm.textContent=\"Trip ID 已使用\";confirm.disabled=true;\n  }else if(inspection.mode===\"create\"){\n    mode.innerHTML=\"<strong>建立新旅程</strong><br>你已獲授權建立新 Trip；寫入 Firebase 後，目前 Google 帳戶會自動成為 Owner。\";\n    confirm.textContent=\"匯入 Firebase\";confirm.disabled=!inspection.canImport;\n  }else if(inspection.mode===\"unchanged\"){\n    mode.innerHTML=\"<strong>沒有變更</strong><br>呢份 JSON 同 Firebase 現有旅程內容相同，所以唔會建立 Snapshot、唔會增加 revision，亦唔會重新寫入資料。\";\n    confirm.textContent=\"完成\";confirm.disabled=false;\n  }else if(inspection.mode===\"replace\"){\n    mode.innerHTML=inspection.canImport\n      ? `<strong>更新現有旅程</strong><br>你目前係 ${escapeHtmlText(inspection.roleLabel||inspection.role||\"Owner / Admin\")}。只因為偵測到實際變更，系統先會建立 pre-import snapshot，再更新 Firebase。`\n      : \"<strong>同一 tripId 已存在</strong><br>只有 Owner / Admin 可以用 JSON 取代現有旅程。\";\n    confirm.textContent=\"Snapshot 後更新\";confirm.disabled=!inspection.canImport;\n  }else{\n    mode.innerHTML=\"未能確認匯入模式。\";confirm.disabled=true;\n  }\n}\nasync function getImportFirebaseUser(){\n  // Fast path: the App may already know the signed-in identity even while the\n  // auth module is still finishing its first cold-start callback.\n  const knownUser=tripLibraryCurrentUser||window.__appAuthUser||null;\n  if(knownUser?.uid)return knownUser;\n  const authService=await getGlobalAuthService();\n  let user=authService.getCurrentUser?.()||null;\n  if(!user && authService.waitForInitialAuth) user=await authService.waitForInitialAuth();\n  return user||tripLibraryCurrentUser||window.__appAuthUser||authService.getCurrentUser?.()||null;\n}\nfunction tripImportInspectionStillCurrent(runId,raw){\n  return runId===tripImportInspectionRun&&tripImportState.raw===raw&&!tripImportState.done;\n}\nasync function inspectPreparedTripImport({showChecking=true}={}){\n  const raw=tripImportState.raw;\n  if(!raw)return null;\n  const runId=++tripImportInspectionRun;\n  const baseInspection={...(tripImportState.inspection||{}),mode:\"checking\",canImport:false};\n  if(showChecking){tripImportSetError(\"\");renderTripImportPreview(baseInspection,{checking:true});}\n  let slowTimer=setTimeout(()=>{\n    if(!tripImportInspectionStillCurrent(runId,raw)||tripImportState.busy)return;\n    const slowInspection={...(tripImportState.inspection||baseInspection),mode:\"inspection-slow\",canImport:false};\n    tripImportState.inspection=slowInspection;\n    renderTripImportPreview(slowInspection);\n  },TRIP_IMPORT_SLOW_INSPECTION_MS);\n  try{\n    const user=await getImportFirebaseUser();\n    if(!tripImportInspectionStillCurrent(runId,raw))return null;\n    if(!user?.uid){\n      const inspection={...baseInspection,mode:\"auth-required\",canImport:false};\n      tripImportState.inspection=inspection;\n      renderTripImportPreview(inspection);\n      return inspection;\n    }\n    const service=await getTripImportService();\n    const inspection=await service.inspectTripImport(raw,user);\n    if(!tripImportInspectionStillCurrent(runId,raw))return inspection;\n    tripImportState.inspection=inspection;\n    tripImportSetError(\"\");\n    renderTripImportPreview(inspection);\n    return inspection;\n  }catch(error){\n    if(!tripImportInspectionStillCurrent(runId,raw))return null;\n    console.error(\"Trip JSON Firebase inspection error\",error);\n    if(tripCloudOffline()){\n      const offlineInspection={...baseInspection,mode:\"offline\",canImport:false};\n      tripImportState.inspection=offlineInspection;\n      renderTripImportPreview(offlineInspection);\n      tripImportSetError(\"目前離線，無法連接 Firebase 比較或匯入。你仍然可以查看已快取旅程；重新連線後再試。\");\n      if(tripImportConfirm)tripImportConfirm.disabled=true;\n      return offlineInspection;\n    }\n    const retryInspection={...baseInspection,mode:\"inspection-error\",canImport:false};\n    tripImportState.inspection=retryInspection;\n    renderTripImportPreview(retryInspection);\n    tripImportSetError(tripImportPermissionMessage(error));\n    return retryInspection;\n  }finally{\n    clearTimeout(slowTimer);\n  }\n}\nfunction detectTravelJsonKind(raw){\n  if(!raw||typeof raw!==\"object\"||Array.isArray(raw))return \"unknown\";\n  if(String(raw.backupFormat||\"\").trim()===\"travel-full-backup\")return \"full-backup\";\n  if(raw.schemaVersion!=null||raw.meta||Array.isArray(raw.days)||raw.snacks)return \"trip-json\";\n  return \"unknown\";\n}\nfunction fullBackupImportMismatchInspection(raw){\n  const portable=raw?.data?.portableTrip||{},counts=raw?.counts||{};\n  const title=String(portable?.meta?.titleSmall||portable?.meta?.titleMain||portable?.meta?.title||raw?.tripId||\"完整 Backup\");\n  return {valid:false,mode:\"wrong-file-full-backup\",canImport:false,warnings:[\"已阻止匯入：Firebase 未有任何改動。\"],summary:{title,tripId:String(raw?.tripId||portable?.tripId||\"\"),dayCount:Number(counts.dayCount||0),itemCount:Number(counts.itemCount||0),savedPlaceCount:Number(counts.savedPlaceCount||0),mapReferenceCount:0}};\n}\nasync function prepareTripImportFile(file){\n  if(!file)return;\n  openTripImportModal();\n  tripImportSetError(\"\");\n  const progress=document.getElementById(\"trip-import-progress\");\n  if(progress)progress.hidden=true;\n  try{\n    if(file.size>5_000_000)throw new Error(\"trip.json 大過 5 MB；請先移除 base64 圖片或其他大型內嵌資料。\");\n    const raw=JSON.parse(await file.text());\n    if(detectTravelJsonKind(raw)===\"full-backup\"){\n      const mismatch=fullBackupImportMismatchInspection(raw);\n      tripImportState={raw,inspection:mismatch,done:false,busy:false};\n      renderTripImportPreview(mismatch);\n      return;\n    }\n    const schema=await import(`./assets/js/trip-schema-service.js?build=${APP_VERSION}`);\n    const validation=schema.validatePortableTrip(raw);\n    const localInspection={...validation,summary:schema.getTripSummary(validation.trip),mode:validation.valid?\"checking\":\"invalid\",canImport:false};\n    tripImportState={raw,inspection:localInspection,done:false,busy:false};\n    renderTripImportPreview(localInspection,{checking:validation.valid});\n    if(!validation.valid){tripImportSetError(validation.errors.join(\"；\"));return;}\n    if(tripCloudOffline()){\n      const offlineInspection={...localInspection,mode:\"offline\",canImport:false};\n      tripImportState.inspection=offlineInspection;\n      renderTripImportPreview(offlineInspection);\n      tripImportSetError(\"目前離線，無法連接 Firebase 比較或匯入。你仍然可以查看已快取旅程；重新連線後再試。\");\n      if(tripImportConfirm)tripImportConfirm.disabled=true;\n      return;\n    }\n    // Run Firebase inspection independently from the file-picker event. A slow\n    // first handshake must never leave the modal permanently disabled.\n    inspectPreparedTripImport({showChecking:false}).catch(error=>console.error(\"Trip JSON inspection task\",error));\n  }catch(error){\n    console.error(\"Trip JSON inspection error\",error);\n    tripImportSetError(tripImportPermissionMessage(error));\n    if(tripImportConfirm)tripImportConfirm.disabled=true;\n  }\n}\ntripImportButton?.addEventListener(\"pointerdown\",()=>{warmTripImportDependencies();},{passive:true});\ntripImportButton?.addEventListener(\"click\",()=>{if(currentTripGlobalLocked())return;warmTripImportDependencies();tripImportFile?.click();});\ntripImportFile?.addEventListener(\"change\",()=>prepareTripImportFile(tripImportFile.files?.[0]||null));\ntripImportCancel?.addEventListener(\"click\",closeTripImportModal);\ntripImportBackdrop?.addEventListener(\"click\",event=>{if(event.target===tripImportBackdrop)closeTripImportModal();});\ntripImportConfirm?.addEventListener(\"click\",async()=>{\n  if(tripImportState.done){closeTripImportModal();return;}\n  if(tripImportState.inspection?.mode===\"wrong-file-full-backup\"){\n    const raw=tripImportState.raw;\n    closeTripImportModal();\n    try{\n      const service=await getTripBackupService(),inspection=service.inspectFullBackup(raw);\n      await routeFullBackupSelection(raw,inspection);\n    }catch(error){\n      console.error(\"Route Full Backup from trip import\",error);\n      dataOperationOpen(dataOperationBaseConfig({kind:\"restore-full-error\",title:\"還原完整 Backup\",subtitle:\"呢份檔案已識別為 Full Backup，但未能進入目前旅程嘅還原流程。\",typeLabel:\"Error\",mode:\"請確認 Backup Trip ID 同目前旅程一致。\",confirmLabel:\"完成\"}));\n      dataOperationState.done=true;dataOperationSetError(backupFriendlyError(error));\n    }\n    return;\n  }\n  if(tripCloudOffline()){\n    tripImportSetError(\"目前離線，無法連接 Firebase 匯入旅程。請重新連線後再試。\");\n    return;\n  }\n  let inspection=tripImportState.inspection;\n  if(!tripImportState.raw||tripImportState.busy)return;\n  if(inspection?.mode===\"unchanged\"){closeTripImportModal();return;}\n  if(inspection?.mode===\"inspection-slow\"||inspection?.mode===\"inspection-error\"){\n    inspectPreparedTripImport({showChecking:true}).catch(error=>console.error(\"Trip JSON reinspection task\",error));\n    return;\n  }\n  if(inspection?.mode===\"auth-required\"){\n    tripImportSetError(\"\");\n    tripImportConfirm.disabled=true;\n    try{\n      const authService=await getGlobalAuthService();\n      const credential=await authService.signInWithGoogle();\n      const user=credential?.user||authService.getCurrentUser?.()||await authService.waitForInitialAuth?.();\n      if(!user?.uid){const e=new Error(\"Google sign-in required\");e.code=\"auth-required\";throw e;}\n      const service=await getTripImportService();\n      inspection=await service.inspectTripImport(tripImportState.raw,user);\n      tripImportState.inspection=inspection;\n      renderTripImportPreview(inspection);\n    }catch(error){\n      if(error?.code!==\"auth/popup-closed-by-user\")tripImportSetError(tripImportPermissionMessage(error));\n      tripImportConfirm.disabled=false;\n    }\n    return;\n  }\n  if(!inspection?.canImport)return;\n  tripImportState.busy=true;tripImportConfirm.disabled=true;tripImportCancel.disabled=true;\n  tripImportSetError(\"\");\n  const progress=document.getElementById(\"trip-import-progress\"),bar=progress?.querySelector(\"span\"),mode=document.getElementById(\"trip-import-mode\");\n  if(progress)progress.hidden=false;if(bar)bar.style.width=\"3%\";\n  mode.innerHTML=inspection.mode===\"replace\"?\"正在建立 snapshot 並更新 Firebase…\":\"正在建立 Firebase Trip 同 Owner 權限…\";\n  appProgressOpen({kicker:\"TRIP IMPORT\",title:inspection.mode===\"replace\"?\"更新旅程中\":\"建立旅程中\",subtitle:\"請保持 App 開啟，完成前請勿重複匯入或切換旅程。\",percent:3,stageTitle:\"準備 Firebase\",stageDetail:inspection.mode===\"replace\"?\"正在比較現有版本並準備安全更新。\":\"正在準備建立 Trip 同 Owner 權限。\",icon:\"↥\"});\n  try{\n    const service=await getTripImportService();\n    const user=await getImportFirebaseUser();\n    if(!user?.uid){const e=new Error(\"Google sign-in required\");e.code=\"auth-required\";throw e;}\n    const result=await service.importTrip(tripImportState.raw,{mode:inspection.mode,user,onProgress:progressInfo=>{const {completed,total}=progressInfo||{};if(bar)bar.style.width=`${Math.max(3,Math.min(100,(Number(completed||0)/Math.max(1,Number(total||1)))*100))}%`;appProgressUpdate(importProgressPresentation(progressInfo,inspection.mode));}});\n    tripImportState.done=true;\n    if(bar)bar.style.width=\"100%\";\n    if(result.mode!==\"unchanged\")restartActiveTripCatalog();\n    if(result.mode===\"create\"&&document.body.classList.contains(\"app-entry-gated\")){\n      try{const {session}=await getTripLoaderServices();appEntryForcedTripId=String(result.tripId||\"\");session.setActiveTripId(result.tripId);updateTripUrl(result.tripId);bootstrapVerifiedFirebaseTrip(result.tripId,user).catch(error=>console.warn(\"Created Trip bootstrap\",error));}catch(error){console.warn(\"Created Trip route\",error);}\n    }\n    if(result.mode!==\"unchanged\"&&result.tripId===tripLibrarySummary?.tripId){\n      import(`./assets/js/trip-access-service.js?build=${APP_VERSION}`).then(service=>service.refreshTripAccess?.()).catch(()=>{});\n    }\n    mode.innerHTML=result.mode===\"unchanged\"\n      ? `<strong>沒有變更</strong><br>${escapeHtmlText(result.tripId)} 同 Firebase 現有內容相同，冇建立 Snapshot，亦冇寫入任何旅程資料。`\n      : `<strong>匯入完成</strong><br>${escapeHtmlText(result.tripId)} · revision ${result.revision} · ${result.mode===\"create\"?\"你已成為 Owner\":\"已建立 snapshot 後更新\"}。`;\n    tripImportConfirm.textContent=\"完成\";tripImportConfirm.disabled=false;\n    tripImportCancel.disabled=true;\n    let liveSynced=true;\n    if(result.mode!==\"unchanged\"&&result.tripId===tripLoaderActiveId)liveSynced=await waitForTripLoaderRevision(result.tripId,result.revision,7000);\n    appProgressFinish({title:result.mode===\"unchanged\"?\"沒有變更\":\"匯入完成\",detail:result.mode===\"unchanged\"?\"Firebase 現有內容同 JSON 相同。\":(liveSynced?`Revision ${result.revision} 已寫入 Firebase，行程畫面亦已同步。`:`Revision ${result.revision} 已寫入 Firebase；畫面仍在背景同步，可按完成返回。`),onAcknowledge:()=>{if(tripImportState.done)closeTripImportModal();}});\n  }catch(error){\n    console.error(\"Trip JSON import error\",error);\n    tripImportSetError(tripImportPermissionMessage(error));\n    tripImportConfirm.disabled=false;tripImportCancel.disabled=false;\n    mode.textContent=\"匯入未完成，現有本機行程不受影響。\";\n    appProgressFail(tripImportPermissionMessage(error));\n  }finally{tripImportState.busy=false;}\n});\n\nasync function handleTripArchiveAction(button,archived){\n  const tripId=String(button?.dataset?.tripArchiveId||button?.dataset?.tripRestoreId||\"\").trim();\n  if(!tripId||button.disabled)return;\n  const verb=archived?\"封存\":\"還原\";\n  if(tripCloudOffline()){\n    alert(`目前離線，無法${verb}旅程。重新連線後再試；已快取旅程仍然可以照常查看。`);\n    return;\n  }\n  const trip=tripLibraryCloudState.trips?.find?.(item=>String(item?.tripId||\"\")===tripId)||null;\n  const title=trip?.title||tripId;\n  const confirmText=archived\n    ? `封存「${title}」？\\n\\n呢個只係你自己嘅旅程清單偏好，其他成員完全不受影響，Firebase 旅程資料同權限亦唔會改變。`\n    : `還原「${title}」？\\n\\n旅程會重新出現喺你嘅 Active Trips。`;\n  if(!window.confirm(confirmText))return;\n  button.disabled=true;\n  try{\n    const service=await import(`./assets/js/trip-preferences-service.js?build=${APP_VERSION}`);\n    const user=window.__appAuthUser||await getImportFirebaseUser();\n    await service.setPersonalTripArchived(tripId,archived,{user});\n\n    tripLibraryCloudState={...tripLibraryCloudState,trips:(tripLibraryCloudState.trips||[]).map(item=>String(item?.tripId||\"\")===tripId?{...item,archived:archived===true,archiveSource:\"personal\"}:item)};\n    renderTripLibrary();\n\n    const visibleId=String(tripLoaderActiveId||dataTripId(window.tripData)||\"\").trim();\n    if(archived&&visibleId===tripId){\n      const next=(tripLibraryCloudState.trips||[]).find(item=>item?.archived!==true&&String(item?.tripId||\"\")!==tripId);\n      if(next){\n        await switchActiveCloudTrip(next.tripId);\n      }else{\n        const {session}=await getTripLoaderServices();\n        session.clearActiveTripId?.();updateTripUrl(\"\");\n        appEntryOptimisticTripId=\"\";appEntryOptimisticUid=\"\";appEntryForcedTripId=\"\";clearAppEntryResumeLatch();endWarmRefreshGate();\n        stopFirebaseTripLoader({fallback:false});\n        applyAppEntryState(\"lobby\");renderTripLibrary();refreshAppEntryState();\n      }\n    }else if(!archived&&document.body.classList.contains(\"app-entry-gated\")){\n      const {session}=await getTripLoaderServices();\n      session.setActiveTripId(tripId);updateTripUrl(tripId);appEntryForcedTripId=tripId;\n      bootstrapRememberedFirebaseTrip(use");
