window.__travelRuntimeParts.push("ice(),user=window.__appAuthUser||tripLibraryCurrentUser||null;\n    const result=await service.removeTripBackgroundImage(tripId,{user});\n    setTripBackgroundMediaProgress(1,{show:true});\n    setTripBackgroundMediaStatus(result?.cleanup?.cleaned===false?\"背景已恢復，但舊 Storage 媒體未能即時清理。\":\"已移除自訂背景，旅程已恢復原有背景。\",{error:result?.cleanup?.cleaned===false});\n    setTimeout(()=>setTripBackgroundMediaProgress(0,{show:false}),700);\n  }catch(error){\n    console.error(\"Trip background remove\",error);setTripBackgroundMediaProgress(0,{show:false});setTripBackgroundMediaStatus(tripBackgroundMediaFriendlyError(error),{error:true});\n  }finally{\n    tripBackgroundMediaBusy=false;setTripMediaMutationBusy(false);renderTripBackgroundSettings(window.tripData,{preserveStatus:true});\n  }\n}\ndocument.getElementById(\"trip-background-settings-open-btn\")?.addEventListener(\"click\",()=>renderTripBackgroundSettings());\ndocument.getElementById(\"trip-background-media-upload\")?.addEventListener(\"click\",openTripBackgroundMediaPicker);\ndocument.getElementById(\"trip-background-media-remove\")?.addEventListener(\"click\",removeTripBackgroundMedia);\nwindow.__tripBackgroundSettingsRefresh=data=>{if(profileCurrentPage===\"trip-background-settings\")renderTripBackgroundSettings(data,{preserveStatus:tripBackgroundMediaBusy||!!tripBackgroundMediaStatusText});};\n\nfunction colourSwatches({selected=\"\",key=\"\",kind=\"trip\",disabled=false}={}){\n  return APPEARANCE_COLOUR_PRESETS.map(hex=>`<button class=\"destination-color-swatch${String(selected).toUpperCase()===hex?\" is-selected\":\"\"}\" type=\"button\" data-appearance-kind=\"${kind}\" data-appearance-key=\"${escapeHtmlText(key)}\" data-appearance-color=\"${hex}\" style=\"--swatch:${hex}\" aria-label=\"選擇 ${hex}\"${disabled?\" disabled\":\"\"}></button>`).join(\"\");\n}\nfunction renderTripColourSettings(data=window.tripData){\n  const list=document.getElementById(\"trip-color-list\"),status=document.getElementById(\"trip-color-status\");if(!list)return;\n  const canEdit=appearanceSettingsCanEdit(),current=String(data?.meta?.accentColor||\"#0A84FF\").toUpperCase();\n  list.innerHTML=`<div class=\"destination-color-item\" data-appearance-item=\"trip\">\n    <div class=\"destination-color-head\"><span class=\"destination-color-preview\" style=\"--destination-preview:${escapeHtmlText(current)}\"></span><span class=\"destination-color-copy\"><strong>旅程主色</strong><small>${escapeHtmlText(current)}</small></span><span class=\"destination-color-default-pill\">${appearanceColourSavingKey===\"trip\"?\"儲存中…\":\"目前\"}</span></div>\n    <div class=\"destination-color-palette\">${colourSwatches({selected:current,kind:\"trip\",disabled:!canEdit||!!appearanceColourSavingKey})}</div>\n    <div class=\"destination-color-actions\"><label class=\"destination-color-custom${canEdit&&!appearanceColourSavingKey?\"\":\" is-disabled\"}\">自訂顏色<input type=\"color\" data-appearance-custom=\"trip\" value=\"${escapeHtmlText(current)}\"${canEdit&&!appearanceColourSavingKey?\"\":\" disabled\"}></label></div>\n  </div>`;\n  if(status){status.classList.remove(\"is-error\");status.textContent=canEdit?\"主色會即時套用至 App 主要 Accent；收藏及支出如設定為跟隨亦會同步。\":(currentTripGlobalLocked()?\"旅程已全域鎖定，目前只可查看顏色設定。\":\"目前為唯讀。只有 Owner 或 Admin 可以修改旅程主色。\");}\n}\nfunction renderFeatureColourSettings(data=window.tripData){\n  const list=document.getElementById(\"feature-color-list\"),status=document.getElementById(\"feature-color-status\");if(!list)return;\n  const canEdit=appearanceSettingsCanEdit(),accent=String(data?.meta?.accentColor||\"#0A84FF\").toUpperCase(),featureColors=data?.meta?.featureColors||{};\n  const defs=[{key:\"savedPlaces\",label:\"收藏\",desc:\"收藏篩選及分類識別色\"},{key:\"expenses\",label:\"支出\",desc:\"支出主要操作及重點識別色\"}];\n  list.innerHTML=defs.map(def=>{\n    const explicit=parseHexColour(featureColors?.[def.key])?String(featureColors[def.key]).toUpperCase():\"\";\n    const current=explicit||accent,isSaving=appearanceColourSavingKey===def.key;\n    return `<div class=\"destination-color-item\" data-appearance-item=\"${def.key}\">\n      <div class=\"destination-color-head\"><span class=\"destination-color-preview\" style=\"--destination-preview:${escapeHtmlText(current)}\"></span><span class=\"destination-color-copy\"><strong>${def.label}</strong><small>${explicit?`自訂 ${escapeHtmlText(explicit)}`:`跟隨旅程主色 ${escapeHtmlText(accent)}`}</small></span><span class=\"destination-color-default-pill\">${isSaving?\"儲存中…\":(explicit?\"已自訂\":\"跟隨\")}</span></div>\n      <div class=\"destination-color-palette\">${colourSwatches({selected:explicit,key:def.key,kind:\"feature\",disabled:!canEdit||!!appearanceColourSavingKey})}</div>\n      <div class=\"destination-color-actions\"><label class=\"destination-color-custom${canEdit&&!appearanceColourSavingKey?\"\":\" is-disabled\"}\">自訂顏色<input type=\"color\" data-appearance-custom=\"${def.key}\" value=\"${escapeHtmlText(current)}\"${canEdit&&!appearanceColourSavingKey?\"\":\" disabled\"}></label><button class=\"destination-color-reset\" type=\"button\" data-appearance-follow=\"${def.key}\"${canEdit&&explicit&&!appearanceColourSavingKey?\"\":\" disabled\"}>跟隨旅程主色</button></div>\n    </div>`;\n  }).join(\"\");\n  if(status){status.classList.remove(\"is-error\");status.textContent=canEdit?\"預設跟隨旅程主色；只在你想刻意分開功能識別時先自訂。\":(currentTripGlobalLocked()?\"旅程已全域鎖定，目前只可查看功能顏色。\":\"目前為唯讀。只有 Owner 或 Admin 可以修改功能顏色。\");}\n}\nfunction applyAppearanceColourLocal(key,color,{feature=false,follow=false}={}){\n  if(follow){\n    const accent=String(window.tripData?.meta?.accentColor||\"#0A84FF\");\n    if(feature)document.documentElement.style.setProperty(key===\"expenses\"?\"--expense-accent\":\"--saved-accent\",accent);\n    return;\n  }\n  if(!parseHexColour(color))return;\n  const hex=String(color).toUpperCase();\n  if(key===\"trip\"){\n    document.documentElement.style.setProperty(\"--accent-color\",hex);\n    const fc=window.tripData?.meta?.featureColors||{};\n    if(!parseHexColour(fc.savedPlaces))document.documentElement.style.setProperty(\"--saved-accent\",hex);\n    if(!parseHexColour(fc.expenses))document.documentElement.style.setProperty(\"--expense-accent\",hex);\n  }else if(feature)document.documentElement.style.setProperty(key===\"expenses\"?\"--expense-accent\":\"--saved-accent\",hex);\n}\nasync function saveAppearanceColour(key,color,{follow=false,preservePicker=false}={}){\n  const tripId=appearanceSettingsTripId();if(!tripId||(!follow&&!parseHexColour(color)))return;\n  const saveKey=key||\"trip\";if(appearanceColourSavingKey&&!preservePicker)return;\n  if(!preservePicker)appearanceColourSavingKey=saveKey;\n  applyAppearanceColourLocal(saveKey,color,{feature:saveKey!==\"trip\",follow});\n  if(!preservePicker){renderTripColourSettings();renderFeatureColourSettings();}\n  const status=document.getElementById(saveKey===\"trip\"?\"trip-color-status\":\"feature-color-status\");\n  try{\n    const service=await getTripAppearanceService(),user=window.__appAuthUser||tripLibraryCurrentUser||null;\n    if(saveKey===\"trip\")await service.updateTripAccentColor(tripId,color,{user});\n    else if(follow)await service.resetFeatureColor(tripId,saveKey,{user});\n    else await service.updateFeatureColor(tripId,saveKey,color,{user});\n    if(status){status.classList.remove(\"is-error\");status.textContent=\"已儲存到 Firebase，其他裝置會同步更新。\";}\n  }catch(error){\n    if(status){status.classList.add(\"is-error\");status.textContent=appearanceFriendlyError(error);}\n    try{applyTripVisualTheme(window.tripData);}catch(e){}\n  }finally{\n    if(!preservePicker){appearanceColourSavingKey=\"\";renderTripColourSettings();renderFeatureColourSettings();}\n  }\n}\nfunction queueAppearanceColourSave(key,color){\n  if(appearanceColourSaveTimer)clearTimeout(appearanceColourSaveTimer);\n  appearanceColourSaveTimer=setTimeout(()=>{appearanceColourSaveTimer=0;appearanceColourSaveChain=appearanceColourSaveChain.catch(()=>{}).then(()=>saveAppearanceColour(key,color,{preservePicker:true}));},500);\n}\ndocument.getElementById(\"trip-color-settings-open-btn\")?.addEventListener(\"click\",()=>{appearanceColourPickerKey=\"\";setTimeout(()=>renderTripColourSettings(),40);});\ndocument.getElementById(\"feature-color-settings-open-btn\")?.addEventListener(\"click\",()=>{appearanceColourPickerKey=\"\";setTimeout(()=>renderFeatureColourSettings(),40);});\n[document.getElementById(\"trip-color-list\"),document.getElementById(\"feature-color-list\")].forEach(list=>{\n  list?.addEventListener(\"click\",event=>{\n    const swatch=event.target.closest?.(\"[data-appearance-color]\");if(swatch&&!swatch.disabled){appearanceColourPickerKey=\"\";saveAppearanceColour(swatch.dataset.appearanceKind===\"trip\"?\"trip\":swatch.dataset.appearanceKey,swatch.dataset.appearanceColor);return;}\n    const follow=event.target.closest?.(\"[data-appearance-follow]\");if(follow&&!follow.disabled){appearanceColourPickerKey=\"\";saveAppearanceColour(follow.dataset.appearanceFollow,\"\",{follow:true});}\n  });\n  list?.addEventListener(\"input\",event=>{\n    const input=event.target.closest?.(\"[data-appearance-custom]\");if(!input||input.disabled)return;\n    const key=input.dataset.appearanceCustom;appearanceColourPickerKey=key;applyAppearanceColourLocal(key,input.value,{feature:key!==\"trip\"});queueAppearanceColourSave(key,input.value);\n  });\n  list?.addEventListener(\"change\",event=>{\n    const input=event.target.closest?.(\"[data-appearance-custom]\");if(input&&!input.disabled){appearanceColourPickerKey=input.dataset.appearanceCustom;queueAppearanceColourSave(input.dataset.appearanceCustom,input.value);}\n  });\n});\nwindow.__appearanceColorSettingsRefresh=data=>{\n  if(appearanceColourPickerKey)return;\n  if(profileCurrentPage===\"trip-color-settings\")renderTripColourSettings(data);\n  if(profileCurrentPage===\"feature-color-settings\")renderFeatureColourSettings(data);\n};\n\n// =========================================================================\n// Phase 2F · v7.7.0.10 · Destination identity settings\n// =========================================================================\nconst DESTINATION_COLOUR_PRESETS=[\"#4A90D9\",\"#3F9F91\",\"#5A9E5A\",\"#D39F27\",\"#E06040\",\"#7B61D1\",\"#C45B8C\",\"#4D9DB0\"];\nlet tripDestinationServicePromise=null;\nlet destinationColorSavingKey=\"\";\n// iOS Safari's native colour picker may emit change/input while the system\n// picker is still on screen. Never rebuild the settings DOM during that\n// interaction or WebKit dismisses the picker immediately.\nlet destinationCustomColourPickerKey=\"\";\nlet destinationCustomColourSaveSeq=0;\nlet destinationCustomColourSaveTimer=0;\nlet destinationCustomColourSaveChain=Promise.resolve();\nfunction getTripDestinationService(){\n  if(!tripDestinationServicePromise)tripDestinationServicePromise=import(`./assets/js/trip-destination-service.js?build=${APP_VERSION}`);\n  return tripDestinationServicePromise;\n}\nfunction destinationSettingsTripId(){return String(tripLoaderActiveId||dataTripId(window.tripData)||\"\").trim();}\nfunction destinationSettingsCanEdit(){const role=String(window.__appTripAccess?.role||\"\");return !currentTripGlobalLocked()&&(role===\"owner\"||role===\"admin\");}\nfunction destinationSettingsFriendlyError(error){\n  const code=String(error?.code||\"\");\n  if(code===\"offline\")return\"目前離線，重新連線後先可以更新目的地顏色。\";\n  if(code===\"operation-busy\")return\"另一個雲端操作仍在進行，完成後再試。\";\n  if(code===\"insufficient-role\"||code===\"permission-denied\")return\"只有 Owner 或 Admin 可以修改目的地顏色。\";\n  if(code===\"destination-not-found\")return\"搵唔到呢個目的地設定，請先確認目前旅程資料。\";\n  if(code===\"invalid-colour\")return\"顏色格式無效。\";\n  return error?.message||\"未能更新目的地顏色。\";\n}\nfunction renderDestinationSettingsPage(data=window.tripData){\n  const list=document.getElementById(\"destination-color-list\"),status=document.getElementById(\"destination-color-status\");if(!list)return;\n  const cities=data?.meta?.cities||{},keys=orderedCityKeys(data,cities),themes=buildCityThemeMap(cities),canEdit=destinationSettingsCanEdit();\n  if(!keys.length){list.innerHTML='<div class=\"destination-color-empty\">目前旅程未設定目的地。</div>';if(status){status.textContent=\"Import 有 meta.cities 嘅 Trip 後，就可以喺呢度設定顏色。\";status.classList.remove(\"is-error\");}return;}\n  list.innerHTML=keys.map(key=>{\n    const cfg=cities[key]||{},theme=themes[key]||{},explicit=parseHexColour(cfg.color)?String(cfg.color).toUpperCase():\"\",current=explicit||String(theme.border||\"#999999\").toUpperCase();\n    const swatches=DESTINATION_COLOUR_PRESETS.map(hex=>`<button class=\"destination-color-swatch${explicit===hex?\" is-selected\":\"\"}\" type=\"button\" data-destination-color-key=\"${escapeHtmlText(key)}\" data-destination-color-value=\"${hex}\" style=\"--swatch:${hex}\" aria-label=\"將 ${escapeHtmlText(cfg.label||key)} 設為 ${hex}\"${canEdit&&destinationColorSavingKey!==key?\"\":\" disabled\"}></button>`).join(\"\");\n    return `<div class=\"destination-color-item\" data-destination-item=\"${escapeHtmlText(key)}\">\n      <div class=\"destination-color-head\">\n        <span class=\"destination-color-preview\" style=\"--destination-preview:${escapeHtmlText(current)}\"></span>\n        <span class=\"destination-color-copy\"><strong>${escapeHtmlText(cfg.label||key)}</strong><small>${explicit?`自訂 ${escapeHtmlText(explicit)}`:`預設 ${escapeHtmlText(current)}`}</small></span>\n        <span class=\"destination-color-default-pill\">${destinationColorSavingKey===key?\"儲存中…\":(explicit?\"已自訂\":\"預設\")}</span>\n      </div>\n      <div class=\"destination-color-palette\" aria-label=\"${escapeHtmlText(cfg.label||key)} 顏色選擇\">${swatches}</div>\n      <div class=\"destination-color-actions\">\n        <label class=\"destination-color-custom${canEdit&&destinationColorSavingKey!==key?\"\":\" is-disabled\"}\">自訂顏色<input type=\"color\" data-destination-custom-key=\"${escapeHtmlText(key)}\" value=\"${escapeHtmlText(current)}\"${canEdit&&destinationColorSavingKey!==key?\"\":\" disabled\"}></label>\n        <button class=\"destination-color-reset\" type=\"button\" data-destination-reset-key=\"${escapeHtmlText(key)}\"${canEdit&&explicit&&destinationColorSavingKey!==key?\"\":\" disabled\"}>還原預設</button>\n      </div>\n    </div>`;\n  }).join(\"\");\n  if(status){status.classList.remove(\"is-error\");status.textContent=canEdit?\"改色後會直接儲存到 Firebase，並同步更新 Day、跨城市漸變、天氣同城市標籤。\":(currentTripGlobalLocked()?\"旅程已全域鎖定，目前只可查看目的地顏色。\":\"目前為唯讀。只有 Owner 或 Admin 可以修改目的地顏色。\");}\n}\nfunction applyDestinationCustomColourLocalUI(cityKey,color){\n  if(!parseHexColour(color))return;\n  const hex=String(color||\"\").trim().toUpperCase();\n  const item=document.querySelector(`[data-destination-item=\"${CSS.escape(String(cityKey))}\"]`);if(!item)return;\n  const preview=item.querySelector(\".destination-color-preview\");if(preview)preview.style.setProperty(\"--destination-preview\",hex);\n  const copy=item.querySelector(\".destination-color-copy small\");if(copy)copy.textContent=`自訂 ${hex}`;\n  const pill=item.querySelector(\".destination-color-default-pill\");if(pill)pill.textContent=\"已自訂\";\n  item.querySelectorAll(\"[data-destination-color-value]\").forEach(button=>button.classList.toggle(\"is-selected\",String(button.dataset.destinationColorValue||\"\").toUpperCase()===hex));\n  const reset=item.querySelector(\"[data-destination-reset-key]\");if(reset)reset.disabled=false;\n}\nfunction cancelDestinationCustomColourSave(){\n  if(destinationCustomColourSaveTimer){clearTimeout(destinationCustomColourSaveTimer);destinationCustomColourSaveTimer=0;}\n}\nfunction queueDestinationCustomColourSave(cityKey,color){\n  cancelDestinationCustomColourSave();\n  destinationCustomColourSaveTimer=setTimeout(()=>{\n    destinationCustomColourSaveTimer=0;\n    destinationCustomColourSaveChain=destinationCustomColourSaveChain.catch(()=>{}).then(()=>saveDestinationColour(cityKey,color,{preserveNativePicker:true}));\n  },500);\n}\nasync function saveDestinationColour(cityKey,color,{reset=false,preserveNativePicker=false}={}){\n  if(destinationColorSavingKey&&!preserveNativePicker)return;\n  const tripId=destinationSettingsTripId();if(!tripId)return;\n  let finalMessage=\"\",isError=false;\n  const saveSeq=++destinationCustomColourSaveSeq;\n  if(!preserveNativePicker){\n    destinationCustomColourPickerKey=\"\";\n    destinationColorSavingKey=cityKey;\n    renderDestinationSettingsPage();\n  }else{\n    applyDestinationCustomColourLocalUI(cityKey,color);\n    const status=document.getElementById(\"destination-color-status\");\n    if(status){status.classList.remove(\"is-error\");status.textContent=\"正在儲存自訂顏色…\";}\n  }\n  try{\n    const service=await getTripDestinationService();\n    if(reset)await service.resetDestinationColor(tripId,cityKey,{user:window.__appAuthUser||null});\n    else await service.updateDestinationColor(tripId,cityKey,color,{user:window.__appAuthUser||null});\n    finalMessage=\"已儲存到 Firebase，其他裝置會跟住同步。\";\n  }catch(error){isError=true;finalMessage=destinationSettingsFriendlyError(error);}\n  finally{\n    if(!preserveNativePicker){\n      destinationColorSavingKey=\"\";renderDestinationSettingsPage();\n    }else if(isError){\n      // A failed save should restore the authoritative value, but only after\n      // the native picker is no longer the active interaction. Keeping the\n      // current DOM intact is more important than repainting mid-picker.\n      destinationCustomColourPickerKey=\"\";\n      renderDestinationSettingsPage();\n    }\n    const status=document.getElementById(\"destination-color-status\");\n    if(status&&finalMessage&&saveSeq===destinationCustomColourSaveSeq){status.classList.toggle(\"is-error\",isError);status.textContent=finalMessage;}\n  }\n}\ndocument.getElementById(\"destination-settings-open-btn\")?.addEventListener(\"click\",()=>{cancelDestinationCustomColourSave();destinationCustomColourPickerKey=\"\";setTimeout(()=>renderDestinationSettingsPage(),40);});\ndocument.getElementById(\"destination-color-list\")?.addEventListener(\"click\",event=>{\n  const custom=event.target.closest?.(\"[data-destination-custom-key]\");\n  if(custom&&!custom.disabled){destinationCustomColourPickerKey=String(custom.dataset.destinationCustomKey||\"\");return;}\n  const swatch=event.target.closest?.(\"[data-destination-color-key]\");if(swatch&&!swatch.disabled){cancelDestinationCustomColourSave();destinationCustomColourPickerKey=\"\";saveDestinationColour(swatch.dataset.destinationColorKey,swatch.dataset.destinationColorValue);return;}\n  const reset=event.target.closest?.(\"[data-destination-reset-key]\");if(reset&&!reset.disabled){cancelDestinationCustomColourSave();destinationCustomColourPickerKey=\"\";saveDestinationColour(reset.dataset.destinationResetKey,\"\",{reset:true});}\n});\ndocument.getElementById(\"destination-color-list\")?.addEventListener(\"input\",event=>{\n  const input=event.target.closest?.(\"[data-destination-custom-key]\");\n  if(input&&!input.disabled){\n    const key=String(input.dataset.destinationCustomKey||\"\");destinationCustomColourPickerKey=key;\n    applyDestinationCustomColourLocalUI(key,input.value);\n    queueDestinationCustomColourSave(key,input.value);\n  }\n});\ndocument.getElementById(\"destination-color-list\")?.addEventListener(\"change\",event=>{\n  const input=event.target.closest?.(\"[data-destination-custom-key]\");\n  if(input&&!input.disabled){\n    const key=String(input.dataset.destinationCustomKey||\"\");destinationCustomColourPickerKey=key;\n    // Critical iOS fix: persist without replacing the <input type=color> DOM.\n    // Replacing that node while the native picker is open makes Safari close\n    // the system colour sheet on the first adjustment. The short debounce also\n    // prevents several WebKit change events from racing Firestore writes.\n    queueDestinationCustomColourSave(key,input.value);\n  }\n});\nwindow.__destinationSettingsRefresh=data=>{\n  if(profileCurrentPage!==\"destination-settings\")return;\n  if(destinationCustomColourPickerKey)return;\n  renderDestinationSettingsPage(data);\n};\n\n\n// =========================================================================\n// Phase 2F · v7.7.1.2 · Team identity colour settings\n// =========================================================================\nlet tripTeamServicePromise=null;\nlet teamColorSavingKey=\"\";\nlet teamCustomColourPickerKey=\"\";\nlet teamCustomColourSaveSeq=0;\nlet teamCustomColourSaveTimer=0;\nlet teamCustomColourSaveChain=Promise.resolve();\nfunction getTripTeamService(){\n  if(!tripTeamServicePromise)tripTeamServicePromise=import(`./assets/js/trip-team-service.js?build=${APP_VERSION}`);\n  return tripTeamServicePromise;\n}\nfunction teamSettingsTripId(){return String(tripLoaderActiveId||dataTripId(window.tripData)||\"\").trim();}\nfunction teamSettingsCanEdit(){const role=String(window.__appTripAccess?.role||\"\");return !currentTripGlobalLocked()&&(role===\"owner\"||role===\"admin\");}\nfunction teamSettingsFriendlyError(error){\n  const code=String(error?.code||\"\");\n  if(code===\"offline\")return\"目前離線，重新連線後先可以更新 Team 顏色。\";\n  if(code===\"operation-busy\")return\"另一個雲端操作仍在進行，完成後再試。\";\n  if(code===\"insufficient-role\"||code===\"permission-denied\")return\"只有 Owner 或 Admin 可以修改 Team 顏色。\";\n  if(code===\"team-not-found\")return\"搵唔到呢個 Team 設定，請先確認目前旅程資料。\";\n  if(code===\"invalid-colour\")return\"顏色格式無效。\";\n  return error?.message||\"未能更新 Team 顏色。\";\n}\nfunction refreshAppearanceColourMenuState(data=window.tripData){\n  const button=document.getElementById(\"team-settings-open-btn\");if(!button)return;\n  const hasTeams=orderedTravellerKeys(data?.meta?.travellers||{}).length>0;\n  const subtitle=document.getElementById(\"team-settings-open-subtitle\");\n  const chevron=document.getElementById(\"team-settings-open-chevron\");\n  const status=document.getElementById(\"team-settings-open-status\");\n  button.disabled=!hasTeams;\n  button.classList.toggle(\"profile-row-disabled\",!hasTeams);\n  if(subtitle)subtitle.textContent=hasTeams?\"統一 Team、航班及行程標籤嘅識別色\":\"目前旅程未設定 Team\";\n  if(chevron)chevron.hidden=!hasTeams;\n  if(status){status.hidden=hasTeams;status.textContent=\"無\";}\n}\nfunction renderTeamSettingsPage(data=window.tripData){\n  const list=document.getElementById(\"team-color-list\"),status=document.getElementById(\"team-color-status\");if(!list)return;\n  const travellers=data?.meta?.travellers||{},keys=orderedTravellerKeys(travellers),canEdit=teamSettingsCanEdit();\n  if(!keys.length){\n    list.innerHTML='<div class=\"destination-color-empty\">目前旅程未設定 Team。</div>';\n    if(status){status.textContent=\"建立或匯入有 Team 資料嘅旅程後，就可以喺呢度設定顏色。\";status.classList.remove(\"is-error\");}\n    return;\n  }\n  list.innerHTML=keys.map(key=>{\n    const cfg=travellers[key]||{},explicit=parseHexColour(cfg.color)?String(cfg.color).toUpperCase():\"\",current=explicit||teamDefaultColour(key);\n    const swatches=TEAM_COLOUR_PRESETS.map(hex=>`<button class=\"destination-color-swatch${explicit===hex?\" is-selected\":\"\"}\" type=\"button\" data-team-color-key=\"${escapeHtmlText(key)}\" data-team-color-value=\"${hex}\" style=\"--swatch:${hex}\" aria-label=\"將 ${escapeHtmlText(cfg.label||key)} 設為 ${hex}\"${canEdit&&teamColorSavingKey!==key?\"\":\" disabled\"}></button>`).join(\"\");\n    return `<div class=\"destination-color-item\" data-team-color-item=\"${escapeHtmlText(key)}\">\n      <div class=\"destination-color-head\">\n        <span class=\"destination-color-preview\" style=\"--destination-preview:${escapeHtmlText(current)}\"></span>\n        <span class=\"destination-color-copy\"><strong>${escapeHtmlText(cfg.label||key)}</strong><small>${explicit?`自訂 ${escapeHtmlText(explicit)}`:`預設 ${escapeHtmlText(current)}`}</small></span>\n        <span class=\"destination-color-default-pill\">${teamColorSavingKey===key?\"儲存中…\":(explicit?\"已自訂\":\"預設\")}</span>\n      </div>\n      <div class=\"destination-color-palette\" aria-label=\"${escapeHtmlText(cfg.label||key)} 顏色選擇\">${swatches}</div>\n      <div class=\"destination-color-actions\">\n        <label class=\"destination-color-custom${canEdit&&teamColorSavingKey!==key?\"\":\" is-disabled\"}\">自訂顏色<input type=\"color\" data-team-custom-key=\"${escapeHtmlText(key)}\" value=\"${escapeHtmlText(current)}\"${canEdit&&teamColorSavingKey!==key?\"\":\" disabled\"}></label>\n        <button class=\"destination-color-reset\" type=\"button\" data-team-reset-key=\"${escapeHtmlText(key)}\"${canEdit&&explicit&&teamColorSavingKey!==key?\"\":\" disabled\"}>還原預設</button>\n      </div>\n    </div>`;\n  }).join(\"\");\n  if(status){status.classList.remove(\"is-error\");status.textContent=canEdit?\"改色後會直接儲存到 Firebase，並同步更新 Team selector、航班、行程標籤及資料卡。\":(currentTripGlobalLocked()?\"旅程已全域鎖定，目前只可查看 Team 顏色。\":\"目前為唯讀。只有 Owner 或 Admin 可以修改 Team 顏色。\");}\n}\nfunction applyTeamCustomColourLocalUI(teamKey,color){\n  if(!parseHexColour(color))return;\n  const hex=String(color||\"\").trim().toUpperCase();\n  const item=document.querySelector(`[data-team-color-item=\"${CSS.escape(String(teamKey))}\"]`);if(!item)return;\n  const preview=item.querySelector(\".destination-color-preview\");if(preview)preview.style.setProperty(\"--destination-preview\",hex);\n  const copy=item.querySelector(\".destination-color-copy small\");if(copy)copy.textContent=`自訂 ${hex}`;\n  const pill=item.querySelector(\".destination-color-default-pill\");if(pill)pill.textContent=\"已自訂\";\n  item.querySelectorAll(\"[data-team-color-value]\").forEach(button=>button.classList.toggle(\"is-selected\",String(button.dataset.teamColorValue||\"\").toUpperCase()===hex));\n  const reset=item.querySelector(\"[data-team-reset-key]\");if(reset)reset.disabled=false;\n  // Mirror the preview into the currently rendered app immediately. Firestore\n  // remains authoritative, but the colour interaction itself should never flash.\n  const live=window.tripData?.meta?.travellers?.[teamKey];\n  if(live){\n    const previewData={...window.tripData,meta:{...(window.tripData.meta||{}),travellers:{...(window.tripData.meta?.travellers||{}),[teamKey]:{...live,color:hex}}}};\n    refreshTeamThemeInPlace(previewData);\n  }\n}\nfunction cancelTeamCustomColourSave(){if(teamCustomColourSaveTimer){clearTimeout(teamCustomColourSaveTimer);teamCustomColourSaveTimer=0;}}\nfunction queueTeamCustomColourSave(teamKey,color){\n  cancelTeamCustomColourSave();\n  teamCustomColourSaveTimer=setTimeout(()=>{\n    teamCustomColourSaveTimer=0;\n    teamCustomColourSaveChain=teamCustomColourSaveChain.catch(()=>{}).then(()=>saveTeamColour(teamKey,color,{preserveNativePicker:true}));\n  },500);\n}\nasync function saveTeamColour(teamKey,color,{reset=false,preserveNativePicker=false}={}){\n  if(teamColorSavingKey&&!preserveNativePicker)return;\n  const tripId=teamSettingsTripId();if(!tripId)return;\n  let finalMessage=\"\",isError=false;\n  const saveSeq=++teamCustomColourSaveSeq;\n  if(!preserveNativePicker){\n    teamCustomColourPickerKey=\"\";teamColorSavingKey=teamKey;renderTeamSettingsPage();\n  }else{\n    applyTeamCustomColourLocalUI(teamKey,color);\n    const status=document.getElementById(\"team-color-status\");if(status){status.classList.remove(\"is-error\");status.textContent=\"正在儲存自訂顏色…\";}\n  }\n  try{\n    const service=await getTripTeamService();\n    if(reset)await service.resetTeamColor(tripId,teamKey,{user:window.__appAuthUser||null});\n    else await service.updateTeamColor(tripId,teamKey,color,{user:window.__appAuthUser||null});\n    finalMessage=\"已儲存到 Firebase，其他裝置會跟住同步。\";\n  }catch(error){isError=true;finalMessage=teamSettingsFriendlyError(error);}\n  finally{\n    if(!preserveNativePicker){teamColorSavingKey=\"\";renderTeamSettingsPage();}\n    else if(isError){teamCustomColourPickerKey=\"\";renderTeamSettingsPage();}\n    const status=document.getElementById(\"team-color-status\");\n    if(status&&finalMessage&&saveSeq===teamCustomColourSaveSeq){status.classList.toggle(\"is-error\",isError);status.textContent=finalMessage;}\n  }\n}\ndocument.getElementById(\"team-settings-open-btn\")?.addEventListener(\"click\",()=>{cancelTeamCustomColourSave();teamCustomColourPickerKey=\"\";setTimeout(()=>renderTeamSettingsPage(),40);});\ndocument.getElementById(\"team-color-list\")?.addEventListener(\"click\",event=>{\n  const custom=event.target.closest?.(\"[data-team-custom-key]\");\n  if(custom&&!custom.disabled){teamCustomColourPickerKey=String(custom.dataset.teamCustomKey||\"\");return;}\n  const swatch=event.target.closest?.(\"[data-team-color-key]\");if(swatch&&!swatch.disabled){cancelTeamCustomColourSave();teamCustomColourPickerKey=\"\";saveTeamColour(swatch.dataset.teamColorKey,swatch.dataset.teamColorValue);return;}\n  const reset=event.target.closest?.(\"[data-team-reset-key]\");if(reset&&!reset.disabled){cancelTeamCustomColourSave();teamCustomColourPickerKey=\"\";saveTeamColour(reset.dataset.teamResetKey,\"\",{reset:true});}\n});\ndocument.getElementById(\"team-color-list\")?.addEventListener(\"input\",event=>{\n  const input=event.target.closest?.(\"[data-team-custom-key]\");\n  if(input&&!input.disabled){const key=String(input.dataset.teamCustomKey||\"\");teamCustomColourPickerKey=key;applyTeamCustomColourLocalUI(key,input.value);queueTeamCustomColourSave(key,input.value);}\n});\ndocument.getElementById(\"team-color-list\")?.addEventListener(\"change\",event=>{\n  const input=event.target.closest?.(\"[data-team-custom-key]\");\n  if(input&&!input.disabled){const key=String(input.dataset.teamCustomKey||\"\");teamCustomColourPickerKey=key;queueTeamCustomColourSave(key,input.value);}\n});\nwindow.__teamSettingsRefresh=data=>{\n  refreshAppearanceColourMenuState(data);\n  if(profileCurrentPage!==\"team-settings\")return;\n  if(teamCustomColourPickerKey)return;\n  renderTeamSettingsPage(data);\n};\nrefreshAppearanceColourMenuState(window.tripData);\n\n// =========================================================================\n// Phase 2F · v7.7.0 · network / cache / operation safety\n// =========================================================================\nlet cloudSafetyService=null;\nlet cloudSafetyState={online:navigator.onLine!==false,operation:null};\nlet accessRevokedState=false;\n\nfunction phase2fOperationLabel(operation){\n  if(!operation)return\"閒置\";\n  return ({import:\"匯入中\",restore:\"還原中\",snapshot:\"備份中\",archive:\"封存中\",member:\"成員更新中\"})[operation.type]||\"雲端操作中\";\n}\nfunction renderCloudSafetyStrip(){\n  const strip=document.getElementById(\"cloud-safety-strip\");\n  const copy=document.getElementById(\"cloud-safety-copy\");\n  if(!strip||!copy)return;\n\n  strip.classList.remove(\"is-offline\",\"is-busy\",\"is-revoked\");\n  if(accessRevokedState){\n    strip.hidden=false;\n    strip.classList.add(\"is-revoked\");\n    copy.textContent=\"目前旅程權限已失效，正在切換至你仍有權存取嘅旅程。\";\n    return;\n  }\n  if(cloudSafetyState.online===false){\n    strip.hidden=false;\n    strip.classList.add(\"is-offline\");\n    copy.textContent=\"離線模式 · 顯示已快取資料；匯入、還原、封存及權限管理暫停。\";\n    return;\n  }\n  if(cloudSafetyState.operation){\n    strip.hidden=false;\n    strip.classList.add(\"is-busy\");\n    copy.textContent=`${phase2fOperationLabel(cloudSafetyState.operation)} · 完成前暫停切換旅程及其他高風險操作。`;\n    return;\n  }\n  strip.hidden=true;\n  copy.textContent=\"\";\n}\nfunction cloudSafetyFriendlyError(error){\n  const code=String(error?.code||\"\");\n  if(code===\"offline\")return\"目前處於離線模式。此操作需要先連接網絡。\";\n  if(code===\"operation-busy\")return\"另一個雲端操作仍在進行，完成後先再試。\";\n  if(code===\"trip-operation-busy\")return\"另一部裝置正更新呢個 Trip，請完成後再試。\";\n  if(code===\"access-revoked\"||code===\"permission-denied\")return\"目前帳戶已無此旅程權限。\";\n  return error?.message||\"操作未完成。\";\n}\nfunction initCloudSafety(){\n  import(\"./assets/js/cloud-safety-service.js\").then(service=>{\n    cloudSafetyService=service;\n    service.subscribeCloudSafety(state=>{\n      cloudSafetyState=state;\n      window.__cloudSafetyState=state;\n      renderCloudSafetyStrip();\n      refreshAppDataDiagnostics();\n    });\n  }).catch(error=>console.warn(\"Cloud safety init\",error));\n}\ninitCloudSafety();\n\nwindow.addEventListener(\"app-trip-access\",event=>{\n  const detail=event.detail||{};\n  try{window.__destinationSettingsRefresh?.(window.tripData);}catch(error){}\n  const currentId=String(tripLoaderActiveId||dataTripId(window.tripData)||\"\");\n  const sameOptimistic=currentId&&currentId===String(appEntryOptimisticTripId||\"\");\n  const serverDecision=detail.serverConfirmed===true;\n  if(sameOptimistic&&serverDecision&&detail.role){\n    appEntryOptimisticTripId=\"\";appEntryOptimisticUid=\"\";\n  }\n  accessRevokedState=Boolean(currentId&&detail.ready&&detail.signedIn&&serverDecision&&!detail.role);\n  renderCloudSafetyStrip();\n  if(accessRevokedState){\n    if(sameOptimistic){appEntryOptimisticTripId=\"\";appEntryOptimisticUid=\"\";}\n    invalidateInstantTripCache(currentId).catch(()=>{});\n    if(dataTripId(window.tripData)===currentId)stopFirebaseTripLoader({fallback:false});\n    // Revoke is a Trip-switch event, not an App-entry event. Prefer another\n    // already-known accessible Trip immediately; only fall back to Access\n    // Lobby when there genuinely is no replacement Trip.\n    routeAwayFromRevokedTrip(currentId).catch(error=>{console.warn(\"Access revoke fallback\",error);refreshAppEntryState();});\n  }else if(serverDecision){\n    refreshAppEntryState();\n  }\n});\n\n// v7.3.13: fade-through navigation for 我的, with the end-of-transition\n// \"pop\" removed.\n//\n// v7.3.12 cross-faded the snapshot clone in above the still-visible live page\n// (\"handoff prime\"), and cross-faded the incoming clone out above the newly\n// committed live page (\"commit\"). Because .profile-card is translucent glass\n// (rgba background + backdrop-filter), painting a clone above its own original\n// stacks two glass layers: the card visibly brightens and thickens, then snaps\n// back the instant the live page is hidden. That double-composite was the hard\n// jump at the start and at the end of every push and pop.\n//\n// v7.3.13 never paints a clone and its original at the same time. The clone is\n// created transparent, rasterised over two idle frames, and then swapped with\n// the live page inside a single frame - visually a no-op, because the two are\n// pixel-identical. The same swap runs in reverse on commit and on cancel.\nconst profileNavHost=document.getElementById(\"profile-nav-host\");\n\n// Phase 2E · v7.6.1 · Trip-wide operation record (itinerary + expense + backup + members).\nlet tripActivityServicePromise=null;\nlet tripActivityRows=[];\nlet tripActivityFilter=\"all\";\nfunction getTripActivityService(){\n  if(!tripActivityServicePromise)tripActivityServicePromise=import(`./assets/js/trip-activity-service.js?build=${APP_VERSION}`);\n  return tripActivityServicePromise;\n}\nfunction activityEsc(value=\"\"){return escapeHtmlText(String(value??\"\"));}\nfunction activityTimeLabel(value){\n  try{\n    const date=value?.toDate?value.toDate():(value instanceof Date?value:new Date(value));\n    if(!date||Number.isNaN(date.getTime()))return \"—\";\n    return new Intl.DateTimeFormat(\"zh-HK\",{month:\"numeric\",day:\"numeric\",hour:\"2-digit\",minute:\"2-digit\"}).format(date);\n  }catch{return \"—\";}\n}\nfunction activityChangeText(summary){\n  if(!summary)return \"\";\n  const parts=[];\n  const push=(label,obj)=>{const n=Number(obj?.added||0)+Number(obj?.modified||0)+Number(obj?.deleted||0);if(n)parts.push(`${label} ${n}`);};\n  push(\"行程項目\",summary.items);push(\"日子\",summary.days);push(\"收藏\",summary.savedPlaces);\n  if(summary.settingsChanged)parts.push(`設定 ${summary.settingsChanged}`);\n  if(summary.tripChanged)parts.push(\"旅程資料 1\");\n  return parts.join(\"、\");\n}\nfunction describeTripActivity(item={}){\n  const type=String(item.actionType||item.type||item.action||\"\");\n  const message=String(item.summary||item.message||\"\");\n  const base={category:\"itinerary\",categoryLabel:\"行程\",icon:\"✎\",title:\"旅程操作\",summary:message||type||\"操作已完成\"};\n  if(type===\"trip.import.create\")return {...base,icon:\"↥\",title:\"建立旅程\",summary:`首次匯入 trip.json${item.revision?` · Revision ${item.revision}`:\"\"}`};\n  if(type===\"trip.import.replace\")return {...base,icon:\"↥\",title:\"匯入 trip.json\",summary:`${activityChangeText(item.changeSummary)||\"更新旅程內容\"}${item.revision?` · Revision ${item.revision}`:\"\"}`};\n  if(type===\"trip.archive\")return {...base,icon:\"🗄️\",title:\"封存旅程\",summary:\"旅程保留喺 Firebase，但由 Active Trips 收起。\"};\n  if(type===\"trip.restore\")return {...base,icon:\"↩\",title:\"還原已封存旅程\",summary:\"旅程重新加入 Active Trips。\"};\n  if(type===\"trip.global.lock\")return {...base,icon:\"🔒\",title:\"鎖定旅程\",summary:message||\"旅程已設為全域唯讀。\"};\n  if(type===\"trip.global.unlock\")return {...base,icon:\"🔓\",title:\"解除旅程鎖定\",summary:message||\"旅程已解除全域唯讀。\"};\n  if(type===\"trip.itinerary.image_updated\")return {...base,category:\"itinerary\",categoryLabel:\"行程\",icon:\"▧\",title:\"更新行程相片\",summary:item.summary||\"已更新行程自訂相片\"};\n  if(type===\"trip.itinerary.image_removed\")return {...base,category:\"itinerary\",categoryLabel:\"行程\",icon:\"▧\",title:\"移除行程相片\",summary:item.summary||\"已移除行程自訂相片\"};\n  if(type===\"trip.snapshot.manual\")return {...base,category:\"backup\",categoryLabel:\"備份\",icon:\"▣\",title:\"建立手動備份\",summary:`Snapshot${item.revision?` · Revision ${item.revision}`:\"\"}`};\n  if(type===\"trip.snapshot.restore\")return {...base,category:\"backup\",categoryLabel:\"備份\",icon:\"↶\",title:\"還原旅程版本\",summary:`${item.sourceRevision?`由 Revision ${item.sourceRevision} 還原`:\"已還原 Snapshot\"}${item.revision?` · 新 Revision ${item.revision}`:\"\"}`};\n  if(type===\"trip.destination.color_updated\")return {...base,icon:\"🎨\",title:\"更新目的地顏色\",summary:message||\"目的地識別色已更新\"};\n  if(type===\"trip.team.color_updated\")return {...base,icon:\"●\",title:\"更新 Team 顏色\",summary:message||\"Team 識別色已更新\"};\n  if(type===\"trip.media.icon_updated\")return {...base,icon:\"▣\",title:\"更新旅程圖示\",summary:message||\"旅程圖示已更新\"};\n  if(type===\"trip.media.icon_removed\")return {...base,icon:\"−\",title:\"移除旅程圖示\",summary:message||\"已恢復旅程原有圖示\"};\n  if(type===\"trip.media.background_updated\")return {...base,icon:\"▤\",title:\"更新旅程背景\",summary:message||\"旅程背景已更新\"};\n  if(type===\"trip.media.background_removed\")return {...base,icon:\"−\",title:\"移除旅程背景\",summary:message||\"已恢復旅程原有背景\"};\n  if(type===\"expense_created\")return {...base,category:\"expense\",categoryLabel:\"支出\",icon:\"＋\",title:\"新增支出\",summary:message||\"新增一筆支出\"};\n  if(type===\"expense_updated\")return {...base,category:\"expense\",categoryLabel:\"支出\",icon:\"✎\",title:\"修改支出\",summary:message||\"修改一筆支出\"};\n  if(type===\"expense_deleted\")return {...base,category:\"expense\",categoryLabel:\"支出\",icon:\"−\",title:\"刪除支出\",summary:message||\"刪除一筆支出\"};\n  if(type===\"expense_restored\")return {...base,category:\"expense\",categoryLabel:\"支出\",icon:\"↩\",title:\"還原已刪除支出\",summary:message||\"還原一筆支出\"};\n  if(type===\"settings_updated\")return {...base,category:\"expense\",categoryLabel:\"支出\",icon:\"⚙\",title:\"更新支出設定\",summary:message||\"更新匯率或支出設定\"};\n  if(type===\"settlement_recorded\")return {...base,category:\"expense\",categoryLabel:\"支出\",icon:\"✓\",title:\"記錄找數\",summary:message||\"新增找數記錄\"};\n  if(type===\"settlement_cancelled\")return {...base,category:\"expense\",categoryLabel:\"支出\",icon:\"↩\",title:\"取消找數紀錄\",summary:message||\"取消找數記錄\"};\n  if(type===\"trip_locked\")return {...base,category:\"expense\",categoryLabel:\"支出\",icon:\"🔒\",title:\"鎖定支出\",summary:message||\"停止修改支出\"};\n  if(type===\"trip_unlocked\")return {...base,category:\"expense\",categoryLabel:\"支出\",icon:\"🔓\",title:\"解鎖支出\",summary:message||\"恢復修改支出\"};\n  if(type===\"member_added\")return {...base,category:\"expense\",categoryLabel:\"支出\",icon:\"👥\",title:\"新增分帳成員\",summary:message||\"新增分帳成員\"};\n  if(type===\"member_removed\")return {...base,category:\"expense\",categoryLabel:\"支出\",icon:\"👥\",title:\"移除分帳成員\",summary:message||\"移除分帳成員\"};\n  if(type.startsWith(\"member.\")||type.startsWith(\"trip.member\"))return {...base,category:\"member\",categoryLabel:\"成員\",icon:\"👤\",title:item.title||\"旅程成員變更\",summary:message||type};\n  const readable=String(item.title||message||type||\"旅程操作\").replace(/^Activity$/i,\"旅程操作\");\n  return {...base,title:readable,summary:message&&message!==readable?message:(type||\"操作已完成\")};\n}\nfunction renderTripActivityPage(){\n  const list=document.getElementById(\"trip-activity-list\");if(!list)return;\n  const rows=tripActivityRows.map(item=>({item,view:describeTripActivity(item)})).filter(row=>tripActivityFilter===\"all\"||row.view.category===tripActivityFilter);\n  if(!rows.length){list.innerHTML=`<div class=\"trip-activity-empty\">${tripActivityRows.length?\"呢個分類暫時未有操作記錄。\":\"暫時未有操作記錄。\"}</div>`;return;}\n  list.innerHTML=rows.map(({item,view})=>`<div class=\"trip-activity-item\"><div class=\"trip-activity-icon\">${activityEsc(view.icon)}</div><div class=\"trip-activity-copy\"><strong>${activityEsc(view.title)}</strong><div class=\"trip-activity-summary\">${activityEsc(view.summary)}</div><div class=\"trip-activity-meta\"><span>${activityEsc(item.actorName||\"未知使用者\")}</span><span>·</span><span class=\"trip-activity-category\">${activityEsc(view.categoryLabel)}</span><span>·</span><span>${activityEsc(activityTimeLabel(item.createdAt))}</span></div></div></div>`).join(\"\");\n}\nasync function loadTripActivityPage(){\n  const status=document.getElementById(\"trip-activity-status\"),list=document.getElementById(\"trip-activity-list\");\n  const tripId=currentBackupTripId();\n  if(!tripId){if(list)list.innerHTML='<div class=\"trip-activity-empty\">未有目前 Firebase Trip。</div>';if(status)status.textContent=\"請先登入並選擇一個 Firebase 旅程。\";return;}\n  if(status)status.textContent=\"正在讀取操作記錄…\";\n  if(list)list.innerHTML='<div class=\"trip-activity-empty\">正在同步 Firebase 操作記錄…</div>';\n  try{\n    const service=await getTripActivityService();\n    tripActivityRows=await service.listTripActivityLogs(tripId,{limitCount:120});\n    renderTripActivityPage();\n    if(status)status.textContent=`已讀取 ${tripActivityRows.length} 筆記錄 · ${tripId}`;\n  }catch(error){\n    console.error(\"Trip activity logs\",error);\n    tripActivityRows=[];\n    if(list)list.innerHTML='<div class=\"trip-activity-empty\">暫時未能讀取操作記錄。</div>';\n    if(status)status.textContent=error?.code===\"permission-denied\"?\"目前帳戶冇權限讀取呢個 Trip 嘅操作記錄。\":\"操作記錄同步失敗。\";\n  }\n}\ndocument.getElementById(\"trip-activity-open-btn\")?.addEventListener(\"click\",()=>{setTimeout(loadTripActivityPage,0);});\ndocument.getElementById(\"trip-activity-filters\")?.addEventListener(\"click\",event=>{\n  const button=event.target.closest?.(\"[data-activity-filter]\");if(!button)return;\n  tripActivityFilter=button.dataset.activityFilter||\"all\";\n  document.querySelectorAll(\"[data-activity-filter]\").forEach(btn=>btn.classList.toggle(\"active\",btn===button));\n  renderTripActivityPage();\n});\n\nconst profilePages=Object.fromEntries([...document.querySelectorAll(\"#profile-view [data-profile-page]\")].map(page=>[page.dataset.profilePage,page]));\nlet profileCurrentPage=\"root\";\nlet profileNavigationTransitioning=false;\nlet profileSwipe=null;\nlet profileNavSurfaces=[];\nlet profileNavAnimationFrame=0;\nlet profileNavStack=[{key:\"root\",scrollTop:0,snapshot:null}];\n\nconst PROFILE_FADE_OUT_END=.48;\nconst PROFILE_FADE_IN_START=.52;\nconst PROFILE_TRANSITION_TRAVEL_RATIO=.075;\nconst PROFILE_TRANSITION_DURATION=430;\nconst PROFILE_SWIPE_SETTLE_MIN=170;\nconst PROFILE_SWIPE_SETTLE_MAX=340;\n\nfunction profileScrollTop(){return Math.max(0,Number(scrollShell?.scrollTop||0));}\nfunction profileHostDocumentTop(){\n  if(!profileNavHost)return 0;\n  const rect=profileNavHost.getBoundingClientRect();\n  return rect.top+profileScrollTop();\n}\nfunction profileClonePage(page){\n  const clone=page.cloneNode(true);\n  clone.hidden=false;\n  clone.classList.remove(\"is-active\",\"is-transition-layer\");\n  clone.classList.add(\"profile-nav-clone\");\n  [clone,...clone.querySelectorAll(\"[id]\")].forEach(node=>node.removeAttribute?.(\"id\"));\n  return clone;\n}\nfunction profileCaptureSnapshot(key,scrollTop=profileScrollTop()){\n  const page=profilePages[key];\n  if(!page)return null;\n  return {key,scrollTop:Number(scrollTop||0),hostTop:profileHostDocumentTop(),pageClone:profileClonePage(page)};\n}\nfunction profileClearAnimation(){\n  if(profileNavAnimationFrame){cancelAnimationFrame(profileNavAnimationFrame);profileNavAnimationFrame=0;}\n}\nfunction profileClearSurfaces(){\n  profileClearAnimation();\n  profileNavSurfaces.forEach(surface=>surface.remove());\n  profileNavSurfaces=[];\n  profileNavHost?.classList.remove(\"is-snapshotting\");\n  document.body.classList.remove(\"profile-navigation-active\",\"profile-edge-swipe-active\");\n}\nfunction profileCreateSurface(snapshot,className=\"\"){\n  if(!snapshot)return null;\n  const surface=document.createElement(\"div\");\n  surface.className=`profile-nav-surface ${className}`.trim();\n  const liveTop=snapshot.hostTop-snapshot.scrollTop;\n  const top=Math.max(0,Math.round(liveTop));\n  const clipped=Math.min(0,liveTop);\n  surface.style.top=`${top}px`;\n  surface.style.height=`${Math.max(1,window.innerHeight-top)}px`;\n  // Surfaces are inserted transparent. This prevents an iOS compositor frame\n  // from ever flashing a newly-created glass clone at full opacity.\n  surface.style.opacity=\"0\";\n  // Exact copy of the live .bg-fixed box, in viewport coordinates, so the glass\n  // cards inside this surface blur the same pixels the live cards blur.\n  const bgSource=document.querySelector(\".bg-fixed\");\n  if(bgSource){\n    const bgRect=bgSource.getBoundingClientRect();\n    const bgStyle=getComputedStyle(bgSource);\n    const bgLayer=document.createElement(\"div\");\n    bgLayer.className=\"profile-nav-surface-bg\";\n    bgLayer.style.left=`${bgRect.left}px`;\n    bgLayer.style.top=`${bgRect.top-top}px`;\n    bgLayer.style.width=`${bgRect.width}px`;\n    bgLayer.style.height=`${bgRect.height}px`;\n    bgLayer.style.backgroundColor=bgStyle.backgroundColor;\n    bgLayer.style.backgroundImage=bgStyle.backgroundImage;\n    bgLayer.style.backgroundSize=bgStyle.backgroundSize;\n    bgLayer.style.backgroundPosition=bgStyle.backgroundPosition;\n    bgLayer.style.backgroundRepeat=bgStyle.backgroundRepeat;\n    surface.appendChild(bgLayer);\n    surface.__profileBgLayer=bgLayer;\n  }\n  const frame=document.createElement(\"div\");\n  frame.className=\"profile-nav-surface-frame\";\n  const content=document.createElement(\"div\");\n  content.className=\"profile-nav-surface-content\";\n  if(clipped<0)content.style.transform=`translate3d(0,${Math.round(clipped)}px,0)`;\n  content.appendChild(snapshot.pageClone.cloneNode(true));\n  frame.appendChild(content);\n  surface.appendChild(frame);\n  document.body.appendChild(surface);\n  profileNavSurfaces.push(surface);\n  return surface;\n}\nfunction profileSetLivePage(activeKey,scrollTop=0,{keepSurfaces=false,keepHidden=false}={}){\n  Object.entries(profilePages).forEach(([key,page])=>{\n    const active=key===activeKey;\n    page.hidden=!active;\n    page.classList.toggle(\"is-active\",active);\n    page.style.transform=\"\";\n    page.style.opacity=\"\";\n    page.style.transition=\"\";\n    page.style.boxShadow=\"\";\n  });\n  profileCurrentPage=activeKey;\n  if(scrollShell)scrollShell.scrollTo({top:Math.max(0,Number(scrollTop||0)),behavior:\"auto\"});\n  if(!keepHidden)profileNavHost?.classList.remove(\"is-snapshotting\");\n  if(!keepSurfaces){\n    profileNavigationTransitioning=false;\n    profileClearSurfaces();\n  }\n}\nfunction profilePrepareSnapshotTransition(fromSnapshot,toSnapshot){\n  profileClearSurfaces();\n  const fromSurface=profileCreateSurface(fromSnapshot,\"is-front\");\n  const toSurface=profileCreateSurface(toSnapshot,\"is-previous\");\n  if(!fromSurface||!toSurface){profileClearSurfaces();return null;}\n  // Important: keep the real page visible until the handoff clone has been\n  // gradually brought to full opacity.\n  document.body.classList.add(\"profile-navigation-active\");\n  const width=Math.max(1,window.innerWidth);\n  fromSurface.style.zIndex=\"1422\";\n  toSurface.style.zIndex=\"1421\";\n  return {fromSurface,toSurface,width};\n}\nfunction profileSmooth01(value){\n  const t=clamp(Number(value)||0,0,1);\n  // Quintic smootherstep: zero velocity and acceleration at both ends.\n  return t*t*t*(t*(t*6-15)+10);\n}\nfunction profileFadeThroughState(progress){\n  const p=clamp(Number(progress)||0,0,1);\n  const outT=profileSmooth01(p/PROFILE_FADE_OUT_END);\n  const inT=profileSmooth01((p-PROFILE_FADE_IN_START)/(1-PROFILE_FADE_IN_START));\n  return {p,outT,inT,outOpacity:1-outT,inOpacity:inT};\n}\nfunction profileApplyFadeThrough(outSurface,inSurface,progress,direction,width){\n  if(!outSurface||!inSurface)return;\n  const state=profileFadeThroughState(progress);\n  const travel=Math.min(22,Math.max(12,width*PROFILE_TRANSITION_TRAVEL_RATIO));\n  const outX=direction*travel*state.outT;\n  const inX=-direction*travel*(1-state.inT);\n  outSurface.style.zIndex=\"1422\";\n  inSurface.style.zIndex=\"1421\";\n  outSurface.style.opacity=String(state.outOpacity);\n  inSurface.style.opacity=String(state.inOpacity);\n  outSurface.style.transform=`translate3d(${outX.toFixed(2)}px,0,0)`;\n  inSurface.style.transform=`translate3d(${inX.toFixed(2)}px,0,0)`;\n  // Cancel the surface transform on the background copy: content drifts, the\n  // background stays welded to the viewport exactly like .bg-fixed.\n  if(outSurface.__profileBgLayer)outSurface.__profileBgLayer.style.transform=`translate3d(${(-outX).toFixed(2)}px,0,0)`;\n  if(inSurface.__profileBgLayer)inSurface.__profileBgLayer.style.transform=`translate3d(${(-inX).toFixed(2)}px,0,0)`;\n  outSurface.style.boxShadow=\"none\";\n  inSurface.style.boxShadow=\"none\";\n}\nfunction profileAnimateValue({from=0,to=1,duration=300,apply,onDone}={}){\n  ");
