window.__travelRuntimeParts.push("profileClearAnimation();\n  const reduced=window.matchMedia?.(\"(prefers-reduced-motion: reduce)\")?.matches;\n  if(reduced||duration<=0){apply?.(to);onDone?.();return;}\n  const started=performance.now();\n  const tick=now=>{\n    const t=clamp((now-started)/duration,0,1);\n    const eased=profileSmooth01(t);\n    apply?.(from+(to-from)*eased);\n    if(t<1){profileNavAnimationFrame=requestAnimationFrame(tick);return;}\n    profileNavAnimationFrame=0;\n    onDone?.();\n  };\n  apply?.(from);\n  profileNavAnimationFrame=requestAnimationFrame(tick);\n}\nfunction profileNextFrames(count,callback){\n  profileClearAnimation();\n  let left=Math.max(1,Number(count)||1);\n  const step=()=>{\n    left-=1;\n    if(left<=0){profileNavAnimationFrame=0;callback?.();return;}\n    profileNavAnimationFrame=requestAnimationFrame(step);\n  };\n  profileNavAnimationFrame=requestAnimationFrame(step);\n}\nfunction profileHandoffToSnapshot(surface,onDone){\n  if(!surface){profileNavHost?.classList.add(\"is-snapshotting\");onDone?.();return;}\n  // Warm the glass clone up while it is still fully transparent, then hide the\n  // live page and reveal the clone in the SAME frame. The clone is a pixel copy\n  // of what is already on screen, so a one-frame swap is invisible - whereas a\n  // cross-fade between the two would stack their translucent card material and\n  // read as a brightness pop.\n  surface.style.opacity=\"0\";\n  void surface.offsetHeight;\n  profileNextFrames(2,()=>{\n    profileNavHost?.classList.add(\"is-snapshotting\");\n    surface.style.opacity=\"1\";\n    onDone?.();\n  });\n}\nfunction profileAnimateFadeThrough(outSurface,inSurface,{from=0,to=1,direction=1,width=Math.max(1,window.innerWidth),duration=470,onDone}={}){\n  profileAnimateValue({from,to,duration,apply:p=>profileApplyFadeThrough(outSurface,inSurface,p,direction,width),onDone});\n}\nfunction profileCommitSnapshotToLive(activeKey,scrollTop,inSurface,{pop=false,push=false}={}){\n  // Build the real DOM while it is still hidden behind the incoming snapshot,\n  // give it two frames to lay out and rasterise, then reveal it and drop the\n  // snapshot in one frame. No overlap, so no glass stacking and no flash.\n  profileSetLivePage(activeKey,scrollTop,{keepSurfaces:true,keepHidden:!!inSurface});\n  if(pop&&profileNavStack.length>1)profileNavStack.pop();\n  if(push)profileNavStack.push({key:activeKey,scrollTop:Math.max(0,Number(scrollTop||0)),snapshot:null});\n  const topEntry=profileNavStack[profileNavStack.length-1];\n  if(topEntry&&topEntry.key===activeKey)topEntry.scrollTop=Math.max(0,Number(scrollTop||0));\n  if(!inSurface){\n    profileNavigationTransitioning=false;\n    profileClearSurfaces();\n    return;\n  }\n  inSurface.style.zIndex=\"1423\";\n  inSurface.style.opacity=\"1\";\n  inSurface.style.transform=\"translate3d(0,0,0)\";\n  if(inSurface.__profileBgLayer)inSurface.__profileBgLayer.style.transform=\"translate3d(0,0,0)\";\n  profileNextFrames(2,()=>{\n    profileNavHost?.classList.remove(\"is-snapshotting\");\n    inSurface.style.opacity=\"0\";\n    profileNextFrames(1,()=>{\n      profileNavigationTransitioning=false;\n      profileClearSurfaces();\n    });\n  });\n}\nfunction profileRestoreLiveAfterCancel(outSurface,currentScroll){\n  // Cancelled gesture: the snapshot is already back at its exact start state,\n  // so hand it back to the live page with the same invisible one-frame swap.\n  if(scrollShell)scrollShell.scrollTo({top:Math.max(0,Number(currentScroll||0)),behavior:\"auto\"});\n  if(!outSurface){\n    profileNavHost?.classList.remove(\"is-snapshotting\");\n    profileNavigationTransitioning=false;\n    profileClearSurfaces();\n    return;\n  }\n  outSurface.style.zIndex=\"1423\";\n  outSurface.style.opacity=\"1\";\n  outSurface.style.transform=\"translate3d(0,0,0)\";\n  if(outSurface.__profileBgLayer)outSurface.__profileBgLayer.style.transform=\"translate3d(0,0,0)\";\n  profileNextFrames(2,()=>{\n    profileNavHost?.classList.remove(\"is-snapshotting\");\n    outSurface.style.opacity=\"0\";\n    profileNextFrames(1,()=>{\n      profileNavigationTransitioning=false;\n      profileClearSurfaces();\n    });\n  });\n}\nfunction profileRunProgrammaticTransition({targetKey,targetScroll=0,direction=1,pop=false,push=false}={}){\n  const currentScroll=profileScrollTop();\n  const currentEntry=profileNavStack[profileNavStack.length-1];\n  const fromSnapshot=profileCaptureSnapshot(profileCurrentPage,currentScroll);\n  const toEntry=pop?profileNavStack[profileNavStack.length-2]:null;\n  const toSnapshot=toEntry?.snapshot||profileCaptureSnapshot(targetKey,targetScroll);\n  if(!fromSnapshot||!toSnapshot)return;\n  if(currentEntry&&currentEntry.key===profileCurrentPage){\n    currentEntry.scrollTop=currentScroll;\n    currentEntry.snapshot=fromSnapshot;\n  }\n  const layers=profilePrepareSnapshotTransition(fromSnapshot,toSnapshot);\n  if(!layers)return;\n  const {fromSurface,toSurface,width}=layers;\n  profileNavigationTransitioning=true;\n  // Seed both surfaces at progress 0 (outgoing solid, incoming transparent)\n  // before the handoff, so the swap frame matches the live page exactly.\n  profileApplyFadeThrough(fromSurface,toSurface,0,direction,width);\n  profileHandoffToSnapshot(fromSurface,()=>{\n    profileAnimateFadeThrough(fromSurface,toSurface,{from:0,to:1,direction,width,duration:PROFILE_TRANSITION_DURATION,onDone:()=>{\n      profileCommitSnapshotToLive(targetKey,targetScroll,toSurface,{pop,push});\n    }});\n  });\n}\nfunction openProfilePage(key){\n  if(profileNavigationTransitioning||key===\"root\"||!profilePages[key]||profileCurrentPage===key)return;\n  closeTripSwitcher();\n  profileRunProgrammaticTransition({targetKey:key,targetScroll:0,direction:-1,push:true});\n}\nfunction backProfilePage(){\n  if(profileNavigationTransitioning||profileCurrentPage===\"root\"||profileNavStack.length<=1)return;\n  const previous=profileNavStack[profileNavStack.length-2]||{key:\"root\",scrollTop:0,snapshot:null};\n  profileRunProgrammaticTransition({targetKey:previous.key,targetScroll:previous.scrollTop||0,direction:1,pop:true});\n}\ndocument.querySelectorAll(\"[data-profile-open]\").forEach(button=>button.addEventListener(\"click\",()=>openProfilePage(button.dataset.profileOpen)));\ndocument.querySelectorAll(\"[data-profile-back]\").forEach(button=>button.addEventListener(\"click\",backProfilePage));\n\nfunction beginProfileEdgeSwipe(event){\n  if(profileNavigationTransitioning||profileCurrentPage===\"root\"||profileNavStack.length<=1||currentAppView!==\"profile\"||event.touches?.length!==1)return;\n  if(document.body.classList.contains(\"expenses-modal-open\"))return;\n  const t=event.touches[0];\n  if(t.clientX>30)return;\n  if(event.target.closest?.(\"input,textarea,select,[contenteditable='true']\"))return;\n  const currentEntry=profileNavStack[profileNavStack.length-1];\n  if(currentEntry&&currentEntry.key===profileCurrentPage)currentEntry.scrollTop=profileScrollTop();\n  const previous=profileNavStack[profileNavStack.length-2]||{key:\"root\",scrollTop:0,snapshot:null};\n  profileSwipe={\n    tracking:true,active:false,handedOff:false,startX:t.clientX,startY:t.clientY,currentX:t.clientX,\n    startedAt:performance.now(),width:Math.max(1,window.innerWidth),currentScroll:profileScrollTop(),\n    previousKey:previous.key,previousScroll:Math.max(0,previous.scrollTop||0),previousSnapshot:previous.snapshot||null,\n    currentSurface:null,previousSurface:null,progress:0,fadeProgress:0\n  };\n}\nfunction moveProfileEdgeSwipe(event){\n  if(!profileSwipe?.tracking||event.touches?.length!==1)return;\n  const t=event.touches[0],dx=t.clientX-profileSwipe.startX,dy=t.clientY-profileSwipe.startY;\n  if(!profileSwipe.active){\n    if(dx<7)return;\n    if(Math.abs(dy)>Math.abs(dx)*.72){profileSwipe=null;return;}\n    const currentSnapshot=profileCaptureSnapshot(profileCurrentPage,profileSwipe.currentScroll);\n    const previousSnapshot=profileSwipe.previousSnapshot||profileCaptureSnapshot(profileSwipe.previousKey,profileSwipe.previousScroll);\n    if(!currentSnapshot||!previousSnapshot){profileSwipe=null;return;}\n    const currentEntry=profileNavStack[profileNavStack.length-1];\n    if(currentEntry&&currentEntry.key===profileCurrentPage)currentEntry.snapshot=currentSnapshot;\n    const layers=profilePrepareSnapshotTransition(currentSnapshot,previousSnapshot);\n    if(!layers){profileSwipe=null;return;}\n    profileSwipe.currentSurface=layers.fromSurface;\n    profileSwipe.previousSurface=layers.toSurface;\n    profileSwipe.width=layers.width;\n    profileSwipe.active=true;\n    profileSwipe.handedOff=false;\n    profileNavigationTransitioning=true;\n    document.body.classList.add(\"profile-edge-swipe-active\");\n    profileApplyFadeThrough(layers.fromSurface,layers.toSurface,0,1,layers.width);\n    profileHandoffToSnapshot(layers.fromSurface,()=>{\n      if(!profileSwipe?.active)return;\n      profileSwipe.handedOff=true;\n      profileApplySwipeProgress();\n    });\n  }\n  event.preventDefault();\n  profileSwipe.currentX=t.clientX;\n  // While the clone is still warming up nothing is drawn yet; the finger\n  // position is simply recorded and applied on the handoff frame.\n  if(!profileSwipe.handedOff)return;\n  profileApplySwipeProgress();\n}\nfunction profileApplySwipeProgress(){\n  if(!profileSwipe?.active||!profileSwipe.handedOff)return;\n  const width=Math.max(1,profileSwipe.width);\n  const distance=clamp(profileSwipe.currentX-profileSwipe.startX,0,width);\n  const progress=distance/width;\n  profileSwipe.progress=progress;\n  profileSwipe.fadeProgress=progress;\n  profileApplyFadeThrough(profileSwipe.currentSurface,profileSwipe.previousSurface,progress,1,width);\n}\nfunction endProfileEdgeSwipe(){\n  if(!profileSwipe?.tracking)return;\n  if(!profileSwipe.active){profileSwipe=null;return;}\n  const swipe=profileSwipe;\n  profileSwipe=null;\n  const width=Math.max(1,swipe.width);\n  const distance=clamp(swipe.currentX-swipe.startX,0,width);\n  const elapsed=Math.max(1,performance.now()-swipe.startedAt);\n  const velocity=distance/elapsed;\n  const goBack=distance>width*.32||velocity>.55;\n\n  if(!swipe.handedOff){\n    // The handoff frame never landed, so nothing has visibly changed yet.\n    profileNavHost?.classList.remove(\"is-snapshotting\");\n    profileNavigationTransitioning=false;\n    profileClearSurfaces();\n    return;\n  }\n\n  const currentSurface=swipe.currentSurface,previousSurface=swipe.previousSurface;\n  const fadeProgress=clamp(Number(swipe.fadeProgress)||0,0,1);\n  const target=goBack?1:0;\n  const remaining=Math.abs(target-fadeProgress);\n  const duration=Math.max(PROFILE_SWIPE_SETTLE_MIN,Math.min(PROFILE_SWIPE_SETTLE_MAX,PROFILE_SWIPE_SETTLE_MIN+remaining*230));\n  profileAnimateFadeThrough(currentSurface,previousSurface,{from:fadeProgress,to:target,direction:1,width,duration,onDone:()=>{\n    if(goBack){\n      profileCommitSnapshotToLive(swipe.previousKey,swipe.previousScroll,previousSurface,{pop:true});\n    }else{\n      // Return the fade-through to its exact start first. Then re-expose the\n      // original live page underneath and dissolve the clone away.\n      currentSurface.style.opacity=\"1\";\n      previousSurface.style.opacity=\"0\";\n      profileRestoreLiveAfterCancel(currentSurface,swipe.currentScroll);\n    }\n  }});\n}\nfunction cancelProfileEdgeSwipe(){\n  if(!profileSwipe?.tracking)return;\n  if(!profileSwipe.active){profileSwipe=null;return;}\n  const swipe=profileSwipe;\n  profileSwipe=null;\n  if(!swipe.handedOff){\n    profileNavHost?.classList.remove(\"is-snapshotting\");\n    profileNavigationTransitioning=false;\n    profileClearSurfaces();\n    return;\n  }\n  profileAnimateFadeThrough(swipe.currentSurface,swipe.previousSurface,{from:swipe.fadeProgress||0,to:0,direction:1,width:swipe.width,duration:220,onDone:()=>{\n    swipe.currentSurface.style.opacity=\"1\";\n    swipe.previousSurface.style.opacity=\"0\";\n    profileRestoreLiveAfterCancel(swipe.currentSurface,swipe.currentScroll);\n  }});\n}\ndocument.addEventListener(\"touchstart\",beginProfileEdgeSwipe,{passive:true});\ndocument.addEventListener(\"touchmove\",moveProfileEdgeSwipe,{passive:false});\ndocument.addEventListener(\"touchend\",endProfileEdgeSwipe,{passive:true});\ndocument.addEventListener(\"touchcancel\",cancelProfileEdgeSwipe,{passive:true});\ndocument.querySelector('[data-profile-open=\"app-data\"]')?.addEventListener(\"click\",()=>{\n  refreshAppDataDiagnostics();\n});\ndocument.querySelector('[data-profile-open=\"data-management\"]')?.addEventListener(\"click\",()=>{\n  renderDataManagementLockState();\n  startFullBackupSyncGateWatch();\n  getTripBackupService().catch(()=>{});\n  warmTripImportDependencies();\n  // Prepare all read-only export sources before iOS gets a chance to suspend\n  // Firestore during the first generated-file download.\n  setTimeout(()=>prepareDataManagementLocalExports().catch(error=>console.warn(\"Local export preparation\",error)),60);\n});\n\nfunction initPhase2AccessContext(data){\n  Promise.all([\n    import(`./assets/js/trip-session-service.js?build=${APP_VERSION}`),\n    import(`./assets/js/trip-access-service.js?build=${APP_VERSION}`)\n  ])\n    .then(([sessionService,accessService])=>{\n      const tripId=sessionService.resolveTripId(data);\n      accessService.initTripAccess(tripId);\n    })\n    .catch(error=>console.error(\"Phase 2 trip services load error\",error));\n}\n\nfunction dispatchExpenseUiAction(action){\n  initExpensesViewSafe();\n  window.__pendingExpenseUiAction=action;\n  window.dispatchEvent(new CustomEvent(\"expense-ui-action\",{detail:{action}}));\n}\n\nasync function startGoogleSignIn({preserveInvite=false,entryHandoff=false}={}){\n  if(window.__appAuthUser)return;\n  if(entryHandoff)setAppEntryLoginHandoff(true);\n  try{\n    if(preserveInvite){const entry=await getAppEntryService();const invite=appEntryTargetInviteId||entry.inviteIdFromLocation?.()||\"\";if(invite)entry.rememberInviteReturn?.(invite);}\n    const {signInWithGoogle}=await getGlobalAuthService();await signInWithGoogle();\n  }catch(error){\n    if(entryHandoff)setAppEntryLoginHandoff(false);\n    refreshAppEntryState();\n    console.error(error);\n    if(error?.code!==\"auth/popup-closed-by-user\")alert(`Google 登入失敗：${error?.code||\"unknown\"}`);\n  }\n}\nasync function signOutTravelUser(){\n  try{\n    try{const creator=await import(`./assets/js/trip-creator-service.js?build=${APP_VERSION}`);creator.clearTripCreatorEntitlementCache?.();}catch(error){}\n    const {signOutCurrentUser}=await getGlobalAuthService();await signOutCurrentUser();\n  }catch(error){console.error(\"Google sign out error\",error);alert(\"暫時未能登出 Google 帳戶。\");}\n}\ndocument.getElementById(\"my-account-btn\")?.addEventListener(\"click\",()=>startGoogleSignIn());\ndocument.getElementById(\"entry-account-btn\")?.addEventListener(\"click\",()=>startGoogleSignIn({preserveInvite:true,entryHandoff:true}));\ndocument.getElementById(\"entry-login-btn\")?.addEventListener(\"click\",()=>startGoogleSignIn({preserveInvite:true,entryHandoff:true}));\ndocument.getElementById(\"profile-signout-btn\")?.addEventListener(\"click\",signOutTravelUser);\ndocument.getElementById(\"entry-signout-btn\")?.addEventListener(\"click\",signOutTravelUser);\ndocument.getElementById(\"entry-import-btn\")?.addEventListener(\"pointerdown\",()=>warmTripImportDependencies(),{passive:true});\ndocument.getElementById(\"entry-import-btn\")?.addEventListener(\"click\",()=>{warmTripImportDependencies();tripImportFile?.click();});\nwindow.addEventListener(\"online\",()=>{refreshAppEntryState();schedulePendingPermanentDeleteResume(window.__appAuthUser);},{passive:true});\nwindow.addEventListener(\"offline\",()=>refreshAppEntryState(),{passive:true});\n\ndocument.querySelectorAll(\"[data-global-setting]\").forEach(button=>{\n  button.addEventListener(\"click\",()=>dispatchExpenseUiAction(`setting:${button.dataset.globalSetting}`));\n});\nscrollShell?.addEventListener(\"scroll\",scheduleCollapseUpdate,{passive:true});\nwindow.addEventListener(\"resize\",()=>{refreshCollapseMetrics();updateBottomNavIndicator();scheduleCollapseUpdate();},{passive:true});\nasync function requestPortraitOrientationLock(){\n  try{\n    const standalone=window.matchMedia?.(\"(display-mode: standalone)\")?.matches||navigator.standalone===true;\n    if(standalone&&screen?.orientation?.lock)await screen.orientation.lock(\"portrait-primary\");\n  }catch(error){}\n}\nvoid requestPortraitOrientationLock();\nwindow.addEventListener(\"orientationchange\",()=>setTimeout(()=>{void requestPortraitOrientationLock();refreshCollapseMetrics();updateBottomNavIndicator();scheduleCollapseUpdate();},220),{passive:true});\n\n// v7.8.2.4 · Keyboard interaction copied from the proven Price Tracker PWA.\n// It only owns keyboard detection + Bottom Navigation visibility. It must never\n// reposition the page, alter scrollTop, or change Profile/DELETE form geometry.\nfunction isKeyboardInput(element) {\n      if (!(element instanceof HTMLElement)) return false;\n      if (element instanceof HTMLTextAreaElement || element.isContentEditable) return true;\n      if (element instanceof HTMLSelectElement) return true;\n      if (!(element instanceof HTMLInputElement)) return false;\n\n      return ![\n        \"button\",\n        \"submit\",\n        \"reset\",\n        \"checkbox\",\n        \"radio\",\n        \"range\",\n        \"color\",\n        \"file\",\n        \"image\",\n        \"hidden\"\n      ].includes(String(element.type || \"text\").toLowerCase());\n    }\n\nfunction setKeyboardTabbarState(isOpen) {\n      const tabbar = document.querySelector(\"#bottom-nav\");\n      if (!tabbar) return;\n      tabbar.classList.toggle(\"is-keyboard-open\", isOpen);\n      document.body.classList.toggle(\"keyboard-open\", isOpen);\n    }\n\nfunction installKeyboardAwareTabbar() {\n      let focusTimer = 0;\n\n      const syncFromFocus = () => {\n        window.clearTimeout(focusTimer);\n        focusTimer = window.setTimeout(() => {\n          setKeyboardTabbarState(isKeyboardInput(document.activeElement));\n        }, 80);\n      };\n\n      document.addEventListener(\"focusin\", (event) => {\n        if (isKeyboardInput(event.target)) {\n          window.clearTimeout(focusTimer);\n          setKeyboardTabbarState(true);\n        }\n      });\n\n      document.addEventListener(\"focusout\", syncFromFocus);\n\n      if (window.visualViewport) {\n        let largestViewportHeight = window.visualViewport.height;\n\n        const syncFromViewport = () => {\n          const viewportHeight = window.visualViewport.height;\n          if (!isKeyboardInput(document.activeElement)) {\n            largestViewportHeight = Math.max(largestViewportHeight, viewportHeight);\n          }\n\n          const keyboardLikelyOpen =\n            isKeyboardInput(document.activeElement) &&\n            largestViewportHeight - viewportHeight > 100;\n\n          setKeyboardTabbarState(\n            keyboardLikelyOpen || isKeyboardInput(document.activeElement)\n          );\n        };\n\n        window.visualViewport.addEventListener(\"resize\", syncFromViewport);\n        window.visualViewport.addEventListener(\"scroll\", syncFromViewport);\n      }\n    }\ninstallKeyboardAwareTabbar();\n\nrefreshCollapseMetrics();\n\n// Android / Chrome PWA support. Versioned app-shell Service Worker; normal launches are cache-first while explicit reloads stay network-first.\nif (\"serviceWorker\" in navigator) {\n  window.addEventListener(\"load\", () => {\n    navigator.serviceWorker.register(`./sw.js?build=${APP_VERSION}`).catch(() => {});\n  });\n}\n\n// Load\nfunction runOptionalAppStep(label,fn){try{return fn();}catch(error){console.error(`${label} error`,error);return null;}}\n// Phase 2G.1 root entry does not wait for trip.json before resolving Google\n// Authentication. Signed-out users still see the Login Gateway promptly. A\n// returning user with a valid local Trip cache stays on the workspace from the\n// first frame while Auth and Trip access reconcile silently in the background.\ninitGlobalAuthSafe();\ngetAppEntryService().then(()=>refreshAppEntryState()).catch(error=>console.error(\"Entry gate init\",error));\n// v7.8.0.5 · Begin IndexedDB Fast Resume before the bundled trip.json network\n// request. Returning users should see their local Trip immediately on both a\n// Home Screen launch and an in-app reload; Auth and membership reconcile behind\n// the already-visible workspace.\nconst appEntryEarlyResumePromise=appEntryWarmRefresh\n  ? bootstrapRememberedTripBeforeAuthFastResume().catch(error=>{console.warn(\"Early Trip Fast Resume\",error);endWarmRefreshGate();return false;})\n  : Promise.resolve(false);\nfetch(getTripJsonUrl())\n  .then(r=>{if(!r.ok)throw new Error(\"HTTP \"+r.status);return r.json();})\n  .then(async data=>{\n    bundledTripData=data;\n    const deferBundled=beginDeferredTripBoot(data);\n    const earlyResumed=await appEntryEarlyResumePromise;\n    const resumedTripId=String(appEntryOptimisticTripId||\"\").trim();\n    const keepInstantTrip=earlyResumed&&resumedTripId&&dataTripId(window.tripData)===resumedTripId&&tripLoaderState?.source===\"instant-cache\";\n    // A later bundled trip.json response must never overwrite a Trip that Fast\n    // Resume has already painted from the user-bound IndexedDB cache.\n    if(keepInstantTrip){\n      if(tripBootDeferred)releaseDeferredTripBoot();\n    }else{\n      window.tripData=deferBundled?null:data;\n      // A remembered/deep-linked Firebase Trip wins before first paint.\n      // If it differs from bundled trip.json, never paint the bundled Trip.\n      if(!deferBundled){\n        render(data);\n        runOptionalAppStep(\"Weather render\",()=>renderWeather(data));\n        runOptionalAppStep(\"Travel info render\",()=>renderInfo(data));\n      }else{\n        if(appEntryWarmRefresh){\n          const applied=await appEntryEarlyResumePromise;\n          if(!applied&&tripBootDeferred)showDeferredTripBootPlaceholder(\"正在開啟上次旅程…\");\n        }else showDeferredTripBootPlaceholder(\"正在開啟上次旅程…\");\n      }\n    }\n    runOptionalAppStep(\"Trip library init\",()=>initTripLibrarySafe(data));\n    runOptionalAppStep(\"Compact header init\",()=>updateCompactHeader());\n    requestAnimationFrame(()=>{runOptionalAppStep(\"Bottom nav update\",updateBottomNavIndicator);runOptionalAppStep(\"Chrome update\",updateCollapsingChrome);});\n    runOptionalAppStep(\"View restore\",()=>{\n      let saved=\"\";\n      try{saved=sessionStorage.getItem(\"trip_current_view\")||\"\";}catch(e){}\n      setView([\"itinerary\",\"snacks\",\"info\",\"expenses\",\"profile\"].includes(saved)?saved:\"itinerary\",{restoreScroll:false});\n    });\n    runOptionalAppStep(\"Preference sync init\",initPreferencesCloudSyncSafe);\n    runOptionalAppStep(\"Global auth init\",initGlobalAuthSafe);\n    runOptionalAppStep(\"Trip access init\",()=>initPhase2AccessContext(data));\n  })\n  .catch(error=>{\n    console.error(\"Trip JSON / critical itinerary render error\",error);\n    const chip=document.getElementById(\"today-chip\"),title=document.getElementById(\"header-title-main\");\n    if(chip)chip.textContent=\"無法載入行程資料\";\n    if(title)title.textContent=\"無法載入行程資料\";\n  });\n");
