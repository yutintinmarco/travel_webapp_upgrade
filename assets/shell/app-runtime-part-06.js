window.__travelRuntimeParts.push("const bundledId=dataTripId(bundledData);\n  const rememberedUid=rememberedCacheUid();\n  const sameRememberedTrip=Boolean(tripBootPreferredId&&bundledId&&tripBootPreferredId===bundledId&&rememberedUid);\n  // v7.7.2.1: even when the remembered Firebase Trip has the same ID as the\n  // bundled fallback, prefer its IndexedDB render cache before painting. The\n  // old path painted bundled default colours first, then replaced them roughly\n  // a second later when Firebase settings arrived.\n  tripBootDeferred=Boolean(tripBootPreferredId&&bundledId&&(tripBootPreferredId!==bundledId||rememberedUid));\n  document.body.classList.toggle(\"trip-boot-deferred\",tripBootDeferred);\n  document.body.classList.toggle(\"trip-boot-warm-same\",sameRememberedTrip);\n  const placeholder=document.getElementById(\"trip-boot-placeholder\");\n  if(placeholder)placeholder.hidden=!tripBootDeferred||sameRememberedTrip;\n  if(tripBootDeferred){\n    armDeferredTripBootFailSafe();\n    const small=document.getElementById(\"header-title-small\");\n    const main=document.getElementById(\"header-title-main\");\n    const chip=document.getElementById(\"today-chip\");\n    const sub=document.getElementById(\"header-sub\");\n    const temporal=document.getElementById(\"trip-temporal-note\");\n    const snapshot=window.__tripBootVisualSnapshot;\n    const canReuseHeader=sameRememberedTrip&&snapshot&&String(snapshot.tripId||\"\")===tripBootPreferredId;\n    if(canReuseHeader){\n      if(small)small.textContent=String(snapshot.titleSmall||\"My Trip\");\n      if(main)main.textContent=String(snapshot.titleMain||\"旅程\");\n      if(chip)chip.textContent=String(snapshot.todayChip||\"\");\n      if(sub)sub.textContent=String(snapshot.headerSub||\"\");\n      if(temporal){const text=String(snapshot.temporalNote||\"\").trim();temporal.textContent=text;temporal.dataset.state=String(snapshot.temporalState||\"\");temporal.hidden=!text;}\n    }else{\n      if(small)small.textContent=\"My Trip\";\n      if(main)main.textContent=\"旅程\";\n      if(chip)chip.textContent=\"同步旅程中…\";\n      if(sub)sub.textContent=\"\";\n      if(temporal){temporal.textContent=\"\";temporal.hidden=true;temporal.dataset.state=\"\";}\n    }\n  }\n  return tripBootDeferred;\n}\nfunction releaseDeferredTripBoot(){\n  clearDeferredTripBootFailSafe();\n  if(!tripBootDeferred)return;\n  tripBootDeferred=false;\n  document.body.classList.remove(\"trip-boot-deferred\",\"trip-boot-warm-same\");\n  const placeholder=document.getElementById(\"trip-boot-placeholder\");\n  if(placeholder)placeholder.hidden=true;\n  requestAnimationFrame(()=>{updateBottomNavIndicator();updateCollapsingChrome();});\n}\nfunction currentActiveDayId(){\n  const idx=Number(document.querySelector(\".day-tab.active\")?.dataset?.dayIndex||0);\n  return String(window.tripData?.days?.[idx]?.dayId||\"\");\n}\nfunction renderSignature(value,fallback=\"\"){\n  try{return JSON.stringify(value);}catch(error){return fallback;}\n}\nfunction travellerStructureForRender(travellers={}){\n  return Object.fromEntries(Object.entries(travellers).sort(([a],[b])=>a.localeCompare(b)).map(([key,cfg])=>{\n    const copy={...(cfg||{})};delete copy.color;delete copy.sortOrder;return[key,copy];\n  }));\n}\nfunction cityStructureForItinerary(cities={}){\n  return Object.fromEntries(Object.entries(cities).sort(([a],[b])=>a.localeCompare(b)).map(([key,cfg])=>[key,{\n    label:cfg?.label||key,startIso:cfg?.startIso||\"\",endIso:cfg?.endIso||\"\"\n  }]));\n}\nfunction tripContentRenderSignature(data){\n  if(!data)return\"\";\n  const meta=data.meta||{};\n  return renderSignature({\n    tripId:dataTripId(data),\n    meta:{\n      titleSmall:meta.titleSmall||\"\",titleMain:meta.titleMain||\"\",dateRange:meta.dateRange||\"\",route:meta.route||\"\",\n      tripStartIso:meta.tripStartIso||\"\",tripEndIso:meta.tripEndIso||\"\",status:meta.status||\"\",footerNote:meta.footerNote||\"\",\n      travellers:travellerStructureForRender(meta.travellers||{}),\n      cities:cityStructureForItinerary(meta.cities||{}),\n      flights:meta.flights||[],outbound:meta.outbound||null,inbound:meta.inbound||null,airlineLogo:meta.airlineLogo||\"\",\n      galleryDefaults:meta.galleryDefaults||{}\n    },\n    days:data.days||[]\n  },`${dataTripId(data)}:${Number(data?.revision)||0}`);\n}\nfunction snacksRenderSignature(data){\n  return renderSignature({snacks:data?.snacks||{},galleryDefaults:data?.meta?.galleryDefaults||{}},\"\");\n}\nfunction infoRenderSignature(data){\n  const meta=data?.meta||{};\n  return renderSignature({\n    tripId:dataTripId(data),start:meta.tripStartIso||\"\",end:meta.tripEndIso||\"\",\n    travellers:travellerStructureForRender(meta.travellers||{}),\n    cities:Object.fromEntries(Object.entries(meta.cities||{}).sort(([a],[b])=>a.localeCompare(b)).map(([key,cfg])=>[key,{label:cfg?.label||key,startIso:cfg?.startIso||\"\",endIso:cfg?.endIso||\"\"}])),\n    hotels:meta.hotels||{},flights:meta.flights||[],outbound:meta.outbound||null,inbound:meta.inbound||null,infoCard:meta.infoCard||{}\n  },\"\");\n}\nfunction destinationThemeRenderSignature(data){\n  const cities=data?.meta?.cities||{};\n  return renderSignature(Object.fromEntries(Object.entries(cities).sort(([a],[b])=>a.localeCompare(b)).map(([key,cfg])=>[key,{color:cfg?.color||\"\",accentColor:cfg?.accentColor||\"\",themeColor:cfg?.themeColor||\"\"}])) ,\"\");\n}\nfunction teamThemeRenderSignature(data){\n  const travellers=data?.meta?.travellers||{};\n  return renderSignature(Object.fromEntries(Object.entries(travellers).sort(([a],[b])=>a.localeCompare(b)).map(([key,cfg])=>[key,{color:cfg?.color||\"\"}])) ,\"\");\n}\nfunction weatherRenderSignature(data){\n  const meta=data?.meta||{};const cities={};\n  Object.entries(meta.cities||{}).sort(([a],[b])=>a.localeCompare(b)).forEach(([key,cfg])=>{cities[key]={label:cfg?.label||\"\",latitude:cfg?.latitude??null,longitude:cfg?.longitude??null,timezone:cfg?.timezone||\"\",startIso:cfg?.startIso||\"\",endIso:cfg?.endIso||\"\"};});\n  return renderSignature({start:meta.tripStartIso,end:meta.tripEndIso,cities,weather:meta.weather||{}},\"\");\n}\nfunction tripVisualRenderSignature(data){\n  const meta=data?.meta||{};\n  return renderSignature({tripId:dataTripId(data),accentColor:meta.accentColor||\"\",featureColors:meta.featureColors||{},tripIconMedia:meta.tripIconMedia||null,backgroundImage:meta.backgroundImage||\"\",coverImage:meta.coverImage||\"\",backgroundImageMedia:meta.backgroundImageMedia||null,coverImageMedia:meta.coverImageMedia||null},\"\");\n}\nfunction expensesRenderSignature(data){return renderSignature(data?.meta?.expenses||{},\"\");}\nfunction tripLibraryDataSignature(data){\n  const meta=data?.meta||{};const days=Array.isArray(data?.days)?data.days:[];const snacks=Array.isArray(data?.snacks?.items)?data.snacks.items:[];\n  return renderSignature({\n    tripId:dataTripId(data),titleMain:meta.titleMain||\"\",titleSmall:meta.titleSmall||\"\",dateRange:meta.dateRange||\"\",\n    start:meta.tripStartIso||\"\",end:meta.tripEndIso||\"\",tripIcon:meta.tripIcon||\"\",backgroundImage:meta.backgroundImage||\"\",coverImage:meta.coverImage||\"\",tripIconMedia:meta.tripIconMedia||null,backgroundImageMedia:meta.backgroundImageMedia||null,coverImageMedia:meta.coverImageMedia||null,\n    dayCount:days.length,itemCount:days.reduce((sum,day)=>sum+(Array.isArray(day?.items)?day.items.length:0),0),savedPlaceCount:snacks.length\n  },\"\");\n}\nfunction setDataSourceLabel(label){\n  const el=document.getElementById(\"app-data-source-label\");\n  if(el)el.textContent=label;\n  refreshAppDataDiagnostics();\n}\nfunction appDataSet(id,value){\n  const el=document.getElementById(id);\n  if(el)el.textContent=value==null||value===\"\"?\"—\":String(value);\n}\nfunction appDataTripStatusLabel(status){\n  return ({draft:\"草稿\",upcoming:\"即將出發\",active:\"旅程中\",completed:\"已完成\",locked:\"已鎖定\"})[String(status||\"\").toLowerCase()]||status||\"—\";\n}\nfunction perfLiteMs(value){const n=Number(value);return Number.isFinite(n)?`${Math.round(n)} ms`:\"—\";}\nfunction perfLiteNavigationLabel(type){return({reload:\"重新整理\",navigate:\"新啟動\",back_forward:\"返回／前進\",prerender:\"預載\"})[String(type||\"\")]||String(type||\"—\");}\nfunction refreshPerfLiteDiagnostics(){\n  const perf=window.__perfLite?.snapshot?.();if(!perf)return;\n  const marks=perf.marks||{},counts=perf.counters||{};\n  appDataSet(\"perf-lite-navigation\",perfLiteNavigationLabel(perf.navigationType));\n  appDataSet(\"perf-lite-visual\",perfLiteMs(marks.visualBootApplied));\n  appDataSet(\"perf-lite-shell\",perfLiteMs(marks.appShellReady));\n  appDataSet(\"perf-lite-first-paint\",perfLiteMs(marks.firstTripPaint));\n  appDataSet(\"perf-lite-instant\",perfLiteMs(marks.instantTripPaint));\n  appDataSet(\"perf-lite-auth\",perfLiteMs(marks.authReady));\n  appDataSet(\"perf-lite-firebase-first\",perfLiteMs(marks.firebaseFirstData));\n  appDataSet(\"perf-lite-firebase-server\",perfLiteMs(marks.firebaseServerReady));\n  appDataSet(\"perf-lite-render-count\",`Full ${Number(counts.fullRender)||0} · Partial ${Number(counts.partialRender)||0}`);\n  appDataSet(\"perf-lite-loader-events\",Number(counts.loaderEvents)||0);\n  appDataSet(\"perf-lite-cache-schedules\",Number(counts.cacheSchedules)||0);\n\n  const io=window.__firestoreIoLite?.snapshot?.()||{};\n  appDataSet(\"io-lite-listeners\",`${Number(io.activeListeners)||0} · Peak ${Number(io.peakActiveListeners)||0}`);\n  appDataSet(\"io-lite-subscriptions\",Number(io.listenerSubscriptions)||0);\n  appDataSet(\"io-lite-snapshots\",Number(io.snapshotEvents)||0);\n  appDataSet(\"io-lite-listener-docs\",`Cache ${Number(io.snapshotCacheDocs)||0} · Server ${Number(io.snapshotServerDocs)||0}`);\n  appDataSet(\"io-lite-explicit-reads\",`${Number(io.explicitReadCalls)||0} calls · ${Number(io.explicitReadDocs)||0} docs`);\n  appDataSet(\"io-lite-writes\",`${Number(io.writeCalls)||0} calls · ${Number(io.writeDocs)||0} docs`);\n  appDataSet(\"io-lite-deletes\",`${Number(io.deleteCalls)||0} calls · ${Number(io.deleteDocs)||0} docs`);\n  appDataSet(\"io-lite-transactions\",`${Number(io.transactionCalls)||0} calls · ${Number(io.transactionAttempts)||0} attempts`);\n}\nwindow.__refreshPerfLiteDiagnostics=refreshPerfLiteDiagnostics;\nwindow.addEventListener(\"app-firestore-io-audit\",refreshPerfLiteDiagnostics);\nfunction refreshAppDataDiagnostics(){\n  const data=(typeof tripData!==\"undefined\"&&tripData)?tripData:{};\n  const meta=data.meta||{};\n  const loader=tripLoaderState||{};\n  const user=window.__appAuthUser||null;\n  const access=window.__appTripAccess||currentProfileAccess||null;\n  const activeId=String(loader.tripId||tripLoaderActiveId||dataTripId(data)||\"\").trim();\n  const revision=Number(data.revision||0);\n\n  let syncLabel=\"未連接\";\n  if(loader.status===\"ready\"){\n    if(loader.serverConfirmed===true&&loader.hasPendingWrites!==true)syncLabel=\"已同步\";\n    else if(loader.hasPendingWrites===true)syncLabel=\"同步中 · 尚有待寫入資料\";\n    else syncLabel=loader.source===\"instant-cache\"?\"同步中 · 即時旅程快取已載入\":(loader.source===\"cache\"?\"同步中 · Firebase 快取已載入\":\"同步中 · 等待完整 Server confirmation\");\n  }\n  else if(loader.status===\"loading\") syncLabel=\"正在連接 Firebase…\";\n  else if(loader.status===\"syncing\") syncLabel=\"正在同步…\";\n  else if(loader.status===\"local\") syncLabel=\"使用本機資料\";\n  else if(loader.status===\"permission-denied\") syncLabel=\"權限不足\";\n  else if(loader.status===\"not-found\") syncLabel=\"Firebase 找不到旅程\";\n  else if(loader.status===\"recovery-needed\") syncLabel=\"更新未完成 · 需重新匯入／還原\";\n  else if(loader.status===\"error\") syncLabel=\"同步錯誤\";\n\n  const rawLifecycleStatus=String(meta.status||data.status||\"\").toLowerCase();\n  const catalogRecord=(tripLibraryCloudState.trips||[]).find(trip=>String(trip?.tripId||\"\")===activeId)||null;\n  const lifecycleStatus=catalogRecord?.status||([\"locked\",\"open\"].includes(rawLifecycleStatus)?\"\":rawLifecycleStatus);\n  const writeStatus=currentTripGlobalLocked()?\"Locked\":\"Open\";\n  const account=user\n    ? (user.displayName ? `${user.displayName}${user.email?` · ${user.email}`:\"\"}` : (user.email||\"Google 已登入\"))\n    : \"未登入\";\n  const role=access?.roleLabel||tripLibraryRoleLabel(access?.role)||access?.role||\"—\";\n\n  appDataSet(\"app-data-sync-status\",syncLabel);\n  appDataSet(\"app-data-trip-id\",activeId);\n  appDataSet(\"app-data-revision\",revision||\"—\");\n  appDataSet(\"app-data-trip-status\",appDataTripStatusLabel(lifecycleStatus));\n  appDataSet(\"app-data-write-status\",writeStatus);\n  appDataSet(\"app-data-google-account\",account);\n  appDataSet(\"app-data-role\",role);\n  appDataSet(\"app-data-network\",cloudSafetyState.online===false?\"離線\":\"在線\");\n  appDataSet(\"app-data-cache\",window.__firestorePersistence===\"persistent\"?\"Persistent IndexedDB\":\"Memory\");\n  appDataSet(\"app-data-operation\",phase2fOperationLabel(cloudSafetyState.operation));\n  refreshPerfLiteDiagnostics();\n}\nfunction updateTripUrl(tripId){\n  try{const url=new URL(location.href);if(tripId)url.searchParams.set(\"trip\",tripId);else url.searchParams.delete(\"trip\");history.replaceState(history.state,\"\",url);}catch(error){}\n}\nasync function getTripLoaderServices(){\n  if(!tripLoaderInitPromise)tripLoaderInitPromise=Promise.all([\n    import(`./assets/js/trip-loader-service.js?build=${APP_VERSION}`),\n    import(`./assets/js/trip-session-service.js?build=${APP_VERSION}`),\n    import(`./assets/js/trip-access-service.js?build=${APP_VERSION}`)\n  ]).then(([loader,session,access])=>{tripLoaderService=loader;tripSessionService=session;tripAccessService=access;return{loader,session,access};});\n  return tripLoaderInitPromise;\n}\n// v7.7.7.4 · Expenses can become interactive before the first membership\n// snapshot resolves on an iOS cold start. Let that module request a refresh\n// through this already-versioned access-service instance, avoiding a duplicate\n// ES-module instance and avoiding a Quit / relaunch recovery path.\nwindow.addEventListener(\"expense-trip-access-refresh-request\",async event=>{\n  const requestedId=String(event.detail?.tripId||\"\").trim();\n  const currentId=dataTripId(window.tripData);\n  if(!requestedId||requestedId!==currentId)return;\n  try{\n    const {access}=await getTripLoaderServices();\n    access.initTripAccess?.(currentId);\n    access.refreshTripAccess?.();\n  }catch(error){console.warn(\"Expense access refresh\",error);}\n});\nasync function getTripRenderCacheService(){\n  if(!tripRenderCacheServicePromise)tripRenderCacheServicePromise=import(`./assets/js/trip-render-cache-service.js?build=${APP_VERSION}`).then(service=>{tripRenderCacheService=service;return service;});\n  return tripRenderCacheServicePromise;\n}\nfunction currentCacheUid(user=tripLibraryCurrentUser||window.__appAuthUser){return String(user?.uid||\"\").trim();}\nasync function persistInstantTripCache(data,{immediate=false}={}){\n  const tripId=dataTripId(data),uid=currentCacheUid();\n  if(!tripId||!uid)return false;\n  rememberCacheUid(uid);\n  const revision=Math.max(0,Number(data?.revision)||0);\n  const incomingConfirmed=data?.cloudMeta?.serverConfirmed===true&&data?.cloudMeta?.hasPendingWrites!==true;\n  if(incomingConfirmed){\n    tripRenderCacheLastConfirmedTripId=tripId;\n    tripRenderCacheLastConfirmedRevision=revision;\n  }\n  // Preserve a trusted same-revision seed across ordinary Firestore cache →\n  // server metadata transitions. A revision change or pending write is allowed\n  // to invalidate trust; an unconfirmed cache snapshot alone is not.\n  const preserveConfirmed=\n    !incomingConfirmed&&data?.cloudMeta?.hasPendingWrites!==true&&\n    tripRenderCacheLastConfirmedTripId===tripId&&\n    tripRenderCacheLastConfirmedRevision>0&&\n    tripRenderCacheLastConfirmedRevision===revision;\n  const payload=preserveConfirmed\n    ? {...data,cloudMeta:{...(data.cloudMeta||{}),serverConfirmed:true,hasPendingWrites:false}}\n    : data;\n  rememberCacheUid(uid);\n  perfLiteInc(\"cacheSchedules\");\n  try{\n    const service=await getTripRenderCacheService();\n    if(immediate||typeof service.queueTripRenderCacheWrite!==\"function\")return await service.writeTripRenderCache(uid,tripId,payload);\n    return await service.queueTripRenderCacheWrite(uid,tripId,payload);\n  }catch(error){console.warn(\"Trip render cache persist\",error);return false;}\n}\nfunction flushInstantTripCacheWrites(){\n  if(!tripRenderCacheServicePromise)return;\n  tripRenderCacheServicePromise.then(service=>service.flushTripRenderCacheWrites?.()).catch(()=>{});\n}\ndocument.addEventListener(\"visibilitychange\",()=>{if(document.visibilityState===\"hidden\")flushInstantTripCacheWrites();},{passive:true});\nwindow.addEventListener(\"pagehide\",flushInstantTripCacheWrites,{passive:true});\nasync function invalidateInstantTripCache(tripIdInput,user=tripLibraryCurrentUser||window.__appAuthUser){\n  const tripId=String(tripIdInput||\"\").trim(),uid=currentCacheUid(user);\n  if(!tripId||!uid)return false;\n  try{\n    const service=await getTripRenderCacheService();\n    const removed=await service.deleteTripRenderCache(uid,tripId);\n    if(removed&&tripRenderCacheLastConfirmedTripId===tripId){tripRenderCacheLastConfirmedTripId=\"\";tripRenderCacheLastConfirmedRevision=0;}\n    return removed;\n  }catch(error){return false;}\n}\nasync function applyInstantTripCache(tripIdInput,user){\n  const tripId=String(tripIdInput||\"\").trim(),uid=currentCacheUid(user);\n  if(!tripId||!uid)return false;\n  try{\n    const service=await getTripRenderCacheService();\n    const cached=await service.readTripRenderCache(uid,tripId);\n    if(!cached?.data||dataTripId(cached.data)!==tripId)return false;\n    if(cached.data?.cloudMeta?.serverConfirmed===true&&cached.data?.cloudMeta?.hasPendingWrites!==true){\n      tripRenderCacheLastConfirmedTripId=tripId;\n      tripRenderCacheLastConfirmedRevision=Math.max(0,Number(cached.data?.revision)||0);\n    }else{\n      tripRenderCacheLastConfirmedTripId=tripId;\n      tripRenderCacheLastConfirmedRevision=0;\n    }\n    // A Firestore result that arrived while IndexedDB was opening always wins.\n    if(tripLoaderActiveId===tripId&&tripLoaderState.status===\"ready\"&&tripLoaderState.source!==\"instant-cache\")return false;\n    const liveRevision=dataTripId(window.tripData)===tripId?Number(window.tripData?.revision)||0:0;\n    if(liveRevision>Number(cached.revision||0)&&tripLoaderState.source!==\"instant-cache\")return false;\n    applyFirebaseTripData(cached.data,{source:\"instant-cache\",tripId});\n    requestAnimationFrame(()=>perfLiteMark(\"instantTripPaint\"));\n    return true;\n  }catch(error){\n    console.warn(\"Trip render cache read skipped\",error);\n    return false;\n  }\n}\nasync function bootstrapRememberedTripBeforeAuthFastResume(){\n  if(!appEntryWarmRefresh)return false;\n  const tripId=String(tripBootPreferredId||resolveBootPreferredTripId()||\"\").trim();\n  const uid=String(appEntryWarmRefreshUid||rememberedCacheUid()||\"\").trim();\n  if(!tripId||!uid)return false;\n  appEntryOptimisticTripId=tripId;appEntryOptimisticUid=uid;setAppEntryResumeLatch(tripId,uid);\n  // Keep the existing workspace surface visible while IndexedDB opens. Do not\n  // transiently replace it with the branded Entry loading screen.\n  applyAppEntryState(\"workspace\");\n  const applied=await applyInstantTripCache(tripId,{uid});\n  if(applied){releaseDeferredTripBoot();refreshAppEntryState();}\n  else{clearAppEntryResumeLatch();endWarmRefreshGate();refreshAppEntryState();}\n  return applied;\n}\n\nasync function waitForVerifiedTripAccess(tripIdInput,{timeoutMs=6500}={}){\n  const tripId=String(tripIdInput||\"\").trim();if(!tripId)return false;\n  const {access}=await getTripLoaderServices();\n  access.initTripAccess?.(tripId);\n  const usable=state=>!!state?.role&&(state.serverConfirmed===true||(navigator.onLine===false&&state.fromCache===true));\n  const terminalDenied=state=>state?.tripId===tripId&&state.serverConfirmed===true&&!state.role;\n  const first=access.getTripAccess?.()||{};if(usable(first))return true;if(terminalDenied(first))return false;\n  return new Promise(resolve=>{\n    let done=false,timer=0,stop=null;\n    const finish=value=>{if(done)return;done=true;clearTimeout(timer);try{stop?.();}catch(error){}resolve(value);};\n    stop=access.subscribeTripAccess?.(state=>{\n      if(state?.tripId!==tripId)return;\n      if(usable(state))finish(true);\n      else if(terminalDenied(state))finish(false);\n      else if(navigator.onLine===false&&state?.ready)finish(!!state.role&&state.fromCache===true);\n    },{immediate:true});\n    timer=setTimeout(()=>finish(false),timeoutMs);\n  });\n}\nasync function bootstrapVerifiedFirebaseTrip(tripIdInput,user){\n  const tripId=String(tripIdInput||\"\").trim(),uid=currentCacheUid(user);if(!uid||!tripId)return false;\n  const verified=await waitForVerifiedTripAccess(tripId);\n  if(!verified){await invalidateInstantTripCache(tripId,user);refreshAppEntryState();return false;}\n  const token=++tripInstantBootToken;\n  // Auth identity and Trip membership are now verified. Keep the Phase 2F\n  // parallel cache + Firestore warm boot, but never paint the cached Trip before\n  // that access gate has passed.\n  const cacheTask=applyInstantTripCache(tripId,user);\n  const loaderTask=startFirebaseTripLoader(tripId);\n  await Promise.allSettled([cacheTask,loaderTask]);\n  refreshAppEntryState();\n  return token===tripInstantBootToken;\n}\nasync function bootstrapRememberedFirebaseTrip(user){\n  const tripId=String(tripBootPreferredId||resolveBootPreferredTripId()||\"\").trim();\n  const uid=currentCacheUid(user);if(!tripId||!uid)return false;\n  const token=++tripInstantBootToken;\n  appEntryOptimisticTripId=tripId;appEntryOptimisticUid=uid;\n  setAppEntryResumeLatch(tripId,uid);\n  // Paint the user-bound local Trip cache immediately after Auth confirms this\n  // Google UID. Firestore access and membership listeners continue in parallel.\n  // If the server later says the user was revoked, the global access handler\n  // invalidates this cache and removes the workspace without waiting for reload.\n  const cacheTask=applyInstantTripCache(tripId,user);\n  const loaderTask=startFirebaseTripLoader(tripId);\n  try{const {access}=await getTripLoaderServices();access.initTripAccess?.(tripId);access.refreshTripAccess?.();}catch(error){console.warn(\"Background access check\",error);}\n  await Promise.race([Promise.resolve(cacheTask),new Promise(resolve=>setTimeout(resolve,180))]);\n  refreshAppEntryState();\n  Promise.allSettled([cacheTask,loaderTask]).then(()=>refreshAppEntryState()).catch(()=>{});\n  return token===tripInstantBootToken;\n}\nfunction resolveTripLoaderWaiters(data){\n  const id=dataTripId(data),revision=Number(data?.revision)||0;\n  [...tripLoaderRevisionWaiters].forEach(waiter=>{if(waiter.tripId===id&&revision>=waiter.revision){tripLoaderRevisionWaiters.delete(waiter);clearTimeout(waiter.timer);waiter.resolve(true);}});\n}\nfunction waitForTripLoaderRevision(tripId,revision,timeoutMs=6500){\n  const id=String(tripId||\"\").trim(),target=Math.max(0,Number(revision)||0);\n  if(dataTripId(window.tripData)===id&&(Number(window.tripData?.revision)||0)>=target&&tripLoaderState.status===\"ready\")return Promise.resolve(true);\n  return new Promise(resolve=>{const waiter={tripId:id,revision:target,resolve,timer:0};waiter.timer=setTimeout(()=>{tripLoaderRevisionWaiters.delete(waiter);resolve(false);},timeoutMs);tripLoaderRevisionWaiters.add(waiter);});\n}\nfunction restoreRenderedDay(dayId){\n  if(!dayId)return;\n  const idx=window.tripData?.days?.findIndex?.(day=>String(day?.dayId||\"\")===dayId);\n  if(!(idx>=0))return;\n  document.querySelectorAll(\".day-tab\").forEach(t=>t.classList.toggle(\"active\",Number(t.dataset.dayIndex)===idx));\n  document.querySelectorAll(\".day-content\").forEach(c=>c.classList.toggle(\"active\",Number(c.dataset.dayIndex)===idx));\n}\nfunction updateTripLibrarySummaryFromData(data,{force=false}={}){\n  const signature=tripLibraryDataSignature(data);\n  if(!force&&signature&&signature===tripLibrarySummaryDataSignature)return false;\n  try{\n    if(tripLibrarySchemaService){const validated=tripLibrarySchemaService.validatePortableTrip(data);tripLibrarySummary=tripLibrarySchemaService.getTripSummary(validated.trip);}\n  }catch(error){console.warn(\"Trip summary refresh\",error);}\n  tripLibrarySummaryDataSignature=signature;\n  renderTripLibrary();\n  return true;\n}\nfunction applyFirebaseTripData(data,{source=\"server\",tripId=\"\",dirtySections=[]}={}){\n  if(!data)return;\n  const currentId=dataTripId(window.tripData),nextId=dataTripId(data)||tripId;\n  const sameTrip=Boolean(currentId&&nextId&&currentId===nextId);\n  const dirty=new Set(Array.isArray(dirtySections)?dirtySections:[]);\n  let dirtyUnknown=dirty.size===0||dirty.has(\"initial\");\n  const dirtyPrefix=prefix=>[...dirty].some(section=>section===prefix||section.startsWith(`${prefix}:`));\n  const knownDirty=[...dirty].every(section=>section===\"initial\"||section===\"days\"||section===\"snacks\"||section===\"expenses\"||section.startsWith(\"items:\")||section===\"trip\"||section.startsWith(\"trip:\")||section===\"general\"||section.startsWith(\"general:\"));\n  if(!knownDirty)dirtyUnknown=true;\n  const tripField=field=>dirtyUnknown||dirty.has(\"trip\")||dirty.has(`trip:${field}`);\n  const generalField=field=>dirtyUnknown||dirty.has(\"general\")||dirty.has(`general:${field}`);\n  const dayDirty=dirtyUnknown||dirty.has(\"days\")||dirtyPrefix(\"items\");\n\n  const itineraryTouched=!sameTrip||dayDirty||[\"titleSmall\",\"title\",\"titleHtml\",\"dateRange\",\"route\",\"startDate\",\"endDate\",\"status\"].some(tripField)||[\"travellers\",\"cities\",\"flights\",\"outbound\",\"inbound\",\"airlineLogo\",\"galleryDefaults\",\"footerNote\"].some(generalField);\n  const snacksTouched=!sameTrip||dirtyUnknown||dirty.has(\"snacks\")||[\"savedPlacesMeta\",\"galleryDefaults\"].some(generalField);\n  const infoTouched=!sameTrip||[\"startDate\",\"endDate\"].some(tripField)||[\"travellers\",\"cities\",\"hotels\",\"flights\",\"outbound\",\"inbound\",\"infoCard\"].some(generalField);\n  const weatherTouched=!sameTrip||[\"startDate\",\"endDate\"].some(tripField)||[\"cities\",\"weather\"].some(generalField);\n  const visualTouched=!sameTrip||[\"accentColor\",\"tripIconMedia\",\"backgroundImage\",\"coverImage\",\"backgroundImageMedia\",\"coverImageMedia\"].some(tripField)||generalField(\"tripIconMedia\")||generalField(\"backgroundImage\")||generalField(\"backgroundImageMedia\")||generalField(\"coverImageMedia\")||generalField(\"featureColors\");\n  const expensesTouched=!sameTrip||dirtyUnknown||dirty.has(\"expenses\");\n  const destinationThemeTouched=!sameTrip||generalField(\"cities\");\n  const teamThemeTouched=!sameTrip||generalField(\"travellers\");\n\n  // Layer 3: only serialize a render section when Firestore says that section\n  // can actually have changed. Item-only edits no longer stringify Saved, Info,\n  // Weather or settings payloads, and vice versa.\n  const nextItinerarySignature=itineraryTouched?tripContentRenderSignature(data):tripRenderSignature;\n  const nextSnacksSignature=snacksTouched?snacksRenderSignature(data):tripSnacksSignature;\n  const nextInfoSignature=infoTouched?infoRenderSignature(data):tripInfoSignature;\n  const nextWeatherSignature=weatherTouched?weatherRenderSignature(data):tripWeatherSignature;\n  const nextVisualSignature=visualTouched?tripVisualRenderSignature(data):tripVisualSignature;\n  const nextExpensesSignature=expensesTouched?expensesRenderSignature(data):tripExpensesSignature;\n  const nextDestinationThemeSignature=destinationThemeTouched?destinationThemeRenderSignature(data):tripDestinationThemeSignature;\n  const nextTeamThemeSignature=teamThemeTouched?teamThemeRenderSignature(data):tripTeamThemeSignature;\n\n  const needsItinerary=!sameTrip||nextItinerarySignature!==tripRenderSignature;\n  const needsSnacks=!sameTrip||nextSnacksSignature!==tripSnacksSignature;\n  const needsInfo=!sameTrip||nextInfoSignature!==tripInfoSignature;\n  const needsWeather=!sameTrip||nextWeatherSignature!==tripWeatherSignature;\n  const needsVisual=!sameTrip||nextVisualSignature!==tripVisualSignature;\n  const needsExpenses=!sameTrip||nextExpensesSignature!==tripExpensesSignature;\n  const needsDestinationTheme=!sameTrip||nextDestinationThemeSignature!==tripDestinationThemeSignature;\n  const needsTeamTheme=!sameTrip||nextTeamThemeSignature!==tripTeamThemeSignature;\n  if(sameTrip&&!needsItinerary&&(needsVisual||needsSnacks||needsInfo||needsWeather||needsExpenses||needsDestinationTheme||needsTeamTheme))perfLiteInc(\"partialRender\");\n\n  const oldDayId=sameTrip&&needsItinerary?currentActiveDayId():\"\";\n  const oldTeam=sameTrip&&needsItinerary?activeTeam:\"all\";\n  const oldScroll=sameTrip&&needsItinerary?Math.max(0,Number(scrollShell?.scrollTop||0)):0;\n\n  const previousLoaderSource=tripLoaderState?.source||\"\";\n  // A persisted seed is evidence for inactive-Day continuity only. It is never\n  // current-session server confirmation by itself; the live loader must contact\n  // Firebase before Full Backup can become eligible.\n  const currentSessionServerConfirmed=source!==\"instant-cache\"&&data?.cloudMeta?.serverConfirmed===true;\n  tripLoaderState={status:\"ready\",source,tripId:nextId,updatedAt:data?.cloudMeta?.updatedAt||\"\",serverConfirmed:currentSessionServerConfirmed,hasPendingWrites:data?.cloudMeta?.hasPendingWrites===true,error:null};\n  window.tripData=data;\n  updateTripMapButton(data);\n  if(tripMapOverlayOpen())void renderTripMapScope(tripMapScope);\n  setDataSourceLabel(source===\"instant-cache\"?\"即時旅程快取\":(source===\"cache\"?\"Firebase 快取\":\"Firebase\"));\n  if(String(appEntryTripSwitchHandoff.toTripId||\"\")===nextId){\n    clearAppEntryTripSwitchHandoff();\n    accessRevokedState=false;renderCloudSafetyStrip();\n  }\n  if(source!==\"instant-cache\")maybeReleaseAppEntryResumeLatch();\n  refreshAppEntryState();\n  refreshAppDataDiagnostics();\n  refreshFullBackupSyncGate();\n\n  if(needsItinerary){\n    render(data,{includeSnacks:needsSnacks});\n    restoreRenderedDay(oldDayId);\n    updateLargeHeaderForView();updateCompactHeader();\n    requestAnimationFrame(()=>{\n      if(sameTrip&&scrollShell)scrollShell.scrollTo({top:oldScroll,behavior:\"auto\"});\n      updateBottomNavIndicator();updateCollapsingChrome();updateNowIndicator();\n    });\n  }else{\n    if(needsVisual)applyTripVisualTheme(data);\n    if(needsSnacks)runOptionalAppStep(\"Firebase saved places render\",()=>renderSnacks(data));\n  }\n\n  if(needsInfo)runOptionalAppStep(\"Firebase travel info render\",()=>renderInfo(data));\n  if(needsInfo||needsItinerary){\n    const desiredTeam=needsItinerary?oldTeam:(activeTeam||\"all\");\n    if(desiredTeam!==\"all\"&&data?.meta?.travellers?.[desiredTeam])applyTeam(desiredTeam);else applyTeam(\"all\");\n  }\n  if(needsWeather)runOptionalAppStep(\"Firebase weather render\",()=>renderWeather(data));\n\n  // v7.7.5.1: the core Trip views are now usable. Remove the boot visibility\n  // gate before optional appearance / library refreshers so a non-critical\n  // exception can never leave the whole workspace permanently hidden.\n  releaseDeferredTripBoot();\n\n  if(needsDestinationTheme){try{refreshDestinationThemeInPlace(data);}catch(error){console.warn(\"Destination theme refresh\",error);}}\n  if(needsTeamTheme){try{refreshTeamThemeInPlace(data);}catch(error){console.warn(\"Team theme refresh\",error);}}\n\n  if(needsExpenses&&document.body.classList.contains(\"expenses-view-active\"))requestAnimationFrame(()=>initExpensesViewSafe());\n  const librarySummaryChanged=updateTripLibrarySummaryFromData(data);\n  if(!librarySummaryChanged&&previousLoaderSource!==source)renderTripLibrary();\n\n  tripRenderSignature=nextItinerarySignature;\n  tripSnacksSignature=nextSnacksSignature;\n  tripInfoSignature=nextInfoSignature;\n  tripWeatherSignature=nextWeatherSignature;\n  tripVisualSignature=nextVisualSignature;\n  tripExpensesSignature=nextExpensesSignature;\n  tripDestinationThemeSignature=nextDestinationThemeSignature;\n  tripTeamThemeSignature=nextTeamThemeSignature;\n\n  const tripDirty=dirtyUnknown||!sameTrip||dirtyPrefix(\"trip\");\n  const generalDirty=dirtyUnknown||!sameTrip||dirtyPrefix(\"general\");\n  if(generalDirty||needsDestinationTheme){try{window.__destinationSettingsRefresh?.(data);}catch(error){}}\n  if(generalDirty||needsTeamTheme){try{window.__teamSettingsRefresh?.(data);}catch(error){}}\n  if(tripDirty||generalDirty){try{window.__appearanceColorSettingsRefresh?.(data);}catch(error){}}\n  if(tripDirty||generalDirty){try{window.__tripIconSettingsRefresh?.(data);}catch(error){}}\n  if(tripDirty||generalDirty){try{window.__tripBackgroundSettingsRefresh?.(data);}catch(error){}}\n  if(generalDirty||needsDestinationTheme||needsTeamTheme){try{refreshAppearanceColourMenuState(data);}catch(error){}}\n\n  resolveTripLoaderWaiters(data);\n  // Keep a tiny synchronous appearance snapshot in localStorage. On the next\n  // document load the head can restore the exact Trip background / accent /\n  // Team colours before the first body paint, while IndexedDB restores the\n  // full itinerary immediately afterwards.\n  persistBootVisualSnapshot(data);\n  // Firestore content updates are queued and coalesced before IndexedDB write.\n  // Metadata-only cache/server transitions never reach this path.\n  if(source!==\"instant-cache\")persistInstantTripCache(data).catch(()=>{});\n}\nfunction applyBundledTripFallback(){\n  if(!bundledTripData)return;\n  if(dataTripId(window.tripData)===dataTripId(bundledTripData)&&tripLoaderState.status===\"local\")return;\n  const sig=tripContentRenderSignature(bundledTripData);\n  window.tripData=bundledTripData;\n  if(sig!==tripRenderSignature){\n    render(bundledTripData);\n    runOptionalAppStep(\"Local travel info render\",()=>renderInfo(bundledTripData));\n    runOptionalAppStep(\"Local weather render\",()=>renderWeather(bundledTripData));\n    updateLargeHeaderForView();updateCompactHeader();\n  }\n  tripRenderSignature=sig;\n  tripSnacksSignature=snacksRenderSignature(bundledTripData);\n  tripInfoSignature=infoRenderSignature(bundledTripData);\n  tripWeatherSignature=weatherRenderSignature(bundledTripData);\n  tripVisualSignature=tripVisualRenderSignature(bundledTripData);\n  tripExpensesSignature=expensesRenderSignature(bundledTripData);\n  tripDestinationThemeSignature=destinationThemeRenderSignature(bundledTripData);\n  tripTeamThemeSignature=teamThemeRenderSignature(bundledTripData);\n  tripLoaderState={status:\"local\",source:\"local\",tripId:dataTripId(bundledTripData),updatedAt:\"\",serverConfirmed:false,hasPendingWrites:false,error:null};\n  setDataSourceLabel(\"本機 trip.json\");updateTripLibrarySummaryFromData(bundledTripData);\n  try{refreshAppearanceColourMenuState(bundledTripData);}catch(error){}\n  try{window.__teamSettingsRefresh?.(bundledTripData);}catch(error){}\n  // Pure local / signed-out use still benefits from first-paint visual restore,\n  // but never overwrite a remembered Firebase appearance with bundled defaults.\n  if(!rememberedCacheUid())persistBootVisualSnapshot(bundledTripData);\n  releaseDeferredTripBoot();\n}\nfunction stopFirebaseTripLoader({fallback=false}={}){\n  if(tripLoaderStop){try{tripLoaderStop();}catch(error){}tripLoaderStop=null;}\n  tripLoaderActiveId=\"\";\n  if(fallback)applyBundledTripFallback();\n}\nlet accessRevokeRoutePromise=null;\nlet accessRevokeRouteSourceId=\"\";\nasync function routeAwayFromRevokedTrip(revokedTripIdInput){\n  const revokedTripId=String(revokedTripIdInput||\"\").trim();\n  if(!revokedTripId)return false;\n  if(accessRevokeRoutePromise&&accessRevokeRouteSourceId===revokedTripId)return accessRevokeRoutePromise;\n  accessRevokeRouteSourceId=revokedTripId;\n\n  // Pick the fallback synchronously before any dynamic import / listener teardown.\n  // That closes the old gap where Entry Gateway could take over between A being\n  // revoked and B being selected.\n  const uid=String(window.__appAuthUser?.uid||tripLibraryCurrentUser?.uid||\"\").trim();\n  const currentTrips=Array.isArray(tripLibraryCloudState.trips)?tripLibraryCloudState.trips:[];\n  const fallbackTrips=String(tripLibraryLastReadyCatalog.uid||\"\")===uid?tripLibraryLastReadyCatalog.trips:[];\n  const knownTripMap=new Map();\n  [...fallbackTrips,...currentTrips].forEach(trip=>{const id=String(trip?.tripId||\"\").trim();if(id)knownTripMap.set(id,trip);});\n  const knownTrips=[...knownTripMap.values()];\n  const candidates=knownTrips.filter(trip=>trip?.archived!==true&&String(trip?.tripId||\"\")!==revokedTripId);\n  const target=candidates[0]||null;\n\n  if(!target){\n    clearAppEntryTripSwitchHandoff();\n    accessRevokeRoutePromise=(async()=>{\n      const {session}=await getTripLoaderServices();\n      session.clearActiveTripId?.();updateTripUrl(\"\");\n      appEntryOptimisticTripId=\"\";appEntryOptimisticUid=\"\";clearAppEntryResumeLatch();\n      stopFirebaseTripLoader({fallback:false});\n      accessRevokedState=false;renderCloudSafetyStrip();refreshAppEntryState();\n      return false;\n    })().finally(()=>{accessRevokeRoutePromise=null;accessRevokeRouteSourceId=\"\";});\n    return accessRevokeRoutePromise;\n  }\n\n  const targetId=String(target.tripId||\"\").trim();\n  setAppEntryTripSwitchHandoff(revokedTripId,targetId,uid);\n  appEntryOptimisticTripId=targetId;appEntryOptimisticUid=uid;\n  // Persist the routing hint immediately, before awaiting module work. The loaded\n  // session service will publish the same ID moments later.\n  try{localStorage.setItem(\"travel_active_trip_id\",targetId);}catch(error){}\n  updateTripUrl(targetId);tripBootPreferredId=targetId;\n  if(currentAppView===\"expenses\")setView(\"itinerary\",{restoreScroll:false});\n  try{window.__suspendExpensesForTripSwitch?.();}catch(error){console.warn(\"Suspend Expenses for Trip switch\",error);}\n  refreshAppEntryState();\n\n  accessRevokeRoutePromise=(async()=>{\n    const {session,access}=await getTripLoaderServices();\n    session.setActiveTripId(targetId);\n    setAppEntryResumeLatch(targetId,uid);\n\n    const cacheTask=applyInstantTripCache(targetId,window.__appAuthUser||tripLibraryCurrentUser);\n    const loaderTask=startFirebaseTripLoader(targetId);\n    try{access.initTripAccess?.(targetId);access.refreshTripAccess?.();}catch(error){console.warn(\"Fallback Trip access refresh\",error);}\n    await Promise.race([Promise.resolve(cacheTask),new Promise(resolve=>setTimeout(resolve,160))]);\n    accessRevokedState=false;renderCloudSafetyStrip();refreshAppEntryState();\n    Promise.allSettled([cacheTask,loaderTask]).then(()=>refreshAppEntryState()).catch(()=>{});\n    return true;\n  })().finally(()=>{accessRevokeRoutePromise=null;accessRevokeRouteSourceId=\"\";});\n  return accessRevokeRoutePromise;\n}\nasync function startFirebaseTripLoader(tripIdInput,{preserveVisibleReady=false}={}){\n  const tripId=String(tripIdInput||\"\").trim();if(!tripId)return;\n  const {loader,access}=await getTripLoaderServices();\n  if(tripLoaderActiveId===tripId&&tripLoaderStop)return;\n  const priorLoaderSource=tripLoaderState.source;\n  const priorTripData=dataTripId(window.tripData)===tripId?window.tripData:null;\n  // A same-Trip listener re-arm after an iOS file handoff must never demote an\n  // already usable Workspace back to the Root Entry loading screen. Keep the\n  // visible cached/server state ready while only the transport is replaced.\n  const keepVisibleReady=preserveVisibleReady===true&&!!priorTripData&&tripLoaderState.status===\"ready\";\n  const preservedState=keepVisibleReady?{...tripLoaderState,tripId,error:null}:null;\n  // Only the trusted IndexedDB render cache is used as the inactive-Day seed.\n  // Bundled trip.json is deliberately excluded because Firebase may be newer.\n  const seedData=priorLoaderSource===\"instant-cache\"?priorTripData:null;\n  const activeDayId=preferredRealtimeDayId(seedData||priorTripData);\n  stopFirebaseTripLoader();\n  tripLoaderActiveId=tripId;\n  if(keepVisibleReady){\n    tripLoaderState=preservedState;\n    setDataSourceLabel(priorLoaderSource===\"server\"?\"Firebase · 背景重新連線\":(priorLoaderSource===\"cache\"?\"Firebase 快取 · 背景重新連線\":\"即時旅程快取 · 背景重新連線\"));\n  }else{\n    tripLoaderState={status:\"loading\",source:\"unknown\",tripId,updatedAt:\"\",serverConfirmed:false,hasPendingWrites:false,error:null};\n    setDataSourceLabel(\"Firebase 同步中…\");\n  }\n  access.initTripAccess?.(tripId);\n  tripLoaderStop=loader.subscribeTripData(tripId,event=>{\n    if(tripLoaderActiveId!==tripId)return;\n    perfLiteInc(\"loaderEvents\");\n    if(event.status===\"ready\"){\n      applyFirebaseTripData(event.data,{source:event.fromCache?\"cache\":\"server\",tripId,dirtySections:event.dirtySections||[]});\n      perfLiteMark(\"firebaseFirstData\");\n      if(!event.fromCache)perfLiteMark(\"firebaseServerReady\");\n      return;\n    }\n    if(event.status===\"metadata\"){\n      const metaSource=event.fromCache?\"cache\":\"server\";\n      const wasServerConfirmed=tripLoaderState.serverConfirmed===true&&tripLoaderState.hasPendingWrites!==true;\n      tripLoaderState={...tripLoaderState,status:\"ready\",source:metaSource,serverConfirmed:event.serverConfirmed===true,hasPendingWrites:event.hasPendingWrites===true};\n      // Seal the render cache once a pending/local transition receives full\n      // server confirmation. This is metadata only, so no UI rerender is\n      // needed; one coalesced IndexedDB write preserves a trusted seed for the\n      // next Local First boot and lets the passive Backup gate stay read-free.\n      const nowServerConfirmed=tripLoaderState.serverConfirmed===true&&tripLoaderState.hasPendingWrites!==true&&metaSource===\"server\";\n      if(dataTripId(window.tripData)===tripId&&window.tripData){\n        window.tripData.cloudMeta={...(window.tripData.cloudMeta||{}),source:metaSource,serverConfirmed:tripLoaderState.serverConfirmed===true,hasPendingWrites:tripLoaderState.hasPendingWrites===true};\n        if(nowServerConfirmed){\n          tripRenderCacheLastConfirmedTripId=tripId;\n          tripRenderCacheLastConfirmedRevision=Math.max(0,Number(window.tripData?.revision)||0);\n        }\n        if(nowServerConfirmed&&!wasServerConfirmed)persistInstantTripCache(window.tripData).catch(()=>{});\n      }\n      setDataSourceLabel(metaSource===\"cache\"?\"Firebase 快取\":\"Firebase\");\n      if(metaSource===\"server\")perfLiteMark(\"firebaseServerReady\");\n      renderTripLibrary();refreshAppEntryState();refreshFullBackupSyncGate();\n      return;\n    }\n    if(event.status===\"syncing\"){tripLoaderState={...tripLoaderState,status:\"syncing\",source:event.source||tripLoaderState.source};setDataSourceLabel(event.reason===\"restore\"?\"Firebase 還原中…\":\"Firebase 更新中…\");refreshFullBackupSyncGate();return;}\n    if(event.status===\"recovery-needed\"){tripLoaderState={...tripLoaderState,status:\"recovery-needed\",source:event.source||tripLoaderState.source};setDataSourceLabel(\"Firebase 更新未完成\");renderCloudSafetyStrip();refreshFullBackupSyncGate();return;}\n    if(event.status===\"permission-denied\"||event.status===\"error\"||event.status===\"not-found\"){\n      const optimisticVisible=String(appEntryOptimisticTripId||\"\")===tripId&&dataTripId(window.tripData)===tripId;\n      console.warn(\"Firebase Trip Loader\",event.status,event.error||tripId);\n      if(event.status===\"permission-denied\"||event.status===\"not-found\"){\n        tripLoaderState={status:event.status,source:\"firebase\",tripId,error:event.error||null};\n        if(String(appEntryOptimisticTripId||\"\")===tripId){appEntryOptimisticTripId=\"\";appEntryOptimisticUid=\"\";}\n        if(String(appEntryResumeLatch.tripId||\"\")===tripId)clearAppEntryResumeLatch();\n        invalidateInstantTripCache(tripId).catch(()=>{});\n        stopFirebaseTripLoader({fallback:false});\n        if(tripLibraryCloudState.status===\"ready\")routeAwayFromRevokedTrip(tripId).catch(error=>{console.warn(\"Revoked Trip fallback\",error);refreshAppEntryState();});\n        else refreshAppEntryState();\n      }else if(optimisticVisible){\n        // Travel-first behaviour: keep the already rendered local Trip usable\n        // through a transient sync failure. Firestore/access listeners retry in\n        // background and an explicit permission-denied still ejects immediately.\n        tripLoaderState={status:\"ready\",source:\"instant-cache\",tripId,error:event.error||null};\n        setDataSourceLabel(\"即時旅程快取 · 背景同步待重試\");\n        refreshAppEntryState();refreshAppDataDiagnostics();\n      }else{\n        tripLoaderState={status:event.status,source:\"firebase\",tripId,error:event.error||null};\n        if(tripBootDeferred)stopFirebaseTripLoader({fallback:true});\n      }\n      refreshFullBackupSyncGate();\n    }\n  },{seedData,activeDayId});\n}\nasync function reconcileFirebaseTripLoader(){\n  const token=++tripLoaderReconcileToken;\n  if(!bundledTripData)return;\n  if(!tripLibraryCurrentUser){stopFirebaseTripLoader({fallback:false});refreshAppEntryState();return;}\n  if(tripLibraryCloudState.status!==\"ready\")return;\n  const trips=(tripLibraryCloudState.trips||[]).filter(t=>t.archived!==true);\n  if(!trips.length){stopFirebaseTripLoader({fallback:false});refreshAppEntryState();return;}\n  const {session}=await getTripLoaderServices();if(token!==tripLoaderReconcileToken)return;\n  const desired=session.resolveTripId(bundledTripData);\n  const bundledId=dataTripId(bundledTripData);\n  const target=trips.find(t=>t.tripId===desired)||trips.find(t=>t.tripId===bundledId)||trips[0];\n  if(!target){stopFirebaseTripLoader({fallback:true});return;}\n  session.setActiveTripId(target.tripId);updateTripUrl(target.tripId);await startFirebaseTripLoader(target.tripId);\n}\nlet manualTripSwitchRecoveryToken=0;\nfunction scheduleManualTripSwitchRecovery(tripIdInput,user,fromTripIdInput){\n  const tripId=String(tripIdInput||\"\").trim(),fromTripId=String(fromTripIdInput||\"\").trim();if(!tripId)return;\n  const uid=String(user?.uid||\"\").trim();\n  const token=++manualTripSwitchRecoveryToken;\n  const recentDownload=Date.now()-Number(window.__lastDataOperationDownloadHandoffAt||0)<120000;\n  setTimeout(async()=>{\n    if(token!==manualTripSwitchRecoveryToken||tripLoaderActiveId!==tripId)return;\n    const visible=dataTripId(window.tripData)===tripId;\n    const ready=visible&&tripLoaderState.status===\"ready\";\n    const serverReady=ready&&tripLoaderState.serverConfirmed===true;\n    if(serverReady||(!recentDownload&&ready))return;\n    try{\n      if(!visible)await applyInstantTripCache(tripId,user);\n      if(token!==manualTripSwitchRecoveryToken||tripLoaderActiveId!==tripId)return;\n      // iOS may briefly suspend the Firestore transport while the native Files /\n      // share handoff owns the foreground. Re-arm the existing realtime\n      // listeners once; this does not add an explicit Firestore read or block\n      // the Workspace on a network promise.\n      stopFirebaseTripLoader({fallback:false});\n      await startFirebaseTripLoader(tripId,{preserveVisibleReady:true});\n      try{const {access}=await getTripLoaderServices();access.refreshTripAccess?.();}catch(error){console.warn(\"Trip switch access re-arm\",error);}\n      refreshAppEntryState();\n    }catch(error){\n      console.warn(\"Trip switch listener re-arm\",error);\n      refreshAppEntryState();\n    }\n\n    // Never leave the App indefinitely showing Trip A while the session points\n    // at Trip B. If neither target cache nor realtime data can paint after the\n    // one listener re-arm, safely route the session back to the original Trip.\n    setTimeout(async()=>{\n      if(token!==manualTripSwitchRecoveryToken||tripLoaderActiveId!==tripId)return;\n      const targetReady=dataTripId(window.tripData)===tripId&&tripLoaderState.status===\"ready\";\n      if(targetReady||!fromTripId)return;\n      try{\n        const {session,access}=await getTripLoaderServices();\n        if(uid)setAppEntryTripSwitchHandoff(tripId,fromTripId,uid);\n        appEntryOptimisticTripId=fromTripId;appEntryOptimisticUid=uid;\n        if(uid)setAppEntryResumeLatch(fromTripId,uid);\n        session.setActiveTripId(fromTripId);updateTripUrl(fromTripId);tripBootPreferredId=fromTripId;\n        stopFirebaseTripLoader({fallback:false});\n        const cacheTask=applyInstantTripCache(fromTripId,user);\n        const loaderTask=startFirebaseTripLoader(fromTripId);\n        try{access.initTripAccess?.(fromTripId);access.refreshTripAccess?.();}catch(error){console.warn(\"Trip switch rollback access\",error);}\n        await Promise.race([Promise.resolve(cacheTask),new Promise(resolve=>setTimeout(resolve,160))]);\n        setDataSourceLabel(\"旅程切換未完成 · 已返回原旅程\");\n        refreshAppEntryState();\n        Promise.allSettled([cacheTask,loaderTask]).then(()=>refreshAppEntryState()).catch(()=>{});\n      }catch(error){\n        console.warn(\"Trip switch safe rollback\",error);\n        clearAppEntryTripSwitchHandoff();refreshAppEntryState();\n      }\n    },6500);\n  },recentDownload?3200:6500);\n}\nasync function switchActiveCloudTrip(tripId){\n  const id=String(tripId||\"\").trim();if(!id||id===tripLoaderActiveId)return;\n  if(document.body.classList.contains(\"app-progress-active\")||cloudSafetyState.operation)return;\n  const allowed=(tripLibraryCloudSt");
