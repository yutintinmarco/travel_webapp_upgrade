window.__travelRuntimeParts.push("\nconst APP_VERSION = \"7.9.9.2\";\nconst EXPENSES_MODULE_VERSION = APP_VERSION;\nwindow.APP_VERSION = APP_VERSION;\nlet tripMapServicePromise=null,transitRouteServicePromise=null,tripEditServicePromise=null,tripMapController=null,tripMapScope=\"day\",tripMapRenderEpoch=0,tripMapSelectedPoint=null;\nlet tripMapResolvedPoints=[],tripMapRegion=\"__all__\",tripMapDayId=\"\",tripMapTeam=\"all\";\nconst TRIP_MAP_SEQUENCE_LINE_KEY=\"travel-map-sequence-line-v1\";\nlet tripMapSequenceLineEnabled=(()=>{try{return localStorage.getItem(TRIP_MAP_SEQUENCE_LINE_KEY)!==\"0\";}catch(_){return true;}})();\nfunction tripMapService(){\n  if(!tripMapServicePromise)tripMapServicePromise=import(`./assets/js/trip-map-service.js?build=${APP_VERSION}`);\n  return tripMapServicePromise;\n}\nfunction transitRouteService(){\n  if(!transitRouteServicePromise)transitRouteServicePromise=import(`./assets/js/transit-route-service.js?build=${APP_VERSION}`);\n  return transitRouteServicePromise;\n}\nfunction tripEditService(){\n  if(!tripEditServicePromise)tripEditServicePromise=import(`./assets/js/trip-edit-session-service.js?build=${APP_VERSION}`);\n  return tripEditServicePromise;\n}\nfunction activeTripPresentationData(){return window.__tripEditPreviewData||window.tripData;}\nfunction tripHasMapCandidates(data=activeTripPresentationData()){\n  if(!data)return false;\n  const days=Array.isArray(data.days)?data.days:[];\n  const itemHas=days.some(day=>(Array.isArray(day?.items)?day.items:[]).some(item=>{const loc=item?.location||{};return !!(loc.mapsUrl||loc.placeId||loc.address||item?.maps||item?.mapsUrl||(Number.isFinite(Number(loc.latitude??loc.lat))&&Number.isFinite(Number(loc.longitude??loc.lng))));}));\n  const raw=data.snacks||{},saved=Array.isArray(raw)?raw:(Array.isArray(raw.items)?raw.items:[]);\n  const savedHas=saved.some(place=>{const loc=place?.location||{};return !!(loc.mapsUrl||loc.placeId||loc.address||place?.maps||place?.mapsUrl||(Number.isFinite(Number(loc.latitude??loc.lat))&&Number.isFinite(Number(loc.longitude??loc.lng))));});\n  return itemHas||savedHas;\n}\nfunction updateTripMapButton(data=activeTripPresentationData()){const btn=document.getElementById(\"trip-map-btn\");if(btn)btn.hidden=!tripHasMapCandidates(data);}\nfunction tripMapOverlayOpen(){return document.getElementById(\"trip-map-overlay\")?.classList.contains(\"is-open\");}\nfunction clearTripMapSelection(){\n  tripMapSelectedPoint=null;\n  try{tripMapController?.setSelectedIdentity?.(\"\");}catch(_){}\n  const card=document.getElementById(\"trip-map-place-card\");if(card){card.hidden=true;card.classList.remove(\"has-media\");}\n}\nfunction tripMapGalleryForPoint(point){\n  try{\n    if(point?.kind===\"itinerary\"){\n      const day=(activeTripPresentationData()?.days||[]).find(row=>String(row?.dayId||\"\").trim()===String(point?.dayId||\"\").trim());\n      const items=Array.isArray(day?.items)?day.items:[];\n      const item=items.find(row=>String(row?.itemId||\"\").trim()===String(point?.itemId||\"\").trim())||items[Math.max(0,Number(point?.order||1)-1)]||null;\n      const gallery=item?storageGalleryForItem(item,String(day?.dayId||\"\")):[];\n      if(gallery?.length)return gallery;\n    }\n    if(point?.kind===\"saved\"){\n      const rows=savedPlaceItemsFromTrip();\n      const place=findSavedPlace(String(point?.placeId||\"\"))||rows[Math.max(0,Number(point?.order||1)-1)]||null;\n      const gallery=place?storageGalleryForSavedPlace(place):[];\n      if(gallery?.length)return gallery;\n    }\n  }catch(error){console.warn(\"Map preview gallery hydrate\",error);}\n  const fallback=Array.isArray(point?.previewImages)?point.previewImages.filter(Boolean):[];\n  if(fallback.length)return fallback;\n  return point?.previewImage?[point.previewImage]:[];\n}\nfunction tripMapHydratedPreview(point){return tripMapGalleryForPoint(point)[0]||null;}\nfunction hydrateTripMapPointPreviews(points){return (Array.isArray(points)?points:[]).map(point=>{const previewGallery=tripMapGalleryForPoint(point);return {...point,previewGallery,previewImages:previewGallery,previewImage:previewGallery[0]||point?.previewImage||null};});}\nfunction renderTripMapPreviewMedia(point){\n  const host=document.getElementById(\"trip-map-place-media\"),card=document.getElementById(\"trip-map-place-card\");if(!host)return false;\n  host.replaceChildren();\n  const gallery=tripMapGalleryForPoint(point);\n  card?.classList.toggle(\"has-media\",gallery.length>0);\n  if(gallery.length){\n    const track=document.createElement(\"div\");track.className=\"trip-map-place-gallery-track\";\n    const dots=document.createElement(\"div\");dots.className=\"trip-map-place-gallery-dots\";dots.hidden=gallery.length<2;\n    gallery.forEach((media,index)=>{\n      const slide=document.createElement(\"div\");slide.className=\"trip-map-place-gallery-slide\";slide.dataset.mapGalleryIndex=String(index);\n      const img=document.createElement(\"img\");img.alt=\"\";img.decoding=\"async\";img.loading=index===0?\"eager\":\"lazy\";\n      const fallback=tripMediaDescriptorFallback(media);\n      img.addEventListener(\"error\",()=>{if(!img.isConnected)return;slide.classList.add(\"is-media-error\");},{once:true});\n      slide.appendChild(img);track.appendChild(slide);\n      bindTripMediaImageElement(img,media,{variant:\"display\",fallbackSrc:fallback});\n      if(itineraryMediaCrop(media))applyItineraryMediaCropVisual(img,media,slide);\n      if(gallery.length>1){const dot=document.createElement(\"span\");dot.className=\"trip-map-place-gallery-dot\"+(index===0?\" active\":\"\");dots.appendChild(dot);}\n    });\n    if(gallery.length>1){\n      let raf=0;const syncDots=()=>{raf=0;const width=track.clientWidth||1,index=Math.max(0,Math.min(gallery.length-1,Math.round(track.scrollLeft/width)));dots.querySelectorAll(\".trip-map-place-gallery-dot\").forEach((dot,i)=>dot.classList.toggle(\"active\",i===index));};\n      track.addEventListener(\"scroll\",()=>{if(raf)cancelAnimationFrame(raf);raf=requestAnimationFrame(syncDots);},{passive:true});\n    }\n    host.appendChild(track);if(gallery.length>1)host.appendChild(dots);return true;\n  }\n  const span=document.createElement(\"span\");span.className=\"trip-map-place-media-fallback\";span.setAttribute(\"aria-hidden\",\"true\");span.textContent=point?.icon||(point?.kind===\"saved\"?\"★\":\"📍\");host.appendChild(span);return false;\n}\nfunction showTripMapSelection(point){\n  tripMapSelectedPoint=point||null;\n  try{tripMapController?.setSelectedIdentity?.(point?.identity||\"\");}catch(_){}\n  const card=document.getElementById(\"trip-map-place-card\"),title=document.getElementById(\"trip-map-place-title\"),meta=document.getElementById(\"trip-map-place-meta\"),detail=document.getElementById(\"trip-map-place-detail\"),open=document.getElementById(\"trip-map-open-google\");\n  if(!card||!point){if(card)card.hidden=true;return;}\n  renderTripMapPreviewMedia(point);\n  if(title)title.textContent=point.title||\"地點\";\n  if(meta){meta.textContent=point.meta||point.subtitle||point.formattedAddress||(point.kind===\"saved\"?\"收藏地點\":\"行程地點\");meta.hidden=!meta.textContent;}\n  if(detail){detail.textContent=point.detail||point.formattedAddress||\"\";detail.hidden=!detail.textContent;}\n  if(open){open.hidden=!point.mapsUrl;open.onclick=()=>{if(point.mapsUrl)window.open(point.mapsUrl,\"_blank\",\"noopener\");};}\n  card.hidden=false;\n}\nfunction savedMapRegionRows(points){\n  const counts=new Map();(Array.isArray(points)?points:[]).forEach(point=>{const region=String(point?.region||\"其他\").trim()||\"其他\";counts.set(region,(counts.get(region)||0)+1);});\n  return [...counts.entries()].sort((a,b)=>a[0].localeCompare(b[0],\"zh-HK\",{numeric:true,sensitivity:\"base\"}));\n}\nfunction renderSavedMapRegionFilters(points){\n  const host=document.getElementById(\"trip-map-region-filters\");if(!host)return;\n  if(tripMapScope!==\"saved\"){host.hidden=true;host.replaceChildren();return;}\n  const regions=savedMapRegionRows(points);const valid=new Set(regions.map(([label])=>label));if(tripMapRegion!==\"__all__\"&&!valid.has(tripMapRegion))tripMapRegion=\"__all__\";\n  host.replaceChildren();\n  const rows=[[\"__all__\",\"全部\",points.length],...regions.map(([label,count])=>[label,label,count])];\n  rows.forEach(([value,label,count])=>{const btn=document.createElement(\"button\");btn.type=\"button\";btn.className=\"trip-map-region-filter\"+(value===tripMapRegion?\" active\":\"\");btn.dataset.mapRegion=value;btn.setAttribute(\"aria-pressed\",value===tripMapRegion?\"true\":\"false\");btn.textContent=`${label} ${count}`;host.appendChild(btn);});\n  host.hidden=false;\n}\nfunction applySavedMapRegion(regionInput,{focus=true}={}){\n  if(tripMapScope!==\"saved\"||!tripMapController)return;\n  tripMapRegion=String(regionInput||\"__all__\");\n  const host=document.getElementById(\"trip-map-region-filters\");host?.querySelectorAll?.(\"[data-map-region]\").forEach(btn=>{const active=btn.dataset.mapRegion===tripMapRegion;btn.classList.toggle(\"active\",active);btn.setAttribute(\"aria-pressed\",active?\"true\":\"false\");});\n  clearTripMapSelection();\n  const visible=tripMapRegion===\"__all__\"?tripMapResolvedPoints:tripMapResolvedPoints.filter(point=>(String(point?.region||\"其他\").trim()||\"其他\")===tripMapRegion);\n  tripMapController.setVisibleIdentities(visible.map(point=>point.identity),{focus});\n  const subtitle=document.getElementById(\"trip-map-subtitle\");if(subtitle)subtitle.textContent=tripMapRegion===\"__all__\"?`收藏 · ${visible.length} 個可定位地點`:`${tripMapRegion} · ${visible.length} 個收藏`;\n  const activeBtn=host?.querySelector?.(`[data-map-region=\"${CSS.escape(tripMapRegion)}\"]`);activeBtn?.scrollIntoView?.({behavior:\"smooth\",block:\"nearest\",inline:\"center\"});\n}\nfunction tripMapDays(){const data=activeTripPresentationData();return Array.isArray(data?.days)?data.days:[];}\nfunction renderTripMapDayFilters(){\n  const host=document.getElementById(\"trip-map-day-filters\");if(!host)return;\n  if(tripMapScope!==\"day\"){host.hidden=true;host.replaceChildren();return;}\n  const days=tripMapDays();if(!days.length){host.hidden=true;host.replaceChildren();return;}\n  if(!days.some(day=>String(day?.dayId||\"\").trim()===tripMapDayId))tripMapDayId=String(days[0]?.dayId||\"\").trim();\n  host.replaceChildren();\n  days.forEach((day,index)=>{const dayId=String(day?.dayId||\"\").trim(),btn=document.createElement(\"button\");btn.type=\"button\";btn.className=\"trip-map-day-filter\"+(dayId===tripMapDayId?\" active\":\"\");btn.dataset.mapDayId=dayId;btn.setAttribute(\"aria-pressed\",dayId===tripMapDayId?\"true\":\"false\");btn.textContent=day?.label||`Day ${index+1}`;host.appendChild(btn);});\n  host.hidden=false;\n  host.querySelector(\".trip-map-day-filter.active\")?.scrollIntoView?.({block:\"nearest\",inline:\"center\"});\n}\nfunction tripMapTravellerRows(){\n  const travellers=activeTripPresentationData()?.meta?.travellers||{},keys=orderedTravellerKeys(travellers);\n  return keys.map(key=>({key,label:String(travellers?.[key]?.label||key).trim()||key,color:teamDisplayColour(key,travellers?.[key]||{})}));\n}\nfunction renderTripMapTeamFilters(){\n  const host=document.getElementById(\"trip-map-team-filters\");if(!host)return;\n  const rows=tripMapTravellerRows();\n  if(tripMapScope!==\"day\"||rows.length<2){host.hidden=true;host.replaceChildren();tripMapTeam=\"all\";return;}\n  if(tripMapTeam!==\"all\"&&!rows.some(row=>row.key===tripMapTeam))tripMapTeam=\"all\";\n  host.replaceChildren();\n  const all=document.createElement(\"button\");all.type=\"button\";all.className=\"trip-map-team-filter\"+(tripMapTeam===\"all\"?\" active-all\":\"\");all.dataset.mapTeam=\"all\";all.setAttribute(\"aria-pressed\",tripMapTeam===\"all\"?\"true\":\"false\");all.textContent=\"全部\";host.appendChild(all);\n  rows.forEach(row=>{const btn=document.createElement(\"button\");btn.type=\"button\";const active=row.key===tripMapTeam;btn.className=\"trip-map-team-filter\"+(active?\" active-team\":\"\");btn.dataset.mapTeam=row.key;btn.dataset.teamColor=row.color;btn.setAttribute(\"aria-pressed\",active?\"true\":\"false\");btn.textContent=row.label;if(active){btn.style.background=row.color;btn.style.borderColor=row.color;}host.appendChild(btn);});\n  host.hidden=false;\n  host.querySelector(\"[aria-pressed=\\\"true\\\"]\")?.scrollIntoView?.({block:\"nearest\",inline:\"center\"});\n}\nfunction tripMapPointMatchesTeam(point,teamInput=tripMapTeam){const team=String(teamInput||\"all\");const who=String(point?.who||\"all\").trim()||\"all\";return team===\"all\"||who===\"all\"||who===team;}\nfunction tripMapRenumberVisibleStops(points){let stopNo=0;return (Array.isArray(points)?points:[]).map(point=>String(point?.itemKind||\"stop\")===\"transit\"?{...point,displayOrder:null}:{...point,displayOrder:++stopNo});}\nfunction tripMapDayPointSummary(points){const list=Array.isArray(points)?points:[],transit=list.filter(point=>String(point?.itemKind||\"\")===\"transit\").length,stops=list.length-transit;return transit?`${stops} 行程點 · ${transit} 交通`:`${stops} 個地點`;}\nfunction tripMapRouteGroups(points){\n  const rows=tripMapTravellerRows(),list=Array.isArray(points)?points:[];\n  if(rows.length<2)return [{points:list,color:\"#007aff\"}];\n  if(tripMapTeam!==\"all\"){\n    const team=rows.find(row=>row.key===tripMapTeam);return [{points:list.filter(point=>tripMapPointMatchesTeam(point,tripMapTeam)),color:team?.color||\"#007aff\",teamKey:tripMapTeam}];\n  }\n  const groups=rows.map(row=>({points:list.filter(point=>tripMapPointMatchesTeam(point,row.key)),color:row.color,teamKey:row.key})).filter(group=>group.points.some(point=>String(point?.itemKind||\"stop\")!==\"transit\")&&group.points.filter(point=>String(point?.itemKind||\"stop\")!==\"transit\").length>1);\n  return groups.length?groups:[{points:list,color:\"#007aff\"}];\n}\nfunction tripMapVisibleDayPoints(points){return tripMapTeam===\"all\"?(Array.isArray(points)?points:[]):(Array.isArray(points)?points:[]).filter(point=>tripMapPointMatchesTeam(point,tripMapTeam));}\nfunction syncTripMapLineToggle(){\n  const btn=document.getElementById(\"trip-map-line-toggle\");if(!btn)return;\n  const dayMode=tripMapScope===\"day\";btn.hidden=!dayMode;\n  btn.classList.toggle(\"active\",tripMapSequenceLineEnabled);btn.classList.toggle(\"is-single-team\",tripMapTravellerRows().length<2);\n  btn.setAttribute(\"aria-pressed\",tripMapSequenceLineEnabled?\"true\":\"false\");btn.textContent=tripMapSequenceLineEnabled?\"✓ 行程線\":\"行程線\";\n}\nfunction setTripMapSequenceLineEnabled(value,{persist=true}={}){\n  tripMapSequenceLineEnabled=Boolean(value);if(persist){try{localStorage.setItem(TRIP_MAP_SEQUENCE_LINE_KEY,tripMapSequenceLineEnabled?\"1\":\"0\");}catch(_){}}\n  syncTripMapLineToggle();try{tripMapController?.setRouteVisible?.(tripMapSequenceLineEnabled);}catch(_){ }\n}\n\nfunction tripMapTopPadding(){const teamRows=tripMapTravellerRows();return tripMapScope===\"day\"?(teamRows.length>=2?236:194):170;}\nasync function renderTripMapScope(scope=tripMapScope){\n  tripMapScope=scope===\"saved\"?\"saved\":\"day\";\n  if(tripMapScope!==\"saved\")tripMapRegion=\"__all__\";\n  const overlay=document.getElementById(\"trip-map-overlay\"),canvas=document.getElementById(\"trip-map-canvas\"),state=document.getElementById(\"trip-map-state\"),subtitle=document.getElementById(\"trip-map-subtitle\"),regionHost=document.getElementById(\"trip-map-region-filters\"),dayHost=document.getElementById(\"trip-map-day-filters\"),teamHost=document.getElementById(\"trip-map-team-filters\"),lineToggle=document.getElementById(\"trip-map-line-toggle\");\n  if(!overlay||!canvas||!state||!tripMapOverlayOpen())return;\n  const epoch=++tripMapRenderEpoch;clearTripMapSelection();tripMapResolvedPoints=[];\n  document.querySelectorAll(\"#trip-map-filters [data-map-scope]\").forEach(btn=>btn.classList.toggle(\"active\",btn.dataset.mapScope===tripMapScope));\n  if(regionHost){regionHost.hidden=true;regionHost.replaceChildren();}\n  if(dayHost){dayHost.hidden=true;dayHost.replaceChildren();}\n  if(teamHost){teamHost.hidden=true;teamHost.replaceChildren();}\n  if(lineToggle)lineToggle.hidden=true;\n  if(tripMapScope===\"day\"){const days=tripMapDays();if(!tripMapDayId||!days.some(day=>String(day?.dayId||\"\").trim()===tripMapDayId))tripMapDayId=currentActiveDayId()||String(days[0]?.dayId||\"\").trim();renderTripMapDayFilters();renderTripMapTeamFilters();syncTripMapLineToggle();}\n  try{tripMapController?.destroy?.();}catch(_){ }tripMapController=null;canvas.replaceChildren();\n  state.hidden=false;state.textContent=\"正在準備地圖…\";\n  try{\n    const service=await tripMapService();\n    if(!service.googleMapsConfigured()){state.textContent=\"Google Maps 尚未設定；請先加入受網站限制嘅 Maps browser key。\";return;}\n    const mapData=activeTripPresentationData(),rawPoints=tripMapScope===\"saved\"?service.savedPlaceMapPoints(mapData):service.itineraryMapPoints(mapData,tripMapDayId);\n    const activeDay=tripMapDays().find(day=>String(day?.dayId||\"\").trim()===tripMapDayId);\n    const teamRow=tripMapTravellerRows().find(row=>row.key===tripMapTeam);\n    if(subtitle)subtitle.textContent=tripMapScope===\"saved\"?`收藏 · ${rawPoints.length} 個可定位地點`:`${activeDay?.label||\"當日\"}${tripMapTeam!==\"all\"&&teamRow?` · ${teamRow.label}`:\"\"} · ${tripMapDayPointSummary(rawPoints)}`;\n    if(!rawPoints.length){state.textContent=tripMapScope===\"saved\"?\"收藏暫時未有可定位嘅地點。\":\"呢一日暫時未有可定位嘅行程。\";return;}\n    state.textContent=`正在定位 0 / ${rawPoints.length}…`;\n    const result=await service.resolveMapPoints(rawPoints,{concurrency:3,onProgress:progress=>{if(epoch===tripMapRenderEpoch)state.textContent=`正在定位 ${progress.completed} / ${progress.total}…`;}});\n    if(epoch!==tripMapRenderEpoch||!tripMapOverlayOpen())return;\n    if(!result.resolved.length){state.textContent=\"暫時未能由現有 Google 地圖參照取得座標；資料冇被修改。\";return;}\n    const resolved=hydrateTripMapPointPreviews(result.resolved);\n    if(tripMapScope===\"saved\"){\n      tripMapResolvedPoints=resolved.slice();\n      tripMapController=await service.createTripMap(canvas,{points:tripMapResolvedPoints,onSelect:showTripMapSelection,onMapTap:clearTripMapSelection,focusPaddingTop:tripMapTopPadding()});\n    }else{\n      const visible=tripMapRenumberVisibleStops(tripMapVisibleDayPoints(resolved));tripMapResolvedPoints=visible.slice();\n      const resolvedByIdentity=new Map(resolved.map(point=>[point.identity,point]));\n      const routeContext=rawPoints.map(point=>resolvedByIdentity.has(point.identity)?{...point,...resolvedByIdentity.get(point.identity)}:point);\n      tripMapController=await service.createTripMap(canvas,{points:visible,routeGroups:tripMapRouteGroups(routeContext),showSequenceLine:tripMapSequenceLineEnabled,onSelect:showTripMapSelection,onMapTap:clearTripMapSelection,focusPaddingTop:tripMapTopPadding()});\n    }\n    if(epoch!==tripMapRenderEpoch){tripMapController?.destroy?.();tripMapController=null;return;}\n    if(tripMapScope===\"saved\"){tripMapRegion=\"__all__\";renderSavedMapRegionFilters(tripMapResolvedPoints);applySavedMapRegion(\"__all__\",{focus:true});}\n    else{\n      const visibleCount=tripMapResolvedPoints.length;if(subtitle)subtitle.textContent=`${activeDay?.label||\"當日\"}${tripMapTeam!==\"all\"&&teamRow?` · ${teamRow.label}`:\"\"} · ${tripMapDayPointSummary(tripMapResolvedPoints)}`;\n    }\n    const unresolved=result.unresolved.length;\n    if(tripMapScope===\"saved\")state.textContent=unresolved?`已顯示 ${tripMapResolvedPoints.length} 個地點 · ${unresolved} 個參照暫時未能定位`:`已顯示 ${tripMapResolvedPoints.length} 個地點`;\n    else state.textContent=unresolved?`已顯示 ${tripMapResolvedPoints.length} 個項目 · ${unresolved} 個參照暫時未能定位`:`已顯示 ${tripMapResolvedPoints.length} 個項目`;\n    setTimeout(()=>{if(epoch===tripMapRenderEpoch&&!tripMapSelectedPoint)state.hidden=true;},2400);\n  }catch(error){\n    console.warn(\"Trip map\",error);\n    if(epoch!==tripMapRenderEpoch)return;\n    state.hidden=false;\n    state.textContent=error?.code===\"maps-not-configured\"?\"Google Maps 尚未設定。\":\"Google Maps 暫時未能載入；行程資料冇受影響。\";\n  }\n}\nasync function openTripMap(){\n  const overlay=document.getElementById(\"trip-map-overlay\");if(!overlay||!tripHasMapCandidates())return;\n  const days=tripMapDays();tripMapDayId=currentActiveDayId()||String(days[0]?.dayId||\"\").trim();\n  const travellerKeys=tripMapTravellerRows().map(row=>row.key);tripMapTeam=activeTeam!==\"all\"&&travellerKeys.includes(activeTeam)?activeTeam:\"all\";\n  overlay.classList.add(\"is-open\");overlay.setAttribute(\"aria-hidden\",\"false\");document.body.classList.add(\"modal-open\");tripMapScope=\"day\";\n  await renderTripMapScope(\"day\");\n}\nfunction closeTripMap(){\n  const overlay=document.getElementById(\"trip-map-overlay\");if(!overlay)return;\n  ++tripMapRenderEpoch;overlay.classList.remove(\"is-open\");overlay.setAttribute(\"aria-hidden\",\"true\");clearTripMapSelection();tripMapResolvedPoints=[];tripMapRegion=\"__all__\";tripMapTeam=\"all\";tripMapDayId=\"\";\n  try{tripMapController?.destroy?.();}catch(_){ }tripMapController=null;document.getElementById(\"trip-map-canvas\")?.replaceChildren();[\"trip-map-region-filters\",\"trip-map-day-filters\",\"trip-map-team-filters\"].forEach(id=>{const host=document.getElementById(id);if(host){host.hidden=true;host.replaceChildren();}});const lineToggle=document.getElementById(\"trip-map-line-toggle\");if(lineToggle)lineToggle.hidden=true;\n  document.body.classList.remove(\"modal-open\");\n}\n\nwindow.__tripMediaMutationBusy = false;\nwindow.__tripMediaPendingByTrip = {};\nconst TRIP_PENDING_MEDIA_OVERLAY_KEY=\"travel_pending_media_overlays_v1\";\nconst tripPendingMediaOverlays = new Map();\nlet tripMediaQueueSnapshot = { jobs: [], pendingByTrip: {}, overlays: {}, updatedAt: 0 };\ntry{\n  const cached=JSON.parse(localStorage.getItem(TRIP_PENDING_MEDIA_OVERLAY_KEY)||\"null\");\n  if(cached&&typeof cached===\"object\"){\n    window.__tripMediaPendingByTrip={...(cached.pendingByTrip||{})};\n    Object.entries(cached.overlays||{}).forEach(([tripId,slots])=>Object.entries(slots||{}).forEach(([slot,overlay])=>{\n      if(overlay?.descriptor&&String(overlay.descriptor.storagePath||\"\").trim())tripPendingMediaOverlays.set(`${tripId}|${slot}`,overlay);\n    }));\n  }\n}catch(error){}\nfunction pendingTripMediaOverlay(tripIdInput,slotInput){\n  const tripId=String(tripIdInput||\"\").trim(),slot=String(slotInput||\"\").trim();\n  if(!tripId||!slot)return null;\n  return tripPendingMediaOverlays.get(`${tripId}|${slot}`)||null;\n}\nfunction perfLiteMark(name){try{return window.__perfLite?.mark(name);}catch(error){return null;}}\nfunction perfLiteInc(name,amount=1){try{return window.__perfLite?.inc(name,amount);}catch(error){return 0;}}\nperfLiteMark(\"appShellReady\");\nwindow.EXPENSES_MODULE_VERSION = EXPENSES_MODULE_VERSION;\n\nfunction getTripJsonUrl(){const p=new URLSearchParams(location.search);return p.get(\"data\")||\"trip.json\";}\nfunction clamp(v,a,b){return Math.max(a,Math.min(b,v));}\nfunction escapeHtmlText(value){return String(value??\"\").replaceAll(\"&\",\"&amp;\").replaceAll(\"<\",\"&lt;\").replaceAll(\">\",\"&gt;\").replaceAll('\"',\"&quot;\").replaceAll(\"'\",\"&#39;\");}\nfunction fmtMMDD(iso){return iso.slice(5).replace(\"-\",\"/\");}\nfunction dateInTZ(tz){try{return new Intl.DateTimeFormat(\"en-CA\",{timeZone:tz||\"UTC\",year:\"numeric\",month:\"2-digit\",day:\"2-digit\"}).format(new Date());}catch(e){return new Date().toISOString().slice(0,10);}}\nfunction normGallery(v){if(!v)return[];if(Array.isArray(v))return v;return[v];}\nlet tripMediaIntegrationPromise=null;\nfunction getTripMediaIntegration(){\n  if(!tripMediaIntegrationPromise)tripMediaIntegrationPromise=import(`./assets/js/trip-media-integration-service.js?build=${APP_VERSION}`);\n  return tripMediaIntegrationPromise;\n}\nfunction tripMediaDescriptorFallback(input){\n  if(typeof input===\"string\")return String(input||\"\").trim();\n  if(!input||typeof input!==\"object\")return\"\";\n  return String(input.src||input.url||input.path||\"\").trim();\n}\nfunction tripMediaHasStorage(input){return Boolean(input&&typeof input===\"object\"&&String(input.storagePath||\"\").trim());}\n// v7.9.6.2 · Keep a last-known-good visual URL for the same immutable media\n// asset, but treat it as a soft visual hint rather than an unquestioned source.\n// Safari can occasionally invalidate an in-memory Blob URL while the underlying\n// IndexedDB bytes remain healthy. A failed continuity URL is discarded and the\n// media binder rebuilds a fresh Object URL without requiring a page refresh.\nconst tripMediaVisualUrlByIdentity=new Map();\nconst TRIP_MEDIA_TRANSPARENT_PIXEL=\"data:image/gif;base64,R0lGODlhAQABAIAAAAAAAP///ywAAAAAAQABAAACAUwAOw==\";\nfunction tripMediaVisualIdentity(input,{variant=\"display\",tripId=\"\"}={}){\n  if(!input||typeof input!==\"object\")return\"\";\n  const mediaId=String(input.mediaId||input.imageId||input.id||\"\").trim();\n  const selectedVariant=String(variant||\"\")===\"thumbnail\"?\"thumbnail\":\"display\";\n  const storagePath=String(selectedVariant===\"thumbnail\"?(input.thumbnailStoragePath||input.storagePath||\"\"):(input.storagePath||\"\")).trim();\n  const resolvedTripId=String(input.tripId||tripId||dataTripId(window.tripData)||\"\").trim();\n  return mediaId&&storagePath?`${resolvedTripId}|${selectedVariant}|${mediaId}|${storagePath}`:\"\";\n}\nfunction bindTripMediaImageElement(img,input,{variant=\"display\",tripId=\"\",fallbackSrc=\"\"}={}){\n  if(!(img instanceof HTMLImageElement))return;\n  const resolvedTripId=tripId||dataTripId(window.tripData);\n  const identity=tripMediaVisualIdentity(input,{variant,tripId:resolvedTripId});\n  const continuitySrc=identity?String(tripMediaVisualUrlByIdentity.get(identity)||\"\").trim():\"\";\n  const fallback=String(continuitySrc||fallbackSrc||tripMediaDescriptorFallback(input)||\"\").trim();\n  if(fallback)img.src=fallback;\n  else if(tripMediaHasStorage(input))img.src=TRIP_MEDIA_TRANSPARENT_PIXEL;\n  const rememberLoaded=()=>{\n    const src=String(img.currentSrc||img.src||\"\").trim();\n    if(identity&&src&&src!==TRIP_MEDIA_TRANSPARENT_PIXEL)tripMediaVisualUrlByIdentity.set(identity,src);\n  };\n  const discardBrokenContinuity=()=>{\n    if(!identity)return;\n    const remembered=String(tripMediaVisualUrlByIdentity.get(identity)||\"\").trim();\n    const current=String(img.currentSrc||img.src||\"\").trim();\n    if(remembered&&(current===remembered||remembered.startsWith(\"blob:\")))tripMediaVisualUrlByIdentity.delete(identity);\n  };\n  img.addEventListener(\"load\",rememberLoaded);\n  img.addEventListener(\"error\",discardBrokenContinuity);\n  if(!tripMediaHasStorage(input)){if(!fallback)img.removeAttribute(\"src\");return;}\n  getTripMediaIntegration().then(service=>service.bindTripMediaImage(img,input,{tripId:resolvedTripId,variant,fallbackSrc:fallback})).catch(error=>console.warn(\"Trip media bind\",error));\n}\nfunction itineraryMediaSlot(dayIdInput,itemIdInput,mediaIdInput=\"\"){\n  const dayId=String(dayIdInput||\"\").trim(),itemId=String(itemIdInput||\"\").trim(),mediaId=String(mediaIdInput||\"\").trim();\n  if(!dayId||!itemId)return\"\";\n  return mediaId?`item:${dayId}:${itemId}:${mediaId}`:`item:${dayId}:${itemId}`;\n}\nfunction pendingItineraryMediaOverlays(tripIdInput,dayIdInput,itemIdInput){\n  const tripId=String(tripIdInput||\"\").trim(),prefix=`${itineraryMediaSlot(dayIdInput,itemIdInput)}:`;\n  const legacySlot=itineraryMediaSlot(dayIdInput,itemIdInput);if(!tripId||!legacySlot)return[];\n  const matches=[];\n  tripPendingMediaOverlays.forEach((overlay,key)=>{\n    const marker=`${tripId}|`,slot=String(key||\"\").startsWith(marker)?String(key||\"\").slice(marker.length):\"\";\n    if(slot===legacySlot||slot.startsWith(prefix))matches.push(overlay);\n  });\n  return matches.sort((a,b)=>(Number(a?.createdAt)||0)-(Number(b?.createdAt)||0));\n}\nfunction pendingItineraryMediaOverlay(tripIdInput,dayIdInput,itemIdInput){\n  const overlays=pendingItineraryMediaOverlays(tripIdInput,dayIdInput,itemIdInput);\n  return overlays.find(overlay=>overlay?.blocking===true)||overlays[overlays.length-1]||null;\n}\nfunction isManagedItineraryMedia(image,itemIdInput=\"\"){\n  if(!image||typeof image!==\"object\"||!tripMediaHasStorage(image))return false;\n  const itemId=String(itemIdInput||\"\").trim(),ownerType=String(image.ownerType||\"\").trim(),slot=String(image.slot||\"\").trim(),ownerId=String(image.ownerId||\"\").trim();\n  if(ownerType&&ownerType!==\"item\")return false;\n  if(ownerId&&itemId&&ownerId!==itemId)return false;\n  return slot===\"primary\"||slot===\"gallery\"||slot.startsWith(\"gallery:\");\n}\nfunction managedItineraryMediaList(item,dayId=\"\",tripId=dataTripId(window.tripData)){\n  const itemId=String(item?.itemId||\"\").trim(),images=Array.isArray(item?.images)?item.images.filter(image=>isManagedItineraryMedia(image,itemId)):[ ];\n  const pending=pendingItineraryMediaOverlays(tripId,dayId,itemId).map(overlay=>overlay?.descriptor).filter(descriptor=>descriptor&&tripMediaHasStorage(descriptor));\n  const merged=[...images.map(image=>({...image}))];\n  pending.forEach(descriptor=>{\n    const mediaId=String(descriptor.mediaId||descriptor.imageId||\"\").trim(),index=merged.findIndex(image=>mediaId&&String(image?.mediaId||image?.imageId||\"\").trim()===mediaId);\n    const next={...descriptor,ownerType:\"item\",ownerId:itemId,slot:String(descriptor.slot||\"gallery\")};\n    if(index>=0)merged[index]={...merged[index],...next};else merged.push(next);\n  });\n  return merged.sort((a,b)=>(Number(a?.sortOrder)||0)-(Number(b?.sortOrder)||0));\n}\nfunction managedItineraryMedia(item,dayId=\"\",tripId=dataTripId(window.tripData)){\n  return managedItineraryMediaList(item,dayId,tripId)[0]||null;\n}\nfunction itineraryMediaCrop(input){\n  const raw=input&&typeof input===\"object\"?input.crop:null;if(!raw||typeof raw!==\"object\")return null;\n  const focusXRaw=Number(raw.focusX),focusYRaw=Number(raw.focusY),zoomRaw=Number(raw.zoom),aspectRaw=Number(raw.aspect);\n  const focusX=clamp(Number.isFinite(focusXRaw)?focusXRaw:0.5,0,1),focusY=clamp(Number.isFinite(focusYRaw)?focusYRaw:0.5,0,1),zoom=clamp(Number.isFinite(zoomRaw)?zoomRaw:1,1,4);\n  return {focusX,focusY,zoom,aspect:Number.isFinite(aspectRaw)?aspectRaw:16/9};\n}\n// v7.9.6.2 · Gallery crop placement is layout-reactive. Multi-image add / move /\n// remove operations rebuild the Gallery immediately, while Safari can still be\n// settling the new slide width. A one-shot crop calculation can therefore use a\n// transient frame size and stay visibly wrong until a full refresh. Keep the\n// canonical crop metadata unchanged and re-apply it whenever the actual frame\n// geometry settles or the underlying Blob URL is promoted to its Cloud URL.\nfunction applyItineraryMediaCropVisual(img,input,frame){\n  if(!(img instanceof HTMLImageElement)||!(frame instanceof HTMLElement))return;\n  try{img.__itineraryCropResizeObserver?.disconnect?.();}catch(error){}\n  img.__itineraryCropResizeObserver=null;\n  const crop=itineraryMediaCrop(input);\n  if(!crop){img.classList.remove(\"itinerary-crop-positioned\");img.style.removeProperty(\"position\");img.style.removeProperty(\"left\");img.style.removeProperty(\"top\");img.style.removeProperty(\"width\");img.style.removeProperty(\"height\");return;}\n  let raf=0,settleTimer=0;\n  const update=()=>{\n    raf=0;\n    if(!img.isConnected||!frame.isConnected){try{img.__itineraryCropResizeObserver?.disconnect?.();}catch(error){}return false;}\n    const rect=frame.getBoundingClientRect(),cw=rect.width||frame.clientWidth,ch=rect.height||frame.clientHeight;\n    const nw=img.naturalWidth||Number(input?.width)||0,nh=img.naturalHeight||Number(input?.height)||0;\n    if(cw<1||ch<1||nw<1||nh<1)return false;\n    const base=Math.max(cw/nw,ch/nh),zoom=clamp(Number(crop.zoom),1,4),w=nw*base*zoom,h=nh*base*zoom;\n    let left=cw/2-crop.focusX*w,top=ch/2-crop.focusY*h;\n    left=clamp(left,cw-w,0);top=clamp(top,ch-h,0);\n    img.classList.add(\"itinerary-crop-positioned\");img.style.width=`${w}px`;img.style.height=`${h}px`;img.style.left=`${left}px`;img.style.top=`${top}px`;\n    return true;\n  };\n  const schedule=()=>{if(raf)cancelAnimationFrame(raf);raf=requestAnimationFrame(()=>requestAnimationFrame(update));};\n  img.addEventListener(\"load\",schedule);\n  if(typeof ResizeObserver===\"function\"){\n    const observer=new ResizeObserver(()=>schedule());observer.observe(frame);img.__itineraryCropResizeObserver=observer;\n  }\n  schedule();\n  // iOS can finish scroll-snap / expand layout after the first resize callback.\n  // One bounded settle pass avoids a persistent wrong crop without polling.\n  settleTimer=setTimeout(()=>{if(img.isConnected&&frame.isConnected)schedule();},160);\n  img.addEventListener(\"error\",()=>clearTimeout(settleTimer),{once:true});\n}\nfunction storageGalleryForItem(item,dayId=\"\"){\n  const itemId=String(item?.itemId||\"\").trim(),tripId=dataTripId(window.tripData);\n  const images=Array.isArray(item?.images)?item.images.filter(Boolean).map(image=>image&&typeof image===\"object\"?{...image}:image):[];\n  const pending=pendingItineraryMediaOverlays(tripId,dayId,itemId);\n  pending.forEach((overlay,pendingIndex)=>{\n    const raw=overlay?.descriptor;if(!raw||!tripMediaHasStorage(raw))return;\n    const descriptor={...raw,ownerType:\"item\",ownerId:itemId,slot:String(raw.slot||\"gallery\")};\n    const mediaId=String(descriptor.mediaId||descriptor.imageId||\"\").trim();\n    const index=images.findIndex(image=>mediaId&&String(image?.mediaId||image?.imageId||\"\").trim()===mediaId);\n    if(index>=0){descriptor.sortOrder=Number.isFinite(Number(images[index]?.sortOrder))?Number(images[index].sortOrder):index;images[index]={...images[index],...descriptor};}\n    else{descriptor.sortOrder=Number.isFinite(Number(descriptor.sortOrder))?Number(descriptor.sortOrder):images.length+pendingIndex;images.push(descriptor);}\n  });\n  return images.length?[...images].sort((a,b)=>(Number(a?.sortOrder)||0)-(Number(b?.sortOrder)||0)):[];\n}\nfunction getGalleryForItem(item, day, cityKey, matchedCities, meta){\n  const mediaImages=storageGalleryForItem(item,String(day?.dayId||\"\"));\n  if(mediaImages.length)return mediaImages;\n  const explicit=[...normGallery(item.gallery),...normGallery(item.booking?.gallery)];\n  if(explicit.length)return explicit;\n  const defs=meta?.galleryDefaults||{};\n  const byIcon=defs.byIcon||{};\n  if(item.icon&&byIcon[item.icon])return normGallery(byIcon[item.icon]);\n  const cityCandidates=[];\n  if(day?.city&&day.city!==\"both\")cityCandidates.push(day.city);\n  if(Array.isArray(day?.cities))day.cities.forEach(c=>cityCandidates.push(c));\n  if(cityKey&&cityKey!==\"both\")cityCandidates.push(cityKey);\n  if(Array.isArray(matchedCities))matchedCities.forEach(c=>cityCandidates.push(c));\n  const byCity=defs.byCity||{};\n  for(const c of [...new Set(cityCandidates)]){\n    if(byCity[c])return normGallery(byCity[c]);\n  }\n  return normGallery(defs.fallback);\n}\n\n\n// Phase 2F v7.7.0.10: one destination colour identity across all city UI.\nconst LEGACY_CITY_THEMES={\n  kyoto:{bg:\"#d4ecd4\",softBg:\"rgba(212,236,212,.66)\",softBorder:\"rgba(90,158,90,.58)\",border:\"#5a9e5a\",text:\"#2a5a2a\",ring:\"rgba(90,158,90,.10)\",shadow:\"rgba(90,158,90,.16)\",darkBg:\"rgba(38,80,38,.75)\",darkSoftBg:\"rgba(38,80,38,.32)\",darkSoftBorder:\"rgba(90,158,90,.58)\",darkBorder:\"#5a9e5a\",darkText:\"#a8dba8\",darkRing:\"rgba(90,158,90,.10)\"},\n  osaka:{bg:\"#ffddd4\",softBg:\"rgba(255,221,212,.66)\",softBorder:\"rgba(224,96,64,.56)\",border:\"#e06040\",text:\"#6a2010\",ring:\"rgba(224,96,64,.10)\",shadow:\"rgba(224,96,64,.15)\",darkBg:\"rgba(100,40,20,.75)\",darkSoftBg:\"rgba(100,40,20,.32)\",darkSoftBorder:\"rgba(224,96,64,.58)\",darkBorder:\"#e06040\",darkText:\"#ffb09a\",darkRing:\"rgba(224,96,64,.10)\"},\n  kobe:{bg:\"#e5ddff\",softBg:\"rgba(229,221,255,.66)\",softBorder:\"rgba(123,97,209,.54)\",border:\"#7b61d1\",text:\"#3e2a84\",ring:\"rgba(123,97,209,.10)\",shadow:\"rgba(123,97,209,.15)\",darkBg:\"rgba(74,58,130,.75)\",darkSoftBg:\"rgba(74,58,130,.32)\",darkSoftBorder:\"rgba(155,138,240,.58)\",darkBorder:\"#9b8af0\",darkText:\"#d8d0ff\",darkRing:\"rgba(155,138,240,.10)\"}\n};\nconst GENERIC_CITY_THEMES=[\n  {bg:\"#dceeff\",softBg:\"rgba(220,238,255,.68)\",softBorder:\"rgba(74,144,217,.54)\",border:\"#4a90d9\",text:\"#245c91\",ring:\"rgba(74,144,217,.10)\",shadow:\"rgba(74,144,217,.15)\",darkBg:\"rgba(30,75,115,.76)\",darkSoftBg:\"rgba(30,75,115,.30)\",darkSoftBorder:\"rgba(106,175,245,.56)\",darkBorder:\"#6aaff5\",darkText:\"#b8dcff\",darkRing:\"rgba(106,175,245,.10)\"},\n  {bg:\"#d8f1ec\",softBg:\"rgba(216,241,236,.68)\",softBorder:\"rgba(63,159,145,.54)\",border:\"#3f9f91\",text:\"#245f57\",ring:\"rgba(63,159,145,.10)\",shadow:\"rgba(63,159,145,.15)\",darkBg:\"rgba(26,83,76,.76)\",darkSoftBg:\"rgba(26,83,76,.30)\",darkSoftBorder:\"rgba(91,191,177,.56)\",darkBorder:\"#5bbfb1\",darkText:\"#b9eee6\",darkRing:\"rgba(91,191,177,.10)\"},\n  {bg:\"#d4ecd4\",softBg:\"rgba(212,236,212,.66)\",softBorder:\"rgba(90,158,90,.58)\",border:\"#5a9e5a\",text:\"#2a5a2a\",ring:\"rgba(90,158,90,.10)\",shadow:\"rgba(90,158,90,.16)\",darkBg:\"rgba(38,80,38,.75)\",darkSoftBg:\"rgba(38,80,38,.32)\",darkSoftBorder:\"rgba(90,158,90,.58)\",darkBorder:\"#5a9e5a\",darkText:\"#a8dba8\",darkRing:\"rgba(90,158,90,.10)\"},\n  {bg:\"#fff0c2\",softBg:\"rgba(255,240,194,.68)\",softBorder:\"rgba(211,159,39,.56)\",border:\"#d39f27\",text:\"#704f00\",ring:\"rgba(211,159,39,.10)\",shadow:\"rgba(211,159,39,.15)\",darkBg:\"rgba(104,75,10,.76)\",darkSoftBg:\"rgba(104,75,10,.30)\",darkSoftBorder:\"rgba(230,184,75,.56)\",darkBorder:\"#e6b84b\",darkText:\"#ffe2a0\",darkRing:\"rgba(230,184,75,.10)\"},\n  {bg:\"#ffddd4\",softBg:\"rgba(255,221,212,.66)\",softBorder:\"rgba(224,96,64,.56)\",border:\"#e06040\",text:\"#6a2010\",ring:\"rgba(224,96,64,.10)\",shadow:\"rgba(224,96,64,.15)\",darkBg:\"rgba(100,40,20,.75)\",darkSoftBg:\"rgba(100,40,20,.32)\",darkSoftBorder:\"rgba(224,96,64,.58)\",darkBorder:\"#e06040\",darkText:\"#ffb09a\",darkRing:\"rgba(224,96,64,.10)\"},\n  {bg:\"#e5ddff\",softBg:\"rgba(229,221,255,.66)\",softBorder:\"rgba(123,97,209,.54)\",border:\"#7b61d1\",text:\"#3e2a84\",ring:\"rgba(123,97,209,.10)\",shadow:\"rgba(123,97,209,.15)\",darkBg:\"rgba(74,58,130,.75)\",darkSoftBg:\"rgba(74,58,130,.32)\",darkSoftBorder:\"rgba(155,138,240,.58)\",darkBorder:\"#9b8af0\",darkText:\"#d8d0ff\",darkRing:\"rgba(155,138,240,.10)\"},\n  {bg:\"#f8ddeb\",softBg:\"rgba(248,221,235,.68)\",softBorder:\"rgba(196,91,140,.54)\",border:\"#c45b8c\",text:\"#793658\",ring:\"rgba(196,91,140,.10)\",shadow:\"rgba(196,91,140,.15)\",darkBg:\"rgba(105,45,73,.76)\",darkSoftBg:\"rgba(105,45,73,.30)\",darkSoftBorder:\"rgba(224,117,167,.56)\",darkBorder:\"#e075a7\",darkText:\"#ffd0e5\",darkRing:\"rgba(224,117,167,.10)\"},\n  {bg:\"#d9f0f4\",softBg:\"rgba(217,240,244,.68)\",softBorder:\"rgba(77,157,176,.54)\",border:\"#4d9db0\",text:\"#285e6a\",ring:\"rgba(77,157,176,.10)\",shadow:\"rgba(77,157,176,.15)\",darkBg:\"rgba(31,82,94,.76)\",darkSoftBg:\"rgba(31,82,94,.30)\",darkSoftBorder:\"rgba(102,187,207,.56)\",darkBorder:\"#66bbcf\",darkText:\"#c0edf6\",darkRing:\"rgba(102,187,207,.10)\"}\n];\nfunction cityThemeHash(key){let h=2166136261;for(const ch of String(key||\"\")){h^=ch.charCodeAt(0);h=Math.imul(h,16777619);}return h>>>0;}\nfunction parseHexColour(value){const m=String(value||\"\").trim().match(/^#([0-9a-f]{6})$/i);if(!m)return null;const n=parseInt(m[1],16);return{r:(n>>16)&255,g:(n>>8)&255,b:n&255};}\nfunction mixRgb(a,b,t){return{r:Math.round(a.r+(b.r-a.r)*t),g:Math.round(a.g+(b.g-a.g)*t),b:Math.round(a.b+(b.b-a.b)*t)};}\nfunction rgbHex(c){return`#${[c.r,c.g,c.b].map(v=>Math.max(0,Math.min(255,v)).toString(16).padStart(2,\"0\")).join(\"\")}`;}\nfunction rgba(c,a){return`rgba(${c.r},${c.g},${c.b},${a})`;}\nfunction cityThemeFromAccent(value){const c=parseHexColour(value);if(!c)return null;const white={r:255,g:255,b:255},black={r:0,g:0,b:0};return{bg:rgbHex(mixRgb(c,white,.78)),softBg:rgba(mixRgb(c,white,.82),.70),softBorder:rgba(c,.55),border:rgbHex(c),text:rgbHex(mixRgb(c,black,.48)),ring:rgba(c,.10),shadow:rgba(c,.15),darkBg:rgba(mixRgb(c,black,.40),.78),darkSoftBg:rgba(mixRgb(c,black,.40),.30),darkSoftBorder:rgba(mixRgb(c,white,.18),.58),darkBorder:rgbHex(mixRgb(c,white,.18)),darkText:rgbHex(mixRgb(c,white,.60)),darkRing:rgba(mixRgb(c,white,.18),.10)};}\nfunction buildCityThemeMap(cities={}){\n  const result={},used=new Set(),keys=Object.keys(cities).sort();\n  const reserveTheme=theme=>{const idx=GENERIC_CITY_THEMES.findIndex(t=>t.border===theme?.border);if(idx>=0)used.add(idx);};\n  keys.forEach(key=>{\n    const cfg=cities[key]||{};\n    const explicit=cityThemeFromAccent(cfg.color||cfg.accentColor||cfg.themeColor);\n    if(explicit){result[key]=explicit;reserveTheme(explicit);return;}\n    if(LEGACY_CITY_THEMES[key]){result[key]=LEGACY_CITY_THEMES[key];reserveTheme(result[key]);}\n  });\n  keys.forEach(key=>{\n    if(result[key])return;\n    let idx=cityThemeHash(key)%GENERIC_CITY_THEMES.length;\n    const start=idx;\n    while(used.has(idx)){idx=(idx+1)%GENERIC_CITY_THEMES.length;if(idx===start)break;}\n    used.add(idx);result[key]=GENERIC_CITY_THEMES[idx];\n  });\n  return result;\n}\nfunction applyCityTheme(el,key,cfg,theme){\n  if(!el||!theme)return;\n  el.dataset.cityKey=key;\n  el.style.setProperty(\"--city-bg\",theme.bg);el.style.setProperty(\"--city-soft-bg\",theme.softBg);el.style.setProperty(\"--city-soft-border\",theme.softBorder);el.style.setProperty(\"--city-border\",theme.border);el.style.setProperty(\"--city-text\",theme.text);el.style.setProperty(\"--city-ring\",theme.ring);el.style.setProperty(\"--city-shadow\",theme.shadow);\n  el.style.setProperty(\"--city-dark-bg\",theme.darkBg);el.style.setProperty(\"--city-dark-soft-bg\",theme.darkSoftBg);el.style.setProperty(\"--city-dark-soft-border\",theme.darkSoftBorder);el.style.setProperty(\"--city-dark-border\",theme.darkBorder);el.style.setProperty(\"--city-dark-text\",theme.darkText);el.style.setProperty(\"--city-dark-ring\",theme.darkRing);\n}\nfunction applyMultiCityTheme(el,keys,themeMap){\n  if(!el)return;const normalized=(keys||[]).map(k=>String(k||\"\").trim()).filter(Boolean);const themes=normalized.map(k=>themeMap[k]).filter(Boolean);if(!themes.length)return;\n  el.dataset.cityMulti=\"true\";el.dataset.cityKeys=normalized.join(\",\");\n  el.style.setProperty(\"--city-multi-bg-light\",`linear-gradient(to right, ${themes.map(t=>t.bg).join(\", \")})`);\n  el.style.setProperty(\"--city-multi-border-light\",`linear-gradient(to right, ${themes.map(t=>t.border).join(\", \")})`);\n  el.style.setProperty(\"--city-multi-bg-dark\",`linear-gradient(to right, ${themes.map(t=>t.darkBg).join(\", \")})`);\n  el.style.setProperty(\"--city-multi-border-dark\",`linear-gradient(to right, ${themes.map(t=>t.darkBorder).join(\", \")})`);\n}\nfunction refreshDestinationThemeInPlace(data){\n  const cities=data?.meta?.cities||{};const themes=buildCityThemeMap(cities);\n  document.querySelectorAll(\"[data-city-key]\").forEach(el=>{\n    const key=String(el.dataset.cityKey||\"\").trim();if(key&&cities[key]&&themes[key])applyCityTheme(el,key,cities[key],themes[key]);\n  });\n  document.querySelectorAll('[data-city-multi=\"true\"]').forEach(el=>{\n    const keys=String(el.dataset.cityKeys||\"\").split(\",\").map(v=>v.trim()).filter(v=>v&&cities[v]);\n    if(keys.length)applyMultiCityTheme(el,keys,themes);\n  });\n}\nfunction refreshTeamThemeInPlace(data){\n  const travellers=data?.meta?.travellers||{};\n  Object.entries(travellers).forEach(([key,cfg])=>{\n    const color=teamDisplayColour(key,cfg);\n    document.documentElement.style.setProperty(`--${key}-color`,color);\n    if(key===\"team1\")document.documentElement.style.setProperty(\"--team1-color\",color);\n    if(key===\"team2\")document.documentElement.style.setProperty(\"--team2-color\",color);\n    document.querySelectorAll(`.team-btn[data-team=\"${CSS.escape(key)}\"]`).forEach(btn=>{\n      btn.dataset.color=color;btn.style.setProperty(\"--team-accent\",color);\n      if(btn.dataset.team===activeTeam){btn.style.background=color;btn.style.border=`1px solid ${color}`;btn.style.borderColor=color;btn.style.color=\"#fff\";btn.style.fontWeight=\"700\";}\n    });\n    document.querySelectorAll(`.flight-team-row[data-team=\"${CSS.escape(key)}\"] .flight-team-label`).forEach(label=>{label.style.background=color;});\n    document.querySelectorAll(`.timeline-item[data-who=\"${CSS.escape(key)}\"]`).forEach(item=>{item.style.borderLeft=`3px solid ${color}`;});\n    document.querySelectorAll(`.who-tag[data-team=\"${CSS.escape(key)}\"]`).forEach(tag=>{tag.style.setProperty(\"--badge-color\",color);});\n    document.querySelectorAll(`.info-team-card[data-team=\"${CSS.escape(key)}\"]`).forEach(card=>{\n      card.style.setProperty(\"--info-team-color\",color);card.style.borderLeftColor=color;\n      const label=card.querySelector(\".info-team-label\");if(label)label.style.background=color;\n    });\n  });\n}\n\nfunction cityDateSortValue(cfg={},key=\"\"){\n  return `${String(cfg.startIso||\"9999-99-99\")}|${String(cfg.endIso||\"9999-99-99\")}|${String(cfg.label||key)}|${key}`;\n}\nfunction cityKeysForDay(day,cities={}){\n  const known=new Set(Object.keys(cities));\n  if(Array.isArray(day?.cities)&&day.cities.length){\n    const out=[];day.cities.forEach(key=>{key=String(key||\"\").trim();if(known.has(key)&&!out.includes(key))out.push(key);});\n    if(out.length)return out;\n  }\n  const single=String(day?.city||\"\").trim();\n  if(single&&single!==\"both\"&&known.has(single))return[single];\n  const iso=String(day?.isoDate||\"\").trim();\n  if(!iso)return[];\n  return Object.keys(cities)\n    .filter(key=>{const cfg=cities[key]||{};return cfg.startIso&&cfg.endIso&&iso>=cfg.startIso&&iso<=cfg.endIso;})\n    .sort((a,b)=>cityDateSortValue(cities[a],a).localeCompare(cityDateSortValue(cities[b],b)));\n}\nfunction orderedCityKeys(data,cities=data?.meta?.cities||{}){\n  const result=[];const push=key=>{if(cities[key]&&!result.includes(key))result.push(key);};\n  (data?.days||[]).slice().sort((a,b)=>(Number(a?.sortOrder)||0)-(Number(b?.sortOrder)||0)).forEach(day=>cityKeysForDay(day,cities).forEach(push));\n  Object.keys(cities).sort((a,b)=>cityDateSortValue(cities[a],a).localeCompare(cityDateSortValue(cities[b],b))).forEach(push);\n  return result;\n}\n\n// Font / Refresh\nfunction getCurrentAppView(){\n  try{\n    if(document.body.classList.contains(\"expenses-view-active\")) return \"expenses\";\n    if(document.getElementById(\"snacks-view\")?.style.display===\"block\") return \"snacks\";\n    if(document.getElementById(\"info-view\")?.style.display===\"block\") return \"info\";\n    if(document.getElementById(\"profile-view\")?.style.display===\"block\") return \"profile\";\n  }catch(e){}\n  return \"itinerary\";\n}\nfunction rememberCurrentAppView(){\n  try{sessionStorage.setItem(\"trip_current_view\", getCurrentAppView());}catch(e){}\n}\ndocument.getElementById(\"refresh-btn\").addEventListener(\"click\",()=>{rememberCurrentAppView();location.reload(true);});\ndocument.getElementById(\"trip-edit-btn\")?.addEventListener(\"click\",()=>void beginTripEditSession());\ndocument.getElementById(\"trip-edit-cancel-btn\")?.addEventListener(\"click\",cancelTripEditSession);\ndocument.getElementById(\"trip-edit-save-btn\")?.addEventListener(\"click\",()=>void saveTripEditSession());\ndocument.getElementById(\"trip-edit-item-cancel\")?.addEventListener(\"click\",closeTripEditItemModal);\ndocument.getElementById(\"trip-edit-item-done\")?.addEventListener(\"click\",()=>void finishTripEditItemModal());\ndocument.getElementById(\"trip-edit-item-backdrop\")?.addEventListener(\"click\",event=>{if(event.target===event.currentTarget)closeTripEditItemModal();});\ndocument.getElementById(\"trip-edit-mode-entry\")?.addEventListener(\"click\",()=>{\n  if(!tripEditHasPermission()||tripEditIsActive())return;\n  setView(\"itinerary\",{restoreScroll:true});\n  requestAnimationFrame(()=>void beginTripEditSession());\n});\nconst FMIN=0.85,FMAX=1.30,FSTEP=0.08;\nfunction applyFont(s){s=clamp(s,FMIN,FMAX);document.documentElement.style.setProperty(\"--font-scale\",s);try{localStorage.setItem(\"trip_font_scale\",s);}catch(e){}return s;}\nfunction updateFontScaleLabel(){\n  const label=document.getElementById(\"font-scale-label\");\n  if(!label)return;\n  const diff=fs-1;\n  label.textContent=Math.abs(diff)<0.035?\"標準\":diff<0?\"較小\":diff>0.18?\"最大\":\"較大\";\n}\nlet fs=applyFont(parseFloat(localStorage.getItem(\"trip_font_scale\"))||1);\nupdateFontScaleLabel();\ndocument.getElementById(\"font-smaller\")?.addEventListener(\"click\",()=>{fs=applyFont(fs-FSTEP);updateFontScaleLabel();syncPreferencesToCloud();});\ndocument.getElementById(\"font-larger\")?.addEventListener(\"click\",()=>{fs=applyFont(fs+FSTEP);updateFontScaleLabel();syncPreferencesToCloud();});\n\n// Theme toggle: auto → light → dark → auto\nconst THEMES=[\"auto\",\"light\",\"dark\"];\nconst THEME_ICONS={\n  \"auto\": `<svg width=\"15\" height=\"15\" viewBox=\"0 0 15 15\" fill=\"none\" xmlns=\"http://www.w3.org/2000/svg\"><path d=\"M7.5 2a5.5 5.5 0 1 0 0 11V2z\" fill=\"currentColor\"/><circle cx=\"7.5\" cy=\"7.5\" r=\"5.5\" stroke=\"currentColor\" stroke-width=\"1.4\"/></svg>`,\n  \"light\": `<svg width=\"15\" height=\"15\" viewBox=\"0 0 15 15\" fill=\"none\" xmlns=\"http://www.w3.org/2000/svg\"><path d=\"M7.5 1v1M7.5 13v1M1 7.5H0M15 7.5h-1M2.93 2.93l-.71-.71M12.78 12.78l-.71-.71M2.93 12.07l-.71.71M12.78 2.22l-.71.71\" stroke=\"currentColor\" stroke-width=\"1.5\" stroke-linecap=\"round\"/><circle cx=\"7.5\" cy=\"7.5\" r=\"2.8\" stroke=\"currentColor\" stroke-width=\"1.5\"/></svg>`,\n  \"dark\"");
