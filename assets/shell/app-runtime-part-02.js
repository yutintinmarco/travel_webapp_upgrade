window.__travelRuntimeParts.push(": `<svg width=\"15\" height=\"15\" viewBox=\"0 0 15 15\" fill=\"none\" xmlns=\"http://www.w3.org/2000/svg\"><path d=\"M7.5 1.5a6 6 0 1 0 6 6 4.5 4.5 0 0 1-6-6z\" stroke=\"currentColor\" stroke-width=\"1.5\" stroke-linecap=\"round\" stroke-linejoin=\"round\"/></svg>`\n};\nlet autoSunrise=null,autoSunset=null;\n// Restore sunrise/sunset from last fetch to avoid theme flash on reload\ntry{\n  const ss=JSON.parse(localStorage.getItem(\"trip_sun\")||\"null\");\n  const today=new Date().toISOString().slice(0,10);\n  if(ss&&ss.date===today){autoSunrise=new Date(ss.rise);autoSunset=new Date(ss.set);}\n}catch(e){}\nfunction applyTheme(t){\n  try{\n    document.documentElement.classList.remove(\"theme-light\",\"theme-dark\");\n    if(t===\"dark\"){document.documentElement.classList.add(\"theme-dark\");}\n    else if(t===\"light\"){document.documentElement.classList.add(\"theme-light\");}\n    else{\n      // Auto: use sunrise/sunset if available, else fall back to system preference\n      if(autoSunrise&&autoSunset){\n        const now=new Date();\n        if(now>=autoSunrise&&now<autoSunset){document.documentElement.classList.add(\"theme-light\");}\n        else{document.documentElement.classList.add(\"theme-dark\");}\n      }\n      // else: no class added, CSS media query handles system preference\n    }\n    const btn=document.getElementById(\"theme-btn\");\n    if(btn){btn.setAttribute(\"data-theme\",t);btn.innerHTML=THEME_ICONS[t]||THEME_ICONS[\"auto\"];}\n    document.querySelectorAll(\"[data-theme-choice]\").forEach(choice=>{\n      choice.classList.toggle(\"active\",choice.dataset.themeChoice===t);\n      choice.setAttribute(\"aria-pressed\",choice.dataset.themeChoice===t?\"true\":\"false\");\n    });\n    const isDark=document.documentElement.classList.contains(\"theme-dark\") || (!document.documentElement.classList.contains(\"theme-light\") && window.matchMedia?.(\"(prefers-color-scheme: dark)\").matches);\n    localStorage.setItem(\"trip_theme\",t);\n  }catch(e){}\n}\nlet currentTheme=\"auto\";\ntry{currentTheme=localStorage.getItem(\"trip_theme\")||\"auto\";}catch(e){}\napplyTheme(currentTheme);\nconst themeBtn=document.getElementById(\"theme-btn\");\nif(themeBtn)themeBtn.addEventListener(\"click\",()=>{\n  currentTheme=THEMES[(THEMES.indexOf(currentTheme)+1)%THEMES.length];\n  applyTheme(currentTheme);\n  syncPreferencesToCloud();\n});\ndocument.querySelectorAll(\"[data-theme-choice]\").forEach(choice=>{\n  choice.addEventListener(\"click\",()=>{\n    currentTheme=choice.dataset.themeChoice||\"auto\";\n    applyTheme(currentTheme);\n    syncPreferencesToCloud();\n  });\n});\napplyTheme(currentTheme);\n\nlet preferencesServicePromise=null;\nfunction getPreferencesService(){\n  if(!preferencesServicePromise) preferencesServicePromise=import(`./assets/js/user-preferences-service.js?build=${APP_VERSION}`);\n  return preferencesServicePromise;\n}\nfunction syncPreferencesToCloud(){\n  getPreferencesService()\n    .then(({updatePreferences})=>updatePreferences({theme:currentTheme,fontScale:fs}))\n    .catch(error=>console.warn(\"Preference sync unavailable\",error));\n}\nfunction initPreferencesCloudSyncSafe(){\n  getPreferencesService()\n    .then(({subscribePreferences})=>subscribePreferences((preferences,source)=>{\n      if(source!==\"cloud\") return;\n      if(preferences?.theme && preferences.theme!==currentTheme){\n        currentTheme=preferences.theme;\n        applyTheme(currentTheme);\n      }\n      const cloudFont=Number(preferences?.fontScale);\n      if(Number.isFinite(cloudFont) && Math.abs(cloudFont-fs)>.001){\n        fs=applyFont(cloudFont);\n        updateFontScaleLabel();\n      }\n    }))\n    .catch(error=>console.warn(\"Preference service load error\",error));\n}\n\n// Pull to refresh\nconst ss=document.getElementById(\"scroll-shell\"),pi=document.getElementById(\"pull-indicator\");\nlet psy=0,pd=0,isp=false;\nss.addEventListener(\"touchstart\",e=>{if(document.body.classList.contains(\"expenses-modal-open\")){isp=false;return;}if(ss.scrollTop===0){isp=true;psy=e.touches[0].clientY;}},{passive:true});\nconst PULL_REFRESH_DISTANCE = 150;\nss.addEventListener(\"touchmove\",e=>{if(document.body.classList.contains(\"expenses-modal-open\")){isp=false;pi?.classList.remove(\"show\");return;}if(!isp)return;pd=e.touches[0].clientY-psy;if(pd>0&&pi){pi.classList.add(\"show\");pi.textContent=pd>PULL_REFRESH_DISTANCE?\"釋放以更新行程\":\"繼續下拉更新行程\";}},{passive:true});\nss.addEventListener(\"touchend\",()=>{if(document.body.classList.contains(\"expenses-modal-open\")){isp=false;pd=0;pi?.classList.remove(\"show\");return;}if(!isp)return;if(pd>PULL_REFRESH_DISTANCE){rememberCurrentAppView();location.reload(true);}else pi?.classList.remove(\"show\");isp=false;pd=0;},{passive:true});\n\n// Weather\nfunction wIcon(c){\n  if(typeof c!==\"number\")return{icon:\"☁️\",label:\"多雲\"};\n  if(c===0)return{icon:\"☀️\",label:\"晴朗\"};if(c===1)return{icon:\"🌤️\",label:\"大致天晴\"};\n  if(c===2)return{icon:\"⛅\",label:\"間有雲\"};if(c===3)return{icon:\"☁️\",label:\"多雲\"};\n  if([45,48].includes(c))return{icon:\"🌫️\",label:\"有霧\"};\n  if([51,53,55,56,57].includes(c))return{icon:\"🌦️\",label:\"毛毛雨\"};\n  if([61,63,65,66,67].includes(c))return{icon:\"🌧️\",label:\"有雨\"};\n  if([71,73,75,77].includes(c))return{icon:\"❄️\",label:\"下雪\"};\n  if([80,81,82].includes(c))return{icon:\"🌧️\",label:\"驟雨\"};\n  if([95,96,99].includes(c))return{icon:\"⛈️\",label:\"雷暴\"};\n  return{icon:\"☁️\",label:\"多雲\"};\n}\nconst WEATHER_CACHE_PREFIX=\"travel_weather_cache_v1:\";\nconst WEATHER_CACHE_TTL_MS=30*60*1000;\nlet wCache={};\nlet activeWeatherCityLabel=\"\";\nlet weatherSelectionToken=0;\nfunction validWeatherCfg(cfg){\n  return !!cfg&&Number.isFinite(Number(cfg.latitude))&&Number.isFinite(Number(cfg.longitude));\n}\nfunction weatherRequestKey(cfg){\n  return [Number(cfg.latitude).toFixed(4),Number(cfg.longitude).toFixed(4),String(cfg.timezone||\"UTC\")].join(\"|\");\n}\nfunction readPersistedWeather(cfg){\n  try{\n    const key=WEATHER_CACHE_PREFIX+weatherRequestKey(cfg);\n    const row=JSON.parse(localStorage.getItem(key)||\"null\");\n    if(!row?.data?.daily||Date.now()-Number(row.savedAt||0)>WEATHER_CACHE_TTL_MS)return null;\n    return row.data;\n  }catch(error){return null;}\n}\nfunction persistWeather(cfg,data){\n  try{localStorage.setItem(WEATHER_CACHE_PREFIX+weatherRequestKey(cfg),JSON.stringify({savedAt:Date.now(),data}));}catch(error){}\n}\nfunction weatherCityPrefix(){return activeWeatherCityLabel?`${activeWeatherCityLabel}｜`:\"\";}\nfunction resetWeatherLoading(){\n  const updated=document.getElementById(\"weather-updated\"),today=document.getElementById(\"weather-today\"),sun=document.getElementById(\"weather-sun\"),days=document.getElementById(\"weather-days\");\n  if(updated)updated.textContent=\"載入中…\";\n  if(today)today.textContent=`${weatherCityPrefix()}正在取得今日天氣…`;\n  if(sun)sun.textContent=\"\";\n  if(days)days.innerHTML=\"\";\n}\nfunction fetchCity(key,cfg,tripDates){\n  const selectionToken=++weatherSelectionToken;\n  activeWeatherCityLabel=String(cfg?.label||key||\"\").trim();\n  if(key===\"default\"&&activeWeatherCityLabel===\"default\")activeWeatherCityLabel=\"\";\n  if(!validWeatherCfg(cfg)){\n    const today=document.getElementById(\"weather-today\"),updated=document.getElementById(\"weather-updated\");\n    if(today)today.textContent=`${weatherCityPrefix()}此旅程未設定目的地天氣位置`;\n    if(updated)updated.textContent=\"未設定\";\n    return;\n  }\n  const requestKey=weatherRequestKey(cfg);\n  if(wCache[requestKey]){renderW(wCache[requestKey],tripDates,selectionToken);return;}\n  const persisted=readPersistedWeather(cfg);\n  if(persisted){wCache[requestKey]=persisted;renderW(persisted,tripDates,selectionToken);return;}\n  resetWeatherLoading();\n  const controller=typeof AbortController!==\"undefined\"?new AbortController():null;\n  const timer=setTimeout(()=>{try{controller?.abort();}catch(error){}},8000);\n  fetch(`https://api.open-meteo.com/v1/forecast?latitude=${encodeURIComponent(cfg.latitude)}&longitude=${encodeURIComponent(cfg.longitude)}&timezone=${encodeURIComponent(cfg.timezone||\"UTC\")}&forecast_days=16&daily=weathercode,temperature_2m_max,temperature_2m_min,sunrise,sunset`,controller?{signal:controller.signal}:undefined)\n    .then(r=>{if(!r.ok)throw new Error(`Weather HTTP ${r.status}`);return r.json();})\n    .then(wd=>{\n      if(!wd?.daily)throw new Error(\"Weather daily data unavailable\");\n      wCache[requestKey]=wd;persistWeather(cfg,wd);renderW(wd,tripDates,selectionToken);\n    })\n    .catch(()=>{\n      if(selectionToken!==weatherSelectionToken)return;\n      const today=document.getElementById(\"weather-today\"),updated=document.getElementById(\"weather-updated\");\n      if(today)today.textContent=`${weatherCityPrefix()}暫時無法連線到天氣服務`;\n      if(updated)updated.textContent=\"稍後再試\";\n    })\n    .finally(()=>clearTimeout(timer));\n}\nfunction renderW(wd,tripDates,selectionToken=weatherSelectionToken){\n  if(selectionToken!==weatherSelectionToken)return;\n  const{time:dates,temperature_2m_max:maxT,temperature_2m_min:minT,sunrise,sunset,weathercode:codes}=wd.daily;\n  const i0=wIcon(codes[0]);\n  document.getElementById(\"weather-today\").textContent=`${weatherCityPrefix()}今日（${fmtMMDD(dates[0])}）  ${i0.icon} ${i0.label}  高 ${Math.round(maxT[0])}°C  低 ${Math.round(minT[0])}°C`;\n  document.getElementById(\"weather-sun\").textContent=`日出 ${new Date(sunrise[0]).toTimeString().slice(0,5)}  日落 ${new Date(sunset[0]).toTimeString().slice(0,5)}`;\n  const now=new Date();document.getElementById(\"weather-updated\").textContent=`更新 ${String(now.getHours()).padStart(2,\"0\")}:${String(now.getMinutes()).padStart(2,\"0\")}`;\n  // Store today's sunrise/sunset for auto theme, then re-apply if in auto mode\n  autoSunrise=new Date(sunrise[0]);autoSunset=new Date(sunset[0]);\n  try{localStorage.setItem(\"trip_sun\",JSON.stringify({date:dates[0],rise:sunrise[0],set:sunset[0]}));}catch(e){}\n  if(currentTheme===\"auto\")applyTheme(\"auto\");\n  const el=document.getElementById(\"weather-days\");el.innerHTML=\"\";\n  dates.forEach((d,i)=>{\n    const lbl=fmtMMDD(d);if(tripDates.length&&!tripDates.includes(lbl))return;\n    const c=document.createElement(\"div\");c.className=\"weather-day\";\n    const inf=wIcon(codes[i]);\n    c.innerHTML=`<div class=\"weather-day-label\">${lbl}</div><div class=\"weather-cond\">${inf.icon} ${inf.label}</div><div class=\"weather-temp\">高 ${Math.round(maxT[i])}°  低 ${Math.round(minT[i])}°</div>`;\n    el.appendChild(c);\n  });\n  if(!el.hasChildNodes()){const x=document.createElement(\"div\");x.style.cssText=\"font-size:11px;color:#777;\";x.textContent=\"預報暫未覆蓋行程日期，出發前幾日再查看。\";el.appendChild(x);}\n}\nfunction renderWeather(data){\n  const citiesRaw=data.meta?.cities||{},legacy=data.meta?.weather||{};\n  const declaredCities=Object.keys(citiesRaw);\n  const legacyDeclared=Object.keys(legacy).length>0;\n  const validCities=Object.fromEntries(Object.entries(citiesRaw).filter(([,cfg])=>validWeatherCfg(cfg)));\n  const legacyValid=validWeatherCfg(legacy);\n  const card=document.getElementById(\"weather-card\"),ctEl=document.getElementById(\"city-tabs\");\n  if(!Object.keys(validCities).length&&!legacyValid){\n    if(ctEl){ctEl.style.display=\"none\";ctEl.innerHTML=\"\";}\n    if(!declaredCities.length&&!legacyDeclared){if(card)card.style.display=\"none\";return;}\n    if(card)card.style.display=\"block\";\n    const today=document.getElementById(\"weather-today\"),updated=document.getElementById(\"weather-updated\"),sun=document.getElementById(\"weather-sun\"),days=document.getElementById(\"weather-days\");\n    if(today)today.textContent=\"此旅程未設定有效嘅目的地天氣位置\";\n    if(updated)updated.textContent=\"未設定\";\n    if(sun)sun.textContent=\"\";\n    if(days)days.innerHTML=\"\";\n    return;\n  }\n  if(card)card.style.display=\"block\";\n  if(legacy?.title)document.getElementById(\"weather-title\").textContent=legacy.title;\n  const tripDates=(data.days||[]).filter(d=>d.isoDate).map(d=>fmtMMDD(d.isoDate));\n  if(!Object.keys(validCities).length&&legacyValid){\n    if(ctEl){ctEl.style.display=\"none\";ctEl.innerHTML=\"\";}\n    fetchCity(\"default\",legacy,tripDates);return;\n  }\n  const cities=validCities;\n  const keys=orderedCityKeys(data,cities);\n  const cityThemes=buildCityThemeMap(cities);\n  ctEl.style.display=keys.length>1?\"flex\":\"none\";ctEl.innerHTML=\"\";\n  let activeKey=keys[0];\n  function setCityTabActive(activeK){\n    ctEl.querySelectorAll(\".city-tab\").forEach(b=>{\n      const isActive=b.dataset.cityKey===activeK;\n      b.classList.toggle(\"active\",isActive);\n      b.setAttribute(\"aria-pressed\",isActive?\"true\":\"false\");\n    });\n  }\n  function datesForCity(k){\n    const c=cities[k]||{};\n    if(c.startIso&&c.endIso){\n      const out=[];\n      const start=new Date(`${c.startIso}T00:00:00`);\n      const end=new Date(`${c.endIso}T00:00:00`);\n      for(let d=new Date(start);d<=end;d.setDate(d.getDate()+1)){\n        out.push(fmtMMDD(`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,\"0\")}-${String(d.getDate()).padStart(2,\"0\")}`));\n      }\n      return out;\n    }\n    const fromDays=(data.days||[]).filter(d=>cityKeysForDay(d,cities).includes(k)).map(d=>d.isoDate).filter(Boolean).map(fmtMMDD);\n    return fromDays.length?fromDays:tripDates;\n  }\n  keys.forEach(k=>{\n    const btn=document.createElement(\"button\");btn.type=\"button\";btn.className=\"city-tab\"+(k===activeKey?\" active\":\"\");\n    applyCityTheme(btn,k,cities[k],cityThemes[k]);btn.textContent=cities[k].label||k;\n    btn.setAttribute(\"aria-pressed\",k===activeKey?\"true\":\"false\");\n    btn.setAttribute(\"aria-label\",`查看${cities[k].label||k}天氣`);\n    btn.addEventListener(\"click\",()=>{\n      activeKey=k;\n      setCityTabActive(k);\n      btn.scrollIntoView({behavior:\"smooth\",block:\"nearest\",inline:\"center\"});\n      fetchCity(k,cities[k],datesForCity(k));\n    });\n    ctEl.appendChild(btn);\n  });\n  setCityTabActive(activeKey);\n  fetchCity(activeKey,cities[activeKey],datesForCity(activeKey));\n}\n\n// Detail interactions\nlet curMaps=\"\",curDP=\"\",curBook=\"\",curGal=[],curGalIdx=0,curSavedPlaceId=\"\",scrollY=0;\nconst savedPlaceGallerySelectionByPlace=new Map();\nfunction lockScroll(){scrollY=ss.scrollTop;ss.style.overflowY=\"hidden\";}\nfunction unlockScroll(){ss.style.overflowY=\"auto\";ss.scrollTop=scrollY;}\nfunction savedPlaceGalleryStateKey(placeIdInput){return `${String(dataTripId(window.tripData)||\"\").trim()}|${String(placeIdInput||\"\").trim()}`;}\nfunction rememberSavedPlaceGallerySelection(placeId,gallery,index){const key=savedPlaceGalleryStateKey(placeId);if(!key)return;const safeIndex=clamp(Number(index)||0,0,Math.max(0,(gallery?.length||1)-1));savedPlaceGallerySelectionByPlace.set(key,itineraryGalleryMediaIdentity(gallery?.[safeIndex],safeIndex));}\nfunction renderDots(){const el=document.getElementById(\"detail-dots\");el.innerHTML=\"\";curGal.forEach((_,i)=>{const d=document.createElement(\"div\");d.className=\"modal-dot\"+(i===curGalIdx?\" active\":\"\");d.addEventListener(\"click\",()=>{curGalIdx=i;rememberSavedPlaceGallerySelection(curSavedPlaceId,curGal,i);updateImg();});el.appendChild(d);});}\nfunction updateImg({animate=true}={}){\n  const img=document.getElementById(\"detail-img\"),wrap=document.getElementById(\"detail-img-wrapper\"),stage=document.getElementById(\"detail-img-stage\");if(!img||!wrap||!stage)return;\n  const media=curGal[curGalIdx]||null;if(!media){stage.classList.remove(\"has-media-crop\");img.removeAttribute(\"src\");delete img.dataset.galleryMediaIdentity;renderDots();refreshSavedPlaceMediaControls();return;}\n  rememberSavedPlaceGallerySelection(curSavedPlaceId,curGal,curGalIdx);\n  img.dataset.galleryMediaIdentity=itineraryGalleryMediaIdentity(media,curGalIdx);\n  const crop=itineraryMediaCrop(media);stage.classList.toggle(\"has-media-crop\",!!crop);\n  const apply=()=>{bindTripMediaImageElement(img,media,{variant:\"display\"});if(crop)applyItineraryMediaCropVisual(img,media,stage);else{try{img.__itineraryCropResizeObserver?.disconnect?.();}catch(error){}img.classList.remove(\"itinerary-crop-positioned\");img.style.removeProperty(\"position\");img.style.removeProperty(\"left\");img.style.removeProperty(\"top\");img.style.removeProperty(\"width\");img.style.removeProperty(\"height\");}renderDots();refreshSavedPlaceMediaControls();};\n  if(!animate){img.classList.remove(\"fade-out\",\"fade-in\");apply();return;}\n  img.classList.add(\"fade-out\");setTimeout(()=>{apply();img.classList.remove(\"fade-out\");img.classList.add(\"fade-in\");setTimeout(()=>img.classList.remove(\"fade-in\"),220);},120);\n}\n\n// v7.9.6.2 · Keep the selected itinerary photo stable across Local First →\n// Firebase rerenders. Identity follows the immutable asset, not its array index,\n// so pending → canonical generation promotion does not jump back to photo 1.\nconst itineraryGallerySelectionByItem=new Map();\nfunction itineraryGalleryStateKey(dayIdInput,itemIdInput){\n  return `${String(dataTripId(window.tripData)||\"\").trim()}|${String(dayIdInput||\"\").trim()}|${String(itemIdInput||\"\").trim()}`;\n}\nfunction itineraryGalleryMediaIdentity(input,index=0){\n  if(input&&typeof input===\"object\"){\n    const mediaId=String(input.mediaId||\"\").trim();if(mediaId)return `media:${mediaId}`;\n    const storage=String(input.storagePath||input.thumbnailStoragePath||\"\").trim();if(storage)return `storage:${storage}`;\n    const fallback=tripMediaDescriptorFallback(input);if(fallback)return `url:${fallback}`;\n  }\n  const text=String(input||\"\").trim();return text?`url:${text}`:`index:${index}`;\n}\nfunction rememberItineraryGallerySelection(dayId,itemId,gallery,index){\n  const key=itineraryGalleryStateKey(dayId,itemId);if(!key)return;\n  const safeIndex=clamp(Number(index)||0,0,Math.max(0,(gallery?.length||1)-1));\n  itineraryGallerySelectionByItem.set(key,itineraryGalleryMediaIdentity(gallery?.[safeIndex],safeIndex));\n}\n// v7.9.6.2 · Before any queue / Firebase rerender destroys the old Gallery\n// DOM, capture the image that is actually on screen. This avoids a race where\n// the scroll RAF has not yet persisted photo 2 when Cloud sync triggers render.\nfunction captureItineraryGallerySelectionsFromDom(){\n  document.querySelectorAll?.('.inline-gallery[data-gallery-day-id][data-gallery-item-id]').forEach(galleryEl=>{\n    const dayId=String(galleryEl.dataset.galleryDayId||\"\").trim(),itemId=String(galleryEl.dataset.galleryItemId||\"\").trim();\n    if(!dayId||!itemId)return;\n    const slides=[...galleryEl.querySelectorAll('.inline-gallery-slide[data-gallery-media-identity]')];if(!slides.length)return;\n    let closest=slides[0],distance=Infinity;\n    slides.forEach(slide=>{const d=Math.abs((slide.offsetLeft||0)-(galleryEl.scrollLeft||0));if(d<distance){distance=d;closest=slide;}});\n    const identity=String(closest?.dataset?.galleryMediaIdentity||\"\").trim();\n    if(identity)itineraryGallerySelectionByItem.set(itineraryGalleryStateKey(dayId,itemId),identity);\n  });\n}\n\nconst itineraryPhotoViewerState={gallery:[],index:0,scale:1,x:0,y:0,pinchDistance:0,pinchScale:1,dragX:0,dragY:0,startX:0,startY:0,lastTapAt:0,moved:false,locked:false,inheritedScrollLock:false};\nfunction itineraryPhotoViewerApplyTransform({animate=false}={}){\n  const img=document.getElementById(\"itinerary-photo-viewer-image\");if(!img)return;\n  img.style.transition=animate?\"transform .22s cubic-bezier(.22,.86,.26,1)\":\"none\";\n  img.style.transform=`translate3d(${itineraryPhotoViewerState.x}px,${itineraryPhotoViewerState.y}px,0) scale(${itineraryPhotoViewerState.scale})`;\n}\nfunction itineraryPhotoViewerResetZoom({animate=false}={}){\n  itineraryPhotoViewerState.scale=1;itineraryPhotoViewerState.x=0;itineraryPhotoViewerState.y=0;\n  itineraryPhotoViewerApplyTransform({animate});\n}\nfunction itineraryPhotoViewerShowIndex(index,{direction=0}={}){\n  const state=itineraryPhotoViewerState,total=state.gallery.length;if(!total)return;\n  state.index=(Number(index)+total)%total;itineraryPhotoViewerResetZoom();\n  const img=document.getElementById(\"itinerary-photo-viewer-image\"),counter=document.getElementById(\"itinerary-photo-viewer-counter\");\n  if(counter)counter.textContent=`${state.index+1} / ${total}`;\n  if(img){\n    img.style.opacity=\"0\";\n    bindTripMediaImageElement(img,state.gallery[state.index],{variant:\"display\"});\n    requestAnimationFrame(()=>{img.style.transition=\"opacity .16s ease\";img.style.opacity=\"1\";});\n  }\n}\nfunction openItineraryPhotoViewer(galleryInput,index=0,title=\"行程相片\"){\n  const gallery=Array.isArray(galleryInput)?galleryInput.filter(Boolean):[];if(!gallery.length)return;\n  const viewer=document.getElementById(\"itinerary-photo-viewer\");if(!viewer)return;\n  // Keep the viewer at document-body level so transformed / composited app\n  // surfaces cannot establish a competing stacking context above it.\n  if(viewer.parentElement!==document.body)document.body.appendChild(viewer);\n  itineraryPhotoViewerState.gallery=gallery;itineraryPhotoViewerState.index=clamp(Number(index)||0,0,gallery.length-1);\n  viewer.setAttribute(\"aria-label\",`${title||\"行程\"} 相片`);viewer.setAttribute(\"aria-hidden\",\"false\");viewer.classList.add(\"active\");\n  itineraryPhotoViewerState.inheritedScrollLock=!!document.getElementById(\"detail-backdrop\")?.classList.contains(\"active\");\n  if(!itineraryPhotoViewerState.inheritedScrollLock){itineraryPhotoViewerState.locked=true;lockScroll();}else itineraryPhotoViewerState.locked=false;\n  itineraryPhotoViewerShowIndex(itineraryPhotoViewerState.index);\n}\nfunction closeItineraryPhotoViewer(){\n  const viewer=document.getElementById(\"itinerary-photo-viewer\");if(!viewer?.classList.contains(\"active\"))return;\n  viewer.classList.remove(\"active\");viewer.setAttribute(\"aria-hidden\",\"true\");itineraryPhotoViewerResetZoom();\n  itineraryPhotoViewerState.gallery=[];\n  if(itineraryPhotoViewerState.locked){itineraryPhotoViewerState.locked=false;unlockScroll();}\n  itineraryPhotoViewerState.inheritedScrollLock=false;\n}\nfunction itineraryPhotoDistance(a,b){const dx=a.clientX-b.clientX,dy=a.clientY-b.clientY;return Math.hypot(dx,dy);}\nfunction setupItineraryPhotoViewer(){\n  const viewer=document.getElementById(\"itinerary-photo-viewer\"),stage=document.getElementById(\"itinerary-photo-viewer-stage\"),close=document.getElementById(\"itinerary-photo-viewer-close\");\n  if(!viewer||!stage||!close||viewer.dataset.bound===\"true\")return;\n  if(viewer.parentElement!==document.body)document.body.appendChild(viewer);\n  viewer.dataset.bound=\"true\";close.addEventListener(\"click\",closeItineraryPhotoViewer);\n  stage.addEventListener(\"touchstart\",event=>{\n    const state=itineraryPhotoViewerState;state.moved=false;\n    if(event.touches.length===2){state.pinchDistance=Math.max(1,itineraryPhotoDistance(event.touches[0],event.touches[1]));state.pinchScale=state.scale;return;}\n    if(event.touches.length===1){const t=event.touches[0];state.startX=t.clientX;state.startY=t.clientY;state.dragX=state.x;state.dragY=state.y;}\n  },{passive:true});\n  stage.addEventListener(\"touchmove\",event=>{\n    const state=itineraryPhotoViewerState;\n    if(event.touches.length===2){event.preventDefault();state.moved=true;const distance=Math.max(1,itineraryPhotoDistance(event.touches[0],event.touches[1]));state.scale=clamp(state.pinchScale*(distance/state.pinchDistance),1,4);itineraryPhotoViewerApplyTransform();return;}\n    if(event.touches.length===1&&state.scale>1.01){event.preventDefault();state.moved=true;const t=event.touches[0];state.x=state.dragX+(t.clientX-state.startX);state.y=state.dragY+(t.clientY-state.startY);itineraryPhotoViewerApplyTransform();}\n  },{passive:false});\n  stage.addEventListener(\"touchend\",event=>{\n    const state=itineraryPhotoViewerState;if(event.touches.length)return;\n    const changed=event.changedTouches?.[0],dx=changed?changed.clientX-state.startX:0,dy=changed?changed.clientY-state.startY:0;\n    const isTap=!state.moved&&Math.abs(dx)<14&&Math.abs(dy)<14,now=Date.now();\n    if(isTap&&now-state.lastTapAt<290){state.lastTapAt=0;if(state.scale>1.01)itineraryPhotoViewerResetZoom({animate:true});else{state.scale=2.5;state.x=0;state.y=0;itineraryPhotoViewerApplyTransform({animate:true});}return;}\n    if(isTap)state.lastTapAt=now;\n    if(state.scale<=1.01&&Math.abs(dx)>54&&Math.abs(dx)>Math.abs(dy)*1.15){state.moved=true;itineraryPhotoViewerShowIndex(state.index+(dx<0?1:-1),{direction:dx<0?1:-1});return;}\n    if(state.scale>1.01){const maxX=Math.max(0,(window.innerWidth*(state.scale-1))/2),maxY=Math.max(0,(window.innerHeight*(state.scale-1))/2);state.x=clamp(state.x,-maxX,maxX);state.y=clamp(state.y,-maxY,maxY);itineraryPhotoViewerApplyTransform({animate:true});return;}\n  },{passive:true});\n}\nsetupItineraryPhotoViewer();\n\nfunction currentRememberedGallerySelection(dayId,itemId,galleryInput=[]){\n  const gallery=Array.isArray(galleryInput)?galleryInput:[],key=itineraryGalleryStateKey(dayId,itemId),remembered=itineraryGallerySelectionByItem.get(key)||\"\";\n  let index=gallery.findIndex((media,i)=>itineraryGalleryMediaIdentity(media,i)===remembered);if(index<0)index=0;\n  return {gallery,index,media:gallery[index]||null,identity:gallery.length?itineraryGalleryMediaIdentity(gallery[index],index):\"\"};\n}\nfunction itineraryMediaSelectedIsRemovable(item,media){\n  if(!item||!media||typeof media!==\"object\")return false;\n  const itemId=String(item.itemId||\"\").trim();if(isManagedItineraryMedia(media,itemId))return true;\n  const identity=itineraryGalleryMediaIdentity(media,0);\n  return (Array.isArray(item.images)?item.images:[]).some((candidate,index)=>itineraryGalleryMediaIdentity(candidate,index)===identity);\n}\nfunction refreshItineraryMediaControls(dayId,itemId,galleryInput=null){\n  const found=findItineraryItem(dayId,itemId);if(!found?.item)return;\n  const gallery=Array.isArray(galleryInput)?galleryInput:storageGalleryForItem(found.item,dayId),selection=currentRememberedGallerySelection(dayId,itemId,gallery);\n  const tripId=dataTripId(window.tripData),pending=pendingItineraryMediaOverlay(tripId,dayId,itemId),busy=itineraryMediaBusyKeys.has(itineraryMediaKey(dayId,itemId)),canEdit=itineraryMediaCanEdit(),baseBlocked=busy||pending?.blocking===true||!canEdit;\n  document.querySelectorAll(`[data-itinerary-media-item=\"${CSS.escape(String(itemId))}\"][data-itinerary-media-day=\"${CSS.escape(String(dayId))}\"]`).forEach(button=>{\n    const action=button.dataset.itineraryMediaAction||\"\";\n    if(action===\"add\")button.disabled=baseBlocked;\n    if(action===\"remove\")button.disabled=baseBlocked||!itineraryMediaSelectedIsRemovable(found.item,selection.media);\n    if(action===\"previous\")button.disabled=baseBlocked||selection.index<=0;\n    if(action===\"next\")button.disabled=baseBlocked||selection.index<0||selection.index>=gallery.length-1;\n  });\n}\nfunction markItineraryMediaAction(button,action,dayId,itemId){\n  button.dataset.itineraryMediaAction=action;button.dataset.itineraryMediaDay=String(dayId||\"\");button.dataset.itineraryMediaItem=String(itemId||\"\");\n  return button;\n}\nfunction buildItineraryGalleryBlock(item,gallery,dayId=\"\"){\n  if(!Array.isArray(gallery)||!gallery.length)return null;\n  const galleryItemId=String(item?.itemId||\"\").trim(),galleryStateKey=itineraryGalleryStateKey(dayId,galleryItemId),galleryIdentities=gallery.map((src,index)=>itineraryGalleryMediaIdentity(src,index));\n  const rememberedGalleryIdentity=itineraryGallerySelectionByItem.get(galleryStateKey)||\"\",rememberedGalleryIndex=Math.max(0,galleryIdentities.indexOf(rememberedGalleryIdentity));\n  const galleryBlock=document.createElement(\"div\");galleryBlock.className=\"inline-gallery-block\";\n  const galleryEl=document.createElement(\"div\");galleryEl.className=\"inline-gallery\";galleryEl.dataset.galleryDayId=String(dayId||\"\");galleryEl.dataset.galleryItemId=galleryItemId;galleryEl.setAttribute(\"role\",\"region\");galleryEl.setAttribute(\"aria-label\",`${item.title||\"行程\"} 相冊`);\n  const track=document.createElement(\"div\");track.className=\"inline-gallery-track\";if(rememberedGalleryIndex>0)galleryEl.style.visibility=\"hidden\";\n  const slides=[];\n  gallery.forEach((src,index)=>{\n    const slide=document.createElement(\"div\");slide.className=\"inline-gallery-slide\";slide.dataset.galleryMediaIdentity=itineraryGalleryMediaIdentity(src,index);slide.setAttribute(\"role\",\"button\");slide.tabIndex=0;slide.setAttribute(\"aria-label\",`放大查看第 ${index+1} 張相片`);\n    const img=document.createElement(\"img\");bindTripMediaImageElement(img,src,{variant:\"display\"});img.alt=`${item.title||\"行程\"} ${index+1} / ${gallery.length}`;img.loading=\"lazy\";applyItineraryMediaCropVisual(img,src,slide);\n    const openViewer=event=>{event.stopPropagation();rememberItineraryGallerySelection(dayId,galleryItemId,gallery,index);openItineraryPhotoViewer(gallery,index,item.title||\"行程\");};\n    slide.addEventListener(\"click\",openViewer);slide.addEventListener(\"keydown\",event=>{if(event.key===\"Enter\"||event.key===\" \"){event.preventDefault();openViewer(event);}});slide.appendChild(img);track.appendChild(slide);slides.push(slide);\n  });\n  galleryEl.appendChild(track);galleryBlock.appendChild(galleryEl);\n  if(gallery.length>1){\n    const dots=document.createElement(\"div\");dots.className=\"inline-gallery-dots\";dots.setAttribute(\"aria-label\",`相片 1 / ${gallery.length}`);\n    const dotButtons=gallery.map((_,index)=>{const dot=document.createElement(\"button\");dot.type=\"button\";dot.className=`inline-gallery-dot${index===0?\" active\":\"\"}`;dot.setAttribute(\"aria-label\",`查看第 ${index+1} 張相片`);dot.setAttribute(\"aria-current\",index===0?\"true\":\"false\");dot.addEventListener(\"click\",event=>{event.stopPropagation();rememberItineraryGallerySelection(dayId,galleryItemId,gallery,index);galleryEl.scrollTo({left:slides[index].offsetLeft,behavior:\"smooth\"});refreshItineraryMediaControls(dayId,galleryItemId,gallery);});dots.appendChild(dot);return dot;});\n    let galleryRaf=0;const setActiveDot=index=>{dotButtons.forEach((dot,i)=>{const active=i===index;dot.classList.toggle(\"active\",active);dot.setAttribute(\"aria-current\",active?\"true\":\"false\");});dots.setAttribute(\"aria-label\",`相片 ${index+1} / ${gallery.length}`);rememberItineraryGallerySelection(dayId,galleryItemId,gallery,index);refreshItineraryMediaControls(dayId,galleryItemId,gallery);};\n    galleryEl.addEventListener(\"scroll\",()=>{cancelAnimationFrame(galleryRaf);galleryRaf=requestAnimationFrame(()=>{let closestIndex=0,closestDistance=Infinity;slides.forEach((slide,index)=>{const distance=Math.abs(slide.offsetLeft-galleryEl.scrollLeft);if(distance<closestDistance){closestDistance=distance;closestIndex=index;}});setActiveDot(closestIndex);});},{passive:true});\n    galleryBlock.appendChild(dots);requestAnimationFrame(()=>{const index=clamp(rememberedGalleryIndex,0,gallery.length-1);if(slides[index])galleryEl.scrollLeft=slides[index].offsetLeft;setActiveDot(index);galleryEl.style.visibility=\"visible\";});\n  }else{rememberItineraryGallerySelection(dayId,galleryItemId,gallery,0);requestAnimationFrame(()=>refreshItineraryMediaControls(dayId,galleryItemId,gallery));}\n  return galleryBlock;\n}\nfunction appendItineraryMediaManagementActions(container,item,gallery,dayId,itemId,{includeStatus=true}={}){\n  const tripId=dataTripId(window.tripData),pending=pendingItineraryMediaOverlay(tripId,dayId,itemId),canManage=itineraryMediaCanManageRole(),canEdit=itineraryMediaCanEdit();\n  if(!canManage||!dayId||!itemId)return;\n  const busy=itineraryMediaBusyKeys.has(itineraryMediaKey(dayId,itemId)),blocked=busy||pending?.blocking===true||!canEdit,selection=currentRememberedGallerySelection(dayId,itemId,gallery);\n  const actions=document.createElement(\"div\");actions.className=\"inline-detail-actions\";\n  const addBtn=markItineraryMediaAction(document.createElement(\"button\"),\"add\",dayId,itemId);addBtn.type=\"button\";addBtn.className=\"inline-action-btn secondary\";addBtn.disabled=blocked;addBtn.textContent=busy?\"正在處理…\":(pending?.blocking===true?\"相片同步中…\":\"加入相片\");addBtn.addEventListener(\"click\",event=>{event.stopPropagation();if(!addBtn.disabled)openItineraryMediaPicker(dayId,itemId);});actions.appendChild(addBtn);\n  if(gallery.length){\n    const prev=markItineraryMediaAction(document.createElement(\"button\"),\"previous\",dayId,itemId);prev.type=\"button\";prev.className=\"inline-action-btn secondary\";prev.textContent=\"向前移\";prev.disabled=blocked||selection.index<=0;prev.addEventListener(\"click\",event=>{event.stopPropagation();if(!prev.disabled)moveItineraryMedia(dayId,itemId,-1);});actions.appendChild(prev);\n    const next=markItineraryMediaAction(document.createElement(\"button\"),\"next\",dayId,itemId);next.type=\"button\";next.className=\"inline-action-btn secondary\";next.textContent=\"向後移\";next.disabled=blocked||selection.index>=gallery.length-1;next.addEventListener(\"click\",event=>{event.stopPropagation();if(!next.disabled)moveItineraryMedia(dayId,itemId,1);});actions.appendChild(next);\n    const remove=markItineraryMediaAction(document.createElement(\"button\"),\"remove\",dayId,itemId);remove.type=\"button\";remove.className=\"inline-action-btn secondary\";remove.textContent=\"移除目前相片\";remove.disabled=blocked||!itineraryMediaSelectedIsRemovable(item,selection.media);remove.addEventListener(\"click\",event=>{event.stopPropagation();if(!remove.disabled)removeItineraryMedia(dayId,itemId);});actions.appendChild(remove);\n  }\n  container.appendChild(actions);\n  if(includeStatus){const status=document.createElement(\"div\");status.className=\"inline-media-status\";if(busy)status.textContent=\"正在本機解碼及壓縮相片…\";else if(pending)status.textContent=mediaQueueStateCopy(pending,\"相片\");else if(!canEdit)status.textContent=currentTripGlobalLocked()?\"旅程已全域鎖定，目前只可查看相片。\":\"目前為唯讀。\";if(status.textContent)container.appendChild(status);}\n  requestAnimationFrame(()=>refreshItineraryMediaControls(dayId,itemId,gallery));\n}\n\nfunction appendInlineDetails(itemEl,item,gallery,dayId=\"\"){\n  const galleryItemId=String(item?.itemId||\"\").trim();\n  const shell=document.createElement(\"div\");shell.className=\"inline-detail-shell\";\n  const clip=document.createElement(\"div\");clip.className=\"inline-detail-clip\";\n  const detail=document.createElement(\"div\");detail.className=\"inline-detail-content\";\n  const routePlanner=buildTransitRoutePlanner(item,dayId);if(routePlanner)detail.appendChild(routePlanner);\n  const galleryBlock=buildItineraryGalleryBlock(item,gallery,dayId);if(galleryBlock)detail.appendChild(galleryBlock);\n  const body=document.createElement(\"div\");body.className=\"inline-detail-body\";\n  if(item.detailLines?.length){item.detailLines.forEach(line=>{const row=document.createElement(\"div\");row.textContent=line;body.appendChild(row);});}else if(item.detail){body.textContent=item.detail;}\n  if(body.textContent.trim())detail.appendChild(body);\n  const actions=document.createElement(\"div\");actions.className=\"inline-detail-actions\";\n  const addAction=(label,url,secondary=false)=>{if(!url)return;const a=document.createElement(\"a\");a.className=`inline-action-btn${secondary?\" secondary\":\"\"}`;a.href=url;a.target=\"_blank\";a.rel=\"noopener\";a.textContent=label;a.addEventListener(\"click\",e=>e.stopPropagation());actions.appendChild(a);};\n  addAction(\"Google 地圖\",item.maps,false);addAction(\"官方網頁\",item.dianping,true);addAction(\"查看預訂\",item.booking?.pdf||item.bookingPdf,true);if(actions.childElementCount)detail.appendChild(actions);\n  appendItineraryMediaManagementActions(detail,item,gallery,dayId,galleryItemId);\n  clip.appendChild(detail);shell.appendChild(clip);itemEl.appendChild(shell);\n}\n\nfunction buildStandaloneItineraryMediaSurface(item,dayId=\"\"){\n  const itemId=String(item?.itemId||\"\").trim(),tripId=dataTripId(window.tripData);if(!tripId||!dayId||!itemId)return null;\n  const gallery=storageGalleryForItem(item,dayId),pending=pendingItineraryMediaOverlay(tripId,dayId,itemId),canManage=itineraryMediaCanManageRole();if(!gallery.length&&!canManage)return null;\n  const selection=currentRememberedGallerySelection(dayId,itemId,gallery),thumbMedia=selection.media||gallery[0]||null;\n  const accessory=document.createElement(\"button\");accessory.type=\"button\";accessory.className=\"itinerary-media-accessory\";accessory.setAttribute(\"aria-expanded\",\"false\");accessory.setAttribute(\"aria-label\",gallery.length?`${item.title||\"行程\"} 相片`:`為 ${item.title||\"行程\"} 加入相片`);if(pending?.blocking===true)accessory.classList.add(\"is-syncing\");\n  if(thumbMedia){const thumb=document.createElement(\"img\");thumb.alt=\"\";thumb.loading=\"lazy\";bindTripMediaImageElement(thumb,thumbMedia,{variant:\"thumbnail\",tripId});accessory.appendChild(thumb);}else accessory.innerHTML='<svg viewBox=\"0 0 20 20\" fill=\"none\" aria-hidden=\"true\"><rect x=\"2.75\" y=\"4\" width=\"14.5\" height=\"12\" rx=\"2.5\" stroke=\"currentColor\" stroke-width=\"1.45\"/><circle cx=\"7.1\" cy=\"8.1\" r=\"1.55\" stroke=\"currentColor\" stroke-width=\"1.35\"/><path d=\"M4.7 14l3.1-3.2 2.25 2.15 2.35-2.55 2.9 3.6\" stroke=\"currentColor\" stroke-width=\"1.35\" stroke-linecap=\"round\" stroke-linejoin=\"round\"/></svg>';\n  const shell=document.createElement(\"div\");shell.className=\"itinerary-media-shell\";const clip=document.createElement(\"div\");clip.className=\"itinerary-media-clip\";const panel=document.createElement(\"div\");panel.className=\"itinerary-media-panel\";\n  const galleryBlock=buildItineraryGalleryBlock(item,gallery,dayId);if(galleryBlock)panel.appendChild(galleryBlock);\n  appendItineraryMediaManagementActions(panel,item,gallery,dayId,itemId);\n  clip.appendChild(panel);shell.appendChild(clip);\n  accessory.addEventListener(\"click\",event=>{event.stopPropagation();const itemEl=accessory.closest(\".itinerary-item\");if(!itemEl)return;const opening=!itemEl.classList.contains(\"media-expanded\");document.querySelectorAll(\".itinerary-item.media-expanded\").forEach(other=>{if(other!==itemEl){other.classList.remove(\"media-expanded\");other.querySelector(\".itinerary-media-accessory\")?.setAttribute(\"aria-expanded\",\"false\");}});itemEl.classList.toggle(\"media-expanded\",opening);accessory.setAttribute(\"aria-expanded\",opening?\"true\":\"false\");});\n  return {accessory,shell};\n}\n\nfunction savedPlaceItemsFromTrip(){const snacks=window.tripData?.snacks||{};return Array.isArray(snacks)?snacks:(Array.isArray(snacks.items)?snacks.items:[]);}\nfunction findSavedPlace(placeIdInput){const placeId=String(placeIdInput||\"\").trim();if(!placeId)return null;return savedPlaceItemsFromTrip().find(place=>String(place?.placeId||place?.id||\"\").trim()===placeId)||null;}\nfunction openSavedItemModal(item){\n  const placeId=String(item.getAttribute(\"data-place-id\")||\"\").trim(),place=findSavedPlace(placeId);curSavedPlaceId=placeId;\n  document.getElementById(\"detail-title\").textContent=item.getAttribute(\"data-title\")||\"\";\n  document.getElementById(\"detail-time\").textContent=item.getAttribute(\"data-time\")||\"\";\n  const lr=item.getAttribute(\"data-detail-lines\"),body=document.getElementById(\"detail-body\");\n  const htmlDetail=item.getAttribute(\"data-detail-html\");\n  if(htmlDetail){body.innerHTML=htmlDetail;}\n  else if(lr){try{body.innerHTML=JSON.parse(lr).map(l=>`<div>${l}</div>`).join(\"\");}catch(e){body.textContent=item.getAttribute(\"data-detail\")||\"\";}}\n  else body.textContent=item.getAttribute(\"data-detail\")||\"\";\n  curMaps=item.getAttribute(\"data-maps\")||\"\";curDP=item.getAttribute(\"data-dianping\")||\"\";curBook=item.getAttribute(\"data-booking-pdf\")||\"\";\n  let gal=place?storageGalleryForSavedPlace(place):[];\n  if(!gal.length){const gr=item.getAttribute(\"data-gallery\");if(gr){try{gal=normGallery(JSON.parse(gr));}catch(e){gal=normGallery(gr);}}}\n  const remembered=savedPlaceGallerySelectionByPlace.get(savedPlaceGalleryStateKey(placeId))||\"\";let rememberedIndex=gal.findIndex((media,index)=>itineraryGalleryMediaIdentity(media,index)===remembered);if(rememberedIndex<0)rememberedIndex=0;\n  const wrap=document.getElementById(\"detail-img-wrapper\"),dots=document.getElementById(\"detail-dots\");\n  if(gal.length){curGal=gal;curGalIdx=rememberedIndex;wrap.style.display=\"block\";dots.style.display=gal.length>1?\"flex\":\"none\";updateImg({animate:false});}\n  else{curGal=[];curGalIdx=0;document.getElementById(\"detail-img-stage\")?.classList.remove(\"has-media-crop\");wrap.style.display=\"none\";dots.style.display=\"none\";document.getElementById(\"detail-img\").src=\"\";refreshSavedPlaceMediaControls();}\n  document.getElementById(\"detail-maps-btn\").style.display=curMaps?\"inline-block\":\"none\";\n  document.getElementById(\"detail-dianping-btn\").style.display=curDP?\"inline-block\":\"none\";\n  document.getElementById(\"detail-booking-btn\").style.display=curBook?\"inline-block\":\"none\";\n  const modalEl=document.querySelector(\"#detail-backdrop .modal\");\n  modalEl?.classList.remove(\"is-booked\");modalEl?.classList.add(\"snack-modal\");\n  if(modalEl){modalEl.style.borderColor=\"\";modalEl.style.borderWidth=\"\";modalEl.style.borderStyle=\"\";}\n  refreshSavedPlaceMediaControls();openSavedItemSheet();\n}\n\nfunction setupClicks(){\n  document.querySelectorAll(\".itinerary-item.clickable\").forEach(item=>{\n    const summary=item.querySelector(\".itinerary-summary\");\n    const shell=item.querySelector(\".inline-detail-shell\");\n    if(!summary||!shell||summary.dataset.bound===\"true\")return;\n    summary.dataset.bound=\"true\";\n    summary.setAttribute(\"role\",\"button\");summary.setAttribute(\"tabindex\",\"0\");summary.setAttribute(\"aria-expanded\",\"false\");\n\n    const finishTransition=()=>{\n      item.classList.remove(\"is-detail-transitioning\");\n      if(!item.classList.contains(\"expanded\"))return;\n      const viewport=document.getElementById(\"scroll-shell\")||document.scrollingElement;\n      if(!viewport)return;\n      const rect=item.getBoundingClientRect();\n      const navSpace=Math.max(92,parseFloat(getComputedStyle(document.documentElement).getPropertyValue(\"--bottom-nav-space\"))||92);\n      const bottomLimit=window.innerHeight-navSpace;\n      if(rect.bottom>bottomLimit){\n        viewport.scrollBy({top:Math.min(rect.bottom-bottomLimit+12,160),behavior:\"smooth\"});\n      }\n    };\n\n    shell.addEventListener(\"transitionend\",e=>{\n      if(e.target===shell&&e.propertyName===\"grid-template-rows\")finishTransition();\n    });\n\n    const toggle=()=>{\n      if(item.classList.contains(\"is-detail-transitioning\"))return;\n      const willOpen=!item.classList.contains(\"expanded\");\n      const oldItems=[...document.querySelectorAll(\".itinerary-item.expanded\")].filter(other=>other!==item);\n\n      oldItems.forEach(other=>{\n        other.classList.add(\"is-detail-transitioning\");\n        other.classList.remove(\"expanded\");\n        other.querySelector(\".itinerary-summary\")?.setAttribute(\"aria-expanded\",\"false\");\n        window.setTimeout(()=>other.classList.remove(\"is-detail-transitioning\"),430);\n      });\n\n      item.classList.add(\"is-detail-transitioning\");\n      item.classList.toggle(\"expanded\",willOpen);\n      summary.setAttribute(\"aria-expanded\",willOpen?\"true\":\"false\");\n      if(willOpen)void ensureTransitRoutePlanner(item);\n      window.setTimeout(()=>{\n        if(item.classList.contains(\"is-detail-transitioning\"))finishTransition();\n      },430);\n    };\n    summary.addEventListener(\"click\",toggle);\n    summary.addEventListener(\"keydown\",e=>{if(e.key===\"Enter\"||e.key===\" \"){e.preventDefault();toggle();}});\n  });\n\n  document.querySelectorAll(\".snack-item.clickable\").forEach(item=>{\n    if(item.dataset.bound===\"true\")return;item.dataset.bound=\"true\";\n    item.addEventListener(\"click\",()=>openSavedItemModal(item));\n  });\n}\n\n// Swipe\nlet tx=0,ty=0,ex=0,ey=0,savedPlaceSwipeMoved=false;\nconst iw=document.getElementById(\"detail-img-wrapper\");\niw.addEventListener(\"touchstart\",e=>{const touch=e.touches?.[0];if(!touch)return;tx=ex=touch.clientX;ty=ey=touch.clientY;savedPlaceSwipeMoved=false;},{passive:true});\niw.addEventListener(\"touchmove\",e=>{const touch=e.touches?.[0];if(!touch)return;ex=touch.clientX;ey=touch.clientY;if(Math.abs(ex-tx)>6||Math.abs(ey-ty)>6)savedPlaceSwipeMoved=true;},{passive:true});\niw.addEventListener(\"touchend\",()=>{if(!savedPlaceSwipeMoved||!curGal.length)return;const dx=ex-tx,dy=ey-ty;savedPlaceSwipeMoved=false;if(Math.abs(dx)<40||Math.abs(dx)<=Math.abs(dy))return;curGalIdx=dx<0?(curGalIdx+1)%curGal.length:(curGalIdx-1+curGal.length)%curGal.length;rememberSavedPlaceGallerySelection(curSavedPlaceId,curGal,curGalIdx);updateImg();});\niw.addEventListener(\"touchcancel\",()=>{savedPlaceSwipeMoved=false;},{passive:true});\ndocument.getElementById(\"detail-img\").addEventListener(\"click\",e=>{if(!curGal.length)return;e.stopPropagation();const visualIdentity=String(e.currentTarget?.dataset?.galleryMediaIdentity||\"\").trim();let index=visualIdentity?curGal.findIndex((media,i)=>itineraryGalleryMediaIdentity(media,i)===visualIdentity):-1;if(index<0)index=clamp(curGalIdx,0,curGal.length-1);curGalIdx=index;rememberSavedPlaceGallerySelection(curSavedPlaceId,curGal,index);openItineraryPhotoViewer(curGal,index,document.getElementById(\"detail-title\")?.textContent||\"收藏\");});\nconst savedSheetBackdrop=document.getElementById(\"detail-backdrop\");\nconst savedSheet=savedSheetBackdrop?.querySelector(\".modal\");\nconst savedSheetGrabber=document.getElementById(\"detail-sheet-grabber\");\nconst savedSheetBody=document.getElementById(\"detail-body\");\nlet sheetTouch=null;\nlet sheetCloseTimer=0;\n\nfunction setSavedSheetOffset(y,{animate=false}={}){\n  if(!savedSheet||!savedSheetBackdrop)return;\n  const offset=Math.max(0,Number(y)||0);\n  savedSheet.style.transition=animate?\"transform .38s cubic-bezier(.22,.86,.26,1)\":\"none\";\n  savedSheet.style.transform=`translate3d(0,${offset}px,0)`;\n  const h=Math.max(savedSheet.getBoundingClientRect().height,1);\n  const progress=Math.min(1,offset/h);\n  savedSheetBackdrop.style.backgroundColor=`rgba(0,0,0,${(0.42*(1-progress)).toFixed(3)})`;\n}\nfunction resetSavedSheetVisuals(){\n  if(!savedSheet||!savedSheetBackdrop)return;\n  savedSheet.style.transition=\"\";\n  savedSheet.style.transform=\"\";\n  savedSheetBackdrop.style.backgroundColor=\"\";\n  savedSheetBackdrop.classList.remove(\"is-dragging\",\"is-closing\");\n}\nfunction openSavedItemSheet(){\n  if(!savedSheetBackdrop||!savedSheet)return;\n  clearTimeout(sheetCloseTimer);\n  resetSavedSheetVisuals();\n  savedSheetBackdrop.setAttribute(\"aria-hidden\",\"false\");\n  lockScroll();\n  requestAnimationFrame(()=>savedSheetBackdrop.classList.add(\"active\"));\n}\nfunction closeSavedItemSheet({velocity=0}={}){\n  if(!savedSheetBackdrop?.classList.contains(\"active\")||!savedSheet)return;\n  savedSheetBackdrop.classList.remove(\"is-dragging\");\n  savedSheetBackdrop.classList.add(\"is-closing\");\n  const distance=Math.max(window.innerHeight,savedSheet.getBoundingClientRect().height+80);\n  const duration=Math.max(180,Math.min(360,300-Math.max(0,velocity)*90));\n  savedSheet.style.transition=`transform ${duration}ms cubic-bezier(.32,.72,0,1)`;\n  savedSheet.style.transform=`translate3d(0,${distance}px,0)`;\n  savedSheetBackdrop.style.transition=`background-color ${Math.min(duration,260)}ms ease, visibility 0s linear ${duration}ms`;\n  savedSheetBackdrop.style.backgroundColor=\"rgba(0,0,0,0)\";\n  clearTimeout(sheetCloseTimer);\n  sheetCloseTimer=setTimeout(()=>{\n    savedSheetBackdrop.classList.remove(\"active\",\"is-closing\");\n    savedSheetBackdrop.setAttribute(\"aria-hidden\",\"true\");\n    curSavedPlaceId=\"\";curGal=[];curGalIdx=0;\n    resetSavedSheetVisuals();\n    savedSheetBackdrop.style.transition=\"\";\n    unlockScroll();\n  },duration+24);\n}\nfunction canStartSheetDrag(target){\n  if(target?.closest?.(\".sheet-grabber-zone\"))return true;\n  if(target?.closest?.(\"button,a,.modal-dots,.modal-img-wrapper\"))return false;\n  return !savedSheetBody||savedSheetBody.scrollTop<=0;\n}\nfunction beginSheetTouch(e){\n  if(!savedSheetBackdrop?.classList.contains(\"active\")||e.touches?.length!==1)return;\n  const t=e.touches[0];\n  sheetTouch={\n    startX:t.clientX,startY:t.clientY,lastY:t.clientY,lastTime:performance.now(),\n    startTime:performance.now(),offset:0,velocity:0,dragging:false,\n    allowed:canStartSheetDrag(e.target),fromGrabber:!!e.target?.closest?.(\".sheet-grabber-zone\")\n  };\n}\nfunction moveSheetTouch(e){\n  if(!sheetTouch||!sheetTouch.allowed||e.touches?.length!==1)return;\n  const t=e.touches[0],dx=t.clientX-sheetTouch.startX,dy=t.clientY-sheetTouch.startY;\n  if(!sheetTouch.dragging){\n    if(dy<=3||Math.abs(dx)>Math.abs(dy))return;\n    if(!sheetTouch.fromGrabber&&savedSheetBody&&savedSheetBody.scrollTop>0){sheetTouch.allowed=false;return;}\n    sheetTouch.dragging=true;\n    savedSheetBackdrop.classList.add(\"is-dragging\");\n  }\n  e.preventDefault();\n  const now=performance.now(),dt=Math.max(1,now-sheetTouch.lastTime);\n  const instant=(t.clientY-sheetTouch.lastY)/dt;\n  sheetTouch.velocity=sheetTouch.velocity*0.72+instant*0.28;\n  sheetTouch.lastY=t.clientY;sheetTouch.lastTime=now;\n  sheetTouch.offset=Math.max(0,dy);\n  setSavedSheetOffset(sheetTouch.offset);\n}\nfunction endSheetTouch(){\n  if(!sheetTouch)return;\n  const state=sheetTouch;sheetTouch=null;\n  if(!s");
