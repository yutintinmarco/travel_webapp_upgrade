# v7.6.3.4 Hotfix

## Fixed
- Expense Version number is structurally inside `.expenses-app` as the final content item.
- Bottom bar clearance now occurs after the Version number, not between expense content and Version.
- Healthy expense sync status is hidden; warnings are shown as concise user-facing inline status.
- Removed accidental Activity Log listener cancellation from `listenToSettlements()`.
- Added missing Firestore Rules for `trips/{tripId}/settlements/{settlementId}`.

## Required after deployment
Republish `firestore.rules` because the previous deployed rules did not grant access to the settlements collection.

Firebase CLI:
`firebase deploy --only firestore:rules`

No Firestore index change is required.
v7.3.13 profile navigation compositor is unchanged.
