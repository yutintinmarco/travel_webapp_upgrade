v7.3.5 Phase 2B fade-through navigation experiment

• Replaced the overlapping push/snapshot look with a two-stage fade-through transition.
• First 48%: outgoing page fades fully away with only a very small horizontal drift.
• 48% to 52%: deliberate low-visibility handoff zone, leaving the fixed trip background visible instead of showing two translucent pages at once.
• Final 48%: incoming page fades in and settles into place.
• Interactive left-edge swipe uses the exact same progress mapping, including cancel and complete animations.
• No page scale, background scale, background re-crop or snapshot background movement.
• Bottom navigation remains body-level and above all profile transition surfaces.

Other v7.3.x Phase 2B refinements remain unchanged.
Phase 2C Firestore create/import/archive writes remain intentionally disabled.
