v7.3.6 Phase 2B navigation polish

• Fade-through endpoints now use quintic smootherstep to remove the quick opacity kick.
• Transition surfaces are born at the correct opacity, avoiding a one-frame double-page flash.
• Final snapshot dissolves gently into the live destination page instead of switching in one hard frame.
• No scale animation is used. Background and Bottom Bar remain fixed.
• Restored the small version number at the bottom of the app views.
• Removed 支出管理 from the 支出 content page.
• Added 我的 → 旅程設定 → 支出管理 with members, rates, expense lock, backup, deleted items and logs.
• Expense setting sheets continue to use the global modal portal, so they can open while 我的 remains the active tab.
