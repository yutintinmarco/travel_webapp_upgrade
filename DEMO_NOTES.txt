Travel Webapp Phase 2B v7.3.2 Refinement

Key fixes
1. Fixed false 「無法載入行程資料」 header state. v7.3.1 renderInfo referenced an undefined esc() helper after the itinerary had already rendered.
2. Optional weather, travel info, Firebase catalog, auth and profile setup are isolated from the critical itinerary render path.
3. Header trip title now opens a compact anchored Liquid Glass trip chooser directly underneath. No full bottom sheet and no Manage Trips button.
4. 我的 first page is intentionally minimal: account plus four navigation rows only.
5. 我的 detail pages use native-style forward push and reverse back transitions.
6. Left-edge right-swipe back follows the finger and uses the Price Track app thresholds: 32% distance or 0.55 px/ms velocity.
7. 我的旅程, appearance, trip settings and app/data content now live inside detail pages.
8. 資料 tab remains dedicated to the current trip visa / Visit Japan / flight / hotel reference information.
9. Archive model remains separate from trip lifecycle status.

Phase 2C is still intentionally disabled for actual Firestore create/import/archive writes.
