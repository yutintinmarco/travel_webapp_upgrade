# v7.6.5.2 Member Layout Hotfix

- Editable Member row returns to the same compact one-row structure as Owner.
- Role selector and Remove button are shown side by side on the right.
- Controls are vertically centered to match the Owner role badge position.
- Card height is no longer increased by a second action row.
- Small-iPhone sizing is tightened so both controls still fit without wrapping.
- No Firestore Rules or index changes.
- v7.3.13 navigation compositor is unchanged.
