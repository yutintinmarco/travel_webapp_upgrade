# v7.7.0.3 Import / Offline / Version Hotfix

- Rebuilt `importTrip()` with a conventional Safari-safe parameter signature.
- Fixes iOS Safari: `Cannot use abbreviated destructuring syntax for keyword 'let'.`
- Archive / Restore Archived Trip works again because it shares the same import module.
- Offline Import is intercepted before Firebase/module loading and shows a clear Chinese explanation.
- Offline Archive / Restore Archived Trip also shows a clear Chinese explanation.
- Failed dynamic-import promises reset so reconnect/retry can work.
- Removed the global version DOM completely.
- Each active view owns one CSS footer.
- Expense version is the final visual child of `.expenses-app`, before bottom-nav clearance.
- Firestore Rules and indexes are unchanged from v7.7.0.
- Protected v7.3.13 navigation compositor is unchanged.
